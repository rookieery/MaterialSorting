# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 运行产物（排料结果 intermediate / SVG / PNG / 曲线）
out/

# 虚拟环境
.venv/
venv/

# Node（前端依赖，本地装）
node_modules/

# 前端构建产物（npm run build 输出；由 materialSorting-web/src/ 编译生成，不入库）
materialSorting-web/static/

# Ralph 自动循环产物（运行时生成，勿入库）
.all_done
.last-branch
archive/
