# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 运行产物（排料结果 intermediate / SVG / PNG / 曲线）
out/

# 虚拟环境
.venv/
venv/

# Node（前端依赖，本地装；产物 static/ 由 US-008 决定是否入库）
node_modules/

# Ralph 自动循环产物（运行时生成，勿入库）
.all_done
.last-branch
archive/
