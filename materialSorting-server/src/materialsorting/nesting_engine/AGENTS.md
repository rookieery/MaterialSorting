# nesting_engine — Agent 速查

> sparrow 求解封装层。可 import `nesting_bounds` + `dxf_parser` + `paths`；**禁 import `web`**（上层）。`sparrow` 作为 pip 包（`spyrrow`）引用，不改源码。
> 改前先看 `.docs/technical/agent-file-map.md` 的 `nesting_engine/` 章节。

## 启动 / 校验

```bash
python -c "from materialsorting.nesting_engine import labeling, constraints, sparrow_baseline, waist_band, prefix, polish"
```

## 文件分工

| 文件 | 角色 |
| --- | --- |
| `constraints.py` | 重合/旋转**全局**上限（2026-08-17 起：`MAX_OVERLAP_MM=10.0` / `MAX_ROTATION_TOL_DEG=45.0`，每片型钳制表已删，版师按片型的工艺参考值在 `.docs/business/排料规则_详细版.md` §3.2/§4）+ 旋转公差离散化 `discretize_orientations`（**US-009 起**自 `web/solver.py` 移入真相源 —— 本包 `waist_band` 同口径消费但分层禁 import web；solver re-export 旧路径零改动）+ 位图腐蚀 + 合法性校验（US-002 起 `PAIR_TYPES` 与成对齐套校验已删 —— 服务于已删除的合成镜像模型） |
| `sparrow_baseline.py` | 基线求解 CLI + ★共享层（`SIZE_PALETTE`/`size_color`（2026-08-20 起**尺码** → 16 色循环表单一真相源，锚点 `SIZE_ANCHOR=DEFAULT_SIZES[0]=28` 稳定绝对映射、同码同色跨片型；US-002~2026-08-19 为 g 码键 `label_color`，更早 `PTYPE_COLORS` US-002 已删）/ `_clean_polygon` / `_transform_polygon` / `solve_with_progress`，被 experiments/export/solver/waist_band 复用；SVG 图例按尺码数值序、标题「尺码」） |
| `sparrow_experiments.py` | 旋转/重合公差实验 CLI（US-002 起 `INTERNAL_TYPES` 已删，内片集合改 `--internal g04,g07` 命令行参数显式给出）；`erode_polygon` 被 solver/waist_band 复用 |
| `labeling.py` | 裁片 g 码编号**单一真相源**（US-001 v2：label 先行、名称无关）：`label_for(idx)` / `code_sort_key(code)` / `master_code_from_block_name(name)` / `centroid` / `size_sort_key` / `parse_member_sort_key(p)`（码内成员稳定排序键）/ `sequential_sort_key(p)`（**T4：group_key 前置**，同一 block 模板跨码同号）/ `collect_master_codes(pieces)`（all-or-nothing，有效片=全部 size≠None）/ `assign_codes(pieces)`（签名无 gmap/group_names；`compute_size_ptype_labels` 已删除） |
| `waist_band.py` | **US-009 腰头成带核心**（依据 `.docs/business/腰头成带_落地方案.md` §2；**v2 2026-08-21 构造性链构造重写**替换 v1 spyrrow 带内子求解）：`build_band_plan(pid_meta, pieces_by_id, *, label, seed, ...)` —— 版师形态「**N 条单副本异码链**」（`chain_k`=每码第 k 副本一条链，链内 size **降序**）：`_chain_nest` 构造性滑移贴靠贪心（首片锚定原点，后续 rot{0,180}×5 y 对齐 × `_slide_touch` 右起 20mm 粗扫+40 次二分到贴触，取 union bbox 增长最小；shapely-only、无 RNG、毫秒级）→ `_flip_chain` 整链点对称翻转（成员 rot+180/tr 取负，合法布纹旋转无镜像）⇒ **开口朝左+最大码在最右**（用户建议1 定稿形态）→ 形态自检：`_chain_gap` ≤ `CHAIN_GAP_EPS_MM`=1.0（**贴触口径 = 版师验收**，非 bbox fill —— 新月片 bbox 空隙是构造性的）+ `_opening_side` ∈ {left,flat}（质心偏移反侧判据，已校准）+ 最大码质心 x 最大（程序断言自校验）→ `_stack_chains` 多链堆叠（**不翻链**保开口方向）+ 带高守卫 ≤ `gate_nest`（= 门幅原样，2026-08-28 版师定案起单一口径；链构造无 strip 约束，显式校验）→ 成员**原始轮廓** union → closing 焊接连通 → `erode(d_g)` → clean → 平移归一化，产 `BandChunk`（`WB_*` pid + offset + 逐副本成员带内位）；`expand_placements` 展开权威式（**offset 减号**，黄金单测锁死）；`band_seed_for=zlib.crc32` 派生（身份标识）；异常 `DegenerateBand`/`BandQualityError`（`fill < fill_floor=45%` 灾难形态守卫，`fill_floor` 是 build_band_plan 唯一可调阈值参数 —— **2026-08-22 简化后唯一守门人**）。**禁 import web**（AST 守卫在 tests/test_waist_band.py）；真实 DXF 自交轮廓经 `buffer(0)` 修复（`_valid_geometry`）。v1 的 spyrrow StripPacking 求解 + US-014 成对重试（`_pairs_complete`/`_slot_fallback`/`BAND_MAX_TRIES`/`BAND_NUM_WORKERS`）已删 —— 根因：strip 目标是最短用布 X 而非贴触，产出 48.2% 对角阶梯坏带形反噬主解 -3.81pt；v2 实测（5336 g05）：6 码×1 单链 fill 71.8%（537×1326）**链内缝隙 0.00mm**、P0 14 副本 79.5%（1153×1271）、端到端 A/B **ON 88.35% vs OFF 86.08%（+2.27pt）**、同 seed 两跑 to_dict JSON 相等。**US-015 填料混带已删（2026-08-22 简化）**：`fillers`/`_fill_gaps`/`BAND_INNER_D_MM`/`FILLER_BISECT_ITERS`/`BandChunk.fillers` 整体移除（连带 `PAIR_ADJ_EPS_MM`/`DEFAULT_BAND_TIME_BUDGET_S`/`time_budget` 参数），纯腰 v2 链构造即全部现行行为 |
| `prefix.py` | **US-001 起始端成套前后幅核心构造 + US-002 段置换钉位/驱逐重插**（依据 `.docs/business/起始端成套前后幅_版师确认清单.md` v3 + P0 探针 `out/tmp_probe/prefix_lib.py` 移植收敛）：`eligible_sizes(quantities, front, back, sizes=None)`（P2 资格规则：该码两 g 码 demand 均 ==2，0/1/3/缺码不合格；sizes 过滤必须给 —— 资格码须真实进主解实例，5336 → {32,33,34,35,38}）/ `pick_prefix_size(eligible, *, seed, front, back)`（seeded 随机取一：`random.Random(zlib.crc32(f'{seed}\|{front}\|{back}'))` 派生 + 排序归一，勿用全局 random/hash()；空集合 `PrefixError` 文案指路数量矩阵）/ `build_prefix_plan(pid_meta, pieces_by_id, *, front_pid, back_pid, d_g, gate_nest, order='interleave')` → `(chunk, gaps, holes)`：4 片（前×2+后×2 同码）interleave 竖排贴靠（`_slide_touch_y` Y 向粗扫+二分，无 RNG）→ 原始轮廓 union → 焊接 → erode(d_g) → clean → 归一化，产 `PS_*` BandChunk（**直接喂 waist_band.expand_placements**）；守卫：order 非法 ValueError、副本不齐 2+2/不同码/竖排高>gate_nest（2026-08-28 起即门幅原样，无 1910 钳制）/贴触>1mm `PrefixError`。**rot180 负坐标坑**：成员先归一原点再做候选对齐+滑触，记账 `tr=(xoff−b0, yoff−b1)`（P0 曾因缺补偿片侧移并排，单测锁死）。**US-002**：`permute_pin(placements, geoms_raw, geoms_eroded, comp_world, prefix_idx, *, flex, eps, skip_at_head) → (placements, shift, stats)` —— 组合片 min_x ≤ `PIN_SKIP_AT_HEAD_MM`=6.0 已锚定即整体跳过（P0 常态路径）；否则 c1=a 硬切 + c2∈[b0, b0+`PIN_CUT_FLEX_MM`=400] 软切（取跨线片最少）A/C/B 三组刚性重组（A 平移 −a 钉布头 / C 右贴 A / B 左贴 C，x 区间两两不交无新重叠、纯 x 平移不碰 y）；跨线片（eps=`PIN_STRADDLER_EPS_MM`=5.0 判据，≥ d 包络外凸防贴壁误判）驱逐 → `reinsert_evicted(..., *, gate_nest) → stats` 三优先序回填：①组移 home + `PIN_NUDGE_LADDER_MM` +x 微调梯 ②`_slide_touch` 右起滑贴（y 候选 = 已占棱边 4 类对齐 ∪ {0, 门幅−h}，x<0 钳壁 —— 探针负坐标 bug 修复）③尾端 +片宽+60mm 追加兜底（width_growth 记 stats，超 `PIN_WIDTH_GROWTH_WARN_MM`=50 warn）；面积降序处理 tie-break 确定性无 RNG。`pin_prefix_layout(...)` 编排**纯函数**（入参快照）：expand_placements 定位成员 → permute → reinsert → `constraints.validate` 全图复检 + y≤gate_nest 复检，失败回滚返回置换前布局（rolled_back=True + issues；引擎不 import cli，`_recheck_layout` 镜像 lns_bands）。`main` 冒烟（`python -m materialsorting.nesting_engine.prefix --intermediate <commit 产物>`）对拍 P0：size34 bbox 1155×1458 / fill 83.3% / gaps 0.00 / holes 0；`--pin-demo --time N` 加跑 FREE vs PIN 演示（P0 回归：4/4 seed 组合片常态锚定 ⇒ skipped=True、layout_identical、Δdensity +0.00pt）。**禁 import web/cli**（AST 守卫 tests/test_prefix.py）；复用 waist_band 私有算子（`_solid_region`/`_exterior_coords`/`_valid_geometry`/`_slide_touch`/`expand_placements`）与 `erode_polygon`/`_transform_polygon`/`_clean_polygon`/`solve_with_progress` |

| `polish.py` | **prd-edit-polish US-001 编辑排料「智能微调」引擎层确定性后处理**：`polish_layout(placed, pieces_by_id, gate_mm, *, exclude=None, compact=False) → (placed_new, report)` 纯函数无 RNG（compact = US-005 压缩回收档：缺省 false = pass ④ 整段跳过，US-001~003 行为逐字节不变 additive）。五部分：①诊断（**物理毛版轮廓口径** = pieces_by_id 原始 polygon 与 /export `placed_to_world` 同源，非 eroded —— 画布红字是腐蚀口径数值可能偏小，差异是文档级口径差）全图两两重合（bbox 预筛 + shapely 交集面积/穿透深度 `OVERLAP_AREA_EPS_MM2`=0.1）+ 旋转偏差审计（dev = min(rot mod 180, 180−rot mod 180)，180° 布纹等价）；②去旋转 dev 降序（平手下标）沿 `discretize_orientations` 同款离散集（当前 dev 充当 tol，同 1°/5° 步进规则）向**最近基线**步进试放，质心锚定 `t'=c_world−R(rot')·c_local` + 邻域候选（质心原位 + 障碍/门幅棱边对齐）按位移升序取首个过守卫位；③去重叠穿透深度降序最小分离平移（`_sep_translate` 镜像 `_slide_touch` 二分：lo 恒碰撞/hi bbox 分离界恒自由 + 1nm 防贴死微抬 ⇒ 终态交集面积**精确 0**），方向 ±y/−x 不增料长按 (量级, ±y 优先) 择一，一片失败换动另一片，都失败记 residual；④压缩回收（US-005，compact=True 才跑）自布头方向逐片 **−x 滑贴**收空隙进料长 —— 按 (minX, 下标) 升序**级联**（左片先贴墙、右片贴新位），每片 `_slide_west_touch`（`COMPACT_SCAN_STEP_MM`=20 粗扫 + 40 轮二分到首个碰撞界 + 1nm 回退自由侧 = 贴触零交，滑程墙 = 自身 minX 到布头 x=0）逐 move 走同款五道守卫（kind=compact）+ **pass 级守卫**：收尾全图 maxX **严格变小**才接受（`COMPACT_GAIN_EPS_MM`=1e-6），否则整体回滚（items/geoms/bounds/moves 四快照恢复 = 无改进逐字节不变）。**逐 move 五道守卫**：y∈[0,gate]（`GATE_EPS_MM`=1e-6 数值容差）/ 全图物理包络不增（width ≤ before+`WIDTH_TOL_MM`=0.5，minX<0 布头外凸同计）/ 位移片 vs 全图零新重合（≤0.1mm²）/ pid 多重集守恒（同 pid N 条**按数组下标**逐实例寻址绝不 pid 去重，与前端 editStore 同口径，出口终检）/ exclude 命中实例（labels/pids 双键 over-conservative 同 pid 全副本）永不动仍作障碍。**无改进返回输入 list 原对象**（逐字节不变量，LNS 同款）。报告 before/after 七指标（density = real 口径 Σ(原面积×副本数)/(width×gate)）+ moves（kind derotate/separate 含 from/to/detail）+ residual（终态重合对 + 旋转残留如实上报）+ excluded + elapsed_sec。`python -m materialsorting.nesting_engine.polish` 冒烟 = 合成夹具十项自检（斜片回正/重合分离/紧密 no-op/守卫越门幅/守卫包络/多副本/排除集/确定性双跑/compact 回收空隙（US-005：三片 30mm 缝 → width 360→300 + moves 全 kind=compact + 两两交集面积精确 0）/compact 无空隙逐元素相同）+ `--demo` 真实母版几何演示（intermediate 前 N 片确定性带病布局 → 前后对比，无 spyrrow 依赖）。实测 110 片 0.5s（预算 ≤5s）。**edit-keyboard US-004（2026-09-05）镜像片**：placed 条目可选 `mirror: true`（omit-when-false；`world=R(rot)·diag(−1,1)·p+t` 与前端 transformPolygon / 后端 `export_geometry.apply_transform` 同一约定）—— `_world_geom` 与 pass ② derotate 共用「local=[(-x,y)] 预处理 + 标准 `_transform_polygon`」（c_local 用**镜像后**多边形质心，t' 补偿公式不变；基线 {0°,180°} 不受影响，diag(−1,1) 保持 x 轴方向），诊断/③分离/④compact 全按镜像几何；items 构造、compact 快照、出口三处重建统一走 `_rebuild_item`（omit-when-false —— 快照丢键 = 回滚路径镜像标志静默蒸发）；无改进返回输入原对象（含 mirror）逐字节不变；缺省 False 键集与行为逐字节不变（745 pytest 全绿）；冒烟自检扩为十二项（⑪ 镜像斜片 derotate+透传+质心锚定 / ⑫ 镜像片 no-op 原对象）。**禁 import web/cli**（AST 守卫 tests/test_polish.py）；复用 `waist_band._valid_geometry` / `sparrow_baseline._transform_polygon` / `constraints.discretize_orientations` |

## labeling 关键约定（原 US-022 立；US-001 v2 起为 g 码单一真相源现行口径）

- **`labeling.py` 是 parse/commit 两处赋码的单一真相源**（US-001 v2）：`assign_codes(pieces)` 无 gmap/group_names 参数，「有效片」= 全部 `size≠None` 片（无名称映射判断、未录入名称不丢片）；顺序模式排序键 `sequential_sort_key = (group_key, -centroid_y, centroid_x, -area_mm2, block_name, piece_index)`（group_key 前置保证跨码同号）+ `label_for(idx)`（0→g01, 1→g02 ...）。
- **依赖方向合规**：本模块仅 import 标准库 + duck-typed PieceOutline 属性，不依赖任何兄弟包。`web/server.py` 可直接 import。
- **AC#5 对齐不变量（v2 简化）**：parse 与 commit 各自对同一母版跑 `assign_codes`（同 collect、同排序键、同母版码规则）→ 同一 `(block_name, size, piece_index)` 必得同 g 码，**不再经 (size, ptype) 中转**（M1787 验证 11 码 × g01..g10 逐片对齐）。
