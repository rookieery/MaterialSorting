# tests/ — Agent 速查

> `materialSorting-server/tests/` 单测目录（US-025 起新建）。pytest 走 `pyproject.toml` 的 `[tool.pytest.ini_options] testpaths=["tests"]`，根目录 `materialSorting-server/`。

## 运行

```bash
cd materialSorting-server
../.venv/Scripts/python.exe -m pytest tests/ -v            # 全跑
../.venv/Scripts/python.exe -m pytest tests/test_solve_proc.py::test_terminate_returns_within_5s -v
../.venv/Scripts/python.exe -m pytest tests/ -k normal     # 按名筛选
```

## 文件

| 文件 | 覆盖 |
| --- | --- |
| `conftest.py` | sys.path 注入 `src/`（未 `pip install -e .` 也能跑）；`real_or_synthetic_pieces` / `synthetic_pieces` fixture（优先读真实 intermediate，缺失则合成 2 片矩形；US-001 起合成片为 **v2 schema**：`pid='g01_28'`、有 `label` 无 `ptype`/`side`） |
| `test_solve_proc.py` | US-025 `solve_worker` + `solve_with_callback_proc`：①正常求解 + density 双口径 ②terminate 5s 内返回 ③build_instance 抛错 ④外部 kill 不 hang ⑤solve_worker 可 pickle |
| `test_ws_stop.py` | US-026 WS `/ws/solve` stop 协议 + 客户端断开清理：①start→frame→stop→stopped+WS 关闭 ②直接断连→进程数回落 ③不发 stop 正常求解收 final（用 `starlette.testclient.TestClient`） |
| `test_export_plt.py` | US-033 `write_marker_plt` HPGL 文本生成器（封装对齐生产 PLT）：头部 `IN;PS<纸长>;SP1;PW0.08;`、CRLF 行尾、尾部 `PU;PG;`、无 VS/LB、坐标×40 round 取整、闭合不变量（PU 首点=PD 末点、门幅框 4 角闭合）、**全程单笔 SP1**（2026-08-24 用户要求统一颜色=门幅框蓝色；生产 PLT 实测同样仅 SP1，首版 SP1-SP5 分笔多色已废弃）、空层跳过、多片平铺、PS 覆盖刺口法线延伸、越界统计 `_plt_frame_stats`、5 层坐标/层序端到端、布纹箭头线+尺码*数量标注（单头指画向 B 端、w=(-uy,ux) 右手系防镜像、侧别/手性/锚位/短杆/越幅裁剪）、空 pieces 防御边界（合成裁片，不依赖 intermediate） |
| `test_load_pieces_notches.py` | `_apply_layer_transforms` 刺口变换回归：notch **点**随片旋转（旧实现只转法线导致竖直布纹片刺口飞出轮廓 3m+，腰/后袋 600 越界点污染 PLT/PNG/DXF）、法线旋转、rot=0 直通（US-001 起镜像分支已删，镜像用例随删） |
| `test_labeling.py` | US-001 v2 `labeling` 单测：`label_for`/`code_sort_key` 边界、`master_code_from_block_name` 保守识别矩阵（含仓内易误伤 block 名）、`collect_master_codes` all-or-nothing（有效片=全部 size≠None，未录名组同样参与）、`assign_codes` 顺序模式（`sequential_sort_key` group_key 前置 / T4 跨码同号 / AC#5 确定性）与母版复用模式 |
| `test_label_representatives.py` | US-001 v2 `_build_label_representatives`：键=g 码、代表取最小码首个 size≠None 片、5 层字段白名单（由 test_ptype_representatives.py 更名而来） |
| `test_commit_pipeline.py` | US-001 v2 commit 全管线（合成「未录入名称」母版）：0 丢片全片有 g 码、`{label}_{size}.dxf`+`pieces_manifest.json` 落盘、manifest 驱动 `load_nest_pieces`（pid=`{label}_{size}`）、AC#5 parse↔intermediate 逐片 label+面积对齐、idempotent 重跑、旧切片目录/v1 intermediate 明确报错「请重新 commit」 |
| `test_solver_label.py` | US-002 求解/导出层 label 键：`size_color`（2026-08-20 尺码键：16 色循环/同码同色跨片型/锚点 28/28-38 全段互异/兜底）+ `size_aci`（`((size-28)%24)+1` 公式与循环）、`build_instance` per_type label 命中即覆盖（2026-08-18 回退 US-004 后单级：erode/tol 落在该 g 码全部码号；未命中与旧 ptype / 旧两级键 no-op；全局上限收边）、quantities demand 直译（0 跳过/N 副本/面积×demand）、pid_meta 无 ptype 键且 color=size_color(size)、`constraints.validate` 删成对齐套校验后裸 pid 裁片可用 |
| `test_cli_config.py` | US-001 `cli/config.py` 7 键配置校验：示例配置 `data/configs/5336_coded_sizes32-38.json` 加载成功（sizes/gate_mm/seeds/quantities 键类型/master_dxf 绝对路径）+ 相对路径 CWD→仓库根两候选解析矩阵；错误矩阵（未知顶层键含旧 seed 三键附迁移提示 / master_dxf 不存在列两候选 / sizes 字符串 / seeds 空列表·负数·非整数·重复 / per_type 非 g 码·未知内键·负值 / quantities 字符串数量提示 JSON 写数字·坏码号键·负数 / gate_mm≤0 / time 非正整数 / 配置文件不存在·非法 JSON·顶层数组）；d>10 或 tol>45 UserWarning 引用 MAX_OVERLAP_MM/MAX_ROTATION_TOL_DEG 钳制提示、恰等上限不警告；模块纯度（AST 断言模块级仅标准库 import、`python -m` 子进程运行零输出零副作用、ConfigError 是 ValueError 子类） |
| `test_cli_pipeline.py` | US-002 `cli/pipeline.commit_from_config`：合成母版（小数坐标暴露 rounding 位数差异）走 CLI commit → run_dir/pieces/ 切片+manifest 条数=size≠None 轮廓数、intermediate 落 run_dir（gate_mm=cfg.gate_mm 配置驱动、无 label_representatives、无 .bak）；`web.solver.load_pieces` 读回（schema v2 自证）；web 事实源零触碰（INTERMEDIATE 哨兵内容/mtime 不变 + uploads 无新条目）；**与 web `_commit_to_nesting_sync` 逐字段对拍**（pid 集合/total_area/逐 pid piece 全等，顶层唯一差异=label_representatives）；`new_run_dir` 时间戳目录幂等；分层（AST 全模块禁 import web.server + 模块级兄弟包仅向下）+ `python -m materialsorting.cli.pipeline` 零输出冒烟 |
| `test_cli_run_config.py` | US-003 `cli/pipeline.solve_pieces` + `cli/run_config.main`：solve_pieces 指标口径（real_density=total_area/(width*min(gate,1910)) 实际幅宽对拍、placed==Σdemand、per_type d>0 → n_eroded 按码计数、quantities demand=2 副本翻倍 + label 命中码号缺 demand=0 跳过）、placed≠Σdemand 抛 RuntimeError（PC-001 起 fake `solve_with_callback_proc` 返回末态 dict 驱动）；main 端到端（rc=0、result.json config/commit/solve/best 结构、末行汇总含 real_density（原面积口径）+run_dir 完整路径、--name 非法字符清洗、--quiet 抑制进度帧、配置失败退出 1 不建 run_dir、管线失败退出 1、web 事实源哨兵零触碰）；**US-004 多 seed 串行与 best**（seeds=[0..4] 串行 5 轮：spy 断言 commit 仅一次 + solve 顺序 [0..4]、预计总时长 `5 轮 × 2s ≈ 10s` 打印、轮次标记「第 i/5 轮」、solve 数组长度=len(seeds)、best=real_density 最大者且 seed 字段正确/并列取先执行者/与对应轮完整指标一致、各 seed 汇总行 + best 行、末行取 best）；单 seed 回归（缺省与显式 seeds=[0] 均无多 seed 启动行/轮标记/best=唯一解）；种子不要求连续（[0,42] 合法顺序求解）；`_clean_run_name` 清洗矩阵；**PC-001 Ctrl-C 中断安全**（第 3 轮抛 KeyboardInterrupt → 退出码 130 + stderr「[中断] 2/3」+ result.json 含已完成 2 轮 + curve_s\*/best_frame_s\* 在场、第 1 轮即中断无 result.json）与 result.json 逐 seed 写入（第 2 轮开始时读到第 1 轮写下的 1 条 solve）；分层（run_config 全模块不 import web、pipeline 对 web.solver 仅 solve_pieces 内唯一延迟 import —— AST 断言模块级无 web） |
| `test_cli_solve_pieces_proc.py` | PC-001 `solve_pieces` 进程化 + 帧轨迹落盘 + `should_stop` 中止：fake `solve_with_callback_proc`（伪 Process 句柄记录 terminate、逐帧同步回调、terminate 后停止投帧按「被终止」形态返回）注入确定帧序列 —— curve_s\*.json 白名单无 placed_items/elapsed 单调不减/best_frame density=曲线最大帧且 frame_index 对齐/等值不覆盖（停在首个极大值）/KeyboardInterrupt 时已收帧写全；should_stop 恒 False 与不传等价（记录逐字段相等）/触发后 killed=True + kill_reason（字符串透传）/density=best-so-far 帧非末帧/中间帧 placed<Σdemand 不校验/首帧即停也有 best-so-far；真实多进程回归（无 should_stop 冒烟：完整解 + 双口径对拍 + 落盘；首帧即停端到端：真子进程 exitcode≠0 + on_process 句柄 join 无孤儿 + active_children 无 solve_worker） |
| `test_cli_portfolio.py` | PC-002 `cli/portfolio` 控制器 + `run_config` portfolio 旗标：fake solve（`run_serial_portfolio(solve=...)` / monkeypatch `rc_mod.solve_pieces`，帧布局编码 seed/idx 验来源）—— incumbent banking（3 seed 交错轨迹取全局最大帧 + 来源字段 / 等值不覆盖 / 被 kill seed 峰值入账 + 回落不覆盖 / Ctrl-C 中断 seed 已收帧入账）；R0（seed2 中途达标 → killed=True + R0 kill_reason + seed3 不启动 + per_seed 无未启动记录 + 触发帧先入账 / 首帧即达标不空手 / 无 --target 跑满全队列）；portfolio 段与 best 语义（engaged 字段齐全含 PC-003 kill_mode 键 + best==incumbent；单 seed 无 --target 空段 + 旧语义 final 口径）；`load_controller_params` 矩阵（BOM 合法/不存在/非法 JSON/顶层非对象）；进度行（per-seed 新最优保留旧格式 / 跨 seed 反超恰一条 incumbent 行 / 首 seed 自我刷新不打 / echo=None 静默不影响 banking）；main() 端到端（R0 退出码 0 + R0 终局行 --quiet 也打 + result.json 落盘结构 / 单 seed 无旗标空段 + best==solve[0] / 多 seed best=incumbent 高于全部终值 / --target 越界与 --params 坏文件退出 1 不建 run_dir）；分层（portfolio 全模块不 import web）+ `python -m materialsorting.cli.portfolio` 零输出冒烟 |
| `test_cli_kill_engine.py` | PC-003 `cli/portfolio` kill 引擎（R1 包络 / R2 压缩期判决 / R3 θ 衰减 + shadow mode）：纯函数判据边界（r1_below_envelope 等值不杀+None 恒 False / r2_below_threshold 门槛 max(θ, I+ε) 三态+I 缺席退化 θ —— 数值取 dyadic 避十进制浮点歧义）；make_envelope 阶梯查表（网格前 None / 空 / 全非法 / 非 dict → None）；resolve_kill_params 覆盖矩阵（负值/bool/字符串/未知键回退 KILL_DEFAULTS）；seed 1 豁免看队列序号不看 seed 值；R0 恒用 --target 真值（θ 衰减后达标仍 R0）；R2 exploring 不判 + 首压缩帧一次性判决 + (seed,rule) 去重 / uplift 默认与 --params 覆盖翻转判决；R3 连杀 m_streak 才衰减（决策日志 theta 字段留证「只影响后续判据」：衰减后同分位 seed 存活）/ min 单调不抬 / 非 kill 结束清零连杀；R1 持续 W 秒杀 / 瞬时下探 4s 追平不杀 / τ≤τ0 不评估 / 无 envelope R1 禁用；shadow 绝不终止求解（fake solve 逐帧记录 should_stop 恒 falsy + 跑满交付）+ 决策字段完整 {t,seed,rule,d,tau,S_tau,theta,I,would_kill}；on 模式真杀 killed=True+kill_reason 且 best 帧入 incumbent（PC-002 联动）；CLI main() 端到端（--kill on 无标定降级 shadow+stderr warn / on+calibrated 真杀 R1 / off 不落 kill_decisions.jsonl / 缺省=shadow 空文件在场 / 无 --target 引擎 off）—— 注意 fake 轨迹须覆盖 config 全部 seeds 且两次 main() 同秒同名 run_dir 会撞目录（--name 区分）、capsys.readouterr() 一次取全 |

| `test_cli_calibration.py` | PC-004 `cli/calibration` 标定管线（batch/variants/analyze）：变体生成器（同参数两次生成逐字节一致 / 抖动只作用 sizes 内条目且 n'∈{max(1,n±1)} / per_type+gate_mm+master_dxf+sizes 与基配置逐字段相同 / 生成物过 load_config）；run_batch 编排（目录契约 <tag>/base/{short,full} + commit 共享 / fake solve 断言任一时刻至多 1 求解且按 seeds 顺序 / 重跑跳过已完整 seed / KeyboardInterrupt → 130 语义 + manifest interrupted + resume 续跑）；run_variants 编排（commit 复用 —— monkeypatch commit_from_config 为 bomb 断言零次重解析 / cfg.quantities 抖动但工艺字段不动 / variant_{i}.json == generate_variants）；分析纯函数（curve_stats 字段 / envelope 单调非降 + τ 网格 20 格（手算对拍）/ uplift q50/q95 / 分离度与秩相关 / replay_r1 复用 kill 判据语义（迟滞、瞬时下探、τ0、空 envelope） / backtest + 奇偶 train/test 切分 / generalization 误杀率 + transferable + 空变体 None）；CLI 端到端（batch/variants/analyze 三子命令 rc=0 + 产物只落 CALIBRATION_DIR —— config_runs 空 + INTERMEDIATE 哨兵 + uploads 不动 / controller_params 过 load_controller_params 往返 / 小样本拒绝 calibrated:false + envelope={} / 错误路径退出 1）；分层（AST 禁 import web）+ 模块 --help 四子命令冒烟（PC-005 补 simulate） |
| `test_cli_simulation.py` | PC-005 `cli/calibration` simulate ETT 离线仿真器：纯函数（truncate/time_to_target R0 现行帧口径/best_density_upto / conditional_gain 条件期望增量 / interpolate_truncated_final —— AC#2 下界 kill 时刻估计 ≥ best-so-far + kill 早于首帧 → None）；envelope_at_budget（成功=预算内达标、τ·B 绝对墙钟重采样、单调、无成功曲线 → {} 禁 R1）；simulate_portfolio 回放（复用 PortfolioController：R0 停队列+seed1 豁免跑满 / R1 kill 迟滞 t=5 起表 t=10 迟滞满（tau0=0.1,W=3,m=0.02）/ 误杀 oracle（被杀者预算内会达标=false_kill）/ wall-time=前序 offset+自身停点 / R3 θ 衰减翻转后续 R2 判决 m_streak=2 vs 3）；AC#1 理论排序（合成池全枚举手算对拍：single 60 > best_of_2 52.5 > best_of_3 46.667 / 激进 kill 37.917 < 保守 kill 43.542 < single，P(达标) 递增，fkr=0）；不可达 incumbent 终值 ≥ 跑满 best 与被杀 best；recommend_strategy 判据（base/变体 ETT 双不劣于单 seed + 双误杀率 <5%，fkr 0.06 淘汰 / 变体劣于基线淘汰 / 无合格档 None 不硬推 / params 键 ⊇ KILL_DEFAULTS+envelope+calibrated+n_seeds/per_seed_time）；shadow_log_stats（would_kill=False 不计 / 缺曲线不进率 / 决策后才达标=假阳性、全程不达标=正确 kill / by_rule 分桶 / 坏 JSON 行计数 / 文件不可读 CalibrationError）；CLI 端到端（表格+推荐行+报告路径 / strategies 8 档 base+variants 双指标 / 推荐参数过 calibrated+envelope / 除 generated 外两次运行逐字节一致 / --budget 200 → eligible=0 无推荐 / --shadow-log 汇总行进报告 / 错误矩阵退出 1 / 缺旗标 argparse exit 2）；simulate --help 冒烟 + pools 契约（k×B=总预算）；US-003 后网格 10 档（+se180/race180）：策略档 k/B 为 None（表格 '-' + note 降级）、pools 增 `pairs`（配对曲线数）键 |
| `test_cli_simulation_strategy.py` | US-003（strategy PRD）simulate 策略双档 se180/race180 配对曲线回放：合成配对池 helper（`_frames` 5s 步长曲线 + `_pair` short 100s / full 300s 95s 起台阶 = 跨 fork 结构 + `_seed_paired_tag` base 6 配对 3 好 3 坏 + variant_0 4 配对）；se 模式（se_plan(600)=(4,180) 筛选 4 + 延长 1 结构、冠军 = 筛选 argmax、delivered = 筛选最大与延长终值取 max、wall=4×90+180、同 seed 只延长一次）；race 模式（race_plan(600)=(5,90.0) 启动 4 > 600//182.5=3 —— 门杀省时再投资、outcomes full/kill 交替 t_stop 180/90、被杀交付门时刻 best-so-far、首 seed 参照豁免 + 后续严格破纪录、每 seed 门评估恰一次）；R0 优先（race 续跑中达标停 / se 筛选中达标跳延长）；tier 指标与 CLI 端到端（metrics 键集 = ETT 同构字段 + E[max] 口径 delivered_mean/miss_max_rate/gain_vs_uniform90/false_kill_rate、plan 字典、base bootstrap + 变体全枚举、race kills>0 fkr=0、两次运行除 generated 逐字节一致）；DRY 断言（`cal.decide_race_kill is pf.decide_race_kill` 单一真相源 + monkeypatch spy 证明回放真调）；推荐含策略档（qualified 含 race180、race180 胜出 → params strategy/time/race_budget/race_gate、se180 胜出 → se_screen/se_extend、fkr 0.06 淘汰）；降级路径（T<275 → metrics None +『最小配置』、空配对 → 『配对』、short-only tag CLI 降级行不崩） |
| `test_cli_lns.py` | PC-007 `cli/lns` LNS 波段重排核心：split_bands（slab 几何/局部密度公式 = 段内原面积和/(段宽×NEST_GATE_MM)/空段密度 0/pid 组按首副本中心归段不拆分/未知 pid 抛 LnsError）；band_solve_params（quantities 从段内实际副本数推导 Σ=段内位置数、demand>1 计 2 副本/无 label 副本 → None 跳过）；子实例同口径（build_instance 母 vs 子：strip_height 同钳 1910、erode 后形逐点相等 + 面积 ≈ (w-2d)(h-2d)、orientations == discretize_orientations(tol)、demand 透传一致、n_eroded/total_area）；run_lns 行为（人工空洞 fixture：一轮总宽缩短 ≥ 空洞 50%、数量 = Σdemand=5、g04 两副本不变、密度升 + 复检双通过；空段 splice 无需求解（两个 1000mm 空段 3700→1700，子求解仅末轮 2 次）；拒绝路径子解不优 → json.dumps 输出 == 输入逐字节；预算耗尽零子求解；y 越界（竖摞超 1910）接受后复检回退 delivered = 输入；重叠护栏（子解把小片压进后续段既占区逐对拒绝）；跨段散布 pid 副本不被「后续左移」推出 x<0（回归：几何判定后续）；缺省 band_width = 1.5×NEST_GATE_MM）；CLI（main 端到端 fake solve：rc=0 + result_lns.json source/band_width/improved/recheck + lns_compare.svg 双面板 scale(1,-1) 与图例 + stdout 汇总行；result.json/intermediate 缺失与无 incumbent 退出 1；--time/--rounds/--band-width 非法退出 1；--help 子进程冒烟；分层纯度 AST 模块级无 web import 且源无 web.server）；真实 sparrow 多进程冒烟（空段 splice 确定改进 + 数量保持 + 双复检过） |
| `test_cli_solver_opts.py` | PC-006 solver_opts 透传与配置轮换：`build_instance` 直测（不传/None/空档/全非法键 → config 字段与现状逐项一致（80/20 自动分段+quadtree 4+workers 4）；exploration_pct=0.6/0.9 → 两段 int 秒 60/40、90/10、和≈budget、小预算两段各≥1s；越界 clamp 0.01→0.1、5.0→0.95；quadtree_depth 5 透传+9→5+1→3、num_workers 0→1、非数值忽略回默认；旋钮不扰 instance（strip_height/items/pid_meta/total_area 与无旋钮相等））；透传链（fake `solve_with_callback_proc` 断言 solve_params 含 solver_opts 且 JSON 往返可序列化 + 记录回显 /不传时记录无 solver_opts 键零回归；真多进程 spawn 消费含 bogus 键的 solver_opts 求解成功）；轮换与 CLI（池 4 档 k=5 第 5 个 seed 回 pool[0]、池首空档；solver_opts_for 缺省不加键保旧 fake 兼容；--solver-opts × --rotate-opts 互斥退出 1 + 坏 JSON/非对象退出 1 且不建 run_dir；固定档全 seed 生效+config 段回显 solver_opts；--rotate-opts 逐 seed 池序列+回显 rotate_opts:true；无旗标 config 段字段集与 PC-001 基线一致） |
| `test_cli_run_config_lns.py` | PC-008 `run_config --lns` LNS 接入编排：fake `solve_pieces`（帧布局按用例构造 —— holey = 末片甩远端 x 制纯空洞段 / compact = 全叠 x≈0）+ fake `_solve_band`（row packer 恒拒，改进只来自空段 splice 确定性路径）—— 改进路径（incumbent 三字段更新 + seed/frame_index 出处保持 + lns 段键集/before/after/delta/rounds_detail/base_seed + best 同步 + result_lns.json/lns_compare.svg 落盘 + 启动行/接受进度行/前后两行汇总 + 末行取 LNS 后口径）；不优路径（result.json 与 LNS 入口快照**逐字节一致**（跨 run 因时间戳路径天然不同）、无 lns 键、明细仍落盘）；R0 提前停后仍执行后处理（calls==[0]、R0 行先于 [LNS] 前）；Ctrl-C 中断安全（环节内 fake 子求解抛 KI → result_lns.json interrupted/已完成轮/主 result.json 完整回写可解析、退出码 130；窗口外 KI → 130 且 result.json 不动）；旗标裁决（--lns-time/--lns-rounds 单独给出或 <1 → 退出 1 不建 run_dir）；--quiet 抑制 LNS echo 保留两行汇总；单 seed 旧语义 best 帧边车回填 + lns 段（portfolio 空/best 旧语义）；LNS 输入错误降级 warn（退出码 0 汇总照常）；--help 子进程冒烟 + 分层（模块级无 web import） |
| `test_cli_run_stats.py` | PC-009 run 统计库 + θ₀ 按实例类校准：`run_stats_class_key`（确定性/10 位 hex/四组件任一变动不同 key/sizes None≠列表/dict 键序无关 sort_keys）；`load_run_stats`（缺文件 []/坏 JSON 行与非 dict 行跳过/空行剔除）；`calibrate_theta0` 纯函数矩阵（命中且 ≥5 → min(target, max+0.003)/min 封顶不抬门槛/0 与 4 条 → target+info=None/恰 5 条触发/不同 class_key 互不污染 —— 他类高样本不触发、补足后用本类 max/坏记录（缺 best_density、非数值、bool、非 dict）不计样本）；θ₀ 只影响 kill 门槛回归（控制器直驱：θ₀=0.7 < target=0.9 时 density∈[θ₀,target) 帧 should_stop 恒 falsy、≥target 才 R0；不给 θ₀ → θ=target 零回归；θ₀ 翻转 R2 判决 —— 默认 θ=0.9 记 R2 决策 theta=0.9 vs θ₀=0.8 同轨迹存活）；main 端到端 fake solve（轨迹条目支持 (elapsed,density,phase) 三元组驱动 compressing 帧）：连跑 2 次 JSONL 恰 2 行 + 10 字段全集 + class_key 与条目组件自洽 + best_density=incumbent；R0 提前停路径也落盘（target 回显 + n_killed=1 计入被停 seed + 无历史不打 θ₀ 行）；θ₀ 端到端（预置 5 条同 class 历史 + --target 0.9 → 校准行含 89.90%/class_key/5 条历史 + kill_decisions.jsonl 决策 theta=0.899 + 队列跑满证明 R0 不受影响 + run 后 6 条同 class 沉淀）；写盘失败（RUN_STATS_JSONL 指向目录 → open OSError）rc 0 + stderr 警告 + 末行汇总照常 + result.json 在场 |
| `test_cli_strategy.py` | US-001（strategy PRD）策略双模式判据纯函数 + 回放回归：判据单测（常量自洽（名义记账=预算+2.5s 启动开销）/race_gate_seconds 算术/strategy_seed_stream（config 优先+max+1 补齐+空 config 从 1+去重保序+n≤0 空+无重复不变量含负数稀疏重复 config）/decide_race_kill（门帧前 None 且不消费名额+门帧边界 90.0/90.001 含等号+等于 bar 杀与严格破纪录 0.860001 放行且门值入 bar+首 seed 有 bar 亦豁免且低门值不抬 bar+bar=None 不可评估不杀且 bar:=d+bar 含被杀者（被杀门值≤bar 不改 bar、含前者的 bar 作后续参照）+每 seed 至多一笔（判杀/放行后任意帧恒 None）+决策行键集与 kill_decisions 同构（tau=elapsed/budget、S_tau=bar、theta=None 重载）+「确定性重放+严格破纪录」联合性质（破纪录续跑后密度回落/再破纪录均不再评估））/se_plan（默认档 600/1200/1800/3600→4/11/17/36 手算对拍+预算不足 274.9 抛 StrategyBudgetError 且 275 恰好 k=1+自定义 30/60 @300→7 同口径记账+显式传默认==缺省））；fixture 契约（8 配对 seed、端点=实测对 (84.27,84.39)...、时间严格递增+密度单调+每秒≤1 帧、b180f@90 门值≠b90s 终值跨 fork噪声）；回放回归（固定 bootstrap seed 20260820、3000 次、长 48 配对重放 uniform90/SE/race：T=1200 与 T=3600 两模式−uniform90≥+0.1pt+量级护栏、T=3600 漏 max 率≤5%（漏 max=交付<采样 seed 全程 max=b180f@180 占比）；顺序池确定性回放（首 seed 豁免/交付 0.8722=门值最高 seed105/启动数少于均分/seed102 门值 0.8376≤bar 0.8430 判杀））。回放器在测试内（不依赖 out/ 标定产物），与 US-003 simulate 共享 portfolio 判据单一真相源 |
| `test_cli_strategy_wiring.py` | US-002（strategy PRD）`--strategy` 双模式 CLI 接线：fake `solve_pieces` 帧协议（on_progress 先于 should_stop、verdict→break 交付 best-so-far killed rec、curve_s{seed}{suffix}/best_frame 落盘、首轮前捕获 strategy.json）；旗标裁决（值域外/se 缺 --time/--kill 显式与 --strategy 互斥 2×3 参数化/从属旗标缺主旗标/--race-gate (0,1) 开区间 0·1.0·1.5·-0.1/预算不足 race_plan·se_plan StrategyBudgetError→退出 1 不建 run_dir）；race_plan 手算对拍（1200→(12,90.0)/240@60·0.5→(6,30.0)/275 恰 2/274.9 抛）；race 端到端（豁免+门杀+续跑+预算收口 1 seed 未启动/决策行 5 行 theta=None S_tau=bar/strategy.json/portfolio.race kept+gated/per_seed phase='race' kill_reason=R5_race_gate/incumbent 全局帧级/config.strategy 回显/--quiet 仍打门杀行/R0 与 race 共存 target 先停/种子流 max+1 补齐 planned [7,2,8,9] 实际 [7,2,8]）；se 端到端（k=5 筛选+冠军 argmax 延长 _ext 边车共存、末 solve phase='extension'、portfolio.se 四键、incumbent=延长终值/冠军并列取先/R0 提前停跳延长）；无 --strategy 时 result.json 无 mode/race/se 键 config 无 strategy 键（零回归哨兵）；solve_pieces artifact_suffix 真实 2s 求解（最优帧密度 ≥ 末帧回报 —— 真实帧序列非单调勿断言相等）；--help 子进程含 5 新旗标 |
| `test_web_strategy.py` | US-004 web 策略桥接四路由（22 例）：start 校验（409 单例×2（内存态非终态/marker 在）/422×3（state 空、doc 缺 doc_id、母版丢失）/400×3（mode/minutes 整数/minutes 字符串）/202 落盘对拍 config 7 键+master_dxf 绝对路径+gate 回退 state+spawn cmd 旗标+marker 5 键+`_STRATEGY_STATE` 快照）；status（run_dir 快照 diff 发现+se plan 解析 / start 全流程 marker.run_dir 回写 / 30s 超时 error 带 stderr 尾部+清 marker / fixture 逐字段（incumbent 摘要无 placed_items、current=最新 mtime+ext 位+density_sparrow、R5 事件滤非 R5、S_tau=bar）/ 缺文件降级 null / done 清 marker 留内存态）；stop（mock `subprocess.run` 断言 `taskkill /PID <pid> /T /F` + 置 stopped + 清 marker + status 稳定报 stopped）；orphan（marker 在+内存空 → alive/pid/mode/doc_id/elapsed + stop 清理 → idle）；result（manifest 与 `build_pid_meta` 逐字段对拍 + `build_instance` 三元组一致性——**提取回归护栏** + doc_id 漂移 warning + stopped 回落 best_frame 最大 + idle 404/running 409）；commit doc_id 落 intermediate；AST 守卫 strategy.py 禁 import cli；TestClient 四路由在场。基础设施：`FakeProc`（poll 打桩）+ `strat_env`（CONFIG_RUNS_DIR/OUT_DIR → tmp + 状态清零）+ `_spawn_capture` + `_pieces_state` 打桩。**真子进程 E2E 不进套件**，手动冒烟须 `MS_OUT_DIR` 环境变量（父子进程同源，见 web/AGENTS.md） |
| `test_waist_band.py` | US-009 `nesting_engine/waist_band` 核心模块（v2 构造性链构造现行口径，14 项）：展开黄金用例（rot=180 手算对拍 offset=(10,20) 减号 + 变换链三步等价 c.rot∈{0,180,177} + 负角 -180 归一）；build_band_plan 真实构造（5336 g05 同构 7 码×demand2=14/14 守恒 + WB_g05 pid + COMPOSITE_ORIENTATIONS=[0,180] + 成员 size-major 定序 + 归一化原点 + fill>45）；包络断言 union(成员原轮廓@展开位) ⊆ composite@主解位 ⊕ d_g（容差 0.5mm，rot 0/180 双向 + 无 WB_ 泄漏）；异常路径（label 不在/demand 全 0 → ValueError；总副本 1 → DegenerateBand；fill_floor=130 强制触发 BandQualityError —— v2 贴触构造矩形 fill ≈116% 确定性（腐蚀口径贴触 ⇒ 原轮廓交叠 ≤2·d_g），130 必触发且不耦合构造质量）；确定性（band_seed_for == zlib.crc32(b'0|g05') 跨 seed/label 可区分 + 同 seed 两跑 to_dict JSON 相等）；弧形形态用例（`_arc_piece` 5336 g05 真实几何族参数 r≈1550+40·码、thick 68、half 19°、**凸侧 −X 与真实 rot0 朝向一致**）：单链贴触 `_chain_gap`≤1.0 + 开口 left + 最大码质心 x 最大，demand=2 双链全链路（守恒 14/14 + fill 灾难下限 + 带高 ≤ PLOT_SAFE_MAX_Y_MM + 整带开口 left）；分层纯度 AST（模块级 import 白名单 + 相对 import 视同层 + 源无 materialsorting.web/..web 引用）。US-015 填料混带 20 项已随 2026-08-22 简化删除 |
| `test_waist_band_ws.py` | US-011 `web/routes_ws` band WS 编排（8 项，TestClient 套路镜像 test_ws_stop；2026-08-22 简化后现行口径）：band 关闭四变体直收 manifest（无 stage 旧行为）；开启全链路 stage→manifest（无 WB_）→frames/final 副本守恒（成员 pid 按 demand 出现 N 次）；manifest on/off 一致性（total_area/pieces 逐字段，exclude_labels 只跳 Item 层）；服务端校验三参数 → 结构化 error 早退（label 格式/不存在/数量全 0）；`_parse_band` 纯单元（返回恰 `{'label'}`，多余键静默忽略，None → None）；总副本 1 → DegenerateBand 只投 error 不投 manifest；band 阶段 stop 无存活子进程（v2 成带毫秒级，stop 用例换 7 码×8 副本弧形腰片撑 ~2s 构造窗口）；`exclude_labels` Item 层单元。预演路由/ack/fillers 用例已随 2026-08-22 简化删除 |

## Windows multiprocessing 测试注意

- **目标函数必须顶层**：`multiprocessing.Process(target=...)` 在 Windows spawn 模式下 pickle target；闭包 / 局部函数不可 pickle。`solve_worker` 是顶层（`materialsorting.web.solve_worker`），不是 `__main__`。
- **不在模块级创建 Process**：pytest import 测试模块时若触发 spawn 会无限递归。所有 Process 创建在 test 函数内。
- **`__main__` 守卫**：`if __name__ == "__main__": pytest.main([__file__, "-v"])` 让脚本直接 `python tests/test_solve_proc.py` 也能跑（不走 spawn 路径）。
- **找子进程**：`multiprocessing.active_children()` 列出当前进程的活子进程；测试用它在 `on_report` 回调里定位 solve worker 并 terminate / kill（模拟 US-026 stop 协议 + 外部 crash）。
- **`exitcode` 语义**：`None`=还在跑、`0`=正常退出、`>0`=异常退出（exit(code)）、`<0`=被信号杀（`-15`=SIGTERM 即 `terminate()`，Windows 实为 TerminateProcess 但 Python 也记负数）。

## fixture 设计

- **`real_or_synthetic_pieces`**：优先读 `paths.INTERMEDIATE`（取码 28 子集加速），失败回退 `_synthetic_pieces()`（2 个简单矩形 400k + 120k mm²）。CI / 全新 checkout 无 intermediate 时也能跑。
- **`fixtures/strategy_curves_8.json`**（US-001 strategy PRD）：8 配对 seed 降采样曲线小宇宙 —— `b90s`（90s 预算 fork：SE 筛选 / uniform90 基线）与 `b180f`（180s 预算 fork：SE 延长 / race 全程）跨 fork 配对结构如实保留（端点 = 5336 实测对 (84.27,84.39)…(83.57,84.26)%；`b180f@90` = race 门值 = b90s 终值 ± 跨 fork 噪声）；每秒 ≤1 帧单调包络。`test_cli_strategy.py` 回放器消费，别手改端点值（回归断言钉死）。
- **`test_ws_stop.py` 码号动态取**（2026-08-20）：`_start_payload` 的 sizes 从 `_PIECES_STATE` 取最小码而非硬编码 28 —— 真实 intermediate 码号集随母版漂移（本机 30~40），硬编码会在母版切换后 manifest 0 片假失败；空状态回退 28。
- **`synthetic_pieces`**：总是合成数据，用于错误路径测试（避免依赖真实 intermediate 的不确定性）。
- **`_isolate_run_stats`（autouse，conftest）**：任一测试把 `paths.RUN_STATS_JSONL` 指到 `tmp_path` 临时文件 —— `run_config.main` 结束会向统计库追加一行，不隔离会把 fake solve 测试数据泄漏进真实 `out/run_stats.jsonl`（θ₀ 校准数据源被污染）。需断言统计内容的测试（test_cli_run_stats 的 `iso_env`）自己覆盖为可读路径。
