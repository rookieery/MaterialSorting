// 码号常量（与后端 nesting_bounds.DEFAULT_SIZES 一致；旧 app.js SIZES）。
// 8 码跳 32（M1787 直筒款），默认全部参与套排（SizePicker 默认勾选全部）。

/** M1787 直筒款 8 码：28..36 跳 32。 */
export const SIZES = [28, 29, 30, 31, 33, 34, 35, 36] as const;
