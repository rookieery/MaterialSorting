# Ralph Progress Log
Started: 2026-08-09






























54. **[2026-09-22] prd-machine-state-file US-001 — state-file 端点：msn 装配与守恒序列化（三期开篇）**：`web/machine.py`（925→1223 行）+ `tests/test_web_machine.py`（1335→1624 行 46 例）。①**`GET /api/machine/solve/{task_id}/state-file`**（第六端点，无请求体）：task_id → `application/gzip` 附件（CD 中文/ASCII 双写同 state_save，文件名 `<原上传名>_状态_<yyyymmdd-HHMMSS>.msn`，ASCII fallback `<原上传名>_state_` / 非 ASCII 源回退 `nesting_state_` 前缀）—— 与浏览器 `POST /api/state-save` 产物**同构可恢复**（schema v1 零变更、`web/statefile.py` 零改动：`build_state_document`/`serialize_state`/`check_placed_conservation`/`STATE_MAX_BYTES`/`STATE_EXTENSION` 导出符号纯复用，模块级 import 同 export 门面先例无环）；用户动线 = YL 下载 → 交付版师 → MS 工作台「状态恢复」上传继续人工调整。token/task_id 公共闸先行 + `_status_common(sid=,gate=False)` 状态推进（orphan 用 marker run_dir）+ touch 刷活性。②**doc 三源链 `_statefile_doc`**：① run_dir `pieces_intermediate.json` 直载（`load_pieces` 返回 doc 即 .msn doc 块含 5 层）→ ② `registry.peek` 会话快照 → ③ per-doc intermediate（doc_id 经状态槽/marker 反查 `uploads/<doc_id>_pieces/`）；三源同内容（commit 单点写入），全空 → 409「裁片数据已不可得（run_dir 与上传快照均缺失）」。**source 归一**（冒烟实测发现的真实分歧修复）：solve 状态槽新增 `source` 键（原上传名快照）—— ① 直载侧 doc.source 是机器上传落盘名 `<doc_id>.dxf`（cfg.master_dxf 重命名件，CLI commit 写 Path(cfg.master_dxf).name）、②③ 是原上传名 → 端点按状态槽 source 浅拷贝改写归一 doc 块与文件名（跨源逐字节一致 AC 的前提，state_save 产物同口径）。③**form/quantities 合成**：源 = 状态槽 start 快照（sizes/per_type/quantities/gate_mm）→ orphan 回落 `_machine_cfg_payload`（machine_cfg 全量读首个合法 dict）；`form.gate = str(gate_mm/10)`（cm 字符串 = 恢复端 `_form_gate_mm` ×10 单一口径，任务门幅 ≠ doc commit 默认门幅时 form 侧胜出；gate 不可得 → 409）；`_stringify_per_type` per_type 数值 → FormState 字符串形态（前端 collectPerType vals.d.trim() 只吃字符串、后端 `_pf` 双形态容错数值等价）；其余键满形态缺省（time=RUN_MODE_SPECS 烘焙秒数/seed='0'/multi_seed=False/seed_count='3'/band_*/prefix_* 关，恢复后表单无 undefined）；`quantities_base`/`pending_strategy_result` 不产（机器无此概念省键式）。④**run 块**：`_best_layout` 与 result/export 同源同序；placed = best.placed_items 原样（demand>1 绝不按 pid 去重）+ seed 透传 + final 按 best 可用键合成（density/density_sparrow/width_mm/elapsed 缺键省略）+ provenance `_STATEFILE_PROVENANCE` 三档映射 normal→solve / advanced→strategy_race / extreme→extreme（config:{time_total_s} 纯展示）；无任何布局帧 → **纯配置档 200 无 run 键**（error 态/starting 均 200，用户在工作台改参数自行重解）；running 期下载 = best-so-far 快照（export 同语义）。⑤**守恒闸**：run 在场时 `check_placed_conservation`（form 侧字符串形态与 build_pid_meta 数值口径等价）防御性失败 → 409「布局与数量矩阵不一致，无法生成状态文件」；生成侧防御：解压后超 `STATE_MAX_BYTES`（与 serialize_state 同 json.dumps 口径量尺寸，恢复端必 413 的文件不生成）→ 409。⑥测试（+9 例 = 46）：`_statefile_setup` 基座（done 任务 + run_dir 产物 + 会话注册 + **`_freeze_msn_time` 冻结 .msn 时间三源**：statefile.datetime saved_at + machine.datetime 文件名 ts + `gzip.time` 头 mtime —— gzip.compress 用 time.time() 写头，跨秒两次下载逐字节可比的确定性前提）；默认档金标（200 application/gzip + gzip 魔数 + CD 双写 + nest_state_<ts>.msn + `parse_state_document` 校验链全过 doc/form/quantities/run 逐块一致 + quantities_base/pending 省键）+ **会话 TTL 逐出（registry.reset）后仍 200 逐字节一致**（doc ① 直载兜底）；roundtrip（`build_pid_meta` 重算 demand 与 result 端点 manifest 逐 pid 相等 + manifest gate_mm==任务 gate_mm **1750/1800 双值抽验**（doc gate 恒 1750 commit 默认，form.gate '175.0'/'180.0' 口径胜出））；三档 provenance（normal 经 best_frame 边车回落仍出完整 run 块 / advanced strategy_race 1200 / extreme 7200 + incumbent 0.88>边车 0.5788 证来源 + final 缺键省略）；doc 三源链逐级（run_dir 无 intermediate → 会话快照 → per-doc intermediate，三源同内容逐字节对拍、三源全空 409）；source 归一（直载 doc_id 名 → 归一回 nest.dxf + 文件名前缀，pieces 不动）；纯配置档（error 态无帧 + starting 无 run_dir 均 200 无 run 键 form 满形态）；守恒失配 409 + 超限 409（monkeypatch STATE_MAX_BYTES=64）；端点契约闸（token 401 先行早于 task_id 400 / 错 token 401 / 非格式 400 / 未知 404 / DELETE 清产物后 404）。⑦验证：pytest test_web_machine **46/46** + 全套件 **1137 passed**（1128 存量 + 9，143.82s 零回归）；`py -3 -c "from materialsorting.web import machine"` 导入过（7 路由，双 AST 守卫仍在，分层未反向）；真实服务器冒烟（:8016，`out/machine_smoke_stf1/smoke_stf1.py` SMOKE_OK）——合成母版 solve（gate 1750 normal g01@28×2+g02@28×1）→ 轮询 done → state-file 200 669B（CD nest_state_/.msn 原上传名归一）→ `parse_state_document` 全过（doc.source=='nest.dxf'、form gate '175.0' time '180'、provenance solve/180、placed 3 条）→ result manifest demand 对拍 → **POST /api/state-restore 真恢复**（multipart .msn → manifest.gate_mm 1750.0、restore_demand==result_demand、placed==run.placed）→ 二次下载逐字节一致（saved_at 豁免）→ 闸矩阵 400/404 → DELETE → state-file 404；首跑冒烟发现 doc.source=<doc_id>.dxf 分歧（即②source 归一修复的由来），修复后重跑全过；服务器 :8016 PID 33812 树杀确认 port free、失败首跑任务 run_dir/marker 清理（陈年 20260921 machine_ 残留留待 7 天机会式清理）。⑧文档同步（/sync-docs 5 文件）：agent-api-reference（状态段六端点全落地 + state-file 路由行全契约 + 注记段六端点 + 契约专节头三期注记 + §0 认证六端点 + §8 表头注记 state-file 行由 US-002 补）/agent-file-map（目录树+web 章表 machine.py 1223 行三期 US-001 段）/web AGENTS（machine.py 行三期段）/tests AGENTS（test_web_machine.py 46 例⑧段）/CLAUDE.md（machine.py bullet 三期段 + 六端点族/六端点 token）。prd US-001 passes:true。
55. **[2026-09-22] prd-machine-state-file US-002 — 契约文档与 YL 透传指引（三期收官，纯文档 Story）**：`.docs/technical/agent-api-reference.md` + `README.md` + 文档同步 4 文件（web AGENTS/CLAUDE.md/business-overview），代码零改动。①**机器契约专节新增 §6.5「GET /api/machine/solve/{task_id}/state-file」自洽小节**（插在 export §6 与 DELETE §7 之间，沿用 WS 节 §1.5 小数插节先例 —— §8 错误码/§10 流程编号不动）：用途（.msn 与 state_save 同构可恢复、用户动线 YL 下载→MS「状态恢复」续作）+ 无请求体/内部自行推进状态/只读幂等（两次下载内容一致仅 saved_at 与文件名 ts 随墙钟）+ 响应契约（200 application/gzip 魔数 1f 8b、CD 中文/ASCII 双写 RFC5987、文件名 `<原上传名去.dxf>_状态_<yyyymmdd-HHMMSS>.msn` + ASCII 侧 `_state_` 后缀/非 ASCII 源回退 nesting_state_/中文空 source 回退「排料」前缀、状态槽 source 归一）+ 任务态语义表（done/stopped 完整档；running = best-so-far 快照 export 同语义；starting/error 未产帧 = 纯配置档 200 无 run 键有价值非降级；**判据 = 有无布局帧与任务态正交**，error 中途已产帧仍带 best-so-far run）+ 错误码触发条件专表（401 token 先行/400 task_id SID_RE/404 未知·外族·已 DELETE·重启丢墓碑/409 四路 = 裁片数据不可得·门幅不可得·守恒防御·解压超 STATE_MAX_BYTES 20MB）+ **.msn 五块服务端来源表**（doc 三源链会话 TTL 逐出后仍可下载逐字节一致/source 归一；form 状态槽→machine_cfg 回落、gate=任务实际门幅 cm 恢复端幅宽单一来源、其余键满形态缺省；quantities 原样 null=全 1；run `_best_layout` 同源同序 placed 绝不去重 + provenance 三档映射；省键块 quantities_base/pending 不产 schema v1 省键式）+ 密度物理口径警示沿用（run.final.density 物理毛版包络口径不可与历史 erode 混比，对账以 width_mm 为锚）。②**YL 透传指引专段**：.msn 不透明字节流（YL 不解析、MS schema 升级零感知）+ 透传 Content-Type/Content-Disposition 保文件名扩展（流式转发不落盘不整体读入内存）+ 克隆既有 PLT 导出下载通道（按钮+代理+token 服务端持有同一配置项不下发前端）+ 代理超时建议 ≥30s + 用户后续路径一条链（下载→交付版师→「状态恢复」上传→编辑/微调/改数量重解/导出 PNG·DXF·PLT，恢复后会话与机器 task_id 无纠缠）+ 生命周期 404 提示重提交；配套 YL 侧规格指向 YL 仓 `tasks/prd-machine-state-file-yl.md`（D:\code\YLPatternMaking\tasks\，先例 prd-ms-nesting-integration.md）。③**§8 错误码汇总表补 state-file**（409 触发新增「裁片数据已不可得/门幅不可得/守恒防御/解压超 20MB」+ 消费端建议「产物不可恢复走重新提交」；表头撤「由 US-002 补齐」占位）+ **§10 对接推荐流程加分支**「需人工继续调整 → GET state-file（终态后及时取件，同 export 运维注记）→ DELETE」+ 步骤 4 幂等锚点改「其余五端点天然幂等（status/result/export/state-file 只读）」+ §7 DELETE 清理后 404 面 + §11 运维注记「及时 export / 下载 state-file」。④**curl 冒烟序列**（§6.5 尾，可直接复制执行）：solve multipart → status 轮询至终态 → `curl -O -J` state-file 按 CD 文件名存盘（file 命令验 gzip 魔数）→ MS 工作台「状态恢复」人工验证（密度与 status 终态 incumbent 物理口径一致）。⑤**README.md 新增「机器对接 API」节**（多会话机制节之后）：一句话六端点族（提交→三档求解→轮询→取布局/导出 PLT/.msn 取件→幂等清理 + MS_MACHINE_TOKEN 可选认证）+ 指向 agent-api-reference 契约专节。⑥文档同步（/sync-docs 4 文件）：web AGENTS（machine.py 行撤「state-file 契约由三期 US-002 补」占位 → 已落册 + 契约专节描述 五端点字段表→六端点 + 六端点族（7 路由）/六端点 token 措辞对齐）+ CLAUDE.md（machine.py bullet 契约专节括注 六端点字段表 + §6.5）+ business-overview（机器对接行补三期段：六端点全落地 + US-001/US-002 落地口径 + 1137 测试基线 + YL 配套 PRD 指向；功能名 二期→二·三期）。⑦验证：`py -3 -c "from materialsorting.web import machine"` 导入过（7 路由，双 AST 守卫经测试仍在）；pytest test_web_machine **46/46** + 全套件 **1137 passed**（141.79s 零回归；首轮一次 test_cli_strategy replay 回归用例偶发失败、隔离重跑与全套件二跑均绿 = 既有 flake 非本 Story 引入，代码零改动）。prd US-002 passes:true —— prd-machine-state-file 两故事全部完成。

56. **[2026-09-23] prd-se-ext-top3 US-001 — 引擎层候选选择与多轮串行延长编排**：`cli/portfolio.py`（1188→1299 行）+ `tests/test_cli_strategy_wiring.py`（1006→1251 行 48→53 例）。①**常量 + 纯函数**：`SE_EXT_BAND=0.005`（0.5pt 绝对百分点）/`SE_EXT_TOP_N=3`（top-3 封顶，超支设计 —— se_plan/k 筛选轮数/--time 规划锚全不变，最坏 +2 轮延长）；`se_extension_candidates(solves, band, top_n)->list[int]` 单一真相源（无 I/O 无 RNG）：冠军 = real_density argmax 并列取先执行者（与 se_champion 口径连续），候选 = 冠军密度−seed 密度 ≤ band 按密度降序稳定排序（并列按筛选执行序）取前 top_n；冠军恒为候选[0]（构造性：argmax 首个最大值在稳定降序排序必居同密度组首位）、top_n=1 恒 [冠军]、<1 防御抬回 1、≥4 带内取最靠近前 3、同 seed 重复首条计、空 solves → []。**闭边界浮点容差 `_EXT_BAND_EPS=1e-9`**：0.83−0.825 这类十进制对在 double 下算出 0.0050000000000000044 > 0.005，字面 `<=` 会误拒人设等号边界（实测 6 组十进制对全部如此）—— 1e-9（=1e-5pt）只救等号不吞真实间隔（带内外最近两档 0.0049/0.0051 差 2e-4 ≫ 容差）。②**延长段循环化**（run_serial_portfolio）：单冠军块改候选循环 —— `se_warm_plan` 前置判定每 run 一次（cfg 级结论对全部候选一致，位置从「轮内」提到「循环前」）；`warm_best_frame=True` 逐轮传递（装载点按 seed 参数化读各自 best_frame_s{seed}.json —— pipeline 零改动天然多文件，回退矩阵逐轮独立）；队列序 `len(seeds)+1+rank` 递增（round_budget/make_progress 既有 i>se_k 判定自然覆盖多轮 → 每轮预算=ext/phase=extension）；回退 notify 逐轮各打一行（格式逐字保留）；中断 break 后续候选不启动、已完成轮照常 on_seed_done 入账。③**控制器归档 additive**：`se_ext_band`/`se_ext_top` kwargs（US-002 --se-ext-top 旗标的接线点预留）+ `se_ext_seeds`（进延长段即定含冠军，中断可审计）+ `se_warm_states`（逐延长轮 {seed,warm,reason}，被中断轮无条目）；`se_warm_state` 保持冠军轮=第 1 延长轮语义（历史键连续，run_config 零改动）；`portfolio_section()` se 段 additive `ext_seeds`（champion 键保留；未进延长为 [] 与 champion=None 同判读口径）；PortfolioRun 结构不变。④测试（+5 例）：纯函数矩阵（典型 3/恰 2/4+ 取前 3/全远超带/等号边界/并列含稳定序与冠军恒[0]/top_n=1 恒单冠军/防御分支）；`_SE_TOP3_TRAJ` e2e（筛选 0.80/0.828/0.81/0.83/0.8265 → 候选 [3,1,4] 名次序串行各得完整 40s ext、三轮 _ext 产物独立成对、seed4 延长反超 0.865 → incumbent、unsupported 回退行 ×3）；`_FakeSolve` 注入面扩展（`missing_best_frames` seed 集模拟边车缺失装载点回退 / `interrupt_on` (seed,suffix) 集抛 KeyboardInterrupt）；逐轮 warm 独立（候选 2 no_best_frame 只降级该轮，config.strategy warm=True 冠军轮语义不变）；中断停在当前候选（rc=130、[中断] 行走 stderr、冠军轮 incumbent 在账、候选 3 未启动、ext_seeds 全集可审计）；控制器归档直接驱动（不经 run_config：跑满三条 se_warm_states + portfolio_section se 五键 / 中断 last_round=(7,1) 队列序递增）；既有 se 测试更新（two_phase portfolio.se 加 ext_seeds:[3]（0.82 差 1pt 超带单候选）；champion argmax 测试并列 0.80/0.80 → 候选 [0,1] 双延长 + 反超 0.815 incumbent）。⑤验证：pytest 全套件 **1142 passed**（1137 存量 + 5，145s；期间 test_cli_extreme 展开等价 / test_cli_strategy replay 偶发失败各 1-2 次 = 既有负载敏感 flake —— 展开等价测试用 `time.strftime` 在 main() 返回后重算 run_dir 名（秒级边界穿越即 miss，new_run_dir 埋点在调用时刻），隔离重跑 5/5 绿 + 换 stash 跑 pre-change 全绿 + 最终全量绿，非本 Story 引入；progress §55 已备案同类）；`py -3 -c "import materialsorting.cli.portfolio"` 导入过（AST 验证模块级仅相对 import + 标准库，不 import web）；`ms-run-config --help` 冒烟过。⑥文档同步（/sync-docs 3 文件）：agent-file-map（portfolio.py 行 +5 例 → 53 例 + US-001 段）/tests AGENTS（wiring 行 US-001 电池段）/CLAUDE.md（portfolio.py bullet 多候选句）。prd US-001 passes:true。
57. **[2026-09-23] prd-se-ext-top3 US-002 — CLI 旗标与 strategy/result/run_stats 可观测**：`cli/run_config.py`（~1010→~1219 行）+ `tests/test_cli_strategy_wiring.py`（1251→1483 行 53→60 例）+ `tests/test_cli_extreme.py`（33→34 例，互斥参数化 +1 条目）。①**`--se-ext-top N` 旗标裁决**（int ≥1 缺省 `SE_EXT_TOP_N`(3)，均 new_run_dir 前拦下不留空目录）：值域 <1（0/-1/-3）退出 1；从属旗标 —— 无策略 / race 模式下给出退出 1（须与 `--strategy se` 同给）；与 `--extreme` 显式同给 → 糖衣互斥守卫拦下（extreme conflicts 列表加 `('--se-ext-top', args.se_ext_top is not None)` —— 极限 SE 臂继承默认 top-3，展开处不写该旗标）；接线 `PortfolioController(se_ext_top=se_ext_top)`；`--se-ext-top 1` = 单冠军旧行为（A/B 哨兵）。②**启动行 + 延长轮头**：se 启动行（--quiet 也打）附「筛选密度与冠军相差 ≤0.5pt 的 seed 一并顺延（至多 top N 个），最多多花 2×{se_ext}s」（band 数字 `f'{SE_EXT_BAND*100:g}'` → '0.5' 随常量走，重标定文案自动跟随）；延长轮头按候选序 —— rank 1 =「筛选冠军」（argmax 语义不变）/ rank≥2 =「候选 i/m」（m = `controller.se_ext_seeds` 全集长）。③**strategy.json 计划态/实际态**：se 段 additive 计划态 `ext_top_n`/`ext_band`(0.005) 开跑即写；落盘抽 `_dump_strategy_json()` 闭包（单写者共用口径）；**首个延长轮启动时**（`_on_seed_start` 检测 `i == n_rounds+1`，队列序严格递增保证恰触发一次；置于 --quiet 早退**之前** —— 文件面非呈现层不随静默丢失）补写实际态 `ext_seeds`（候选全集含冠军）+ `extra_rounds`(m−1)；不进延长（R0 / 中断于筛选段）不补写，文件保持计划态（读侧 `_parse_plan` 每次轮询重读，additive 键零兼容问题）。④**result.json**：config.strategy additive `ext_top_n` + `ext_warm_rounds:[{seed,warm,reason}]`（逐延长轮实际灌入态，`controller.se_warm_states` 归档；既有 `warm`/`warm_reason` 键保持冠军轮=第 1 延长轮语义历史连续，m=1 时即全量信息）；`portfolio.se.ext_seeds` 由 US-001 引擎层归档。⑤**run_stats 行 config 段 additive `se_ext`** = `{top_n, band, candidates:[{seed, screen_density, gap_pt}]}`（`_se_ext_stats_section` helper：筛选密度 = solves 前 `len(controller.seeds)` 条 real_density（与 `se_extension_candidates` 同口径），gap_pt = 冠军密度−该 seed 密度 ×100 round 3（冠军恒 0）；2026-09-23 决议**不带**延长终值/反超冗余字段 —— per_seed `phase='extension'` + incumbent.seed join 可得，自包含到「筛选密度+间隔」= 0.5pt 阈值下轮校准唯一依据深度；进过延长段才落键）；race/legacy 行零新增键、class_key 组成不变。⑥测试（+7 例 → 60 + extreme +1 → 34）：旗标裁决矩阵（值域 0/-1/-3、无策略 / race 从属笔误）；`_FakeSolve` 加 `strategy_json_by_call` 逐调用快照（补写时机断言：`[0]` 计划态无实际态键 / `[5]` 首延长轮 solve 前已补写 ext_seeds/extra_rounds；保留 `strategy_json_at_first_solve` 兼容）；`--se-ext-top 2` 接线 e2e（候选截断 [3,1]、seed4 不延长、启动行附注、「候选 2/2」轮头、三面可观测逐键断言、se_ext candidates gap_pt 0.0/0.2 对拍）；缺省 top-3 + --quiet 口径（附注不静默、轮次头抑制、补写不受 quiet 影响）；R0 保持计划态（无实际态键 / 无 ext_warm_rounds / 无 se_ext / ext_seeds=[]）；**`--se-ext-top 1` 单冠军哨兵 stdout 逐字节对拍**（23 行全推演：启动行含附注 + 5 轮筛选头/进度/takeover + 冠军延长头 + 延长 2 帧 + warm 回退行 + digest + best + 末行；行集与旧单冠军行为同构，additive 差异仅附注文字）；race e2e 补 run_stats 零 se_ext 键断言；--help 7 策略族旗标含 --se-ext-top；extreme 互斥矩阵加 `--se-ext-top 3` 条目 + se 臂展开等价臂补多候选键断言（计划态 ext_top_n/ext_band = 3/0.005、补写 ext_seeds=[0]/extra_rounds=0、ext_warm_rounds 单条）。⑦验证：pytest 全套件 **1150 passed**（1142 存量 + 8，144.62s 零回归）；`py -3 -m materialsorting.cli.run_config --help` 展示 `--se-ext-top N`；`py -3 -c "import materialsorting.cli.run_config"` 导入过（分层无反向）。⑧文档同步（/sync-docs 4 文件）：agent-file-map（run_config.py 行 prd-se-ext-top3 US-002 段 + portfolio.py 行护栏 53→60 例）/tests AGENTS（wiring 行 US-002 段 + extreme 行补 2026-09-20 se 臂 9 例与本次 +1 → 34 例 —— 前次 se 臂落档遗漏一并补记）/CLAUDE.md（cli 行 run_config.py 句多候选旗标与三面可观测括注）。prd US-002 passes:true。
