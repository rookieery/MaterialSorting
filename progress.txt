# Ralph Progress Log
Started: 2026-08-09






















38. **[2026-09-13] prd-session-expiry-auto-recovery US-003 — 前端启动期恢复与停留期引导弹窗**：会话过期自动恢复第三故事（消费 US-001 存储 + US-002 `/api/state-recover`：启动期探测 401 自动换 sid 恢复工作状态；停留期 401 不自动恢复、弹窗引导刷新）。①`lib/session.ts` 增 `peekPersistedSessionId()` **非铸造只读**（缓存→localStorage 双查，空/损坏→null；**时序红线：peek 必须先于 clear** —— getSessionId lazy-mint 会覆写旧值，恢复链 peek→clear→get 同序由单测钉死）。②`store/uiStore.ts` 增 `sessionRecovering` 语义字段 + setter（轻加载态）。③`lib/api.ts` 三改：(a) **钩子槽** `registerStartupSessionRecovery(fn)` 模块级依赖倒置 —— api.ts 保持 store-free 约束，sessionRecovery.ts 模块加载尾自注册，App.tsx 静态 import 保证先于任何 effect 驱动的 apiFetch；(b) ensureSession 探测分支扩为 **401 三分支**：401 `code=session_expired` 且钩子在 → `await startupRecovery()`（单飞：sid 换新+recover+重探全在 once promise 内，并发 apiFetch 等终态、原请求在探测门前未发出无需重放）→ blocked 则吞、否则**裸 fetch 重探**再走 inspect；401 无 code → 不恢复不阻断放行（防非结构化体误触发）；无钩子（防御装载序缺口）→ 落回老路径阻断；(c) `triggerSessionBlock` **不再清 sid**（旧 sid 留给刷新后启动期恢复作 from_sid）+ 抽 `readSessionErrorCode`（clone 读 code，本体留给调用方）。④`lib/sessionRecovery.ts` 新建：`peek旧sid → clearPersistedSessionId() → getSessionId() 铸新 → sessionRecovering=true → 裸 fetch POST /api/state-recover {from_sid}` —— 200 → `applyRestorePayload`（stateFile.ts 零改动复用，恢复 5 store 含自动切超排 Tab）+ toast「工作状态已恢复」；404/网络错 → 静默 + toast「上次会话已过期，已开启新会话」；429 → triggerSessionBlock('session_limit')；finally 复位 recovering。**裸 fetch 仅两特权点**（api.ts 探测重探 + sessionRecovery recover，均需手工 sid 头）。⑤`components/SessionRecoveryNotice.tsx` 新建（idle→null；恢复中 → role=status aria-live=polite 顶部轻提示「正在恢复工作状态…」）+ App.tsx 接线（静态 import sessionRecovery + 渲染 Notice）+ style.css `.session-recovery-notice`（fixed 顶部、z-index 1400、绿左边框、pointer-events:none）。⑥`SessionExpiredModal.tsx` 停留期文案改版：session_expired text → 「会话已过期，刷新页面后将恢复工作状态」（title/limit 文案不变；刷新 = 启动期恢复入口）。⑦测试 **+22 例**：session.test.ts +5（peek 空库不铸造/预置原值/缓存命中/损坏 null/peek→clear→get 时序链）；api.test.ts AC8 重写（session_expired 保 sid）+2（探测 401 无钩子回退阻断恰一次探测；401 无 code 放行）；sessionRecovery.test.ts 新建 10（mockRoute 按 X-Session-Id 路由：200 全链 URL 序 /api/session→/api/state-recover→/api/session→/api/ptypes + from_sid + 新 sid 全场 + localStorage + uploadStore done + 数量矩阵实值 + 自动切超排 Tab + toast + 旗标复位；404 兜底；429 阻断恰 2 次请求无 toast；网络错；直调 from_sid 捕获/空 sid；3 并发单飞 2 探 1 恢复 3 业务全新 sid；sessionRecovering 生命周期 vi.waitFor；健康探测零 recover；**停留期**（markSessionProbedForTest→401→blocked+sid 保留+urls=['/api/ptypes']））；SessionRecoveryNotice.test.tsx 新建 3；uiStore.test.ts +2。**全量 vitest 68 文件 / 1153 例零回归 + tsc --noEmit + npm run build 全绿**。⑧浏览器验证 `scripts/us003_recovery_verify.mjs` **20/20**（短 TTL 起服 `MS_SESSION_TTL_SEC=45 MS_SESSION_MAX=6 MS_EDIT_HOLD_SEC=5`）：A 相位启动期恢复全链（上传 5336 母版→commit→g01@30 改 2→3 码 5s 求解→state-save 载荷手工 POST /api/state-checkpoint 打点（US-004 未落地的替代）→空闲 70s 过期→reload：sid 换新 + __netLog 佐证 recover from_sid=旧A + 探测两跳 401→200 + toast 工作状态已恢复 + 自动切超排 Tab（#restart 在场）+ 布局 polygon 回显 + 预览 Tab 数量矩阵实值 g01@30=2/总 111）；B 相位停留期（再空闲 70s→高级配置 apiFetch 401→阻断弹窗新文案 + ms_sid 未清 + 无新 recover 调用）+ 刷新链（checkpoint 已被 A 消费 single-use→404 兜底 toast 已开启新会话 + 干净新会话无母版残留）。**视觉数值对拍**：恢复后 nest-label "seed 0 · 80.60% · 长度 190.67 cm" == checkpoint run.final（density 0.8059803801111168 / width 1906.6954345703125mm）精确一致 + 60 polygon。**踩坑备档**：(a) 恢复后 B 相位死活不过期 —— 根因 `/api/state-recover`→rebuild_session_from_document 内含 `edit_hold.refresh(new_sid)` 把恢复会话**钉住 2h**（生产语义「恢复后会话 2h 不再过期」是 US-002 后端既定行为，测试须 `MS_EDIT_HOLD_SEC=5` 解锁「恢复后再过期」才能覆盖停留期弹窗）；(b) verify `__netLog` 经 addInitScript **每次导航重置**，跨 reload 断言须按新日志口径（恰 1 recover + 2 探测）；(c) 求解管线尾声打 checkpoint 有短 TTL 跨线竞态 —— 打点前显式 POST /api/session 续命紧邻写入；(d) A3 保存按钮/commit-status 在恢复后被 applyRestorePayload 置 idle —— 回显断言改用 `.qty-cell-input` 实值口径。⑨文档同步（/sync-docs 口径）：agent-component-map（api.ts 钩子槽+401 三分支、sessionRecovery.ts 新建、SessionRecoveryNotice、uiStore 扩、SessionExpiredModal 文案改版、App 接线）+ agent-file-map（新文件 4 行）+ web/AGENTS.md（US-003 关键约定：恢复钩子架构/peek 时序红线/停留期不清 sid/文案改版/裸 fetch 两特权点/恢复后 2h edit-hold 钉住语义/us003_recovery_verify.mjs 用法含短 TTL 前置）。prd US-003 passes:true。
39. **[2026-09-13] prd-session-expiry-auto-recovery US-004 — 前端自动 checkpoint 调度与启动清理**：会话过期自动恢复第四故事（消费 US-001 存储：前端工作台状态变更自动打 checkpoint 无需手动保存，丢失窗口 ≤ 3s 去抖；会话存活 F5 仍干净重置）。①`store/runRegistry.ts` 增 done 观察者：`RunDoneListener`/`subscribeRunDone` + **`markRunDone(rec)` 单一出口**（幂等；done 置位与通知原子、观察者异常吞）—— useSolveRun.finish 与 synthRunStore.applySyntheticRun（字段全落位后才标记，观察者读终态；registry 单条 bestRun 必命中 = 策略应用/启动期恢复完成即自动重落快照，恢复链在新 sid 下自我延续）两处接入。②`lib/api.ts` 增 `probeExpired` 旗标（探测 401 session_expired 且恢复分支接管时置位）+ `wasStartupProbeExpired()` + resetSessionForTest 一并复位。③`lib/sessionCheckpoint.ts` 新建（App.tsx 静态 import 装载接线，sessionRecovery 同款先例）：触发面五路 = formStore/qtyStore 订阅（引用比较）→ 去抖 `CHECKPOINT_DEBOUNCE_MS=3000`（常量可调）/ subscribeRunDone 观察者 → bestRun 达 done **立即**（无 lastFrame error run 与非 best 晚到 run 跳过）/ editStore 订阅识别 **save 专属写序**（savedDirty false→true 且 working 引用未换——open 必换新数组不误触）立即 / visibilitychange→hidden 立即 flush 清去抖 / pagehide → keepalive fetch 同步发起（不走 sendChain——卸载期微任务不可靠）。发送 = 一律 apiFetch POST /api/state-checkpoint（统一出口不新增裸 fetch 特权点；端点 peek 口径不刷活性）+ body=buildSavePayload() 不带 save_as + 无 doc 静默跳过 + 全异常吞不重试（SessionBlockedError/网络错/{stored:false,empty|conservation} 零处理）。**串行+合并**：sendChain promise 链保 POST 依序完成（防乱序回写）+ 载荷**发送时刻**现取 → 排队 send 在场时新 flush 跳过。`clearCheckpointAfterProbe()` 启动清理（App 挂载 probeSession().then 消费）：blocked → 跳过（429 满员快照保留）/ wasStartupProbeExpired → 跳过（快照留给恢复消费；恢复路径里 applySyntheticRun 重落快照依赖 DELETE 被跳过 = 时序红线）/ 否则 DELETE（F5 = 干净重置防幽灵回潮）；与 pagehide keepalive POST 理论乱序竞态接受为 best-effort。④App.tsx 接线（静态 import + effect .then 清理；StrictMode 双跑 DELETE 幂等）。⑤测试 `lib/__tests__/sessionCheckpoint.test.ts` **16 例**：去抖合并（4 变更恰 1 发/最新值/无 save_as）+ 重排窗口 + empty 容忍不重试 + doc 空跳过全程零请求 + hidden 立即不补发 + visible 不触发 + 求解完成立即（bestRun 门三态）+ 编辑保存立即（拖草稿不发/重开不误触）+ pagehide keepalive（blocked 闸）+ 阻断吞错 + 启动清理三态（存活 DELETE 恰一次/401 恢复不清且恢复态重落 checkpoint/阻断不清）。**踩坑备档**：beforeEach 的 resetCheckpointForTest 必须置于 store 复位**之后**（qtyStore.resetQuantities 换 quantities 引用会触发一次调度，不清掉会跨用例定时器污染）；jsdom hidden 断言 = Object.defineProperty(document,'visibilityState')+dispatchEvent，afterEach 还原 visible。⑥验证：vitest **1169 全绿**（69 文件 +16）+ tsc --noEmit + npm run build 全绿；后端未动（test_web_checkpoint 39 例 sanity 零回归）。⑦浏览器验证 `scripts/us004_checkpoint_verify.mjs` **15/15**（默认 TTL 起服不涉过期）：A2 改数量去抖一发 stored:true（载荷 g01@30=2 实值/无 run/无 save_as/sid 同源）+ A3 求解完成立即 checkpoint（最后一条含 run 块 density 0.8059803801111168 / placed 30）+ A4 F5 存活（DELETE 恰一次 + 探测 200 + 零 recover + 干净重置空态 + sid 不变）+ A5 重传同母版矩阵默认值（g01@30=1/总 110 = 非恢复态 + 无恢复 toast）。**生产实拍注记**：上传 hydrate（doc 物化默认矩阵）也排去抖，commit 未完成时该发落后端 200 {stored:false,reason:'empty'}（PRD 既定容忍路径）—— 冒烟断言按「改数量那一发」锚定而非总计数。⑧文档同步（/sync-docs 口径）：agent-component-map（US-004 专节：文件表 + 五不变量（apiFetch 统一出口/markRunDone 必经/清理门序/载荷现取/save 写序形状）+ 头部摘要行）+ web/AGENTS.md（US-004 关键约定节 + App.tsx/lib/store 三行注记）。prd US-004 passes:true。
40. **[2026-09-13] prd-session-expiry-auto-recovery US-005 — 端到端冒烟验收与文档闭环**：收官故事（US-001~004 全链「过期→刷新→启动期恢复」的端到端回归锁 + 三端点/环境变量文档闭环）。①新建 `materialSorting-web/scripts/smoke_session_recovery.mjs`（playwright，范本 smoke_state_file.mjs；**自举起服**无需手工 ms-web：spawn `.venv/Scripts/python.exe` uvicorn 起 FastAPI 于 :8010 避开常驻 :8000，env=`MS_SESSION_TTL_SEC=60`（极短 TTL；**须>深度解析+commit 单程 ~40s**，否则上传中途 401 卡死 —— 实测 45s 会竞态翻车）/`MS_SESSION_MAX=1`（P5 429 双客户端模拟）/`MS_EDIT_HOLD_SEC=5`（**必带** —— /api/state-recover 共享 rebuild 给新会话挂 edit_hold 钉住，生产缺省 2h 会挡住「恢复后再过期」相位）；退出 taskkill /F /T 树杀）；前置=static/ 为 npm run build 产物；NET_SPY addInitScript 包 fetch 记 `__netLog`（sid/方法/状态码 + checkpoint·recover 响应体捕获）+ tour localStorage 预关。②**五路径与 sid 链 A→B→C→D→E**（顺序按「checkpoint 在场时机」编排）：S1 上传 5336→commit→g01@30 改 2（总 111）→3 码 5s 求解 done→US-004 自动 checkpoint（求解完成立即含 run 块 stored:true）→过期前三面快照（数量/表单/布局）；P1 停留期交互（空闲 75s 过期→高级配置 /api/ptypes 吃 401→引导弹窗新文案「会话已过期，刷新页面后将恢复工作状态」+未自动恢复（零 recover 调用+sid 未清）→点「刷新页面」→启动期恢复：sid A→B + recover 200 from_sid=旧A + 探测两跳 401→200 + toast 工作状态已恢复 + 三面逐项对拍）/ P2 直接 F5（前置断言恢复态自动重落 checkpoint[B] —— US-004 恢复链自我延续；再过期→reload→**无弹窗**直接恢复 sid B→C + 同构对拍）/ P3 F5 存活清理（checkpoint[C] 在场→reload→探测 200 放行 + **DELETE 恰一次** + 零 recover + 前端干净重置空态 + sid 不变 C）/ P4 无 checkpoint 兜底（再过期→reload→recover **404**（快照已被 P3 清）→静默新会话 toast「上次会话已过期，已开启新会话」+无弹窗+干净空态 sid C→D）/ P5 429（重传+改数量→checkpoint[D]→空闲 95s 过期+扫描器逐出腾名额→Node fetch 第二客户端占满唯一名额 F→主页面 reload→recover 429→session_limit 弹窗「当前使用用户过多」在场；名额释放后 Node 直连 recover(from_sid=D) **200 recovered_from=D** = 429 不删快照的端到端证明；前置不成立走 checkSkip 记 **SKIP 不计失败**，PRD 允许 skip-if-flaky）。③**时序坑记档（5 条）**：(a) captureState 收尾停在预览 Tab，后续点超排 Tab 内控件前必须先切回（双 .page display:none，Playwright 对隐藏元素 click 超时 —— 首跑 6/7 翻车根因）；(b) TTL 60 下限（深度 DXF 解析 ~40s 无请求无 touch，45s 会吃掉 TTL 预算→commit 中途 401 阻断卡死 —— 二跑 30/31 翻车根因）；(c) 短 TTL 重传前必须显式 POST /api/session 续命紧邻上传（us003 A2 同款：P4 断言耗时后 D 的 last_active 已老化）；(d) MS_SESSION_MAX=1 下第二客户端建会话/coda recover 前必须等 **30s daemon 扫描器**真正逐出旧会话（resolve(create) 查 dict 计数而非惰性过期，活跃计数含未扫描僵尸；TTL+扫描余量 95s + 429 重试兜底）；(e) `__netLog` 每次导航重置 + 恢复态 checkpoint 断言用 waitCheckpoint 轮询前置门而非 sleep 硬等（求解完成 checkpoint 在探测 promise 内排队）。④验证：冒烟三跑定稿 **35/35 全绿**（报告 out/smoke_session_recovery/report.txt + 5 截图；服务确认已杀 8010 释放）；后端 pytest **891 全绿**零回归；前端 vitest **1169 全绿**（69 文件）+ tsc --noEmit + npm run build 全绿；`python -m materialsorting.web.checkpoint` 29/29 PASS；分层 AST 复核 checkpoint.py 仅 import sessions/statefile/edit_hold 无反向。⑤文档闭环（/sync-docs 口径）：agent-api-reference.md checkpoint 专节补关键不变量 #9（US-003/004 前端消费方写-恢复-清理链）+ #10（本冒烟五路径/自举 env/时序坑索引）；README「多会话机制」补「过期自动恢复」生命周期条 + 环境变量表 2 行（MS_CHECKPOINT_TTL_SEC=7200 恢复窗=墓碑 1h+会话 TTL 10min+余量 / MS_CHECKPOINT_MAX=16 FIFO ≈3MB 纯内存）；materialSorting-web/AGENTS.md 新增「US-005 关键约定」节（自举起服/五路径 sid 链/时序坑 5 条/环境变量速查/复跑指引）；server 侧 agent-file-map.md 与 web/AGENTS.md 的 checkpoint.py 行及 env 表 US-001 已落（核对无需改）。prd US-005 passes:true。
41. **[2026-09-13] prd-strategy-pending-result-checkpoint US-001 — 后端 done 结果槽与恢复响应回传**：策略待确认结果落快照第一故事（保存/恢复/checkpoint 管线认识 `pending_strategy_result` 槽，省键式 additive schema v1 不 bump；仅 done 态入槽——running 被 alive hook 滚动钉住不会过期、stopped 是人为操作基本不撞自动过期，两态均不入案，槽内无 state 字段恒 done）。①`web/statefile.py`：新增常量 `_PENDING_MODES=frozenset({'se','race','extreme'})` + `_PENDING_BEST_NUMERIC_KEYS=(seed,frame_index,elapsed,density,density_sparrow,width_mm)`；`build_state_document` 增 additive 参数 `pending_strategy_result=None`（truthy 才写键，无 pending 载荷/.msn 逐字节不变）；抽取 `_validate_placed_list(placed, pieces, *, path, form, quantities)` 共享校验器（逐条形态 id/rotation/translation/mirror + pid 全命中 + `check_placed_conservation` 守恒终检，path 参数化消息前缀 'run.placed'/'pending_strategy_result.best.placed_items'——run 路径消息加前缀后仍包含既有测试断言子串，run 块与槽**同一判据单一实现**），`_validate_run_block` 重写为委托该共享器 + final/provenance 检查（行为与消息不变）；新增 `_validate_pending_block`（恢复端全量校验：mode 枚举 fail-fast 400「状态文件损坏」/ best 六数值键 `_finite` 严格（拒 None/NaN/bool——done 态 incumbent 恒有真实值，stopped 回落可能有 null 但已排除在外；误拒走 checkpoint 静默吞、用户态零感知）/ summary 在场须 dict 展示级宽松 / placed_items 走共享校验器）+ `_check_pending_save`（保存端共享：dict→mode 枚举→best dict→placed_items 非空 list→逐条 id→守恒；state_save 与 checkpoint._store_checkpoint 同函数——**两端深度不对称**（保存端浅校验防把不可恢复垃圾写进 .msn/快照、恢复端全量校验保恢复必成）为文档化决策）；`parse_state_document` 校验链第 8 步（在场时：非 dict → 400「pending_strategy_result 须为对象」→ `_validate_pending_block`）；`state_save` handler body 增 `pending_strategy_result` 键（run 块分叉后镜像分叉：StateConservationError → 400 与 run 同文案「数量矩阵/尺码选择与当前结果不一致，请先重新求解或改回数量再保存」——改数量未重解场景 run 块与槽同生共死用户视角一个错；(ValueError,TypeError) → 400 形态非法；StateFileError → e.message/e.status；异常序 StateConservationError 先于 StateFileError（子类关系））；`rebuild_session_from_document` 响应 additive `pending_strategy_result = document.get('pending_strategy_result') or None`（/api/state-restore 与 /api/state-recover 共享函数**免费同形增益**）；`__main__` 冒烟 25→31 项（pending 夹具 + 带槽 build 省键/入档两路 + pending_bytes 往返 parse 通过 + `_tamper_pending` 六路必败：mode magic/zz_99 母版外 pid/pop() 守恒/density NaN/rotation 'flat'/summary 'ok'）。②`web/checkpoint.py`：import `_check_pending_save`；`_store_checkpoint` run 分叉后同镜像分叉（StateConservationError → 200 {stored:false, reason:'conservation'} last-good 先前好快照字节不变 / StateFileError → e.message/e.status）+ `build_state_document(..., quantities_base, pending)`；`__main__` 冒烟 29→34 项（mode='extreme' 第三枚举夹具 + 带槽 round-trip / 守恒失败 last-good / mode 非法 400 / recover 响应含 pending 透传 / response 键集含 pending_strategy_result）。③测试 **+29 例（后端 891→920）**：test_web_statefile 80→101（`_pending(mode)` 槽夹具 + `_save_msn` 增 pending 参；保存端带槽往返逐字段/省键对拍、槽守恒 400 两款（count_mismatch 无 run 独立触发 + 母版外 pid）同 run 指路文案、槽形态 400 参数化 6；恢复端带槽文件响应回传逐字段（mode=race）/无槽 → null、mode 枚举必败 + se/race/extreme 三枚举通过、placed 母版外两款（pid 不在 doc + sizes 码选过滤出 demand）、副本数≠demand（**踩坑：`placed_items.pop()` 删末条 g02_30 非首条，断言须对 g02_30**）、全量形态 400 参数化 9（best 非对象/density NaN/width_mm None/seed bool/placed_items 缺失/条目 rotation·translation·mirror 同 run.placed 判据/summary 非 dict）；build_state_document 单元扩 pending 省键/嵌入两路）；test_web_checkpoint 39→47（mode='extreme' 夹具 + 带槽 stored:true 往返对拍（run 并存互不影响）/ 无槽省键 / 槽守恒失败 200 conservation last-good 带槽字节逐字节不变（无 run 独立触发）/ 槽形态 400 参数化 5 全程无入库 / recover 响应 additive 回传逐字段（mode 路由键原样 + 新会话照常重建 + single-use 删）/ default 会话键集断言更新含 null）。④验证：pytest 全量 **920 全绿零回归**；两模块 `python -m` 冒烟 31/31 + 34/34；分层 AST 复核（checkpoint 仅 import sessions/statefile 无反向、statefile 无 server import）；前端 vitest stateFile/sessionCheckpoint/sessionRecovery 47/47（additive 响应键对前端无害实证——前端按字段名取用无精确键集断言）。⑤文档同步（/sync-docs 口径）：agent-api-reference.md（state-save/state-restore/checkpoint+recover 四专节补槽：请求 jsonc 示例/响应键/错误表行/守恒语义/manifest 不入档理由（恢复端 build_pid_meta 确定性重算单份几何）+ 测试计数 101/47 与冒烟 31/34）+ agent-file-map.md（statefile.py/checkpoint.py 两行补槽细节与共享校验器）+ tests/AGENTS.md（两测试文件行补 pending 覆盖摘要）。prd US-001 passes:true。
