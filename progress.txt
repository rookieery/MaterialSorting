# Ralph Progress Log
Started: 2026-08-09











16. **[2026-09-05] prd-edit-polish US-002 — POST /api/edit-polish 会话族端点**：编辑「智能微调」web 接线，纯后端零前端/引擎改动（polish_layout US-001 成品直用）。① `web/server.py` 新增 `POST /api/edit-polish`（紧邻 `/api/edit-hold` 同款样板）：读 `X-Session-Id`（缺省 default）→ `session_registry.resolve()`（`create=False` 缺省：过期/墓碑 401 `session_expired`、非法 400，`SessionError` 统一 `JSONResponse(e.payload(), e.status)`）→ 载荷校验 fail-fast 全家桶（body 非 JSON/非对象、placed 空/缺/非列表、条目缺 id/translation 非 2 元、**pid 全匹配才跑**——任一 pid 未匹配会话 `pieces_by_id` → 400 文案含「母版已变更？请重新求解/上传」不做部分降级（与引擎 PolishError 同文案双保险）、gate_mm 字符串/NaN/payload 与 state 两处皆无 → 400、exclude 非 dict → 400）→ gate_mm 缺省回退会话 `state['gate_mm']`（`float(payload or state)` /export 同法，NaN 用 `not x > 0` 拦）→ exclude（labels/pids 双键）/compact（US-005 预留缺省 false）原样透传引擎 → **polish 构造段 `run_in_threadpool` 执行**（prefix-preview 先例防阻塞事件循环；`from ..nesting_engine.polish import` 函数级 import——monkeypatch 引擎模块即生效）→ PolishError/ValueError/TypeError 兜底 400 `微调失败：…`（数值字段非法不炸 500）→ 成功顺手 `edit_hold.refresh(sid)`（编辑钉住与心跳同语义；`if sid:` default 不进钉住表，/api/edit-hold 同口径）→ `{ok:true, placed, report}`（placed 条数+pid 多重集守恒 = 引擎出口 Counter 终检 + 路由入口全匹配双保险）。② 新增 `tests/test_web_edit_polish.py` 14 例（镜像 test_web_edit_hold 套路 + AC 六项）：200 全链路真引擎（test_polish AC#2 可分离重合对夹具 → 重合对 1→0/report 四段七指标/恰一片最小分离 ~5mm/payload 不带 gate_mm 同证 state 回退放行守卫）、多副本 Counter 守恒、default 不钉住+sid 成功续期 hold_until≈clock+EDIT_HOLD_SEC、sid 会话状态隔离（sid 只注册 sx_30 → default 的 g01_30 在 sid 下 400「母版已变更」/default 下 200）、FakeClock 过期 401 失败不续期、非法 400、载荷 400 全家桶（NaN 走原始 JSON 字面量——httpx `json=` 序列化拒 NaN，开发期实测）、gate 回退 vs payload 优先 + exclude/compact 透传（spy 捕获引擎实参）、**run_in_threadpool 运行时线程级断言**（spy 包装 `server_mod.run_in_threadpool` 记录事件循环线程 + 引擎侧记录执行线程 → 非同一线程，比源码级断言更强的 AC 满足形态；开发期修正两处测试口径：分离 mover 是 (i,j) 下标序先试 g01 −y 非固定 g02 +y）。验证 = pytest 全量 **728 passed**（基线 714+14）+ `python -m materialsorting.web.server` 实起（端口被上一会话残留 PID 12140 占用致首试 bind 失败误报「Not Found」——taskkill 后重起）真打端点：invalid sid 400/空 placed 400/unmatched pid 400 文案逐字 + 真实母版 5336 前两片（g01_30 rot25 + g02_30 叠放）→ 200 重合对 1→0、rot 25°→0°、width/density 不变、0.027s；验收后 ms-web 已杀（port 8000 free）。文档 = agent-api-reference.md（HTTP 路由表 +edit-hold 补漏行/+edit-polish 行 + 会话速查表合并行 + /api/edit-polish 专节：请求/响应/错误表/四不变量，红字口径差异注记留给 US-004 其交付物）、agent-file-map.md（web 表 server.py 行 425→581+会话族三路由归并 + 目录树行尾）、web/AGENTS.md（server.py 行）、tests/AGENTS.md（test_web_edit_polish 行）、CLAUDE.md（web 模块清单 server.py 条目）。遗留 US-003+：前端「智能微调」按钮+对比卡+一级撤销（exclude best-effort 组装 band label/prefix pids）。
17. **[2026-09-05] prd-edit-polish US-003 — 前端「智能微调」按钮+对比卡+一级撤销**：编辑弹窗接上 US-002 端点，纯前端零后端/引擎改动。① 新建 `materialSorting-web/src/lib/editPolish.ts` —— 纯数据组装+单一请求出口：`parsePrefixMemberPids(pid,size,extra)` 组合片 pid→成员 pid 集合（**组合片权威式 `PS_{front}+{back}@{size}(+{extra}@{extra_size})`，front 段是裸 label 无 @size**（开发期首版误按全段 label@size 解析丢 front 成员修正），size 取 stats.size 补全，lastIndexOf('@') 两端任一空=畸形段静默跳过，extra.pid 直收去重）；`buildExclude(run)`（band enabled+label→labels / RunRecord.prefix→pids / 皆无→undefined 载荷省略键 —— 策略合成 run 无 WS final.prefix 记录自然省略）；`buildPolishPayload`（placed 逐字段拷贝 translation 拷断引用+gate_mm 取 manifest；无 manifest/working 空→null 不发请求）；`postEditPolish`（apiFetch POST，!ok 透传服务端 error 带「微调失败：」前缀 / 响应形态校验）。② 数据源补记录：runRegistry RunRecord 扩 `band: BandConfig|null`（useSolveRun.start 自 cfg 记录——WS 载荷本就带 band 此前只发不存）+ `prefix: FinalPrefixStats|null`（ws.ts FinalMsg 扩 additive `prefix?` 段 applyFinal 记录）；editStore 扩 `replaceWorking(items)` —— **pid 逐位守恒闸**（条数+逐下标 id 全等）通过才深拷贝整体替换 working，false=形态异常调用方按失败处理；不 save/bump/动 lastFrame = 微调结果只是编辑草稿。③ EditLayoutModal 智能微调 state 机（busy/report/error/prePolish 四态，生命周期=modal state 关窗 Inner 卸载自然清零）：handlePolish（busy 闸→postEditPolish→**replaceWorking 失败按失败处理（快照在守恒闸通过后才落——失败路径不留孤儿撤销按钮，开发期序修正）**→成功 setPrePolish(点击前 working 拷贝)+setReport；catch 统一补「微调失败」前缀——postEditPolish 错误已带前缀直显、fetch 网络层 "Failed to fetch" 等浏览器英文文案补前缀保卡内可读（浏览器验收 P5 发现））；handleUndoPolish（replaceWorking(快照)+清三态）；经可选 `polish` prop 受控下发 EditCanvas（onModeChange 同款模式）。④ EditCanvas：工具区（全览/形态 select 之下竖排）「智能微调」按钮（testid edit-polish-btn，busy 禁用+文案「微调中…」，title 说明口径与不自动保存）+ 右下 **`.edit-br-stack` 卡栈**（绝对锚点自 .edit-guide 上提、指南卡零位移）内 `.edit-polish-card` 对比卡（report||error 时渲染）：六指标行「重叠对数/最大穿透mm/旋转偏差片/Σ旋转偏差°/料长mm/密度%」前→后（fmt NaN→'—'）+ 口径脚注「物理毛版轮廓口径（与导出一致；画布红字为腐蚀后轮廓口径，数值可能偏小）」+ 撤销按钮；**整栈 pointer-events:none 不挡画布拖动热区，撤销按钮是栈内唯一 pointer-events:auto 交互点**。⑤ 测试 +22：`lib/__tests__/editPolish.test` 11（parsePrefixMemberPids 四片/size-null 只收带@段/五片/extra 去重/畸形；buildExclude 四态；buildPolishPayload 逐字段+拷断+null 案）+ `components/edit/__tests__/EditLayoutModal.polish.test` 11（只 mock lib/api：载荷形态无 exclude 键/band labels/prefix pids、成功 working 替换+lastFrame 不动+polygon points 跟随+卡六指标+脚注+撤销+状态条 1160、loading 禁重复零新增、撤销恢复+卡清空、再微调快照覆盖（撤销回上一次前非最初）、关窗 dirty 确认弃稿重开清零+working 回基线、400 error 透传+working 逐字段不变、网络错+UI 不炸、placed 错位按失败）。验证 = vitest 全量 **962 passed**（基线 940+22，64 文件）+ `npm run build` 过（302.70KB/gzip 98.86KB）+ 浏览器验收（prod :8000 + Playwright chrome headless，5336 母版 3 码+高级配置 d=3/tol=30 制造工艺余量→20s 短求解 30 片）**12/12**（out/us003_edit_polish_verify/：工具区按钮在案/loading 捕获/请求 200 placed=30+gate_mm+无 exclude 键+报告四段（overlap 48→44、rotΣ 200→170、width/density 守恒、moves=4）/卡六指标渲染/画布 points 变化片数=moves/守恒不等式/微调后✕dirty 确认/撤销 points 逐片回微调前+卡清空/再微调+保存 placed 守恒/网络注入 route.abort→「微调失败：Failed to fetch」进卡弹窗不炸按钮恢复）+ DOM bbox 几何目检（visual_check.mjs：按钮在工具区 select 之下、对比卡在卡栈与指标面板/工具区/指南卡/foot 零交集、卡上 guide 下 8px 栈距、撤销 card 内右对齐唯一 pointer-events:auto）；中途两坑：verify 脚本 createRequire 相对路径多一级 ../ 解析到 D:\code\materialSorting-web（修正两级）+ 连续验收 5 会话触发 429（edit-hold 钉住会话滞留内存——重启 ms-web 清会话表复跑）；验收后 browser.close()、ms-web 已杀（port 8000 free；残余 chrome 核实为用户默认会话不动）。文档 = .docs/technical/agent-component-map.md（lib/types/store 树行补 editPolish/FinalPrefixStats/replaceWorking+band·prefix + 文末 edit-polish US-003 落地节：文件表/六不变量（不自动保存/快照后落/exclude best-effort/口径脚注/卡栈透点/守恒闸）/验证记录；顺手补记 lib/editHold 缺档）+ materialSorting-web/AGENTS.md（lib/store/edit 树行 + 文末「edit-polish US-003 关键约定」七红线；lib 行同补 editHold.ts 缺档）。遗留 US-004+：弹窗内红字口径差异注记、US-005 compact 载荷开关。
18. **[2026-09-05] prd-edit-polish US-004 — 端到端冒烟与文档闭环**：新建 `materialSorting-web/scripts/smoke_edit_polish.mjs`（Playwright Edge 通道，模板对齐 smoke_edit_layout/smoke_prefix_extra：msedge 头选 + chrome 兜底、rawFetch/WS framereceived 抓帧、dismissTour）+ 文档同步五处。① 冒烟 24 检查七相位：S0 sid 落库 → S1 上传 5336 母版 → commit → per_type 全 g 码 d=3/tol=30（制造工艺余量重合与旋转，polish 有实事可做）→ 3 码 32/33/34 × Σdemand=30 片 20s 短求解出 final（density 0.9004）→ S2 编辑弹窗微调（请求 200 placed=30 gate=1750 无 exclude 键 + 报告四段 before/after/moves/residual + 四守恒不等式：overlap_pairs 42→38 严格下降、rotΣ 210°→190°、width 1762.039→1761.778mm 不增、density 89.707→89.72% 不降 −1e-6 容差 + moves=4 residual=61 + 对比卡六指标前→后渲染含口径脚注与撤销按钮）→ S3 撤销（画布 points 逐片回微调前 + 卡清空）→ S4 再微调确定性双跑（placed 深相等 + report 除 elapsed_sec 全等）→ S5 保存 → 导出 PLT（plt-clean 128057B PU≥100）+ DXF（542749B 含 POLYLINE 无 LWPOLYLINE）placed 双双=30 且与微调响应 placed 深相等（守恒）→ S6 band on g05 重解（30 片含 g05×3）→ 弹窗微调请求 exclude.labels=['g05'] + 带形态区域 g05 毛版 points 前后逐一不变 + report.excluded=[6,7,8] 恰覆盖 g05 实例。报告落 `out/smoke_edit_polish/report.json`（24/24 PASS exit 0）+ 截图 4 张。② 实现三坑记档：Playwright Response 无 `.postData()`（须 `r.request().postData()`）；/export 附件 body 被页内 fetch→blob 消费后网络层拿不到（exportOnce 页内 fetch 包装 `res.clone().arrayBuffer()` 捕获，__exportOrigFetch 守卫 + 用后还原）；done 相位 SolveControls 只有 `#restart`（`page.click('#restart, #start')` 兼容两态）；连续冒烟 edit-hold 钉住会话触发 429（每轮跑前重启 ms-web 清会话表）。③ 质量门 = pytest 全量 **728 passed** + vitest 全量 **962 passed**（零新增用例——纯冒烟脚本无组件改动）+ `npm run build` 过（302.70KB/gzip 98.86KB）+ `python -m materialsorting.nesting_engine.polish` 冒烟 exit 0。④ 文档同步五处 + 加一：agent-api-reference.md（/api/edit-polish 专节顶部**红字口径差异注记**：编辑画布=erode 后轮廓（碰撞可行性口径，数值恒≤物理口径）、polish 报告=物理毛版轮廓（与 /export 导出同源）；同一布局两套数值并存是设计非 bug，前端对比卡脚注同文案锚点；本期不切换画布口径 = PRD 非目标 + 节首补冒烟脚本指针）+ agent-file-map.md（web 树补 edit_hold.py 缺行——US-003 时漏档）+ agent-component-map.md（EditLayoutModal 微调区 dated 注记 + 文末 US-004 落地节：smoke 文件表/四守恒不变量/双格式导出/band exclude 锁/验证记录 + 头部覆盖行补 edit-polish）+ materialSorting-server/src/materialsorting/web/AGENTS.md（启动校验块补 /api/edit-polish curl 例含口径差异注记指针）+ CLAUDE.md（polish.py 行尾补冒烟脚本与红字注记指针）+ materialSorting-web/AGENTS.md（文末 US-004 端到端冒烟收官节：24 检查清单摘要 + 两实现坑）。验收后清理：ms-web 已杀（port 8000 无 LISTENING）、Playwright browser.close()（残余 chrome 核实为用户默认会话不动）。
19. **[2026-09-05] prd-edit-polish US-005 — 压缩回收档（compact pass，可选低优先级）**：引擎 pass ④ + 端点旗标补语义 + 前端 checkbox + 冒烟 S7，全链 additive（缺省 false = US-001~004 行为逐字节不变）。① 引擎 `nesting_engine/polish.py`：pass ④ 压缩回收（compact=True 才跑）—— 按 (minX,下标) 升序**级联**处理（左片先贴墙、右片贴新位），每片 `_slide_west_touch`（新助手函数：`COMPACT_SCAN_STEP_MM`=20 粗扫 + 40 轮二分到首个碰撞界 + `SEP_NUDGE_MM` 1nm 回退自由侧 = 贴触零交；起点已碰撞→0 / 全程自由→t_wall 贴布头 x=0；滑程墙 = 自身 minX）逐 move 走同款五道守卫（kind='compact'，`_move_ok` 复用：障碍集 = 滑移路径 x 可达 + y 带重叠的其余片，exclude 片永不动仍作障碍）+ **pass 级守卫**：收尾全图 maxX 严格变小（`COMPACT_GAIN_EPS_MM`=1e-6）才接受，否则整体回滚（items/geoms/bounds/moves 四快照恢复 = 无空隙可收时逐字节不变，AC ② 实现根基）；冒烟夹具 ⑨（三 100×160 @ x=0/130/260 gate=160 → width 360→300 + moves 全 kind=compact + 两两交集面积精确 0.0）⑩（98/196 紧密链 → compact=true 与 false 输出逐元素相同（elapsed 剔除口径）），八项自检 → 十项。② 端点 `web/server.py`：docstring 补 compact 语义（US-002 已预埋接线 `bool(payload.get('compact') or False)` 透传引擎，本故事零路由代码改动）。③ 前端：`lib/editPolish.ts` `PolishPayload.compact?: boolean` + `buildPolishPayload(working, run, compact=false)` 第三参（false 省略键 / true 才发 compact:true）；EditLayoutModal `polishCompact` state（默认 false）受控下发 EditCanvas `EditPolishUi.compact/onCompactChange`；对比卡内 density 行与 foot 之间 checkbox「回收空隙缩短料长（下次微调生效）」（testid edit-polish-compact，title 悬浮说明滑贴语义；`.edit-polish-opt` pointer-events:auto = 卡内第二个交互点（撤销按钮外），勾选态跨多次微调保持、撤销/关窗随卡清零）；style.css `.edit-polish-opt`（inline-flex gap 5 / input accent #3f8f5f / 11px #aab）。④ 测试：test_polish.py +6 例 25 项（AC① 三片 30mm 缝 → width ≤ before−29 + 落点 x≈100/200 + 几何级零重合 + 确定性双跑；AC② 紧密夹具逐元素相同（原「预留键位恒 no-op」占位测试按 AC 语义重写——旧夹具两片自由空间真有可收空隙，compact 落地后滑到墙 maxX 480→200 故必须换紧密夹具）；exclude 片挡滑移路径 → 后片贴它非贴墙；无包络收益整体回滚；纠缠片跳过；`_slide_west_touch` 三单元）+ test_web_edit_polish.py +1 例 15 例（`_PIECES_STATE` 换三片带缝夹具 try/finally 还原 → 200 + width ≤ before−29 + moves 全 compact + pid 序守恒）+ vitest +4（editPolish.test compact 省略/true 两例 + EditLayoutModal.polish.test checkbox 默认不勾无键在卡内 / 勾选→compact:true 受控保持撤销随卡清空，13+13 项）。⑤ 冒烟 S7 相位 5 检查（S7a 默认不勾 / S7b 受控切换 / S7c compact:true 载荷+placed 30 守恒+报告四段 / S7d width ≤ 非 compact 档（S6 结果）+1e-6 / S7e 本轮守恒不等式 width 不增·密度不降·重合对不增）+ s7_compact.png + report.json compact 段 → **29/29 PASS**（该 band 布局本无空隙可收：compact 1748.918 = 非 compact 1748.918 相等，AC 不等式成立）。⑥ 验证：`python -m materialsorting.nesting_engine.polish` 十项自检 exit 0；pytest 全量 **734 passed**（728+6）—— **全量必须 `PYTHONDONTWRITEBYTECODE=1 python -B -m pytest` 跑**：`test_cli_strategy.py::test_replay_regression_t3600_gain_and_miss_max` 在普通全量跑下必挂（`_at` 的 `for pt, d in curve:` 处随机炸 int unpack/空表解包/'float' context manager），单跑绿 / 纯 python 永绿 / `PYTHONHASHSEED=0` 确定性挂 / `--assert=plain` 或 `-p no:cacheprovider` 绿 —— pytest 9.1.1 断言重写 .pyc 缓存 + hash-seed 环境级 flake，**git stash 干净基树同样复现 = 预存与 US-005 无关**，-B 规避已入档；vitest 全量 **966 passed**（962+4）；`npm run build` 过（303.04KB/gzip 99.08KB）；浏览器 DOM bbox 几何目检 PASS（checkbox 在卡内 density 行下 foot 上、与撤销按钮/foot/指南卡零交集、pointer-events:auto、点击受控切换 —— Read 工具读截图仅回 CDN 链接不可直读，Playwright bbox 断言替代目检）；验收后 browser.close() + ms-web 已杀（port 8000 free）、throwaway 脚本已删。⑦ 文档：agent-api-reference.md（compact 键位节改落地语义 + moves kind 补 compact + 冒烟 29 检查 + 测试 15 例）+ agent-file-map.md（polish.py 行/`_slide_west_touch` 行/两常量/树行 + server.py 两行 compact 措辞）+ agent-component-map.md（文末 US-005 落地节：文件表/三不变量（additive 缺省零变化/pass 级严格变小/checkbox 生命周期=卡）/验证记录含 -B flake 备档）+ nesting_engine/web/tests 三处 AGENTS.md + materialSorting-web/AGENTS.md（lib/edit 树行/关键约定第 13 项 compact 档/US-004 冒烟节 29+S7/style.css 行）+ CLAUDE.md（polish.py 行 compact 落地 + server.py 行 + 冒烟 29）。prd 收官：US-004 归档 prd_archive.json、US-005 passes:true（prd-edit-polish 五故事全闭环）。
