# Ralph Progress Log
Started: 2026-08-09



- [US-003] 前端极限运行按钮与弹窗（极限运行第三块，web 前端泛化改造 + 新 3 测试文件 28 例 + Playwright 冒烟）：① 设计 = 泛化优先于复制（PRD 实现要点）—— 三层参数化而非复制三件套：strategyStore 重构为家族工厂 `createRunStore<P>(spec)`（`RunFamilySpec{base,ownsMode,netError}` → `useStrategyStore`（base=/api/strategy，ownsMode 认 undefined/null/'se'/'race'）+ `useExtremeStore`（base=/api/extreme，只认 'extreme'）双实例，start/stop/refresh/reset 行为共享一份实现）；useStrategyPoll 参数化 `useStrategyPoll(open, store)`（store 缺省 useStrategyStore 存量调用零改动）；StrategyRunModal 四态组件 ProgressState/ResultState/ErrorState/OrphanState 导出共享（ProgressState 加 `modeLabel?` 覆盖标题、ResultState 加 `extraHint?` 渲染 `strategy-result-extra-hint`，复用组件 inner testid 仍 strategy-* 勿另起一套）；② **家族过滤是命门**：后端两族共享每会话状态槽（US-002），任一端点 status 都可能报对方家族的 running（mode 透传）—— store refresh 只认领 `state==='idle' || spec.ownsMode(st.mode)`（idle 恒认领 = stuck 'starting' 复位出口）⇒ 极限在跑时高级运行入口不误染徽标、弹窗停留配置态；gen 计数器丢弃陈旧在途回包；两条 PRD 不变量守住：每端点恰一轮询实例（每族入口按钮各挂一份、弹窗自身不挂轮询，idle 态轻量 mount/open 各一次 refresh、interval 只在 starting|running）+ start 载荷与主画布同源（collectStartContext 单一实现 + 弹窗时长换算）；③ ExtremeRunButton（与 StrategyRunButton 并排 .strategy-entry-row，`.strategy-btn.extreme` 琥珀色 + `extreme-badge` 徽标）+ ExtremeRunModal（modal==='extreme_run' 单例互斥）：配置态四档预设 60/**120（默认）**/240/480 分钟 + 自定义 16~720 整数（parseCustomMinutes 越界/非整 null→置灰+提示）；预计轮数 `estimateExtremeRounds(T)=max(1,1+floor((T−602.5)/347.5))` 实时更新（60→9/120→19/240→40/480→82/16min→2，常量 EXTREME_FIRST_ROUND_S/EXTREME_PER_ROUND_S 与 cli.portfolio race_plan 同口径镜像不 import）+「实际轮数 ≥ 预测（省出预算自动多跑）」标注；**参数完全隐藏**（弹窗全文无 exploration_pct/early_termination/num_workers/quadtree_depth 字样、无模式下拉）；band/prefix 恒不写载荷键（后端在场即 400），`ctx.band!==null||ctx.prefix!==null` → 执行前置置灰 + `extreme-layout-warning`「⚠ 极限运行暂不支持腰头成带/起始端成套，请先在高级配置中关闭」；结果态「已固化实验参数（按实验结论固定，不可调）」只读提示不列值；④ 结果应用复用 NestingPage.applyStrategyResult（onApplyExtreme 同链，合成 RunRecord/导出三格式零改动），状态行按 `result.mode==='extreme'` 区分「极限/策略 run 已应用」（顶层 mode 判据，summary.mode='race' 是 CLI --extreme 内部展开不掺和）；HTTP 全走 apiFetch（store 工厂内一处）；style.css 沿 .strategy-* 族扩展（.strategy-entry-row/.strategy-btn.extreme/.extreme-preset-*/.extreme-custom-*/.extreme-rounds）无 CSS 框架；⑤ 测试 28 例（vitest 全量 775 绿 +28）：extremeStore.test 7（载荷契约无 band/prefix 键、409 文案透传、家族过滤双向（race/se/缺 mode 不认领、extreme 认领、idle 恒认领）、result 每 run 恰拉一次、stop、reset）；ExtremeRunButton.test 4（disabled prop、开 'extreme_run'、running 徽标、mount refresh 只打 /api/extreme/status）；ExtremeRunModal.test 17（公式对拍 7 点、parseCustomMinutes 7 值、渲染/默认 120/轮数 19、预设切换 9/40/82、自定义 16 可执行 15/721 置灰、四参数名零出现 + 无模式下拉、band 闸门+警告行、执行载荷等值断言 {time_total_s:14400,seed:5,gate_mm:1980,sizes:[30,32],per_type:null,quantities:{…}} 且 band/prefix undefined、disabled 三态、进度态（标题「极限运行」+大数字 86.32%+预算条+killed chip+门杀事件行+终止）、stop 端点、结果态（88.38%+应用按钮+extraHint+再次运行回配置态）、onApplyExtreme 透传、409 error 态+重试原载荷（3rd body）、orphan 清理走 stop 路由、ESC/遮罩/✕ 三关均不 stop）；npm run build + tsc 零错误；⑥ Playwright 冒烟 scripts/smoke-extreme-run.mjs（msedge→chrome，范本 smoke-band-preview）实跑全绿：上传 5336 → commit（等 `[data-testid="commit-status"].done`）→ 超排 Tab → 勾前两码（US-017 默认码号空）→ 双入口在场 → 弹窗预设/轮数对拍（120 默认 19 轮/60→9/240→40）→ 发起 60min → 进度态（标题「极限运行 · 总预算 1 小时」+徽标）→ 首帧密度 72.52% → 终止 → 结果态「已终止 · 保留终止前最优 72.67%」+固化参数提示 → 截图三张；⑦ **现场 409 互斥对拍**（一次性脚本不入库）全绿：极限 running → 高级入口无徽标 + 弹窗配置态 → 高级 start → 409「已有进行中的极限运行（或检测到遗留 marker），请先停止/清理」（后端文案带 mode 前端透传即区分）→ 终止 → 结果态复原；⑧ 排障记档（三坑，已写进 component-map/AGENTS.md）：超排 Tab 解锁联动 parse done 非 commit done（`.upload-status.done` 同时命中 parse-done 元素须按 commit-status testid 等，否则极限 start 吃 422「排料数据为空」）；Playwright fresh context = 新 sid（MS_SESSION_MAX=4/TTL 600s）连跑多轮冒烟吃 429 session_limit → 重启 ms-web 清注册表即恢复（本 session 两次）；stopped 后 result 拉取异步先闪「正在读取运行结果…」瞬态 → 断言等 strategy-result-head 落地；⑨ 收尾清理：ms-web 已杀（netstat :8000 空、无残留 python/ms-run）、遗留 marker（2026-08-27 旧 race）已清；文档同步 2 处：agent-component-map（树行 4 处更新 + 新「极限运行 US-003 落地」节：改动文件表/6 条关键不变量/实现要点含共享 testid 决策与 smoke 三坑）、web/AGENTS.md（新「极限运行 US-003 关键约定」节 8 条）；US-004（4h 三臂对拍验收与文档）留待后续 session
- [US-001] prefix 核心选码搜索 + 顶部异码补片构造（prd-prefix-extra-piece 引擎层，nesting_engine/prefix.py 1286→1295 行 + tests/test_prefix.py 40 项）：① 构造抽函数 —— build_prefix_plan 成员放置循环抽出 _place_members（4 片基座）/ _place_next（单成员同款候选 x 集 {0,ub[0],ub[2]−w,pb[0],pb[2]−w} + _slide_touch_y 贴触滑移 eroded 口径）/ _eroded_at_rot，4 片路径逻辑零变化（extra_pid=None 逐字节回归 = 既有 31 项用例不动全绿红线）；② build_prefix_plan additive extra_pid/extra_rot —— 异码补片第 5 成员：rot 缺省遍历新常量 EXTRA_ROT_CANDIDATES=(0.0,180.0) 取 union bbox 面积增长最小平手取 0.0（search 方传 extra_rot 钉死朝向防 info 与 members 不一致）；记账同权威式 tr=(xoff−b0,yoff−b1)；新守卫 PrefixError（补片无可用副本/与套装同码/非前·后幅 label）+ 5 片竖排高守卫 H≤gate_nest；gaps 扩 4 条（GAP_EPS_MM=1.0 守卫搬进 _place_members 使搜索能 catch-skip）；chunk.pid 追加 +{extra_label}@{extra_size}（PS_g02+g03@34+g02@32 形态）；H/bbox/offset 口径统一走 _raw_union（成员原始轮廓@记账位 unary_union，与竖排高守卫同源）；③ 新 select_prefix_plan(pid_meta, pieces_by_id, *, front_label, back_label, quantities, sizes, d_g, gate_nest, seed, order='interleave', trace=None) → (chunk, gaps, holes, info)：elig=eligible_sizes（空抛 PrefixError 现行文案）→ 逐 A∈sorted(elig) _place_members 基座（超高/贴触失败 skip）→ 候选池 _extra_candidates 从 pid_meta 派生（label∈{front,back}、数字码、≠A、demand≥1，front 先 size 升序）→ 逐 (片型,B,rot) 滑移 → H=_stack_height（5 片原始轮廓 union bbox 高）→ 可行 = H≤gate+1e-6 且贴触达标 → 取 H 最大者（严格 >best+1e-9，平手靠迭代序 = A 升序/front 先/B 升序/rot0 先 确定性裁决）全流程无 RNG；全无可行 → 兜底 pick_prefix_size(seeded)+4 片 build（与现行行为完全一致，info.fallback=True）；胜者 extra_rot=rot 跑一次完整管线；info={'size','extra':{pid,label,size,rotation}|None,'height_mm','residual_mm','fallback','n_candidates'}；trace 列表逐组合落 {size,label,extra_size,rotation,height_mm,feasible,reason} 供 main 候选表；④ 测试 +9 → 40 项（新 describe「选码搜索（顶部异码补片，2026-09-02 需求）」）：EXTRA_ROT_CANDIDATES 常量锁 / 最优拟合（_stack_ctx 同宽矩形夹具 = x 候选坍缩 {0} 纯竖排 H=Σheights 精确手算：6 码可行取最大 套装@38+g02@36 H=1960 residual=20、第 5 成员在顶、pid 断言、trace==n_candidates==24）/ 候选池资格（demand=0 与 null 通用码排除、B≠A，winner 回退次优 g03@36）/ 平手裁决+双跑确定性（L 夹具全组合 H 同 2094 → 选 (34,g02,36,rot0)、seed 0/7 输出 to_dict+info 全等、无 RNG）/ 兜底对拍（gate=1250 全不可行 → chunk 与手调 pick+build 逐字节一致 + fallback=True/4 成员）/ 补片 rot180 记账+包络（gaps 4 条 ≤1mm、双朝向展开包络 buffer(mitre) contains）/ build 补片守卫 4 态 / 5 成员展开黄金 rot0+rot180 手算 translations 对拍（pytest.approx 逐元素 zip —— 嵌套结构 approx 不支持曾踩）；⑤ main() 冒烟升级：选码搜索段打印逐组合候选表（A@size + label@extra rot / H / 可行·reason）+ 选定组合线 + chunk 细节 —— 5336 真实 intermediate 实测 120 组合 4.4s（<US-003 5s 红线，优化留后续）选定 套装@38 + 顶部 g02@32（rot 0°）H=1978.4mm residual=1.6mm 贴触全 0.00 退出码 0；⑥ 验证：后端全量 pytest 670 全过（669 + test_cli_strategy 时间型 flake 1 复跑即绿、与本次无 import 关系 —— 环境既有记档）；分层 AST 守卫过（prefix 无 web/cli import）；web 零改动（solve_worker 仍走 eligible→pick→build 旧链，接线在 US-002~004）；⑦ 文档同步 4 处：agent-file-map（树行 + §prefix 详节：2026-09-02 机制段/常量段 EXTRA_ROT_CANDIDATES/build_prefix_plan 行签名/select_prefix_plan 新行/main 行选码搜索段）、business-overview（prefix 行 2026-09-02 增补开工注记）、tests/AGENTS.md（test_prefix.py 31→40 项新 describe 摘要）、CLAUDE.md（prefix.py 条目 additive 尾注）；README/版师确认清单 §9/agent-api-reference 留 US-005 文档闭环统一收口

2. **[2026-09-02] US-002 — demand 部分扣减 + worker/WS 接线（分支 ralph/prefix-extra-piece，commit 即将落）**：prd.json 首个 passes:false 故事，单一故事零批处理。① web/solver.py build_instance exclude_pids 升级双形态（additive，iterable[str]=现行整 pid 跳过逐字节不变；Mapping[str,int]=每 pid 扣 n 份 Item demand=meta['demand']-n 扣尽<=0 跳过该 Item、其它 Item 与 pid_meta/total_area/manifest 逐字段不动——禁 quantities=0 口径事故防线沿用）；docstring 双形态同步。② web/solve_worker.py：_build_prefix 弃 eligible→pick→build 旧链改调 select_prefix_plan 单入口（eligible→搜索→build 三步内聚进引擎层，本函数只剩参数搬运），:136 exclude_pids 接线 Counter(m['pid'] for m in prefix_chunk.members)——5 片组合片含 1 份异码 pid 时该 pid 余量照排主解而非整跳，placed 条数守恒=全量 Σdemand；prefix_ctx/stage 消息 additive 增 extra_label/extra_size/residual_mm/fallback（兜底路径 fallback=True、无补片 extra_*=None 统一键形）；_prefix_record 增 extra/residual_mm/fallback，final 消息 prefix 段随 record 整体透传。③ routes_ws.py on_stage 白名单 +3 键。④ prefix_accept.py :265/:274 长度护栏 4→in (4,5)（US-005 回放器兼容新形态，manifest/判据零改动）。⑤ tests/test_prefix_ws.py 扩展 23→26：exclude_pids Mapping 部分扣减/过扣/Counter≡set 等价三态对拍（demand 差值 + pid_meta/total_area 全等）；fallback 路径 stage+final 新键透传；gate=2100 夹具 5 片守恒端到端（monkeypatch MS_OUT_DIR、stage size=28/fallback=False/extra_label=g03/extra_size=29/residual~40、manifest demand 不变、last_frame placed 计数 {g02_28:2,g03_28:2,g02_29:2,g03_29:2,g01_28:1} 恰=Σdemand=9、工件 5 成员 pid 含 '+g03@29'）。验证：后端全量 673 全过（670+3）；真实 5336 双码量 end-to-end（solve_with_callback_proc + accept_quantities）stage size=38/fallback=False/extra_label=g02/extra_size=31/residual=1189.373、final prefix 全键、placed=105=total_demand、manifest/frame/final 无 PS_ 泄漏、g02_31 恰 1 份（chunk 顶部补片独占，余量 0 照排边界正确）；ms-web 冷启动 200 无回归；分层未反向（prefix.py 无 web import）。文档同步 6 处：agent-api-reference（§1.5 stage 新键/§4 final prefix 段/US-003 段选码与扣减口径/preview 构造注记/build_instance+solve_worker 表行/流程图 on_stage 白名单注）、agent-file-map（4 树行+7 表行：routes_ws 431→433/solver 647→673/solve_worker 513→554/prefix_accept 783→788）、web/AGENTS（表行+US-003 关键约定节：worker 形态/manifest 不变量/消息序/工件/测试计数）、tests/AGENTS（test_prefix_ws 26 项）、CLAUDE.md（prefix.py 条目尾注改双 US 落地+数据流主线 prefix 段 select_prefix_plan/双形态扣减/守恒口径）、business-overview（prefix 行增补 US-002 落地段）

3. **[2026-09-02] US-003 — prefix-preview 换选码真相源 + 线程池 + 响应扩展（分支 ralph/prefix-extra-piece，commit 随本条落地）**：prd.json 首个 passes:false 故事，单一故事零批处理，纯后端 additive（前端类型/文案在 US-004，本故事无 SVG/UI 改动故无浏览器环节）。① routes_views.py prefix_preview 构造段重构：eligible→pick→build 旧链整体替换为 select_prefix_plan 单一真相源（与 solve_worker._build_prefix 同函数同参 —— 同 payload（sizes/quantities/params/per_type/gate_mm/seed）⇒ 与求解同选 A/片型/B/rot，预览=求解时第一列精确形态）；构造段（pdef 解析 + _resolve_d_tol + build_pid_meta + select_prefix_plan）收进局部 _construct() 经 await run_in_threadpool 整体入工作线程（选码搜索秒级防阻塞事件循环——多会话并发下主进程卡顿；会话解析/_parse_prefix 校验仍主线程先行，401/400 早退语义不变；PrefixError/ValueError 包络 catch 原样，run_in_threadpool 原样透传线程内异常）；_construct 返回 (pid_meta, chunk, gaps, holes, info)（pid_meta 供成员循环着色——首版误将其留闭包内致外层引用 NameError，自查修正）；模块顶新增 from fastapi.concurrency import run_in_threadpool，函数内旧 import eligible_sizes/pick_prefix_size/build_prefix_plan 清退只留 PrefixError+select_prefix_plan。docstring 改写：seed 说明从「seeded 随机预览与求解同码」换「选码确定性化后不再依赖 seed 对齐——搜索路径与 seed 无关恒同选，seed 仅兜底路径 pick_prefix_size 消费（缺省 0=与求解同参同选）」；响应示例加新键与 n_members 4/5 注。② 响应 additive：extra:{label,size}|null（只带两键，info.extra 的 pid/rotation 不出——补片真实 pid 在 members 自然可见，PS_ 哨兵不变）、residual_mm（round 3 与 stage 消息同精度）、gate_mm（实际参与构造门幅=payload>intermediate 回退后值）、fallback；members 循环泛型天然带第 5 片（pid/size/color/tag/polygon 零改动）；失败包络 200+ok:false 不变。③ tests/test_prefix_preview_api.py 8→11 新增 3 用例：5 片补片形态（gate 2400：n_members=5、extra={'label':'g02','size':29}、fallback=False、residual=210 矩形手算、gate_mm 回显、pids 序 [g02_28,g03_28,g02_28,g03_28,g02_29]、顶部第 5 成员 y 最大、异码色跨片型 size_color、5 片形态确定性重放、无 PS_）；兜底形态（默认 gate 1980：extra=null/fallback=True/n_members=4/residual=240/gate_mm 回退 intermediate）；预览与求解同选对拍（同 payload 直调 select_prefix_plan（=solve_worker 同参）：size/extra/fallback/residual_mm/成员 pid 序逐项一致，gate 2400/1980 双形态）。**既有 8 用例函数体逐字节零改动**（git diff 仅 + 行）：夹具 _prefix_pieces 高度 350/330→450/420 —— 基座 1740<1980<最矮 5 片 2160 ⇒ 默认门幅近满幅搜索必兜底 4 片，旧 4 片断言语义在新选码下零改动延续（5 片形态 gate_mm=2400 显式触发 H=2190 选 套装@28+g02@29）；不调夹具则原尺寸（基座 1360、5 片 1710≤1980 可行）下 ok_full_payload 的 n_members==4/tags 计数必翻车——「既有用例兼容零改动」靠夹具高度选择达成，已在夹具 docstring 记明沿革。④ 性能红线实测（验收项）：真实 5336（gate1980 intermediate 110 片 + 5336_coded_really 数量阵）经 TestClient 走完整路由端到端 4.45s < 5s（US-001 引擎层直测 4.30s 同量级；build_pid_meta 0.01s 可忽略），未超线故不做 rot0 先筛/几何缓存优化；选定与求解 worker 全等：套装@38+顶部 g02@32 H=1978.4mm residual=1.55mm 贴触全 0、无 PS_ 泄漏。⑤ 验证：后端全量 pytest 676 全过（上一故事基线 673+3）；import materialsorting.web 无误、python -m materialsorting.nesting_engine.prefix 冒烟退出码 0（选定行与 preview 一致）；分层未反向（routes_views 属 web 层 import nesting_engine 合法方向；引擎层本故事零改动）。⑥ 文档同步 5 处：agent-api-reference（路由表行 + 预览专节 4 处：seed 口径改写/响应示例 5 片形态含四新键/「主进程同步跑构造」段换线程池+真相源口径并删「US-003 将把预览换…」欠账注/关键不变量 #2 extra 只带 label-size + #4 同选对拍锁定）、agent-file-map（树行 + routes_views 表行 396→504 全述）、web/AGENTS（routes_views 表行 prefix-preview 段更新）、tests/AGENTS（新 test_prefix_preview_api 行：11 项三段覆盖 + 夹具高度沿革）、business-overview（prefix 行增补 US-003 落地段）；CLAUDE.md/README 无 prefix-preview 旧链表述（CLAUDE.md「worker/预览/CLI 同真相源」在 US-002 时预写、本故事后终成事实，零改动）
