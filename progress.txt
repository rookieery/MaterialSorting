# Ralph Progress Log
Started: 2026-08-09














22. **[2026-09-05] prd-edit-keyboard-transform US-003 — 画布与主视图镜像渲染贯穿**：US-001 纯函数层 + US-002 store 之后的渲染/载荷接线（镜像片所见即所得）。①EditCanvas 全链 mirror 参：主渲染 effect 读 working 项 `it.mirror === true` → `placementSig(mode,rot,tr,mirror)`（**签名加 mirror 段** —— 签名跳过前提是「同 rot/tr 即同放置」，mirror 翻转改变世界几何，replaceWorking 翻转镜像必须重写 5 层，漏段 = 画布陈旧）→ `applyPlacement(entry,rot,tr,mode,mirror)` 5 层 pointsStr/transformPt 第 4 参 x 取负；applyPlacement/applyEntryPlacement/commitDragPlacement 的 mirror 形参**必填**（内部链路漏传编译红），pointsStr/transformPolygon/transformPt/clampPlacement 家族保持**缺省 false**（外部零回归红线，缺省路径逐字节不变）；`clampPlacement` 第 5 参 mirror —— 钳制按**镜像 bbox**（镜像片 x 取负后 minX/maxX 与非镜像不同，测试用「非镜像钳不住/镜像钳得住」判别性夹具）；MoveDrag/RotateDrag 会话快照 `mirror0: it.mirror === true`（pointerdown 定格，setWorkingItem patch 不改 mirror ⇒ 会话内恒定），帧内三处透传：clampPlacement / DOM 5 层 / `applyEditPlacement(ep,rot,tr,mirror)` 池增量（US-001 覆写语义：缺省覆回 false → 重合指标按错几何算，精确面积断言锁死）。②NestSVG frame 路径透传 `it.mirror === true` → 5 层同参：solver 原生帧永无此键 → 缺省路径逐字节不变（回归锁）；编辑保存写回 lastFrame 的镜像副本在主视图所见即所得。③EditLayoutModal prePolish 快照显式字段 map 补 mirror（omit-when-false）：逐字段重建漏键 = 撤销微调静默丢镜像标志。④editPolish.ts buildPolishPayload placed 项 mirror omit-when-false（PolishPayload.placed 类型加 mirror?: boolean）—— 恒发 mirror:false 会红掉 EditLayoutModal.polish 精确锁键集用例（该用例零改动保持绿是验收项）。⑤单测 +11（994→1005）：EditCanvas 镜像 describe 5（5 层手算对拍/mirror+rot90 复合/签名双向翻转重写/镜像 bbox 钳制判别/镜像片拖动池增量精确面积 196000.0mm²·深度 10.0mm）+ NestSVG mirror 帧 3（5 层手算/复合/solver 原生回归锁）+ editPolish 2（精确键集 toEqual/mirror:false 省略键）+ EditLayoutModal.polish 1（已保存镜像布局 → 载荷恰一项带键·引擎关镜像 → working 镜像消失·撤销 → 快照恢复 mirror + 画布毛版 points x 取负对拍）。⑥验证：vitest 全量 **1005 passed**（994+11，64 文件）+ `npm run build`（tsc+vite）过（303.36KB/gzip 99.23KB）、static/ 已重建；**浏览器 dev 模式 15/15**（out/usk003_mirror_verify：上传 5336 → 3 码 30 片短求解 + solver 原生帧无 mirror 键 + 画布基线与页面内 pointsStr 直算逐字节一致 → `page.evaluate` 动态 import editStore replaceWorking 翻转片 K mirror（故事 AC 认可的「人为改 store」，US-005 键盘未落地）→ 画布镜像渲染 + 其余 29 片逐字节不变 + 镜像片拖动不炸 → 保存 → lastFrame.mirror=true + 主视图恰 1 片 points 变化且与直算一致 → 重开保持 + 微调载荷恰一项 mirror:true；DOM↔纯函数逐字节对拍强于目测，3 截图归档）。⑦文档同步：materialSorting-web/AGENTS.md（nests/edit/lib 三树行 dated fragment + 文末「edit-keyboard US-003 关键约定」节：全链 mirror 参/签名 mirror 段/会话快照 mirror0 三透传/NestSVG 透传/prePolish 补键/载荷 omit-when-false/验证门）+ .docs/technical/agent-component-map.md（文末 US-003 落地节：文件表/四不变量/验证记录含浏览器 15 检查项明细）。prd US-003 passes:true。
23. **[2026-09-05] prd-edit-keyboard-transform US-004 — 后端导出与微调引擎 mirror 支持**：前端 US-001~003（数据模型/store/渲染）之后的后端闭环——镜像片在 /export 三格式与 /api/edit-polish 全部按正确镜像几何。①`web/export_geometry.py`：`apply_transform(polygon, rot, tr, mirror=False)` 加缺省 False 第 4 参（`x0 = -x if mirror else x` 一行式：先局部 x 取负再旋转，`world=R(θ)·diag(−1,1)·p+t`，与前端 transformPolygon 同输入输出逐点相等）；`_transform_normal(nx, ny, rot, mirror=False)` 法线按**向量**反射（nx 取负再旋转，无平移项）；`placed_to_world` 读 `it.get('mirror') is True` 贯穿全部 5 层（毛版/净版/内部线点变换 + notch 点按点变换·法线按向量镜像 + 布纹线两端点）—— /export 路由零改动（placed 键直通）。②`nesting_engine/polish.py`：`_world_geom` 与 pass ② derotate 共用「local=[(-x,y)] 预处理 + 标准 `_transform_polygon`」（c_local 用**镜像后**多边形质心，t' 补偿公式不变；基线 {0°,180°} 不受影响——diag(−1,1) 保持 x 轴方向）；诊断/③分离/④compact 全按镜像世界几何；items 构造、compact 快照、出口三处重建统一走新增 `_rebuild_item`（**omit-when-false**：mirror 只在 True 时带键——恒发 mirror:false 会红掉前端精确锁键集用例；快照丢键 = compact 回滚路径镜像标志静默蒸发，专项测试锁死）；无改进返回输入 list 原对象（含 mirror）逐字节不变；五道守卫与 Counter 终检零改动。③`server.py` /api/edit-polish 仅 docstring 补 mirror 键说明（校验宽松键直通，零代码改动）。④测试 +10（后端 735→745）：test_polish +6（25→31，镜像 L 形非对称夹具 `_l_piece`——右上空缺角镜像后到左上、矩形镜像平移可等价判别力不足：`_world_geom` 手算对拍 rot0 精确/rot90 近似、镜像斜片 25° derotate+mirror 透传+质心锚定、判别性诊断分离（小方块落「镜像后才有材料」空缺角对侧 → before overlap=1 漏 mirror 则 0 + 终态交集精确 0 + 键集双向）、no-op 原对象、compact 镜像贴真实右缘落位手算、compact 回滚不蒸发 mirror）+ test_web_edit_polish +4（16→20，mirror 载荷 no-op 逐位透传精确键集 + 真引擎判别夹具（`_PIECES_STATE` 换 L+方块 try/finally）+ export 侧三对拍：apply_transform mirror 公式手算/缺省=显式 False=旧公式逐字节、_transform_normal 向量反射、placed_to_world 5 层贯穿 rot90+mirror 逐项手算 + 无键=显式 False 全等）；冒烟自检扩为**十二项**（⑪ 镜像斜片 derotate+透传+质心锚定 / ⑫ 镜像片 no-op 原对象）。⑤验证：`python -m pytest` 全量 **745 passed** 零回归（既有用例零改动——两测试文件 diff 全为纯新增）+ `python -m materialsorting.nesting_engine.polish` 冒烟 exit 0（十二项全 PASS）+ `from materialsorting.web.server import app` 导入通过、AST 分层守卫不动（polish.py 零新 import）；本 story 无 UI 面（缺省路径逐字节不变），浏览器验证与 E2E 冒烟归 US-005/US-007。⑥文档同步：agent-file-map.md（polish.py 树行+专节 US-004 dated fragment+polish_layout/main 行+`_world_geom`/`_rebuild_item` 新行、server.py 树行 edit-polish 补 mirror、export_geometry.py 树行）+ agent-api-reference.md（/export payload placed 加 mirror 注释行、apply_transform 签名行+_transform_normal 新行+placed_to_world 行 mirror 说明、/api/edit-polish 请求 jsonc+mirror bullet+测试计数 20 例）+ 后端三 AGENTS.md（nesting_engine polish 行 / web export_geometry 行 / tests 两行 dated fragment）。prd US-004 passes:true。
24. **[2026-09-05] prd-edit-keyboard-transform US-005 — 键盘变换交互（L/K/空格/O/I）**：US-001~003 mirror 地基之上的编辑键盘交互层（已定案 2026-09-05：L=顺时针 +1°、K=逆时针 −1°、Shift+L/K=±10°、空格=180° 掉头、O=水平镜像=toggle mirror（rot 不变）、I=垂直镜像=toggle mirror+rot+180（diag(1,−1)=R(180°)·diag(−1,1) 复合律共用单 mirror 标志）；零后端、零渲染公式改动，全复用 US-003 落地链路）。①EditCanvas：window keydown 挂**指针交互同一 effect**（复用闭包 commitDragPlacement/refreshMetrics/updateHandle，监听器仍只挂一次 —— PRD 文字「新增 window keydown effect」按精神落为同 effect 内新 window 监听，理由头注备档）；守卫链三段任一命中零变换：`interactionRef.current` false 总闸 → target ∈ INPUT/SELECT/TEXTAREA/BUTTON 或 isContentEditable（键盘归表单控件，含画布左上形态 select；tagName 大写比较防 SVG 小写 tagName）→ 无选中片；单字符键统一小写（Shift+L 的 'L'/CapsLock 同键命中）；L/K 放行 e.repeat（auto-repeat 每击一档 = 按住连转）、幂等键（空格/O/I）忽略 e.repeat 防抖、空格 preventDefault 防 body 滚动；`applyKeyTransform(index, rot, mirror)` 质心锚定唯一出口：`t' = c_world − R(rot')·M(m')·c_local`（c_local/c_world = base/当前世界多边形顶点均值；质心是仿射量 ⇒ 零平移变换后质心恰 = R·M·c_local，片原地变换不漂移）→ clampPlacement（带 mirror 参，Y∈[0,gate]/minX≥0 与拖动同口径，右界永不钳）→ commitDragPlacement 泛化路径（DOM 5 层 + setWorkingItem + 池增量 + 指标/手柄，O(单片)）；commitDragPlacement 加可选 `mirrorPatch?: boolean`（缺省 undefined = 不改 mirror，指针会话零回归；键盘 O/I 显式传目标值）；新 prop `interactionEnabled?: boolean`（缺省 true，`interactionRef` 渲染期回写 modeRef 先例 —— 监听器不重挂、确认层开关即时生效）；R 键分支已留（return 空）接线归 US-006；指南卡新增键盘行「键盘：L/K 微转 · 空格 180° · O 水平镜像 · I 垂直镜像 · R 重置此片」（「形态」「保存」反向锁不破）。②EditLayoutModal：传 `interactionEnabled={!confirmDiscard}` —— ✕ dirty 确认层打开 = 画布全键禁用（确认层按钮/回车不被画布键劫持；指针交互不受影响，确认层 overlay 已挡 pointer 热区）；编辑弹窗仍不挂 ESC 关闭（仓库红线不变）。③editStore：setWorkingItem patch 扩可选 `mirror?: boolean`（US-002 节预告「届时扩 patch」落地）：显式 true 带键 / 显式 false 键消失（omit-when-false）/ 缺省保持现值。④单测 +11（1005→1016）：EditCanvas 键盘 describe 10（L/K/Shift 精确值 + 质心不漂移（世界多边形顶点均值回 c_world）+ DOM points 手算 r2 对拍 + 整型往返回原位 / clamp 两界（minY 上抬 + minX 右推闭式 500·sin1°）+ 旋转手柄 cy 同帧随质心上移 4.325 / 空格 rot+180 + defaultPrevented + 同足印 / O toggle 双向 + 大写 O 同键 + 'mirror' in w[0] 键消失 / I 复合律 (600,750) / 键盘 O 池增量实时指标 164000→200000 mm²（锁 applyEditPlacement 显式传 mirror 接线）/ e.repeat 规则（L×2 → 2°；空格/O/I repeat 全忽略）/ 守卫链四条：真实形态 select + input/textarea/button/contentEditable（Object.defineProperty isContentEditable）五类控件零变换、无选中零变换、interactionEnabled=false 全键禁用 + 恢复 true 后同键生效（ref 读现值））+ editStore patch.mirror 三态 1。⑤验证：`npx tsc --noEmit` 干净 + vitest 全量 **1016 passed**（1005+11，64 文件）+ `npm run build`（tsc+vite）过（304.77KB/gzip 99.65KB）、static/ 已重建；**浏览器 dev 模式 26/26**（out/usk005_keyboard_verify/verify2.mjs + report2.json + 3 截图：P1 上传 5336 → 3 码 30 片短求解 + 弹窗 + 指南键盘行在场无回潮；P2 真实 trusted 按键 L/K/Shift+L/Shift+K —— 页面内动态 import editGeometry/editStore 纯函数直算期望 placement，DOM↔纯函数逐字节对拍（内容匹配恰 1 片）+ 指标面板旋转角同帧刷新；P3 空格×2 掉头往返/O×2 镜像双向/I×2 垂直镜像复合往返（v2_p3c_mirror.png/v2_p3e_vmirror.png 归档）；P4 连发 20 次 repeat:true L → +20° 耗时 78.1ms（<3000ms 不卡顿）+ 空格/O/I repeat 全忽略；P5 守卫链：形态 select 聚焦/无选中/✕ dirty 确认层全键禁用/取消确认层后 L 恢复生效（interactionRef 渲染期回写，前轮失败点修复）；P6 全程无 pageerror）。⑥文档同步：materialSorting-web/AGENTS.md（store 树行 + edit 树行 dated fragment + 文末「edit-keyboard US-005 关键约定」节：keydown 同 effect/守卫链顺序/interactionRef 回写/e.repeat 分界/小写归一/质心锚定唯一出口/mirrorPatch 三态/指南行反向锁/验证门）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-keyboard US-005 落地」节：文件表/四不变量/验证记录含浏览器 26 检查项明细）；agent-file-map.md 后端专属不适用、agent-api-reference.md 无 API 改动不适用（US-001 同款口径）。prd US-005 passes:true。
