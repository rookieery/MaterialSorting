# Ralph Progress Log
Started: 2026-08-09



















32. **[2026-09-11] prd-state-file US-002 — 恢复端点与会话重建 manifest 重算（后端）**：状态文件第二故事（POST /api/state-restore + parse_state_document 校验链，纯后端零前端零 UI = 浏览器验证不适用，UI 入口属 US-003/004）。①`web/statefile.py`（续）新增**恢复校验链** `parse_state_document(raw)`（纯函数，全 fail-fast StateFileError 400/413）：gzip 魔数 `1f 8b` 嗅探（纯 JSON 也接受，手改友好；坏 gzip → 400）→ 解压后 >20MB → 413（gzip 炸弹防线；裸上传 >20MB handler 先拦同 /api/parse-dxf 口径）→ json.loads 容错（ValueError/RecursionError → 400「状态文件损坏」；顶层非对象同 400）→ schema_version 版本门（缺失/非 int（bool 拒收）/v<1 → 400；过新 → 400 文案含双版本号「v99，本程序支持至 v1」）→ form/quantities 块形态 → doc 块逐片形态（`_validate_doc_block`：gate_mm>0 有限、pieces 非空、pid 非空唯一（pid 重复 → 400，防 pieces_by_id 静默坍缩）、label 须 gNN 1-3 位（既挡乱码更保证 _DocPieceView 母版码模式必中）、size 整数（bool 拒收；commit 从不落 size=None 片故不设 None 分支）、polygon ≥3 顶点逐点有限（NaN/Inf 拒收）、bbox 4 有限数、area_mm2 有限）→ run 块（`_validate_run_block`：placed 非空逐条 id:str/rotation:有限数/translation:[x,y] 有限/mirror 在场须 bool（omit-when-false 透传不归一）、final 在场须 dict、provenance 在场须 dict 且 kind ∈ solve/strategy_se/strategy_race/extreme 四值枚举（config 宽松纯展示）→ **pid 全命中 doc.pieces**（否则 400「状态文件内部不一致：placed 引用母版外裁片」）→ **守恒终检**（与保存端同一 check_placed_conservation；unknown_pid（码选过滤/退化石 = 未命中重算 demand）沿用母版外文案 + detail、count_mismatch → 400「placed 副本数与数量矩阵不符」；form/quantities 形态异常 ValueError/TypeError → 400 状态文件损坏））。②**POST /api/state-restore handler**：multipart 收文件（扩展名 .msn/.json 否则 400「仅支持」）→ `run_in_threadpool(parse_state_document)`（**解析在会话解析之前**：坏文件不建会话名额；测试白盒 spy 断言 calls==['parse_state_document']）→ `registry.resolve(sid, create=True)`（commit 先例：写入**当前 sid** 覆盖语义、不占 MS_SESSION_MAX 新名额——满员 4 会话恢复既有 sid 照样 200；过期 401/超限 429/非法 400 SessionError 统一映射）→ 纯内存重建：doc_id 铸新 uuid hex + `runtime._state_from_doc`（US-001 提取的 dict 直建变体）；sid 会话 `st.state = 新 dict`、default 会话走 runtime 锁内 clear+update 原子重绑（`_reload_pieces_state` 同法但不读盘）；**不镜像写 paths.INTERMEDIATE、不落盘 uploads**（他人文件不污染本机事实源，设计 §一.7 定案；pytest 字节不变断言）→ manifest 确定性重算 `build_pid_meta(doc.pieces, form.sizes, per_type, quantities)`（params 缺省全 0 = web 口径；含 raw_polygon/d_mm 物理毛版，routes_ws.on_manifest/strategy result 同形逐键）→ parse 载荷复用 `parse_payload._build_parse_payload`：新 `_DocPieceView` 属性视图（doc piece dict → polygon_mm/5 层/size/block_name/piece_index/group_key 十属性 slots）喂同一函数，**block_name = 已存 label** → assign_codes 走母版码复用模式必中且码内唯一（pid={label}_{size} 唯一 ⇒ 码内 label 唯一）→ parse 载荷 label 与 doc/manifest/quantities 键同源零漂移（校验链 gNN 门是此不变量的前置锁；码内展示顺序按 (code_sort_key, centroid/area) 与原上传预览可能不同序 = 展示级差异不承重）→ 成功 `edit_hold.refresh(sid, clock)`（default 豁免不进钉住表，edit-polish 同口径）→ 响应 `{doc_id, filename, parse, manifest, final, placed, run, form, quantities}`（final/placed = run 块摘出、无 run 纯配置档均 None；**run 块 additive 整块透传** —— seed/provenance 供 US-004 前端来源写回，故事响应清单的超集键，响应内几何三份 ~2-3MB 本机/局域可接受，文件仍单份几何）。③路由注册 `register_statefile_routes` 增 `/api/state-restore` 行（server.py 零改动 —— US-001 已挂注册函数）。④`__main__` 自检 7→**18 项**（新增：gzip/纯 JSON/无 run 三路 parse 通过、坏 gzip/坏 JSON 必败、v99 双版本号文案、placed 越界/副本数不符/provenance 非法三路 400、_DocPieceView parse 载荷 label 同源 + 5 层透传、manifest 重算 demand/total_area/n_eroded）。⑤`tests/test_web_statefile.py` 23→**70 例**（US-002 47 例，本仓首个 multipart `files=` 测试）：200 完整载荷（doc_id 铸新 uuid≠文件原 id、parse label 同源 + 5 层、form/quantities/final/placed/run 原样往返含 mirror）/ manifest 与 build_pid_meta 同 form 重算**逐字段对拍**（id/size/color/area/polygon/raw_polygon==intermediate 原始 polygon/d_mm/label/demand/5 层 + gate/total_area 含 demand 乘数/n_eroded）/ per_type d=2 erode（polygon 腐蚀≠raw、raw_polygon 原样、d_mm=2、n_eroded=1 = 物理毛版口径 round-trip）/ 纯 JSON .json 扩展名 + 无 run 纯配置档 / 校验链全家桶（坏 gzip/坏 JSON/schema_version 缺失·'1'·True·v99 双版本号/解压后 >20MB 413（21MB padding gzip 后 <1MB 炸弹形态）/裸上传 20MB+1 413/doc 块 11 路参数化（gate 缺·0/pieces 空/缺 doc/polygon 2 顶点/NaN 顶点/pid 重复/label 乱码/size 字符串/bbox 3 元/area None）/run 块 7 路参数化（placed 缺·id 缺/rotation 字符串/translation 单元/mirror 'yes'/final 数值/provenance 字符串）/扩展名 .dxf·.txt 拒绝）/ placed pid 越界·副本数≠demand（detail 含 pid）·码选过滤 sizes=[32]·provenance kind 非法（**四合法枚举 + config 宽松同通过** round-trip）/ **threadpool spy**（monkeypatch statefile.run_in_threadpool 断言）/ default 恢复 runtime 原子重绑（st.state is _PIECES_STATE 同一 dict + doc_id/pieces_by_id/gate 断言）且 **INTERMEDIATE 读字节 + uploads rglob (size,mtime_ns) 快照前后全等 = 不落盘断言** / 满员 4 会话 + 新 sid 429 对照 → 恢复既有 sid 200 = 不占名额 + st.doc_id 写入 / 过期 sid 401（FakeClock 先装再建）/ 非法 sid 400 / **sid 隔离**（A 恢复后 B 仍空会话：save 422 + ptypes 空 reps）/ **端到端**（恢复后该 sid /api/ptypes = label_representatives 透传、/export dxf 200 application/dxf、/api/edit-polish 200 且 polish 出口 Counter(placed) 守恒）/ **链式传递** save→restore→save→restore 二次往返（两代 .msn 的 form/quantities/run 逐字段全等、doc 弹掉 doc_id 后全等、两代响应 manifest/placed/final 全等、parse 弹掉 doc_id 后全等 = 确定性重算）/ 路由在场白盒；夹具 _doc() 补 label_representatives 真实条目（ptypes 断言载体）+ _save_msn/_restore/_edit_msn（解包改 document 重打包的手改文件构造器）三助手。⑥验证：pytest 全量 **818 passed**（771 存量 + 47 新增零回归；首轮 1 例 test_cli_extreme 时间戳断言偶发失败、复跑与隔离均绿 = 与本次改动无关的既有 flake）+ `python -m materialsorting.web.statefile` 自检 18/18 + 分层依赖 AST 复核（下层三包零 web import；statefile 兄弟 import = parse_payload/routes_views/runtime/sessions/solver + 函数内 edit_hold，无环 standalone import ok）+ 纯后端无 SVG/坐标/可视化改动 → 浏览器验证不适用。⑦文档同步（/sync-docs 口径）：`.docs/technical/agent-api-reference.md`（端点计数 19→20 + 路由表 +/api/state-restore 行 + 新专节：multipart 请求/响应 jsonc/错误表（11 行全场景）/关键不变量 8 条 —— threadpool、纯内存不落盘、覆盖语义、manifest 确定性、parse 载荷复用、端到端可用、链式传递、47 例清单；state-save 专节不变量 5/7 同步改 import 面 + 测试计数）+ `.docs/technical/agent-file-map.md`（statefile.py 行扩 US-002 全貌）+ `web/AGENTS.md`（启动校验段 +restore curl 与自检 18 项注记 + server.py 行双路由 + statefile.py 行重写）+ `tests/AGENTS.md`（test_web_statefile.py 行 23+47=70 例全貌）+ 根 `CLAUDE.md`（web 段 state-save 句后接 state-restore 一句）。prd US-002 passes:true。
33. **[2026-09-11] prd-state-file US-003 — formStore 重构与保存/恢复前端入口**：状态文件第三故事（前端入口：FormState 外提 formStore + 导出下拉「状态文件（.msn）」独立 /api/state-save + 上传口 .msn 分流 /api/state-restore 最小处理；applyRestorePayload 恢复编排属 US-004）。①新建 `src/store/formStore.ts`：FormState 13 字段模块级 zustand 单例（`patch` 合并 / `hydrate(form, token)` = `{...DEFAULT_FORM, ...form}` 缺省合并 + 记 token / `resetForDoc(docId)` = token 匹配 → 保留 form 不消费（**水合优先于重置** + StrictMode 双 effect 幂等；US-004 applyRestorePayload 以 restore 响应 doc_id 作 token，mint 新 id 每次不同无误匹配）/ 否则 DEFAULT_FORM + 清 token / `reset`）。②新建 `src/types/stateFile.ts` 契约类型（RunProvenanceKind 四枚举/RunProvenance/RunOrigin/StateRunFinal/StateSaveRun/StateSavePayload/StateRestoreResponse——manifest `Omit<ManifestMsg,'type'>` 与后端恢复响应同形）。③新建 `src/lib/stateFile.ts` `buildSavePayload(): {form, quantities, run?}`：form ← formStore 原样；quantities ← qtyStore **全量扁平化不按码选过滤**（与 WS serializeQuantities 的有意分歧——状态文件跨机完整还原未勾码数量，后端按 form.sizes 自行过滤 demand；空 → null）；run 仅 `done && lastFrame` 的 bestRun（stopped done=true 同中间方案导出先例；running 不存）→ placed 深拷贝原序（同 pid 多副本多条、translation 数组拷断不别名、`mirror===true` 才带键 editStore.deepCopyItems 同口径）/ origin 缺席不写 provenance 键（后端缺省 'solve' 老文件零惩罚）/ final 摘要 n_frames=frames.length、n_eroded=manifest?n_eroded:0。④`runRegistry.RunRecord` 增可选 `origin?: RunOrigin`（US-003 仅类型锁，写入方 = US-004 applyStrategyResult/applyRestorePayload）。⑤`lib/download.ts`：ExportFmt 扩 'state' + EXPORT_FORMATS 追加 `{value:'state',label:'状态文件（.msn）'}` 排 PNG 后 + DEFAULT_EXPORT_FMT 不动（仍 'plt-clean'）+ parseContentDisposition fallback 'state'→nesting.msn（**state 是唯一不走 /export 路由族的格式**，注释标注例外）。⑥`hooks/useExport.ts` 增 `saveState()`（无参——文件名后端从会话 doc.source 生成 `<母版名去.dxf>_状态_<ts>.msn`，与 exportAs 的 filename 载荷键不同；body = buildSavePayload() JSON 原样；与 PNG/DXF/PLT **共用 exporting state/ref 单一防连击旗互斥**；StatusLine 三态同一 onStatus：正在生成 状态文件 …/已导出 xxx.msn/导出失败：{error}）。⑦`ControlPanel.tsx`：useState\<FormState\> → `useFormStore((s)=>s.form)` + `useEffect([docId])` → `resetForDoc(docId)`（重传/首传（undefined→id）重置、切 activeSize 保留——**表单行为与重构前完全一致，既有 vitest 零改动全绿 = 硬红线**）+ handleExport `fmt==='state'` → `void saveState(); return`（不进 openModal('export_info')，14 字段手输面板对状态文件不打开——手输字段是本机记忆不入文件）。⑧`ExportButtons.tsx`：fmt==='state' 底部说明行切换保存范围文案（partial 警示优先级更高）；disabled 沿用 hasLastFrame 同口径（「纯配置档」UI 不可达 = 契约注记，端点层容忍无 run）。⑨`useParseDxf.ts` upload 分流：`.msn` 后缀（小写比较，MIME 不判）→ `restoreStateFile(file)` POST /api/state-restore（multipart file 字段，apiFetch 注 sid）→ 成功 toast「状态文件校验通过，恢复编排将在下一 Story 落地」+ status 回 idle（doc 不写/commit 不触发/错误走 toast 不进 uploadStore.error 红字——toast 是该路径统一提示通道）+ 失败 toast「状态文件恢复失败：{error}」+ uploading 期间清 commit 残留；`.dxf` → `uploadDxf(file, commit)` 原逻辑独立成函数**行级零变化**（首调 /api/parse-dxf 回归锁）；防连击 uploadingRef 共用。⑩`UploadPanel.tsx` accept `.dxf,.msn` + 校验/文案双扩展名放行（大小写容错同 .DXF 先例）。⑪测试 **+44 例**（vitest 1081→**1125 passed** 67 文件）：formStore.test.ts 8（patch 合并/不可变/resetForDoc 默认含 undefined/hydrate 即时+token/缺键 DEFAULT 合并/水合优先/同 token 幂等/异 docId 重置+清 token/双写序收敛）+ stateFile.test.ts 12（quantities 全量未过滤+显式 0 保留+空→null/run 门控无 run·未 done·无 lastFrame 整键缺席/final 摘要逐字段/placed 原序+重复 pid+mirror 键集/深拷贝不别名/manifest 缺 n_eroded 0/bestRun 最高密度/origin→provenance 三态）+ download.test.ts +4（state 项排 PNG 后+label/默认值不动/RFC5987 `_状态_` 中文 round-trip/fallback .msn）+ ExportButtons.test +3（state 说明行文案/partial 优先/disabled 同口径）+ ControlPanel.test 状态分支 3（POST /api/state-save body=buildSavePayload 断言 form.gate/quantities null/run.placed 原序 + 不走 /export + 无 strategy-modal + StatusLine 两态/失败 400 结构化 error 文案/exporting 互斥 state 在飞时 PNG 不发第二请求）+ useParseDxf.test .msn 分流 7（POST state-restore FormData file 字段单 fetch 不经 parse-dxf/大写 .MSN/成功 toast+idle+doc 不写+commit 不触发/失败 400 toast 不进 error 红字/网络错 toast/uploading 清 commit 残留+uploadingRef 复位后 .dxf 续传/.dxf 零回归首调 parse-dxf）+ UploadPanel.test +2（.msn 过校验触发 state-restore 单 fetch/.MSN 大写）；另 2 处既有断言随 options（4→5）/accept 扩容更新。⑫验证：`npx tsc --noEmit` 干净 + vitest 全量 **1125 passed** + `npm run build` 过（313.53KB/gzip 102.75KB，static/ 已重建）+ 后端 pytest 818 passed 复跑无回归（后端零改动）+ **浏览器冒烟 `materialSorting-web/scripts/smoke-state-file.mjs` 8/8 PASS**（Playwright Edge 通道，dev :5173 → 真 ms-web :8000：上传 5336_coded → commit done → 超排 5s 短求解 → state 说明行切换「保存母版快照…不含导出表格手输字段」→ 点导出下载 `5336#老六订单14%7%围加9_coded_状态_20260911-233916.msn`（母版名+状态+时间戳）+ StatusLine「已导出 …」+ 无 ExportInfoModal → 回预览页上传该 .msn → toast「状态文件校验通过，恢复编排将在下一 Story 落地」→ .dxf 重传 parse+commit 回归；验证后 ms-web 已杀 :8000 释放，轮换文件已清）。⑬文档同步（/sync-docs 口径）：`materialSorting-web/AGENTS.md`（文件分工头 + 状态文件 US-003 段落 + 文末「状态文件 US-003 关键约定」节：formStore token 语义/buildSavePayload 四口径/'state' 是 /export 例外/恢复提示通道 = toast）+ `.docs/technical/agent-component-map.md` 文末「状态文件 US-003 落地」节（文件表 10 行/关键不变量 5 条/验证记录；api-reference 两专节 US-001/002 已在案零补）。prd US-003 passes:true。
34. **[2026-09-11] prd-state-file US-004 — 恢复编排与端到端冒烟**：状态文件收官故事（前端恢复编排 applyRestorePayload + applySyntheticRun 共享抽取 + run-provenance 来源小字 + 33 检查端到端冒烟；E2E 冒烟实测揪出两处后端 400 根因一并修复）。①新建 `src/store/synthRunStore.ts` —— 合成 run **store-signal 两层拆分**：`applySyntheticRun(manifest, frameLike, seed, origin?, note?)` 模块级落笔（清场 runRegistry.clear + editStore.invalidate + setSeekTime(-1) → `deepCopyPlaced` 深拷贝（translation 拷断 + mirror omit-when-false，US-003 起保存侧同实现不再各持一份）→ 单条 done RunRecord（frames=[合成帧 phase='final']/lastFrame/双口径密度/viewBoxMaxW/origin additive）→ `signal()` bump token）；NestingPage `useEffect([synthToken])` 消费（seeds/phase/status 是本地 state 只能走信号；信号只载 UI 参数 seed/note/origin，几何真相在 runRegistry）。**lastSynthTokenRef 守卫**：token 未变（含挂载初跑）即 return —— 修模块级 store 常驻导致的 stale-token 重放（NestingPage.test 三连挂根因）+ StrictMode 幂等。`provenanceText(origin, seed)` 四枚举 + config 括号（extreme time_total_s→"(Ns)"/strategy minutes→"(Nmin)"，缺席/未知键无括号）。②`src/lib/stateFile.ts` 增 `applyRestorePayload(res)` 恢复编排（顺序敏感单一实现）：uploadStore.setState（done/doc=parse 载荷/activeSize=最小码/commit 回 idle）→ qtyStore.hydrate 默认物化 → **hydrateFlat 实值覆盖**（新 API：{label:{sizeKey:N}} clampQty 归整、null/空 no-op、多出 sizeKey 并入、多出 label 不新建行——矩阵行集以 doc 为准）→ formStore.hydrate(form, token=res.doc_id)（先于 React 提交，ControlPanel resetForDoc 匹配 token 水合保留）→ ptypeStore.invalidate()（commit-done 同口径）→ run 块在场 applySyntheticRun（manifest={type:'manifest',...res.manifest} + origin=run.provenance??{kind:'solve'} + `run.final` 缺席走 finalizeFromLayout 客户端现算兜底：computeLayoutStats 物理口径，density_sparrow/elapsed=0 展示参考）+ setNestingEnabled(true) + setTab('nesting')；无 run 纯配置档不合成不切 Tab。③`NestingPage.tsx`：applyStrategyResult 改委派 applySyntheticRun（行为对齐 + 深拷贝强化——修旧实现直共享 result.best.placed_items 被 applyToRun 原地写回穿透污染策略 store 的别名 bug）；`originOfStrategyResult(result)`（mode 定族：'extreme'→extreme kind + extremeStore.lastStart.time_total_s、'se'/'race'→strategy kind + strategyStore.lastStart.minutes；mode null→undefined、lastStart null→无 config 段）+ handleStart setProvenance(null)（WS 普通求解小字退场）。④`ControlPanel.tsx` 增可选 prop runProvenance（undefined 不渲染）+ `div.run-provenance[data-testid=run-provenance]` 小字。⑤`useParseDxf.ts` .msn 成功路径：applyRestorePayload(res) + toast「状态文件已恢复：{源名}（含排料结果）」（替换 US-003 占位；status=done doc 已写入）。⑥**后端修复（E2E 冒烟实测根因，两处）**：(a) `solver.py` `_pf` + `_resolve_d_tol` 重写 —— per_type 值字符串形态容错（前端 form 按 input.value 原样入档 `{g码:{d:'1',tol:''}}`，''/None/非数值 → 继承全局档 base；此前 `float('')` ValueError 打穿 state-save/state-restore 400「form.sizes/per_type/quantities 形态非法」；与前端 collectPerType「空串=继承不写键」同口径，手改文件非法值同 no-op）；(b) `statefile.py` `_form_gate_mm` —— 恢复 manifest gate_mm 取 `form.gate`（cm 字符串 ×10，与求解路径 routes_ws 前端 gate_mm 覆盖 + lib/params.parseGate 同口径）而非 doc.gate_mm（母版 commit 快照）：改幅宽求解后保存（180cm 求解 + 175cm 母版）恢复 manifest/导出幅宽须 1800 与布局一致，缺省/非法/非正回退 doc.gate_mm。⑦新建 `materialSorting-web/scripts/smoke_state_file.mjs` **33 检查端到端冒烟**（prod :8000，模板 smoke_drag_snap.mjs：Edge 通道/addInitScript ms.tour.*/WS framereceived 捕获/页内 fetch 包装抓 POST /export 载荷/pointer 事件流拖片）：S1 上传 5336 → 数量矩阵 g01@30=2（总片数 110 默认+1=111）→ gate 180.00 + per_type g01 d=1 + 腰头成带 g05（缩略非错误态）→ 勾 32/33/34 5s 求解 final placed=30（Σdemand 守恒，band WB_ 组合片已 expand 回成员）→ S2 编辑弹窗左键拖片 +40mm 保存（料长 1798mm）→ S3 导出 .msn Node gunzip 断言（顶层键 schema_version=1/app='materialsorting'/无 provenance 键/placed=30 守恒/恰 1 条被移动 dx≈+40 其余逐位全等/form sizes·gate·time·band·per_type d='1' 字符串原样/quantities g01@30=2+g01@31=1 全量扁平/final.width_mm=编辑态料长）→ S4 全新 context（新 sid）上传 .msn：toast「状态文件已恢复…（含排料结果）」+ 来源小字「来源：普通求解 · seed 0」+ nest-label 密度 85.47%/料长 179.80cm 与 .msn final 全等 + 毛版 polygon[data-label]=30 + 预览 Tab 总片数 111/cell 0-0='2'/列头 10/净版层 ≥1 + 表单回填 gate/time/sizes + 高级配置 band/per_type 回填 + 编辑弹窗基线料长全等/裁片 30 + PLT 导出 placed 深全等 + gate 1800/width/density 一致 + 重解新 final 86.61% + 小字退场；报告 out/smoke_state_file/report.json。**踩坑备档**：toast 不自动消失右上栈拦截 overlay 按钮（读断言后逐条 ✕）；数量矩阵 data-cell="列-行"按码集动态（5336 首行=30 非 32）；主画布 polygon 计数须 `polygon[data-label]`（net/collide 层同名标签无 data-label）。⑧测试：前端 **+26 例**（vitest 1125→**1150 passed** 68 文件）：synthRunStore.test 8 新建 + stateFile.test +8（applyRestorePayload 全量五 store 落笔/run 合成切 Tab/纯配置档/provenance 缺省 solve/深拷贝解耦/finalizeFromLayout 兜底）+ qtyStore.test +6（hydrateFlat）+ NestingPage.test +4（run-provenance race·minutes/extreme·time_total_s/mode null 无小字/lastStart null 无括号）+ useParseDxf.test 恢复成功断言重写；后端 **+3 例**（pytest 818→**821 passed**）：test_solver_label 字符串形态容错 + test_web_statefile 字符串 per_type 保存/恢复双端 200 + manifest gate 取 form 非 doc（含 None/'abc'/'0'/'-5' 回退四路）。⑨验证：`npx tsc --noEmit` 干净 + vitest 1150 + `npm run build` 过（316.22KB/gzip 103.77KB）+ pytest 821 + `python -m materialsorting.web.statefile` 自检 18/18 + 冒烟 smoke_state_file.mjs **33/33 SMOKE PASS**（prod :8000）+ 最小冒烟 smoke-state-file.mjs 9/9（dev :5173）；验证后 ms-web 已杀（:8000 释放）、探针脚本已清。⑩文档同步（/sync-docs 口径）：`.docs/technical/agent-component-map.md` 文末「状态文件 US-004 落地」节（文件表 6 行/关键不变量 6 条/验证记录）+ `agent-api-reference.md`（restore 专节不变量 4 补 _form_gate_mm 与 _pf 口径 + 测试计数 47→72 + US-004 前向引用落地注记）+ `agent-file-map.md`（solver.py 行 _pf + statefile.py 行 _form_gate_mm）+ `materialSorting-web/AGENTS.md` 文末「状态文件 US-004 关键约定」节（synthRunStore 两层拆分/lastTokenRef 守卫/深拷贝防别名/applyRestorePayload 顺序/冒烟四坑备档）+ `web/AGENTS.md`（solver 行 _pf + statefile 行 _form_gate_mm）+ 根 `CLAUDE.md` web 段 US-004 落地与两修复一句。prd US-004 passes:true。
