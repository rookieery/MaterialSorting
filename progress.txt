# Ralph Progress Log
Started: 2026-08-09








10. **[2026-09-04] prd-edit-nesting-layout US-001 — 编辑几何与状态地基（editGeometry/overlap/editStore + polygon-clipping）**：编辑排料（版师单片拖动/旋转微调，保存同步主视图与导出，重置回算法基线）计算与状态三件套，后端零改动。① `package.json` 引入 polygon-clipping ^0.15.7（Martinez–Rueda 纯 TS MIT，唯一新依赖；凹多边形布尔交——Sutherland–Hodgman 凸-凸不可用、clipper 系整数坐标需缩放均否决）；② `src/lib/editGeometry.ts` 纯函数库——transformPolygon 数组版与 pointsStr 同公式逐点对拍（单测锁死，但全精度不 r2：布尔交/bbox 对舍入敏感，计算 vs 渲染两出口）、polygonArea 多 ring 求和（params.ts 私有 shoelace 导出复用，唯一存量改动）、bboxOf/bboxIntersect（边界接触计入，预筛宁多勿漏）、pointInPolygon（PNPOLY 凹形正确）、penetrationDepth（A 内顶点落入 B / B 内落入 A 的点到对方边界最近距最大值；十字交叉如实低估 0——面积指标互补，手算夹具锁死）、clientToWorld（flipGroup.getScreenCTM().inverse() 矩阵通路，涵盖 xMinYMid letterbox + scale(1,-1)；jsdom 无 CTM → null，测试 mock 复合仿射矩阵）；③ `src/lib/overlap.ts` 重合计算器——precomputeEditPieces 按 placed_items **数组下标**展开（多副本第 k 次=第 k 副本，与 NestSVG 出现序同语义）+ computeOverlap bbox 预筛→polygon-clipping intersection→交集外环（openRing 剥闭合尾点回项目无重复起点口径）+面积（外环−孔）+最大穿透；④ `src/store/editStore.ts` Zustand——open 快照不可变基线全套（placed_items 深拷贝 translation 逐项拷断别名 + frameIndex/widthMm/density/finalDensity/viewBoxMaxW）、computeLayoutStats 单一真相源（width=ceil(包络 maxX−1e-9)——ε 抵 90° 旋转 ~3e-14 float 噪声防未编辑即 +1mm，下限 1mm；density=total_area/(width×gate) real 口径）、save 原地保序写回（placed_items 数组身份不变→frames[] 同 FrameMsg 引用一致；陈旧 run registry 不含拒绝；density_sparrow 恒不动；bumpRenderTick+savedDirty）、reset 恢复基线全套（幂等）、invalidate 清态、setWorkingItem（US-003 消费口径）。**测试夹具三处手算校准**：penetration 凹形 L×方块双向采样实为 √200≈14.142 非 10（B 顶点 (40,40) 落入 A 到凹角 (50,50) 距 √200 主导）；梳形×矩形穿透 = 2 非 1（A 齿顶点 (10,4)/(10,6) 到 B 右边 x=12 距 2 主导）；rot-90 方块包络 maxX 因 cos(π/2)≈6.1e-17 产生 +3e-14 噪声裸 ceil 无辜 +1mm → computeLayoutStats 加 1e-9 ε（工程定案：sub-nm 是噪声非物理）。验证 = vitest 全量 **859 passed**（815→859，+44：editGeometry 18 + overlap 9 + editStore 17）+ typecheck 干净 + build 过（gzip 81KB 含 polygon-clipping 增量）；文档 = web/AGENTS.md 新增「编辑排料 US-001 关键约定」节（依赖方向/两出口公式锁步/穿透口径/下标寻址/真相源/三重防御）+ agent-component-map.md 同日落地节。遗留 US-002+：pieceDom 提取、EditLayoutModal/EditCanvas 渲染、拖动指标、保存接线、冒烟。
11. **[2026-09-04] prd-edit-nesting-layout US-002 — 编辑弹窗渲染与查看（完整版/毛板 + 缩放平移 + 禁关闭）**：US-001 地基之上的 UI 面，后端零改动。① `nests/pieceDom.ts`（新）= createPieceEntry + SVGNS + PieceEntry 自 NestSVG **机械提取**（唯一动现有渲染代码的点，纯搬移零行为变化，41 项 NestSVG 既有单测锁回归）—— NestSVG 与 edit/EditCanvas 共用 = 弹窗与主视图同构同观感的地基；② controlPanelStore ControlPanelModalId 加 'edit_layout'（注释明示**有意偏离全站 ESC/遮罩关闭惯例**：编辑草稿不可误触丢弃，唯一关闭路径 = 右上 ✕（US-004 接 dirty 确认）与右下保存）；③ `edit/EditCanvas.tsx`（新）命令式画布——数据源 editStore.working（每下标恰一份 5 层节点 = 出现序多副本天然满足）、翻转组 translate(0 gate) scale(1 -1) setAttribute、初始 viewBox = 0 0 ceil(包络) gate、毛板模式 4 层工艺 display:none 可逆、**滚轮指针锚缩放**（native listener passive:false；锚 = clientToWorld 世界点经 `userY=gate−worldY` 换算到用户空间——直接用 worldY 会上下镜像，两档缩放单测手算锁死 "88 60 880 800"/"158.4 108 704 640"；CTM 不可得退化中心锚）+ 空白拖动平移（毛版 polygon pointerdown 早退 = US-003 拖动接管预留；比尺 min(宽比,高比) letterbox 差分抵消无需 CTM）+ ±/重置按钮（工具置**左上**，右上留 US-003 指标面板）；**两处实现陷阱已修并锁死**：zoomBy 必须回写 vbRef（漏写则第二档起锚在陈旧视图——浏览器验收首轮 S2m/S2k 连锁 FAIL 即此）、writeViewBox 显示值 r6 截断（CTM 反变换 ~1e-13 float 噪声不进 attr）；bg 跟随 viewBox 无限画布感、fab 世界锚定宽 = computeLayoutStats 同真相源；④ `edit/EditLayoutModal.tsx`（新）受控 Portal（范本 ExportInfoModal，Inner key 重挂载=重新 open 快照 bestRun）—— 顶部状态条料长/利用率/Δ；**Δ 基线 = computeLayoutStats(baseline.placedItems)（同 ceil 口径）非裸 frame.density**：solver 小数 width_mm（实测 6148.38）下后者未编辑就 −0.01pt 取整伪影（浏览器验收首轮 FAIL 归因），Δ 只度量编辑效果初值恒 +0.00pt；弹窗 ceil 口径与主视图 NestLabel 差 ≤0.02pt 是料长双向伸缩设计固有取整（保存后主视图同步即一致），单测 frame.density=0.99 脱钩夹具锁死；左下形态 select + 右下**仅**「保存当前布局」（save()+close，取消按钮已废弃）+ 空态；不挂 ESC listener、遮罩无 mousedown handler；⑤ ControlPanel 挂 `<EditLayoutModal />` 单例 + style.css `.edit-layout-*` 族（overlay z-index **1250** = ptype-preview 1200 之上 band-zoom 1300 之下）。验证 = vitest 全量 **885 passed**（859→885，+26：EditCanvas 14 + EditLayoutModal 11 + controlPanelStore 1；jsdom 缺口补法 = PointerEvent polyfill beforeEach / setPointerCapture try-catch optional / getScreenCTM+createSVGPoint mockMat 复合矩阵）+ typecheck 干净 + build 过（gzip 84KB）；浏览器验收 **22/22**（`out/us002_browser_verify/`：vite dev :5173 + ms-web :8000，882 母版 3 码 20s → final 83.60% 90 片；overlay z1250 全屏/状态条初值 computeLayoutStats 口径且与主视图差 0.01pt ceil 伪影界内/Δ +0.00/翻转组同款/毛版 90/90/工艺层 net 90+internal 170+notch 300+grain 90/**points 集合与主视图逐串一致同构对拍**/毛板 650 工艺节点全隐可逆/真实滚轮 "0 0 6149 1750→614.9 175 4919.2 1400"+重置/真实拖动平移+重置/ESC+遮罩 mousedown 不关/✕ 关/保存关；验收后浏览器关闭、复用的前会话残留 ms-web+vite 进程一并 taskkill）。文档 = web/AGENTS.md 新增「编辑排料 US-002 关键约定」节（禁关闭红线/同构约定/Δ 口径/world→user 换算/vbRef SSOT/事件分层/jsdom 补法）+ 文件树 4 行（nests/pieceDom、edit/ 目录、store editStore+'edit_layout'、style.css .edit-layout-*）+ agent-component-map.md 同日落地节（新增/改造文件表 + 6 条关键不变量 + 验证记录）。遗留 US-003+：拖动/旋转/重合指标、保存 dirty 确认与主界面集成、端到端冒烟。
12. **[2026-09-04] prd-edit-nesting-layout US-003 — 拖动、旋转与重合指标（含实时利用率状态条）**：US-002 画布之上的交互层，后端零改动。① EditCanvas **指针三分发**（单一 effect、状态全走 refs+getState 防陈旧闭包）：`data-edit-role="rotate"` 手柄 → 绕选中片**质心**旋转（角度经 clientToWorld CTM 方位角差——letterbox 对 dx 抵消但角度不抵消；pivot 公式 `t'=C+R(dθ)(t−C)` translation 随动质心不动；wrapDeg180 归一；无 CTM 防御 no-op）；`closest('polygon')` → selectPiece（选中+懒建 UI 覆盖层 overlapG(pointer-events:none)+handleG+raiseEntry 提层置顶到覆盖层之下）+ 平移拖片（setPointerCapture try/catch，监听挂 svg 兜底）；空白 → 平移（<3px down-up = 取消选中）；② **拖动帧 rAF 节流**：pointermove 只记最新坐标+scheduleFrame（rAF 缺失同步回退）→ 帧内 clampPlacement（Y∈[0,gate] 按被拖片 bbox：minY<0 上抬/maxY>gate 下压优先超高片贴底锚；x<0 钳 0；**右界永不钳**=料长双向伸缩设计）→ 只 setAttribute 被拖片 5 层 → setWorkingItem（**每帧一次非每 move**；状态条经 zustand 响应式同帧刷新）→ 池内 applyEditPlacement 单片增量 → refreshMetrics+updateHandle；pointerup 先同步 flushFrame（尾帧不丢=jsdom 测试确定性）；平移增量 = 屏幕位移/viewScale（meet min 比 letterbox 差分抵消无需 CTM）；③ **重合指标**：ensurePool（自 working 全量展开一次）→ computeOverlap（manifest erode 几何=solver 碰撞口径，面板脚注「按算法碰撞口径」）→ 红色半透明交集高亮 + 三值（面积 mm²÷100 cm²/穿透 >MAX_OVERLAP_MM=10 红 >0 琥珀/旋转偏离 min(|r|,|r−180|,|r−360|) >MAX_ROTATION_TOL_DEG=45 红——如实显示不报警）；**布尔交异常降级** catch → bbox 交估算（矩形环高亮+「· bbox 估算」+data-degraded）+穿透独立纯函数照常+拖动不阻塞；④ 旋转手柄 = 质心+(0,3r)（r=clamp(vb.w×0.015,4,30)）；渲染 effect **签名跳过**（`mode|rot|tx|ty`）拖动帧已应用片零写；⑤ overlap.ts 扩 precomputeEditPiecesFromItems（PlacedItem[] 直入口，池源=working 非 lastFrame）+ applyEditPlacement（池内原地，basePolygon 只读共享引用不动）+ **polygon-clipping 双产物互操作**（ESM dist 只导出 default/CJS 具名 → namespace 导入+default 优先回退；具名 import vite build 报「"intersection" is not exported」——US-001 未炸因无生产引用）；⑥ EditLayoutModal **零改动**（状态条本就 working 响应式）+ style.css `.edit-metrics` 族（右上角 pointer-events:none 不挡拖动，--warn #d9a05b/--danger #e06666）+ `.edit-rotate-handle` cursor。验证 = vitest 全量 **904 passed**（885→904，+19：EditCanvas 14→27 拖动/钳制/旋转/指标手算锁死+degraded 新建 1+EditLayoutModal 11→14 状态条三态+overlap +2）+ typecheck 干净 + build 过（294.27KB/gzip 96.19KB——polygon-clipping 进生产包）；浏览器验收 **17/17**（`out/us003_browser_verify/`：882 母版 3 码 20s → 83.60% 90 片 6149mm；真实鼠标全链路——拖右端片 translation 精确增量 1087.6mm ±2mm 其余 89 片不动/**拖动中未抬手状态条 6149→6693 实时增长**/料长 7236·利用率 71.03·Δ −12.56pt/提层置顶；点选带布纹线片→手柄+面板、拖柄自由转角 111.94° 非吸附、质心漂移 550.9<半对角线 621mm、布纹线端点随片变化；左移压邻居→三值 44861.0mm²/32.0mm 红/0.0°+红色交集高亮；左移腾空尾部（**右缘多片同 ceil 桶实测教训：须逐片拖**）6149→6095·84.33%·Δ +0.74pt；✕ 重开恢复基线；**提层后文档序≠working 序**——S3e 首轮 FAIL 归因，跨渲染寻址改 points 字符串当片身份；验收后浏览器关闭+ms-web/vite 已杀）。文档 = web/AGENTS.md 新增「编辑排料 US-003 关键约定」节（三分发/rAF 帧/双坐标通路/钳制/自由角/降级/提层寻址/签名跳过/测试套路）+ 文件树 2 行扩 + agent-component-map.md 同日落地节（文件表+8 条不变量+验证记录）。遗留 US-004+：保存 dirty 确认、主界面编辑入口、端到端冒烟。
13. **[2026-09-04] prd-edit-nesting-layout US-004 — 保存/✕关闭/重置与主界面集成+导出闭环**：US-003 交互层之上的集成收口，后端零改动，useExport/ExportInfoModal **零改动**（均读 bestRun().lastFrame，save 写回后 placed/density 自动继承）。① `ControlPanel/EditLayoutControls.tsx`（新）=「编辑排料」标题区块（.field-label + 编辑/重置两按钮，插 StatusLine 与 ExportButtons 之间 =「导出最优方案」上方）；激活口径与导出一致 `some(lastFrame) && phase!=='running'`（stopped best-so-far 与策略/极限合成 record 零改动命中）；三层 disabled 防御同 TabBar（native → onClick guard → store 自守卫 open/reset 返 false 幂等）；重置 = 自定义暗色 confirm → editStore.reset() 恢复基线全套 + bump；订阅 renderTick 同 ExportButtons（mutable lastFrame 靠 bump 重算）。② `edit/EditConfirmLayer.tsx`（新）= 共用确认层（纯受控 message/双回调/可选按钮文案，Portal body z-index 1350 = 全栈最上逃逸 edit-modal overflow:hidden；**无 ESC/遮罩关闭** —— 与编辑弹窗同哲学只有按钮一条路；确认红 #8a3b3b = strategy stop 同款）。③ EditLayoutModal ✕ 接 dirty 二次确认：dirty = `!itemsEqual(working, lastFrame.placed_items)`（**vs 已保存布局非基线** —— save 精确拷贝 ⇒ 保存后直关）→「放弃未保存的修改？」确认弃稿关窗/取消留窗；ESC 与遮罩仍永不关闭（含确认层显隐期间）。④ editStore 扩 `itemsEqual`（dirty 判定单一真相源：长度+id+rot/translation ε=1e-9 同 FLOAT_EPS，拖回原位 ~1e-13 残差不误报）+ **基线锚定口径（行为变更）**：同 run 重复 open **不换锚**（基线恒 = 会话首次 open 的算法布局，invalidate/换 run 唯一换锚时机；working 恒从当前 lastFrame 深拷贝承接已保存编辑、savedDirty 重算 = lastFrame 偏离基线）——「重置回初始布局」恒指算法原始结果，两轮 拖→存 后重置仍全量回滚（US-005 冒烟口径的地基）。⑤ NestingPage handleStart / applyStrategyResult 双挂点各加 `useEditStore.getState().invalidate()`（registry 校验拒陈旧但清 working/baseline 防残留；弹窗开着 overlay 阻断下标安全）。⑥ style.css `.edit-controls` 族（.export-group 同款分隔 + 蓝编辑钮 + 中性描边重置钮 hover 琥珀）+ `.edit-confirm-*` 族。验证 = vitest 全量 **936 passed**（904→936，+32：EditConfirmLayer 4 新建 + EditLayoutControls 11 新建 + EditLayoutModal 14→20 ✕ dirty 三态/保存后直关/dirty 复现/确认层期间禁关闭 + NestingPage +3 双挂点（局部 MockWS）+ useExport +2 闭环（save 后 payload placed/width/density 反映编辑值、density_sparrow 不动、缩短场景）+ ControlPanel +1 区块 DOM 序 + editStore +5 基线锚定/itemsEqual）+ typecheck 干净 + build 过（gzip 96.89KB）；浏览器验收 **29/29**（`out/us004_browser_verify/`：prod :8000 DOM/路由抓包，882 母版 3 码 20s → 83.60%/90 片/6149mm —— 区块位置+求解前/running 置灰→final 解禁、编辑钮开弹窗、拖片右移 300 保存 → 主视图**恰一片 points 重绘**其余 89 片逐字节不动 + NestLabel 83.60%→79.70%/614.84→644.90cm + viewBox/fab 扩 6449、POST /export 抓包 placed 与 solver 终帧 diff 非空 + width/density 79.70%、✕ dirty 确认层文案/取消留窗/确认弃稿主视图不动、尾片左移保存 → viewBox 收缩 6449→6149/83.59%、弹窗外重置 → NestLabel/viewBox/全 90 片 points 逐一回算法基线；验收后浏览器关闭、ms-web 已杀）。文档 = web/AGENTS.md「编辑排料 US-004 关键约定」节（dirty 口径/基线锚定/激活口径/确认层单路径/invalidate 双挂点/导出零改动红线/测试套路）+ 文件树 4 处扩 + agent-component-map.md 同日落地节（文件表+7 不变量+验证记录）。遗留 US-005：端到端冒烟（smoke_edit_layout.mjs 全链路）+ 文档红线入册。
