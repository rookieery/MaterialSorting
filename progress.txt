# Ralph Progress Log
Started: 2026-08-09
















- 环境修复（预存失败，非本 story 回归）：`tests/test_ws_stop.py` `_start_payload` sizes 硬编码 28 → 从 `_PIECES_STATE` 动态取最小码（本机真实 intermediate 为 30~40 码母版 → manifest 0 片假失败；git stash 验证 clean tree 同样失败）；已记 tests/AGENTS.md。
- 验证：pytest 全量 **278 passed**（257→278，0 failed）；`python -m materialsorting.cli.portfolio` 导入冒烟零输出零副作用（既有守卫复跑绿）；分层未反向（portfolio 仅标准库+pipeline，既有 AST 守卫绿）；文档同步 `.docs/technical/agent-file-map.md`（portfolio.py 行+cli/ 节头）与 `tests/AGENTS.md`（test_cli_strategy 行+fixture 段+ws_stop 码号动态取备注）。

## 2026-08-20 — US-002 双模式 CLI 旗标接线与真实冒烟（strategy PRD）
- `cli/run_config.py`：`--strategy [se|race]`（nargs='?' const='race' = 裸旗标缺省 B 方案；choices 外退出 1）+ 策略模式 `--time` 总预算必填（缺省退出 1 提示「策略模式需 --time 总预算」）+ 从属旗标 `--se-screen/--se-extend/--race-budget`（≥1）/ `--race-gate`（(0,1) 开区间）—— 值域外/缺主旗标/与 `--kill` 显式互斥/`race_plan`·`se_plan` 预算不足均 new_run_dir 前退出 1；种子流 `strategy_seed_stream` 回显；commit 打印后首解前写 `run_dir/strategy.json`（{mode,total_budget,planned_seeds,started_at,race|se}）；策略模式 kill_mode=off 但 race 决策仍逐行落 kill_decisions.jsonl（schema 复用：S_tau=bar、theta=null，README 配置驱动求解节加过载备注）；门杀/预算收口行 --quiet 也打；θ₀ 校准仅 --target 且非 strategy 执行；无 --strategy 全链路逐字节零回归。
- `cli/portfolio.py`：`race_plan` 名义规划（full_unit=budget+2.5 / gate_unit=gate+2.5，最小 = 1 全程 + 1 门段）；PortfolioController 加 mode/total_budget/race_budget/race_gate_tau/se_k/se_screen/se_ext；`make_should_stop` R0 恒先 → race 走 `_evaluate_race`（US-001 decide_race_kill 单一真相源；首 seed 豁免；判杀走既有 R5 terminate 链交付 best-so-far）；`make_progress` current_phase（se screen/extension 两段，重置 _frames_seen 对齐 _ext 帧号）；`can_start_next` race 名义收口（killed 记 gate_unit、跑满记 full_unit）；`round_budget` 分段预算；`finish_seed` phase+kept/gated；`portfolio_section` 条件 mode + race/se 子段；`run_serial_portfolio` se 冠军延长轮（argmax real_density，suffix='_ext'，phase='extension'）。
- `cli/pipeline.py`：`solve_pieces` 加 `artifact_suffix=''`（延长轮 curve_s{seed}_ext.json / best_frame_s{seed}_ext.json 与缺省名互不覆盖）。
- 测试 `tests/test_cli_strategy_wiring.py`（28 用例）：旗标裁决矩阵（值域/--time 必填/--kill 互斥 2×3/从属缺主/gate 开区间/预算不足）、race_plan 手算对拍、race 端到端（门杀+续跑+收口+决策行 schema+strategy.json+portfolio.race+quiet 仍打门杀行+R0 共存+种子流 max+1 补齐）、se 端到端（k 筛选+冠军 _ext 共存+phase+并列取先+R0 跳延长）、无旗标零回归哨兵、artifact_suffix 真实 2s 求解（真实帧序列非单调 → 最优帧 ≥ 末帧回报，勿断言相等）、--help 子进程。
- 真实冒烟（data/configs/5336_coded_really.json，rc 均 0）：race `--strategy --time 240 --race-budget 60 --race-gate 0.5` → seed0 豁免 86.98%、seed1/2/3 于 ~30s 门杀（86.33/86.43/85.59% ≤ bar 86.93%）、seed4 续跑 89.57%、预算收口 1 seed 未启动（222.5/240 名义）、best=seed4 89.61%、决策 5 行 theta=null；se `--strategy se --time 300 --se-screen 30 --se-extend 60` → k=7 筛选 + 冠军 seed4 延长 89.65% ≥ 筛选最大 89.48%、_ext 边车在场、portfolio.se {k_screens:7,screen_s:30,ext_s:60,champion:4}。
- 验证：pytest 全量 **306 passed**（278→306，0 failed）；文档同步 `.docs/technical/agent-file-map.md`（cli 节头/叙述段/pipeline·portfolio·run_config 树行+详表行）与 `tests/AGENTS.md`（test_cli_strategy_wiring 行）+ README kill_decisions 过载备注。
