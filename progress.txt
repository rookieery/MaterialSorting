# Ralph Progress Log
Started: 2026-08-09







- [US-011] 后端编排接线：build_instance 增 exclude_labels（只跳 Item 层，pid_meta/total_area 原样，band on/off manifest 逐字段一致）+ extra_items（组合片构造期注入——spyrrow instance.items 是 Rust 副本，构造后 append 静默失效）；solve_worker 增 band 参数：_build_band 进程内同步跑 build_band_plan（不 spawn 孙进程，stop 无孤儿；失败=成带失败 error 不投 manifest）+ _write_band_artifact 落 OUT_DIR/band_runs/*.json + _emit_placed 展开单点（WB_ 帧前展开回成员 placement，永不泄漏）；routes_ws 增 _parse_band 服务端校验（g 码/存在母版/数量>0/硬警告形态需 ack，非法结构化 error 早退+显式 close）+ on_stage 投 stage 消息；新增 tests/test_waist_band_ws.py 16 项全绿，test_solve_proc/test_ws_stop 零改动，全量 pytest 374 passed 零回归；文档同步 agent-api-reference/agent-file-map/web AGENTS
- [US-012] 前端参数链路（纯参数层，无 SVG/坐标改动）：① types/ws.ts 加 BandConfig{enabled,label,ack?}+StageMsg，StartPayload.band?: BandConfig|null，ServerMsg 联合加 StageMsg；② lib/params.ts FormState 加 band_enabled/band_label（DEFAULT false/''）+ collectBand 三态（关/开未选/非 ^g\d+$ → null 静默降级；开且有效 trim 后 → {enabled:true,label}，ack 不在此层）+ bandMemberCount 三态（missing→1 与后端 _band_demand/build_pid_meta 缺 label→1 口径对齐、显式 0→0、未选码过滤不计；null 通用码不计=后端 sizes 过滤 want 数字集镜像）+ StartContext.band；③ runRegistry RunRecord 加 stage: StageMsg|null（信息记录不动 phase/done）；④ useSolveRun StartConfig.band 透传（payload band??null）+ case 'stage' 写 rec.stage+onStage 回调 run 不 finish（未知 type 仍 default:break）；⑤ NestingPage onStage → 状态行「腰头成带中：带内聚排…」（秒级提示不进 phase 五态状态机）+ handleStart 透传 cfg.band；ControlPanelStartPayload 补 band 字段（ctx spread 自动携带，逻辑零改动）。策略 run 白名单键不受影响（FR-6：handleExec 不拷 band）。测试：params.test.ts +18 / useSolveRun.test.tsx +4（含既有 StartPayload toEqual 扩 band:null 护栏）/ useSolveRun.stop.test.tsx +2 NestingPage 集成（stage→状态行+phase 仍 running+final 后覆盖；旧后端无 stage 行为一致），vitest 651 passed（基线 633）、npm run build（tsc --noEmit && vite build）通过、后端 test_waist_band_ws 16 passed 契约确认；文档同步 agent-component-map（US-012 节+5 不变量）/materialSorting-web AGENTS（目录树 5 处+总述段）。未做浏览器验证：本 story 无 SVG 渲染/坐标变换改动（纯参数链路+状态行文案，jsdom 集成测试覆盖），chrome-devtools-mcp 亦不在本会话工具集；US-013 弹窗 UI 落地时统一目检


- [US-013] 弹窗布局设置 UI+校验+互斥+预览回显：① PerTypeOverridesModal「布局设置」分区（.per-type-band 暗色 #26282e/#2ea06c）：成带勾选+腰头编号下拉（值域 = orderedLabels（reps∪values）∪ 选中 label 防受控 select 空白；fetch 失败降级纯文字不阻塞）+ 80×80 缩略图/徽章（点击 openPreviewLabel 双层 modal）+ draft+confirm 三字段（enabled/label/ack，取消/遮罩/ESC 丢弃）；② 预演 POST /api/band/preview（新建 web/routes_band.py，executor 线程 5s 预算 build_band_plan，_resolve_d_tol/build_pid_meta 与 _build_band 同口径；200 {ok,fill_pct,bbox,elapsed,break_even:[62.4,63.6]}，几何失败 200 ok:false=结果数据不阻塞确定，400/409/422 结构错误；不落 band_runs 工件）：选中有效 g 码即发（body=buildStartContext() 经 ref 取+band 草稿），回显「填充 X% · 占用 W×Hmm —— 达到/低于盈亏参考线」（参考线后端携带不双写）；③ 硬警告 ack 流（关键发现：真实 5336 g05 长宽比 6.9 >6 属硬警告形态，无 ack 恒 422）：routes_ws._parse_band 硬警告分支改抛 BandAckRequired（ValueError 子类，WS 路径 str(e) 行为不变）→ 预演路由回 422 {error,hard_warning:true} → 弹窗渲染 amber 二次确认勾选框「我已确认该裁片形态特殊（细长/小片），仍要成带」→ 勾选带 ack:true 重试（成功后保持可见可反勾）→ 确定 写回 form.band_ack（params.ts FormState+collectBand ack 键 + types/ws.ts BandPreviewResponse.hard_warning）；切换 g 码/取消勾选重置 ack；④ ControlPanel band 启动闸门（未选编号/选中 g 码 bandMemberCount===0 → startDisabled 置灰+StatusLine band 段文案+handleStart 兜底）+ FR-6 互斥（band 开 → strategy-btn disabled+title 说明，关即恢复；StrategyRunButton 加 title prop）+ bandStore 单向镜像（form.band_* → setBandMirror → QtyMatrix bandCol 奇数格 .qty-cell.odd+title「该码不成对」+列头 .qty-odd-badge 徽章，无反向回写）；⑤ style.css .per-type-band*/.qty-cell.odd/.qty-odd-badge/.per-type-band-ack（amber #d9b06a 警示色区分主勾选绿）。测试：后端 test_waist_band_ws +9（preview ok/几何失败 ok:false/结构错误×4/硬警告 422 标记+其它 422 不带+ack 重试放行/空态 409）全量 pytest 383 passed；前端 PerTypeOverridesModal.test 38 项（+10 含 422→ack→重试→confirm ack:true 全链路/切码重置）+ ControlPanel.test 41 项（+5 含 ack 全链路写回 start payload）+ QtyMatrix.test 42 项（+4）+ params.test +2，vitest 675 passed、npm run build（tsc --noEmit && vite build）通过。浏览器验证 scripts/us013_verify.mjs（Playwright 系统 Chrome headless，需 addInitScript 预置 ms.tour.seen.* 跳过 z-2000 指引浮层）17/17 PASS：上传 5336→QtyMatrix g05@30=3 奇→超排→弹窗（布局设置/未勾选 disabled/#26282e）→勾选+选 g05→422 长宽比 6.9 提示+ack 勾选→勾选重试 fill 50.6% 低于参考线→确定→启动可用+高级运行互斥置灰（title 正确）→真实 WS 求解 stage「腰头成带中：带内聚排…」→完成 84.89%→上传预览 g05 列头「不成对」徽章+奇数格 title→关 band 高级运行恢复；截图 out/us013_verify/01~07。文档同步 agent-api-reference（/api/band/preview 节+路由表+band ack 语义）/agent-file-map（routes_band.py+BandAckRequired）/agent-component-map（US-013 节+树）/materialSorting-web AGENTS（目录树+US-013 关键约定节）


- [US-014] A/B 验收与形态判据闭环（verdict accept，四判据全过）：① 判据②成对口径修正 —— pair_adjacency 改边距权威（shapely Polygon.distance ≤ PAIR_ADJ_EPS_MM=10mm，单一真相源 waist_band.PAIR_ADJ_EPS_MM；PRD 中心距公式为同朝向并排特例，FR-8 头尾翻转 180° 成对实测边距 1.5mm/中心距 713mm 会误判，中心距降级对照字段 worst_center_mm；g05_34 回归锁单测）；② 成对形态重试选解（根因：spyrrow 不按喂入序聚排，size-major 输入序假设被 3 seed 实测否定 2/3 散落 400mm+；同码全成对解出现率 ~50%/次）——build_band_plan 固定派生 seed 序列 crc32(band_seed|try{k}) 重试 ≤BAND_MAX_TRIES=6 取首个 _pairs_complete 解（预算均摊 max(1,total//6)），6 败走 _slot_fallback 确定性 bbox 槽位兜底（矩形 fill~100% 即最优、弧形由 fill≥45 下限拦截）；带内幅宽钳 min(gate,PLOT_SAFE_MAX_Y_MM)（组合片 1979/1980>1910 会主解不可置，test 断言改 ≤PLOT_SAFE_MAX_Y_MM）；曾试分码分块拼接（成对 100% 但均值密度 −3.75pt，宽 1818×1271 块主解难包）已否决；③ 判据③确定性尾帧规则 —— frame_series_equal（核心前缀 min(n)−1 帧逐帧相等 + 帧数差 ≤FRAME_TAIL_TOLERANCE=8；wall-clock 预算截断使截止快照漂 1~3 帧，实测 2005/2008 与 1167/1166 两形态均 final 相等、分歧仅在短列末帧；核心分歧或大面积漂移 FAIL，三态单测锁死）；④ A/B 终验（band_accept run_all，uploads 源/web 默认 g05 d=0.0/无 kill 口径三差异只与同配置 off 对照，main 120s×2×3seed + 重跑 + 导出）：密度 off 85.69/86.16/87.86 vs on 87.53/86.87/87.15，均值劣化 −0.614pt=提升（最差 seed2 +0.71 ≤1.0 线）PASS；形态成对率 100%×3（13/13 多副本、最差边距 0.2~0.76mm；off 臂对照 15.4/30.8/38.5% 证明非自然涌现）、span 1178~1208×1479~1515mm 全过；确定性 frames/final/artifact 三重全等（本次恰 2008/2008 逐帧相等）；导出 on/off 双臂 PNG/R12-DXF/PLT 全成 + WB_ 三重字节哨兵零命中；⑤ 浏览器终验 scripts/us014_verify.mjs（Playwright，修复 waitForFunction 二参陷阱：{timeout} 被当 arg 全部落默认 30s，final 120s 求解超时 → 改三参 undefined,{timeout}）11/11 PASS：上传→P0 表→超排 120s→成带 g05+ack 硬警告流→stage/final 截图 .docs/business/→终值对拍 UI 87.51% vs A/B on 臂 seed0 87.531%（差 0.021 ≤0.05 确定性交叉印证）→UI 导出三格式经真实 HTTP /export→前端 WS 1987 消息 WB_ 零命中→服务端日志「导出跳过：pid」零出现；⑥ 报告落盘 .docs/business/腰头成带_AB验收报告_US014.md + 3 截图（stage/final/export_on）；测试：test_band_accept 新增 10+1 项（边距口径/尾帧规则/冒烟 main 6s —— 2s 档 wall-clock 截断小实例帧列漂移 6s 收敛）全量 pytest 395 passed、前端 build+vitest 675 passed 零回归；文档同步 business-overview（no-go 行翻案为 US-014 accept）/README 索引/agent-file-map/web+nesting_engine+tests AGENTS/CLAUDE.md 求解支线
