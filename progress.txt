# Ralph Progress Log
Started: 2026-08-09
















26. **[2026-09-05] prd-edit-keyboard-transform US-007 — 端到端冒烟扩展与文档同步**：键盘/镜像/重置收官 —— 零组件/零后端/零渲染公式改动，把新不变量锁进 E2E 冒烟 + 按仓库 dated fragment 惯例同步三份文档。①smoke_edit_polish.mjs 新增 **S8 键盘/镜像段 17 检查（29→46）**：S7 compact 保存 → 镜像前基线 DXF 导出（placed 守恒 + 自研 R12 解析器 parseDxfOutlines：严格 code/value 成对步进，**门幅边框无 layer 键落缺省 '0' 不入 layer1 列表**）→ 选片决策（eroded 质心锚定预测不触门幅 ≥2mm + 镜像可辨位移 ≥20mm + 非带排除片，k=12 g06_32 margin=700.6mm disp=635mm）→ 重开承接（points 内容匹配恰 1 片）→ 合成 pointerdown 选中（selectByPoints 按内容寻址 + 直发 polygon —— 真实 hit-testing 被叠片抢 target；blurActive 后 trusted keydown 防按钮聚焦命中守卫②）→ **O 键镜像**（DOM 多重集差恰 1 出 1 进 + 新 points = 质心锚定反射对拍 reflErr=0.0059mm ≤0.05）→ 保存 → 导出 PLT/DXF：**POST placed 恰一项 mirror:true**（下标=选中片 + rot 不变 + eroded 质心锚定补偿 anchorErr=0 ≤1e-6 + 其余 29 项与镜像前逐位全等 + PLT/DXF 载荷一致）+ **正文几何镜像坐标对拍**（闭式公式 期望=R(θ₁)·M·R(−θ₀)·(镜像前 DXF layer1 顶点−t₀)+t₁，θ/t 取两次导出 POST placed[k] —— 不依赖 intermediate 磁盘文件；PLT err=1.41unit ≤2（lead 偏移自门幅框 stroke 首点推导）/ DXF err=0.0095mm ≤0.05，非镜像反事实 50801unit/1270mm = mirror 键真实驱动几何）→ 重开承接镜像片 → **微调 mirror 逐位透传**（请求/响应恰一项同下标同 pid，其余 29 项无 mirror 键，请求项 byteEqual）→ **R 键重置**（恰改一片 + points 逐字节 = S6 末帧算法基线串 + ∈ 首开快照 m6Points + 基线 pid 对位）。配套纯 Node 算子：pointsStrJs（lib/geometry.ts 逐字节复刻 —— prod 构建页面内无法 import 源码模块；Math.round(x*100)/100 同 V8 语义）/ parsePltStrokes（PU 起新笔/PD ≤10 点分块拼接，stroke[0]=门幅框、stroke[k+1]=placed[k]）/ multisetDiff（恰改一片判据）。②文档同步：materialSorting-web/AGENTS.md（edit/ 树行 US-007 fragment + 冒烟节 29→46+S8 bullet + 文末「编辑键盘/镜像关键约定」汇总节：六键语义/质心锚定（O 锚 eroded、导出画 raw，口径差由锚定 translation 吸收）/omit-when-false（恒发 mirror:false 会红精确锁键集用例）/resetItem 下标守卫/46 检查端到端门）+ .docs/technical/agent-component-map.md（头部覆盖行 + EditLayoutModal 行 dated 注记（零改动、S8 锁保存→重开承接/透传不变量）+ 文末「edit-keyboard US-007 落地」节：文件表/四不变量/验证记录）+ .docs/technical/agent-api-reference.md（/export placed mirror 块补端到端几何对拍注记 + /api/edit-polish mirror 键补透传锁注记 + 冒烟覆盖行 29→46）。③验证：冒烟 **46/46 PASS**（既有 29 复跑全绿 = mirror=false 零回归门；报告 out/smoke_edit_polish/report.json mirror 段全指标在案）+ vitest 全量 **1021 passed**（64 文件）+ 后端 pytest **745 passed** + `npm run build` 过（304.95KB/gzip 99.74KB）、static/ 已重建（冒烟跑在重建后 prod 构建）。④/sync-docs 已跑（改动映射：前端 E2E 脚本 → component-map、API 契约注记 → api-reference；无后端改动 agent-file-map 不适用）。验证后 ms-web 已杀（:8000 释放）。prd US-007 passes:true。
27. **[2026-09-06] prd-edit-drag-snap US-001 — 吸附几何算子（contactT + firstContactDistance）**：右键拖动松手贴附 PRD 首故事（纯前端零后端零组件，编辑弹窗吸附的解析地基）。①editGeometry.ts 中段新分节：`contactT(v,dir,a,b)` 顶点沿单位方向平移到线段的参数 t 解 —— 叉积线性解 t = −cross(v−a,d)/cross(dir,d) + 命中点须落线段本体 [a,b]（含端点，1e-9 容差）；null 三情形不抛异常 = 零长退化边 / 近平行（|cross(dir,d)| ≤ EDGE_PARALLEL_EPS=1e-9·|d| 即 |sinθ|≤1e-9 —— 弧片密集顶点实况，恰共线时无 eps 会除零产 ±Infinity/NaN 污染最小值，真实触点在端点由相邻边覆盖）/ 命中点在线段延长线上；负 t 原样返回（触点在身后），过滤正 t 是调用方职责。`firstContactDistance(moved,obstacle,dir)` 两多边形沿方向首触距离 = moved 全顶点×obstacle 全边（正向）+ obstacle 全顶点×moved 全边（contactT 传 −dir 相对运动视角）两向取最小正 t（mm）—— 解析零迭代零 RNG、固定枚举序（< 保留首遇值相等与序无关）、空多边形/零向量 dir 安全 null。②单测 +16（1022→1038，editGeometry.test 24→39）：contactT 7（垂直精确 100±1e-9 / 斜向 50√2±1e-9 / 延长线 null / 近平行斜率 1e-12+恰共线 null 不抛 / 负 t 原样 −50 / 端点命中含 s=0、s=1 / 零长边 null）+ firstContactDistance 9（矩形 +x 精确 150±1e-9 / 3-4-5 斜向两向同值 250/3±1e-9 / 凹形 L 首触点恰落凹口内角 (50,50) t=10 整数精确且共线近平行对被跳过 / 近平行弧段密集顶点全无良态交点 → null 不抛 / 近平行弧弦在场真触点良态照常 40 / 前方无交点 null / 空多边形防御 / 镜像+旋转 vs 直接构造同形态同 t 口径无关性锁（x 预取负恒等式 + 逆序/换起点序无关 + 非对称 base 自证 200≠136.11）/ 确定性双跑逐位全等）。③验证：`npx tsc --noEmit` 干净 + vitest 全量 **1038 passed**（64 文件）+ `npm run build` 过（305.07KB/gzip 99.81KB）、static/ 已重建；零后端零组件改动（无 SVG/UI 变更，浏览器验证不适用）。④文档同步：materialSorting-web/AGENTS.md（lib/ 树行 US-001 fragment + 文末「edit-drag-snap US-001 关键约定」节：两算子职责/dir 单位契约/近平行 null 特性/负 t 语义/口径无关红线/平手裁决归属/改函数须同步测试）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-drag-snap US-001 落地」节：文件表/四不变量/验证记录）。prd US-001 passes:true。

28. **[2026-09-06] prd-edit-drag-snap US-002 — 松手吸附引擎纯函数（retreat + attract）**：右键拖动松手贴附 PRD 第二故事（纯前端零后端零组件，消费 US-001 几何算子的引擎层）。①新建 `materialSorting-web/src/lib/snap.ts`：常量收口顶部 —— `ATTRACT_MAX_GAP_MM=10`（2026-09-06 定案，导出供测试/文档锁值）/ 粗扫步 20mm / 二分 ≤14 轮 / `SEP_NUDGE_MM=1e-9` / `COLLIDE_AREA_EPS_MM2=1e-9`（后两者与后端 polish.py 同名常量同口径；另 CLAMP_EPS_MM=1e-6 同 GATE_EPS_MM、ZERO_DISP_EPS_MM=1e-9 零位移判定）。核心 `computeSnapCorrection(dragged, rawTr, sess, others, opts)` → `{tr, kind:'free'|'retreat'|'attract', partnerKey}`：**P0 retreat**（落点违谓词时沿 lo=sess.lastSafeTr → hi=rawTr 线段参数化 t(mm)，20mm 粗扫括出**首个**穿越小段——凹形自由区间不连通、朴素二分会跳过窄障碍隧道穿越（后端 _slide_west_touch 同款机器）→ 小段内二分 ≤14 轮 → 自由侧回退 1nm，终态布尔交精确 0；锚点 null/路径零长/锚点自身违谓词/粗扫不可达均防御回落 raw）；**P1 attract**（落点合谓词时沿末帧拖动位移方向 rawTr−lastSafeTr、位移 <1nm 零位移帧退化逐邻居质心连线，用 US-001 firstContactDistance 解析取首触 t，t≤10 才吸、吸附位=落点+(t−1nm)·dir；多邻居 t 最小、平手 key 最小（与 others 数组顺序无关））；**谓词** = computeOverlap(pieceAt(拖片,位), others).areaMm2 ≤ sess.startOverlapMm2+1e-9（复用 overlap.ts oracle，起手存量重叠不恶化不误清）；**clamp 守卫** = 候选 minX≥−1e-6/minY≥−1e-6/maxY≤gate+1e-6 否则弃用回落 raw（引擎不重钳 raw）；**fail-open** = 整体 try/catch 任何内部异常返回 raw；**只改 translation 永不动 rot/mirror**；邻居寻址全程 EditPiece.key 逐个枚举绝不 pid 去重。②新建 `src/lib/snap.test.ts`（PRD 指定扁平路径，vitest include 覆盖）22 项：retreat 基础夹具（解析对拍 0.01mm + 布尔交 ≤1e-9 + 分离 ≥1e-9，1nm 断言容 1e-12 ulp 消去噪声并注释）+ 隧道穿越夹具（窄障碍 150-155 中途 + 远墙 400-500 终点：朴素二分收敛 300 vs 粗扫正确回 50）+ 基线不恶化（恶化位 900>500 retreat 回锚点、终态 = 基线不清零）不误清（改善位 300/持平位 500 均不动）+ 5 副本同 pid 求和锁（各 50 ≤ 基线 200、合计 250 > 200 → retreat 到解析位 54，去重实现会误判 free；边界五副本同面积平手 → partnerKey=key 最小）+ attract 6mm 吸（10−1nm 精确）/ 15mm 不动 / 零位移质心退化（rawTr===lastSafe 与 null 锚点两变体，东 4mm 北 8mm 取东）/ 平手 key 小（东 key9 数组首位 vs 北 key2，取 2）/ 同 pid 寻址锁（t=6/8 取 6 的后位副本）/ 背离方向不吸 / clamp 三变体（gate 顶越界、minX、minY）+ retreat 候选违 clamp 同回落 / getter 注入 basePolygon 抛异常 fail-open 不外抛 / 确定性双跑 JSON 逐位全等 / 空邻居（tr 拷贝不别名入参）· 无锚点 · 含自身 key 跳过。③验证：`npx tsc --noEmit` 干净 + vitest 全量 **1060 passed**（65 文件，1038+22）+ `npm run build` 过（305.07KB/gzip 99.81KB，snap.ts 尚未进组件图 —— US-003 接线后入包）、static/ 已重建；零后端零组件改动（纯函数层无 SVG/UI 变更，浏览器验证不适用）。④文档同步：materialSorting-web/AGENTS.md（lib/ 树行 US-002 fragment + 文末「edit-drag-snap US-002 关键约定」节：职责边界红线/retreat 机器/attract 机器/谓词基线/clamp 与 fail-open/多副本红线/常量口径含 1nm 断言浮点余量缘由/改引擎须同步 22 项测试）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-drag-snap US-002 落地」节：文件表/五不变量/验证记录）。prd US-002 passes:true。
