# Ralph Progress Log
Started: 2026-08-09













- 文档同步（/sync-docs）：README（配置驱动求解新增 PC-007 块 + 用法示例两行 + 命令表 ms-lns 行）、CLAUDE.md（配置驱动求解条目 PC-007 摘要）、.docs/technical/agent-file-map.md（console_scripts 7→8 + 目录树 lns.py 行 + cli 节标题/导语 PC-007 + 详表 lns.py 行 + 入口表 ms-lns 行）、tests/AGENTS.md（test_cli_lns.py 行）。
- 本会话改动仅限后端 CLI 求解链（web/server 路由、WS 协议、前端零改动），lns_compare.svg 为文件产物非浏览器渲染面，无可视化回归。


## 2026-08-19 US-008（PC-008）LNS 接入 portfolio 编排
- `run_config.py` 新增旗标 `--lns [--lns-time 30] [--lns-rounds 5]`：portfolio 跑完（含 R0 提前停路径 —— 对达标解也可再压宽度）后对最优布局自动跑 US-007 LNS 核心循环，无需手工二次 `ms-lns`。布局来源 = engaged 的 `portfolio.incumbent`；单 seed 无旗标旧语义经 `_incumbent` 回退 `best_frame_s{seed}.json` 边车。
- **严格更优才回写** result.json：`controller.incumbent` 三字段（density/width_mm/placed_items）更新（seed/frame_index/elapsed 保持来源帧出处）+ 新增 `lns` 段（`_lns_section`：before/after/delta/rounds_detail/recheck/base_seed/产物文件名；placed_items 不入段控体积）后 `_flush_result()` **一次性整体重写**（best 与 portfolio.incumbent 同一 dict 自动同步，末行汇总取 LNS 后口径）；不优则 result.json 逐字节不变（无 lns 键），明细仍写 `result_lns.json` + `lns_compare.svg`。stdout 汇总加 `[LNS] 前/后` 两行（终局口径 `--quiet` 也打；run_lns 逐段接受进度行走 `--quiet` 抑制）+ 启动说明行。
- **中断安全**：LNS 环节 Ctrl-C（run_lns 内部捕获 interrupted=True）→ 已完成轮保底落 result_lns.json、改进（若有）已一次性回写、主 result.json 完整不半写、退出码 130；窗口外 Ctrl-C 兜底 except 同 130；Ctrl-C 中断的 portfolio 不做后处理（按中断路径收口）。LNS 输入错误（LnsError/OSError/JSONDecodeError）降级 stderr warn 跳过（退出码 0，不否定求解交付物）。
- **lns.py 重构**：从 `main` 抽出 `postprocess_run_dir(run_dir, *, time_budget, rounds, band_width, echo, solve)` run_dir 级共用编排入口（读 result.json 选布局 → run_lns → 双产物落盘，返回写盘 payload）—— ms-lns CLI 与 run_config --lns 同一条代码路径，stdout/退出码逐字不变（21 例旧测零回归）。
- 旗标裁决：`--lns-time`/`--lns-rounds` 须与 `--lns` 同给（单独给出 = 笔误）且值 ≥1，配置错误在 new_run_dir 前拦下（不留空目录）。
- 新增 `tests/test_cli_run_config_lns.py` **11 例**：fake solve（holey = 末片甩远端 x 制纯空洞段 → 空段 splice 确定性改进；compact = 全叠 x≈0 → 差解恒拒不改进）+ fake `_solve_band`（row packer / KI 求解）。改进路径（incumbent 三字段 + lns 段键集 + best 同步 + 末行取后口径）；不优路径 result.json 与 LNS 入口快照逐字节一致（跨 run 因时间戳路径天然不同，以入口快照为口径）；R0 提前停后仍执行后处理；环节内/窗口外 Ctrl-C 两路径 130；旗标裁决退出 1 不建目录；--quiet 口径；单 seed 边车回填路径；输入错误降级 warn；--help 子进程冒烟；分层（模块级无 web）。
- 验证：pytest 全量 **245 passed**（234→245）；`python -m materialsorting.cli.run_config --help` 跑通；端到端冒烟 `ms-run-config data/configs/5336_coded_really.json --time 5 --target 0.5 --lns --lns-time 5 --lns-rounds 5` rc=0 全链路（R0 达标 72.03% 停队 → LNS 真实子求解 4 次尝试 → 无改进 → result.json 不含 lns 段（逐字节不变路径）+ result_lns.json/lns_compare.svg 在场）；`ms-lns --run-dir` 重构后行为逐字一致。
- 文档同步（/sync-docs）：README（配置驱动求解新增 PC-008 块 + 用法示例两行 + 命令表 ms-run-config 行注记）、CLAUDE.md（配置驱动求解条目 PC-008 摘要）、.docs/technical/agent-file-map.md（cli 节标题/导语 PC-008 + 目录树 lns.py 行 + 详表 lns.py/run_config.py 行 + 入口表 ms-run-config 行）、tests/AGENTS.md（test_cli_run_config_lns.py 行）。
- 本会话改动仅限后端 CLI 求解链（web/server 路由、WS 协议、前端零改动），lns_compare.svg 为文件产物非浏览器渲染面（write_compare_svg 未改动，仅调用点迁移），无可视化回归。
