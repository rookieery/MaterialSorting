# Ralph Progress Log
Started: 2026-08-09



    - 排障记录：初跑 422「母版信息缺少 doc_id」根因 = 本机残留双 ms-web 实例端口劫持（页面 commit 落 A 实例、策略 POST 落 B 实例旧内存态）——杀全部残留 + 单实例重启后通过；server/Chrome 已全清（8000/9223 free）
    - 文档同步: .docs/technical/agent-component-map.md（头部覆盖注记 + 目录树 5 行 + US-005 专节文件表 + 关键不变量 6 条）、materialSorting-web/AGENTS.md（文件分工行 + 树行）、.docs/technical/agent-api-reference.md（策略桥接节补前端轮询口径注记）

[2026-08-20] US-006 — 策略结果应用到画布与导出闭环
  环境: Windows / Node v22.15.0 / Chrome 151 headless+CDP / Python 3.11.6（ms-web = .venv/Scripts/ms-web.exe）
  改动:
    - materialSorting-web/src/components/NestingPage.tsx: 新增 applyStrategyResult(result) 并 onApplyStrategy prop 链传 ControlPanel —— ①runRegistry.clear() 清场（关旧 WS，显式按钮不自动应用；phase==='running' 兜底 return）+ doneCountRef/totalSeedsRef 重置 0/1；②合成 manifest（result.manifest = build_pid_meta 快照口径，补 type:'manifest' 判别键转 ManifestMsg 同形）+ 合成终局帧 FrameMsg（type:'frame'/index=best.frame_index/elapsed/phase:'final'/density 双口径/width_mm/placed_items）+ 置换单条 RunRecord（frames=[帧]/lastFrame 同帧/finalDensity 双口径/viewBoxMaxW/done=true/ws=null/stopped=false/error=null）；③setSeeds([seed])+setPhase('done')+setSeekTime(-1)+setStatus('策略 run 已应用：seed N · X.XX%')+clearHovered/hideTooltip
    - materialSorting-web/src/components/ControlPanel/StrategyRunModal.tsx: 应用按钮 title 更新（已接线「会替换当前画布的排料方案」/未传回调「应用回调未接线」；disabled 语义不变）—— 弹窗内部零改动（US-005 prop 链预留直接生效）
    - materialSorting-web/src/components/__tests__/NestingPage.test.tsx 新建（5 项集成，全链路渲染 NestingPage→弹窗→点应用）：registry 恰 1 条字段齐全（旧 run 被清；manifest demand/frames FrameMsg 同形/lastFrame 同引用/finalDensity 双口径/viewBoxMaxW/done/ws=null/stopped=false）；phase='done'（状态行+#restart+seekbar 解禁 max=ceil(elapsed)）；ExportButtons 非 disabled + NestSVG 多副本渲染（demand=2 → 2 可见 polygon 且 points 互异）；点导出 POST /export 载荷=合成帧（seed/gate_mm/width_mm/density/placed 多副本）；不点 apply registry 不变（显式按钮不自动应用）
    - scripts/us006_verify.mjs 新建（范本 us005_verify.mjs）：真实 WS 求解 25s 捕获 manifest+终帧（g01@30 demand=2 多副本）→ fetch stub /api/strategy/status|result 成 done 结果态 → 真实 UI 上传/commit/开弹窗/应用 → 主画布 + 导出 DXF/PNG 全断言（R12 AC1009 + POLYLINE 非 LWPOLYLINE + PNG magic + /export 载荷对拍）
  验收:
    - vitest 全量 **630 passed**（625→630，0 failed；NestingPage 5 项新增）；npm run build（tsc --noEmit）通过
    - 浏览器验证 scripts/us006_verify.mjs **22/22 PASS**（ms-web 真后端 :8000 + Chrome 151 headless CDP :9223）：结果态（完成·最优 75.72%/seed 7·用布 1.22m/race 2 轮中 1 轮门杀/应用按钮已接线）→ 应用（状态行「策略 run 已应用：seed 7 · 75.72%」/NestCard 恰 1/多副本全可见 40≥20/翻转组 translate(0 1980) scale(1 -1)/viewBox 0 0 1219.46×1980/红虚线边界/收敛曲线/seekbar max=26/导出解禁/#restart）→ 导出（/export 载荷 seed=7·width·density=best·g01_30=2 条 placement；DXF 359KB AC1009+POLYLINE 无 LWPOLYLINE；PNG 484KB magic 有效）；shapely 几何复核：20 placement 全部在 width×gate_nest 界内、两两相交 0 对、g01_30 两副本质心互异
    - 导出链路零改动：ExportButtons/useExport/bestRun()//export placed_to_world 未触碰；母版漂移 warning 展示与 400 兜底沿用 US-004/US-005 既有行为（弹窗单测覆盖）
    - server/Chrome 全清（8000/9222/9223 free）；文档同步: .docs/technical/agent-component-map.md（头部覆盖注记+NestingPage 树行+US-005 不变量 #5 收口+US-006 专节文件表+关键不变量 5 条）、materialSorting-web/AGENTS.md（文件分工行+树行 2 处+策略 PRD US-006 关键约定节）
