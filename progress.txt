# Ralph Progress Log
Started: 2026-08-09











## 2026-08-19 US-006（PC-006）solver_opts 透传与配置轮换
- `web/solver.py`：`build_instance` 加可选 `solver_opts: dict | None`（additive 白名单，签名尾部追加、不传 = 现行行为原样）。清洗单一真相源 `_normalize_solver_opts`（`exploration_pct` clamp [0.1,0.95] / `quadtree_depth` clamp [3,5] 取整 / `num_workers` 下限 1 取整；非数值/NaN/±inf/未知键一律忽略）+ `_clamp_finite` + `_split_time_budget`（total 秒按 pct 切两段 int 秒，各 >=1s、和约等于 total，小预算极端下保两段非 0）。关键坑（单测固化）：spyrrow `StripPackingConfig` 的 `total_computation_time` **缺省 600 非 None** —— 两段模式必须显式传 `total_computation_time=None`，否则 not-all-3 ValueError；两段与 total 互斥由 spyrrow 自身校验。不传旋钮时构造与旧版逐字段一致（total 模式 80/20 + quadtree 4 + workers 4）。
- `web/solve_worker.py`：零代码改动（`**solve_params` 直通），docstring 键清单补 solver_opts（全 JSON 可序列化，Windows spawn 安全，worker 不解释旋钮语义）。
- `cli/pipeline.py`：`solve_pieces` 加同形 `solver_opts` 参数并入 solve_params（主进程 meta 构造与子进程求解同一清洗口径）；非空时返回记录附带 `solver_opts` 回显、空档不加键（无旗标冒烟 result.json solve 记录字段集零回归）。
- `cli/portfolio.py`：`run_serial_portfolio` 加可选 `solver_opts_for(index, seed)`（0 起队列序 → 非空 dict 才以 `solver_opts=` 传该轮 solve；缺省不加键保旧 fake 兼容）。
- `cli/run_config.py`：新旗标 `--solver-opts <JSON>`（固定档全 seed 生效）/ `--rotate-opts`（默认 OFF，内置 `SOLVER_OPTS_POOL` 4 档含池首空档=默认行为，`rotation_opts_for(seed_index)` 取 `pool[队列序 % 4]` 去相关轮换）；两旗标互斥 / JSON 坏串 / 非 JSON 对象 → 配置错误退出 1（new_run_dir 之前拦下不留空目录）；旋钮生效方式一行说明（--quiet 也打）；result.json config 段**旗标给了才**回显 `solver_opts`/`rotate_opts` 键。argparse help 中字面 `%` 必须 `%%` 转义（help 字符串走 %-format，裸 % 直接 ValueError 崩 --help）。
- 新增 `tests/test_cli_solver_opts.py` 13 例：build_instance 直测（无旋钮=基线逐项一致 / pct=0.6·0.9 两段 int 秒 60/40、90/10 和约等于 budget、小预算两段各 >=1s / 越界 clamp 0.01→0.1、5.0→0.95 / quadtree 5 透传+9→5+1→3、workers 0→1、非数值忽略 / 旋钮不扰 instance 四元组）；透传链（fake `solve_with_callback_proc` 断言 solve_params JSON 往返含 solver_opts + 记录回显、不传无键零回归 / 真多进程 spawn 消费含 bogus 键 opts 求解成功）；轮换与 CLI（池 4 档 k=5 第 5 个 seed 回 pool[0] / solver_opts_for 缺省不加键 / 互斥+坏 JSON+非对象退出 1 且不建 run_dir / 固定档双 seed 同档+config 回显 / --rotate-opts 池序列+rotate_opts:true / 无旗标 config 段字段集=PC-001 基线）。
- 验证：pytest 全量 **213 passed**（200→213）；`python -c "from materialsorting.web.solver import build_instance"` 导入冒烟 + `python -m materialsorting.cli.run_config --help` 含新旗标；真实冒烟（5336_coded_really.json）：无旗标 `--time 5` = 83.07%/119 片与基线一致；`--solver-opts '{"exploration_pct":0.5,"quadtree_depth":5}'` 跑通（83.06%，config 段与 solve 记录均回显旋钮）；`--rotate-opts` 3 seed 轮换逐 seed 取档 None/{0.9}/{0.6,q5}（result.json per-seed solver_opts 实证）；AST 分层零反向（cli → web 单向沿用既有断言）。
- 文档同步（/sync-docs）：README（配置驱动求解新增 PC-006 块 + 用法示例两行）、CLAUDE.md（配置驱动求解条目 PC-006 摘要）、.docs/technical/agent-file-map.md（cli 节导语 + 目录树 solver/solve_worker/pipeline/run_config 行 + 详表 solver/solve_worker/portfolio/run_config 行 + 行数订正）、.docs/technical/agent-api-reference.md（build_instance 签名与 solver_opts 段）、web/AGENTS.md（solver.py 行 US-006 注记）、tests/AGENTS.md（test_cli_solver_opts.py 行）。
- 本会话无 chrome-devtools-mcp 且改动仅限后端求解链透传（web/server 路由、WS 协议、前端零改动），无可视化回归面。
