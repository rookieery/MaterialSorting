# Ralph Progress Log
Started: 2026-08-09

## 2026-08-18 US-004 高级配置弹窗矩阵化（g码×码号 d/tol）
- 数据层：types/v03.ts PerTypeOverrides 改两级嵌套 Record<label, Record<sizeKey, {d?,tol?}>>；lib/params.ts 增 PerTypeFormMap（弹窗草稿字符串形状）+ perTypeSizeKey（number→String / null→'null'，与 serializeQuantities 同口径单一实现）；collectParams 双层遍历 label→sizeKey（空串档剥离、双侧全空格剔除、整体空→null）。
- URL 分享：perTypeToUrlParam → `label@sizeKey=d,tol;...`（仅非空格产出）；perTypeFromUrlParam 正则 `^(g\d{1,4})@(null|\d{1,4})=([^;,]*),([^;]*)$` 逐段校验，旧 ptype 键/中文/NaN/乱拼段静默跳过不抛错（lenient decode）。
- PerTypeOverridesModal 矩阵化重建：行 = doc.sizes（升序 null 通用殿后）∪ 已配置 sizeKey 追加行（旧配置不静默丢），doc=null → SIZES fallback；列 = /api/ptypes reps 键 ∪ 已配置键，compareByLabel 数值序（先长度再字典序 g99<g100）；列头 = 缩略图（点击放大预览）+ .qty-label-badge g 码 + .qty-rowfill-btn「≡」；格 = (g 码, 码号) d/tol 双输入（testid d-/tol-${label}-${sk}，空 = 继承默认 0/0，blur clampDraft 收边）；doc 缺片格「—」disabled（label 不在 doc 不判缺，parse/commit 暂不同步不阻塞）。
- ColumnFillPopover（文件尾内部组件）：「≡」整列设值弹层，Portal body + fixed 表容器可视区中心（QtyMatrix 同款定位）；复用 .qty-fill-* 样式 + --per-type modifier（z 1150/1160 盖过 overlay 1100）；应用写全部非 missing 行（不造 phantom 键），留空侧 = 继承默认；Enter 快捷应用。
- 三层独立 ESC：modal listener 判 previewLabel===null && fill===null 才关；PtypePreviewModal 只关预览；popover 只关自己。草稿 + 确定：mount 深拷贝（不再预填 '0'），确定 pruneDraft 剔全空格回写。
- style.css：.per-type-rowhead 52px / .ptype-col 92px、.qty-colhead-meta 居中、.per-type-cell 双 input 堆叠、.per-type-missing、.qty-popover-backdrop--per-type(1150) / .qty-fill-popover--per-type(1160)。
- PtypePreviewModal：US-003 已 label 键就绪，浏览器联调缩略图 → g 码放大预览正常（harness 断言）；nestingTour params 步文案改矩阵口径（不 bump TOUR_VERSION）。
- 测试：PerTypeOverridesModal.test.tsx 全量重写 30 项；params.test.ts per_type 嵌套组重写 + URL 分享 6 项新增；ControlPanel.test.tsx 矩阵断言（d-g01-28 + cfg.per_type['g01']['28']={d:1,tol:1}）；useSolveRun fixture 随动 —— npx vitest run 605/605 绿、tsc --noEmit 零错、npm run build 过（209.50 kB）。
- 后端命中冒烟：build_instance per_type={'g03':{'28':{'d':1.5}}} → n_eroded 0→1（仅 g03_28 被腐蚀，(label,sizeKey) 键命中）。
- 浏览器验证（CDP headless Chrome harness scripts/us004_verify.mjs，15/15 PASS + 截图 out/us004_verify/）：上传母版 → 矩阵 10 动态 g 码列 × doc.sizes 11 码号行 + d/tol 格 + 10 个 ≡；三层 ESC（预览/整列设值独立）；配 g03@28 d=1.5 + g05 整列 d=2 → 确定 → 选码 → 开始求解 → patch WebSocket.prototype.send 捕获 WS start payload 断言 per_type.g03['28'].d===1.5（端到端 AC）+ g05 全码号键。
- 文档同步：agent-component-map（header + US-018 节 5 行改写 + 不变量 2/3/6/7 修订 + 新增 11~13 + params.ts/style.css/PerTypeOverrides.tsx/ControlPanel.test.tsx 行）、agent-api-reference（WS per_type 注释补 US-004 矩阵产出与 sizeKey 口径）、materialSorting-web/AGENTS.md（header + 目录注释 + US-018 节 US-004 矩阵条目重写 + params 约定 3 处修订）。
