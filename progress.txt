# Ralph Progress Log
Started: 2026-08-09









- 验证：`ms-run-config data/configs/5336_coded_really.json --time 5` 冒烟（real 83.07%、119 片、curve 1013 帧单调、best_frame=曲线最大帧）；新增 `tests/test_cli_solve_pieces_proc.py` 9 例（fake solve 注入帧序列 + 真子进程 terminate 端到端：exitcode≠0/on_process 句柄 join 无孤儿/active_children 无 solve_worker）；pytest 全量 **124 passed**（含 3 例 test_ws_stop 环境数据漂移自愈 —— 本会话重跑 `_commit_to_nesting_sync` 回写 M1787 intermediate，out/ 为 gitignore 区）；compileall + `python -m` 导入冒烟 + AST 分层全包零反向。
- 本会话无 chrome-devtools-mcp 工具且改动仅限后端 CLI 求解链（web/server、solver、前端零改动），无可视化回归面。

## 2026-08-19 US-002 portfolio 调度器与最优帧入账（PC-002）
- 新建 `cli/portfolio.py`（294 行）：`PortfolioController` 状态机（incumbent 全局最优帧 banking —— 每帧 density 严格大于即入账 {density,width_mm,seed,frame_index,elapsed,placed_items 完整布局}，被 kill/Ctrl-C 中断 seed 的最优帧同样参与；`make_progress` 兼进度行输出（per-seed 新最优/30s 心跳逐字保留旧格式、跨 seed 反超才打 incumbent 行、echo=None 即 --quiet 全静默）；`make_should_stop` R0 达标即停（仅 --target 给定时挂载 → 无旗标 solve 调用形与旧版完全一致，触发帧先入账后停）；`finish_seed` per_seed {seed,killed,kill_reason,best_density,elapsed}；`portfolio_section()` engaged(target 给定或 seeds≥2)/不激活(单 seed 无 --target 全空段)；`best_record()` engaged→incumbent / 否则旧语义 max(real_density)）+ `run_serial_portfolio`（串行编排，solve 可注入 fake、Ctrl-C 捕获为 interrupted+last_round、求解异常上抛、queue_stopped 剩余 seed 不启动）+ `load_controller_params`（--params 校验加载：存在/合法 JSON 对象/utf-8-sig 容错 BOM，ControllerParamsError）。
- `cli/run_config.py`：串行循环改经控制器转发；新旗标 `--target P`（(0,1] 越界退出 1）与 `--params FILE`（坏文件退出 1）；result.json 新增 portfolio 段、best 语义升级（engaged → best==portfolio.incumbent 帧级全局最优含完整 placed_items；单 seed 无 --target → 空段 + best=solve 记录，无旗标冒烟对拍兼容）；`_best_summary()` 归一两种 best 形态保末行汇总格式不变；轮次头扩展到单 seed+--target；R0 提前停打终局行（--quiet 也打）退出码 0；删 `_make_progress_printer`（进度行职责移入控制器）。
- 新增 `tests/test_cli_portfolio.py` 20 例（fake solve 双层驱动：run_serial_portfolio 注入 + monkeypatch rc_mod.solve_pieces 走 main()）+ 更新 test_cli_run_config 2 例（result keys + 多 seed best=incumbent 与 best_frame 文件对拍）。
- 验证：pytest 全量 **144 passed**（124→144）；`ms-run-config data/configs/5336_coded_really.json --time 5` 无旗标冒烟 = PC-001 基线（83.07%/119 片，best==solve[0] 旧语义 + 空 portfolio 段）；`--time 20 --target 0.5` 真实 R0 冒烟（frame 1 达标即停、killed=R0_target_reached、退出码 0、best==incumbent）；2 seed 真实冒烟验证 best=incumbent 帧峰 83.11% > 两 seed 终值 83.07%/82.36%（盲区修复实证）；compileall + `python -m materialsorting.cli.run_config --help` 含新旗标 + AST 分层全包零反向（portfolio 模块级仅 .pipeline+stdlib）。
- 本会话无 chrome-devtools-mcp 工具且改动仅限后端 CLI 求解链（web/server、solver、前端零改动），无可视化回归面。
