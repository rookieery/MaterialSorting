# Ralph Progress Log
Started: 2026-08-09





4. **[2026-09-02] US-004 — 前端类型 + 状态行 + 预览放大层文案（分支 ralph/prefix-extra-piece，commit 即将落）**：prd.json 首个 passes:false 故事（US-005 收官未动）。后端 US-002/003 已落地的 stage extra_*/residual_mm 与 preview extra/residual_mm/gate_mm/fallback 补前端类型镜像与两处双形态呈现，协议 additive 零破坏。① types/ws.ts：StageMsg 加可选 extra_label/extra_size/residual_mm（补片 g 码/尺码/gate−组合片高 round 3；null 与键缺席同判=兜底 4 片，旧后端无键安全）；PrefixConfig/StartPayload.prefix/useSolveRun 与 NestingPage/PerTypeOverridesModal 文件头注释口径同步改写 seeded 随机→近满幅几何搜索（seed 仅兜底路径）。② types/band.ts：PrefixPreviewResponse 加可选 extra:{label,size}|null（只两键——PS_/rotation 永不出端点）/residual_mm/gate_mm/fallback；n_members 注释「恒 4」→「4（兜底）或 5（补片）」。③ 呈现双形态：NestingPage onStage prefix 有补片→「起始端成套构造中（尺码 A＋g@B）…」（＋全角），兜底/无键→现行文案；PerTypeOverridesModal prefix-zoom 放大层 hint 有补片追加「＋ 顶部 {label}@{size} 异码片 · 余 {residual_mm}mm 近满幅」（residual 缺席显 —）；5 片缩略自动渲染（BandPreviewMember 泛型零改动，异码片 size_color(B) 同码同色跨片型，tag=g 码）；params.ts/collectPrefix/ControlPanel 闸门零改动（prefix 契约仍恰三键）。④ 测试：useSolveRun.stop.test.tsx +3（有补片状态行「尺码 38＋g02@32」+rec.stage 持新键不 finish/兜底 null 回落现行形态/旧后端无键不炸）+ PerTypeOverridesModal.test.tsx +2（PREFIX_EXTRA_OK 5 成员 fixture：缩略 5 片+异码 fill #ff7f0e data-size 29+hint 含「＋ 顶部 g02@29 异码片 · 余 210mm 近满幅」+5 标签；无键回落现行 hint 不含「＋ 顶部」「近满幅」）= vitest 810→815 全过 + typecheck 干净 + build 过；后端零改动全量 pytest 676 全过（基线复跑）。⑤ 浏览器验证（out/us004_extra_verify/verify.mjs CDP 无头 25/25 全过，截图 01-05：5336 全量数量+per_type g02/g03 d=2/tol=1 → 预览 5 片 hint「＋ 顶部 g02@32 异码片 · 余 1.55mm 近满幅」与求解 stage 同选同残量（同源真相源）→ 求解状态行「起始端成套构造中（尺码 38＋g02@32）…」→ final 88.03%/width 7163/placed=105=Σdemand/min_x=0.10mm/PS_ 零泄漏/首列 g02_38→g03_38→g02_38→g03_38→g02_32 前-后-前-后 180° 交替、异码片 g02@32 在列端相切、列高 1978.7mm 近 gate 1980；弹窗前置 per_type 草稿进 preview 载荷已验证（弹窗内改 d 不重开预览即变）。⑥ 发现移交 US-005（引擎潜藏问题，非本故事范围，本次未改引擎）：a) 稀疏数量+无 per_type（d_g=0）时 select_prefix_plan 可选近零间隈组合（@34+g02@38 residual=0.307mm）sparrow 放不下引发 Rust panic，worker 降级 error「求解失败：solver 返回 None」；全量数量+per_type d=2/tol=1 下选 @38+g02@32 residual 1.55 稳定可解——建议选码加 d_g 侵蚀后间隈下限或残量阈值守护；b) sparrow 可将整组合片旋转 180° 落位（成员 rot 全 +180）→ 构造顶部异码片实际落在世界底部——形态判据须 flip 容忍（prefix_accept/A-B 验收注意）；c) DOM/manifest 渲染 erode 后几何（比 residual 用的原始轮廓 union 高短 ~2×d_g），勿用 DOM span 反推 residual。⑦ 文档同步：web/AGENTS 6 处（types 行 additive 键/NestingPage 行双形态/PerTypeOverridesModal 行 hint 双形态/文件分工头与尾部新条目/不变量 2 条 seeded→几何搜索改写）、agent-component-map（覆盖头新条目+types/band.ts 行+「起始端成套 US-004 落地」节 2026-09-02 补记小节+seeded 口径 3 处改写）、agent-api-reference（1.5 stage 节前端双形态补记）。
5. **[2026-09-02] US-005 — 端到端验收与文档闭环（分支 ralph/prefix-extra-piece，commit 随本条落地）**：prd.json 首个（且唯一）passes:false 故事，单一故事零批处理；本故事零后端/前端源码改动（纯冒烟脚本 + 文档），分层依赖天然未反向。① 新建 `materialSorting-web/scripts/smoke_prefix_extra.mjs`（~370 行，模板 smoke-band-preview.mjs CDP 套路，Node ≥22 原生 WebSocket 零依赖）：chrome port 9226 无头 → 上传 5336#老六订单14%7%围加9_coded.dxf → 数量矩阵 g01-g05 × {32,33,34,35,38}→2, 36→3（Σdemand=105，勾 7 码 [31,32,33,34,35,36,38]）→ per_type g02/g03 d=2/tol=1 → 布局设置开 prefix（前 g02 后 g03）→ 预览断言（thumb 5 片 sizes [38,38,38,38,32]、异码色 #9467bd、放大 hint「＋ 顶部 g02@32 异码片 · 余 1.55mm 近满幅」）→ #time=60 求解 → 状态行「起始端成套构造中（尺码 38＋g02@32）…」→ final+末帧检查（final 无 placed_items 键=WS 协议设计、末帧 placed=105=Σdemand 守恒 front2+back2+extra2、首列 interleave g02_38→g03_38→g02_38→g03_38→g02_32 头尾 180° 交替、min_x=0.10mm 锚定 pin.skipped、DOM 贴触缝隙全负（咬合）、colResidual 5.65≈residual 1.55+2×d_g=2 erode、密度 88.03% width 7163mm）→ 导出 PLT（fetch 抓包 476809 字节 b'PS_' 缺席、PU 笔数 2839）→ prefix_runs 工件快照（mtime > solveT0、5 成员 gaps [0,0,0,0]）→ report.json 落 out/smoke_prefix_extra/。29 检查项（1a/2a-2e/3a-3f/4a-4c/5a-5h/6a-6e/7a-7b）全 PASS。排障三坑入档：WS final 协议不带 placed_items（placed 须取末帧 frame，us004 同口径）；final.prefix.pid 含 PS_ 属设计回显（泄漏探针改判 final 无 'placed_items' 键）；PREFIX_RUNS 须 import.meta.url 锚定 ROOT（resolve() 基于 CWD 踩坑 ENOENT）。② prefix_accept 密度 sanity + 确定性验收：`py -3.11 -m materialsorting.web.prefix_accept --seeds 0,1 --time 30 --report out/config_runs/_probes/prefix_extra_accept_report.json` EXIT=0 accept —— 密度 off 88.386%/on 87.829% 劣化 0.556pt ≤1.0pt、双开劣化 0.297pt；形态 2/2（min_x −2.068/−0.921mm ≤6mm、gaps [0,0,0]）；确定性 size_equal=True、frames 1137==1137 逐帧全等、final_best_equal=True、artifact_replay_equal=True（即 chunk.to_dict() 全等）best 88.425%；导出 on/off 零泄漏（总 311.9s）。③ `python -m materialsorting.nesting_engine.prefix` 冒烟 exit 0：选定 套装@38 + 顶部 g02@32 rot0 H=1978.4mm residual=1.6mm（120 组合无 RNG）、PS bbox 1175×1978 fill 82.0% members=5。④ 基线全绿：后端 pytest 全量 676 passed；前端 vitest 815 passed（53 files）+ tsc 干净 + npm run build 过；ms-web/chrome 用后即杀（cmd //c taskkill，端口归零确认）。⑤ 文档九处同步：CLAUDE.md（prefix.py 条目尾 US-005 收官数据 + 数据流主线 prefix 段收官指针 + line47 配置驱动 seeded 措辞换几何搜索）、README（prefix config 行 seeded→近满幅几何搜索口径 + 冒烟/验收指针）、.docs/business/起始端成套前后幅_版师确认清单.md 追加 §9（9.1 需求 / 9.2 三项定案：兜底退回 4 片、不设缝隙阈值、保留 0°/180° / 9.3 实现口径 / 9.4 端到端验收表 + 判定 accept + 两残余观察项：组合片可被 rot180 落位、稀疏数量无 per_type 近零间隙可致 solver 降级）、agent-api-reference（stage 节 US-005 端到端指针 + preview 不变量 #4 冒烟实测补记）、agent-file-map（prefix.py 树行 + prefix_accept 树行/表行收官实测数据）、agent-component-map（scripts 表新增 smoke_prefix_extra.mjs 行 + 不变量 #1 stale eligible→pick→build 措辞修正为 select_prefix_plan 线程池口径）、business-overview（prefix 行增补 US-005 收官段）、web/AGENTS（起始端成套 US-004 节尾新增 US-005 冒烟条目+回归指引）、tests/AGENTS（test_prefix_accept 行 2026-09-02 收官实测注记：本故事零新用例、验收数据在案）、memory 新条目 prefix-extra-piece-acceptance.md（选码口径沿革：近满幅几何搜索取代 seeded 随机 + 验收数据 + 两残余观察）+ MEMORY.md 索引行
6. **[2026-09-02] 修复 — prefix 无 per_type 求解必炸「solver 返回 None」（门幅安全余量 + panic 透传）**：用户报 web 运行算法 seed 0 报「求解失败: 求解失败: solver 返回 None」（US-004 ⑥-a 移交问题的真实触发，当时验收全走 per_type d=2 路径未修）。根因链（本地 5336 无 per_type 100% 重现）：① d_g=0（web 上传默认无 per_type）时 `select_prefix_plan` 近满幅搜索选出贴线组合（PS_g02+g03@34+g02@38 H=1979.69 / gate 1980，residual 0.307mm），组合片碰撞轮廓不做 erode 收缩 ≈ 条带高；② spyrrow 放置器放不下 → Rust panic `strip-width is running away (item 68 = PS_ 组合片)`，探针实证片高 ≥ strip_height（含恰好等高）必 panic、留余量即 OK；③ pyo3 `PanicException` 是 **BaseException 子类**，`solve_worker._solve` 的 `except Exception` 捕不住 → 求解线程静默死亡、holder 空 → 走「理论上不可达」防御兜底误报「solver 返回 None」（内层 worker:209 + 外层 routes_ws:344 双层包装）。修复（用户确认 A+B、margin=10）：① `prefix.py` 新增 `PREFIX_GATE_MARGIN_MM=10.0`——`select_prefix_plan` 可行线 `limit = gate_nest − 10`（取次高安全候选，residual ≥ 10 恒成立）+ `build_prefix_plan` 竖排高守卫同口径（安全条带界 = 门幅−10，兜底 4 片路径同样防贴线；d_g>0 时 erode 对非水平边缘收缩 <2·d_g 故不按 d_g 折算）；② `solve_worker._solve` 与 `solver.solve_with_callback`（threading 兼容版）`except Exception` → `except BaseException` 字符串化（带异常类型前缀）——下次任何 Rust panic 都能看到真因而非误导兜底文案（daemon 线程收不到 KeyboardInterrupt，捕获无副作用）。测试 +4：test_prefix.py（贴线候选拦下取次高 / 全贴线兜底 / 兜底基座贴线 PrefixError「安全条带界」）+ test_solve_proc.py（2200 高片段条带 1000 → err 含 PanicException+strip-width 且不含「solver 返回 None」）。回归：pytest 全量 680 passed；无 per_type CLI（事故路径）跑通 86.68%/119 片（修复前必炸）；带 per_type CLI 87.87% 跑通（margin 10 < erode 4mm 缓冲的既有 per_type 场景零影响）。既有独立问题（本次未动、修复前同败）：`python -m materialsorting.nesting_engine.prefix` 冒烟在 grouped 备档构造 FAIL（最大缝隙 932.64mm > 1mm，PrefixError 形态守卫）——与 margin 无关，另行排查。
