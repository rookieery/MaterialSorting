# Ralph Progress Log
Started: 2026-08-09
- [US-005] 前端会话接入与阻断弹窗（web 多会话第五块，纯前端：新 lib/session.ts + lib/api.ts + SessionExpiredModal.tsx 三新文件，8 处裸 fetch 全换 apiFetch，后端零改动）：① lib/session.ts —— sid 单一真相源：localStorage 键 ms_sid、uuid4 hex 32 位（RFC4122 version=4/variant 位置位，crypto.getRandomValues 优先 Math.random 降级）、getSessionId get-or-create（模块缓存 + 非法落盘值重铸 + 落盘失败静默走内存）、clearPersistedSessionId（墓碑出口）、resetSessionIdForTest（仅清模块缓存，测试先写库再清缓存模拟刷新）；② lib/api.ts —— apiFetch 全站统一 HTTP 出口 = **唯一裸 fetch 点**（grep 'fetch(' 仅命中本文件）：三合一 = 注入 X-Session-Id（mergeSessionHeaders 把 Headers 实例/数组/对象归一 **plain object** + sid 覆写，保调用方/测试 Record 属性直取旧口径）+ 会话先行门（ensureSession 模块级 once-promise POST /api/session，并发共享、失败静默缓存不重试；**mount 竞态修复**：React 子组件 effect 先于父组件跑，NestingPage 策略轮询 mount 即发请求会先于 App 探测吃 401 误弹「已过期」—— 首次 apiFetch 统一 await 探测，任何会话作用域请求结构性晚于建会话；probedSettled 置位后同步直进 fetch，行为与旧裸 fetch 逐字节一致，存量 deferred-response 用例零改动）+ 401/429 拦截（inspectSessionError：res.clone().json() 读 code 键，session_expired/session_limit → triggerSessionBlock，400 无 code/网络错/非 JSON 不触发；原 Response 原样交还调用方）；阻断态 = 模块级 pub/sub（lib 不引 zustand 防层环）：getSessionBlock/subscribeSessionBlock/triggerSessionBlock（幂等首个 code 定终身）+ SessionBlockedError（阻断期间 apiFetch 直接抛，**请求不发出** swallow）；**session_expired 弃 sid / session_limit 保 sid**：后端墓碑 1h 拒重建旧 sid，刷新带旧 sid 只会探测 401 死循环 —— triggerSessionBlock('session_expired') 顺手 clearPersistedSessionId()，刷新即铸全新 sid 得干净会话（「刷新重来」唯一通路）；测试钩子 markSessionProbedForTest（预置已探测跳过 POST /api/session）/resetSessionForTest；③ lib/ws.ts —— solveWsUrl 拼 ?sid=<sid>（浏览器 WS 不能自定义 Header，对后端 US-003 query 口径）；types/ws.ts ErrorMsg 加可选 code?（additive 旧前端忽略）；useSolveRun case 'error' 前置 msg.code 检查 → triggerSessionBlock（WS 与 HTTP 同一阻断入口）；④ SessionExpiredModal.tsx —— 阻断式全屏模态：useSyncExternalStore 订阅阻断态；.session-block-overlay[role=alertdialog][aria-modal] z 3000（盖过 tour 2000/策略 1100）；**不可点遮罩/ESC/✕ 关闭、无关闭回调**，唯一出口 = 「刷新页面」按钮 location.reload()；文案与后端 PRD 逐字一致（expired=「会话已过期（5 分钟无操作），请刷新页面」不显示上次活动时间 / limit=「当前使用用户过多（最多 4 人同时在线），请稍后尝试」）；无阻断返回 null；App.tsx mount useEffect probeSession()（探测幂等 StrictMode 双跑安全）+ 弹窗单例；⑤ 8 处替换：useParseDxf/useCommitToNesting/useExport/ptypeStore/strategyStore×3/PerTypeOverridesModal×2 fetch → apiFetch 其余零改动；style.css .session-block-* 族（暗色卡 #26282e 同策略弹窗系）；⑥ 测试 +32 共 **741 全绿**：session.test 6（生成 32hex+落库/幂等/预置刷新不变/非法重铸/version+variant 位多取样/clearPersistedSessionId）+ api.test 18（Header 注入无 headers/与 Content-Type 合并/Headers·数组归一；先行门探测先于业务请求/once 并发共享/探测 429 排队请求 swallow/markSessionProbedForTest 跳过/ensureSession=probeSession 同一 promise；401·429 触发+原 Response 未消费/blocked 后续抛错且 fetch 计数冻结/无 code·400·200 不触发/trigger 幂等+订阅一次通知/probeSession 200·429·网络错三态；**session_expired 弃 sid（ms_sid=null+下次取值换新）/session_limit 保 sid**）+ SessionExpiredModal.test 4（无阻断 null 渲染/expired 文案+reload 点击/limit 文案/ESC·遮罩无 ✕ 不可关）+ useSolveRun WS 用例改写（URL 断言 ?sid=32hex + 精确拼接 + 业务 error 无 code 不阻断 + session_expired code 帧阻断）；存量 5 个 fetch 计数文件 beforeEach 加 markSessionProbedForTest **零断言改动**（useParseDxf/useCommitToNesting/useExport/UploadPanel/useSolveRun 既有）；npm run build ✓；grep 'fetch(' src 非测试非 api.ts **零命中**；⑦ 浏览器验证 scripts/us005_session_verify.mjs（playwright + chrome channel，createRequire 指向 web 包解析 devDependency；主相位 P1-P5 前置 = 重启 ms-web 保注册表 4 空席，过期相位 --expire 前置 = MS_SESSION_TTL_SEC=6）：**主相位 15/15** —— sid 各窗 32hex 互不相同/刷新不变/探测 POST /api/session 带 Header/ptypes 全部带本窗 sid；双窗口上传互不串台三层取证（弹窗 thead g 码徽章 —— **选择器限定 .per-type-modal**：上传预览 QtyMatrix 同名 .qty-label-badge 全局选择器假阳性，且大母版 commit 1-2 分钟、commit done 才 invalidate→弹窗 refetch 出 reps，轮询给足 240s，否则空 reps 假失败/各自 sid 直取 ptypes 响应体对比/g01 缩略图 polygon points 对比）；WS URL ?sid=sidA；第 5 窗口页面加载即弹「用户过多」（无需先上传）+ 第 3/4 窗口无弹窗（**前置陷阱记档：注册表残留 sid（如验证前 curl 探测）会让第 4 窗口误吃 429 —— 跑前必须重启服务器**）；阻断期间上传 /api/parse-dxf 0 请求 + 唯一出口刷新按钮；**过期相位 5/5** —— TTL=6 静置 8s 后开高级配置（apiFetch 读路径）→「会话已过期（5 分钟无操作），请刷新页面」弹窗 → ms_sid=null（弃 sid）→ 点刷新 → 新 sid ≠ 旧 sid + 无弹窗（干净新会话）；服务器生命周期全程记 PID 验后杀净端口确认释放；⑧ 文档同步 4 处：materialSorting-web/AGENTS.md（文件分工六行 + 新「多会话 US-005 关键约定」节 8 条）、agent-component-map（新落地节：改动文件表 + 不变量 6 条 + 测试验证）、agent-api-reference（session 节生命周期第 8 条前端接入 + WS 节前端侧 bullet）、CLAUDE.md（前端迁移 bullet 补多会话 US-005 句）；US-006/007（uploads TTL 磁盘清理/端到端验收）留待后续 session

- [US-006] uploads 磁盘 TTL 清理（web 多会话第六块，后端三文件改动 + 两新文件：sessions.py 加 active_doc_ids() / server.py 两触发点 / 新 web/diskclean.py + 新 tests/test_web_diskclean.py 17 例）：① diskclean.py —— scan_uploads 全参数可注入（dry_run/uploads_dir/config_runs_dir/ttl_days/now/registry），删超龄（MS_UPLOAD_TTL_DAYS 缺省 14 天按 mtime，_env_days 非法/非正 warn 回退）<doc_id>.dxf + <doc_id>_pieces/ **成对**目录 + 孤儿单边 + 超龄 strategy_cfg_*.json；**成对判龄关键防误删**：同一 doc 任一侧未超龄 → 整对保留（commit 重写 pieces 目录刷新其 mtime 但不动 dxf 的上传时刻 mtime，按单侧独立判龄会误删「上传很久刚 commit」的母版），孤儿单边按自身 mtime 独立判；保护集 = registry.active_doc_ids() ∪ out/config_runs/.web_strategy_active*.json 全 marker 内 doc_id（glob 前缀经 strategy._MARKER_SID_PREFIX rstrip('_') 单一真相源延迟 import，default 旧名 + sid 分文件都覆盖；**会话 B 已过期但策略 run 在跑 → marker 是唯一保护来源**——注册表逐出不通知磁盘）∪ mtime 未超龄；_mtime 读不到返 +inf 按未超龄（宁漏删不误删）；非 web 命名（doc_id 不匹配 SID_RE 的 dxf / 非 *_pieces 目录 / 非 strategy_cfg_*.json）一律不动；② 容错双层 —— 单条目删除失败（rmtree 被占用等）scan 内 warn 跳过继续，扫描阶段意外异常由 trigger_cleanup 整体吞（**绝不向调用方抛**，返回已删清单/失败 []）；③ sessions.SessionRegistry 补 active_doc_ids()（锁内快照 st.doc_id ∪ state['doc']['doc_id'] —— 后者覆盖 default 启动 reload 形态：SessionState.doc_id 仅 commit 路由填写）；④ server.py 两触发点 —— commit 路由尾 `await loop.run_in_executor(_executor, diskclean.trigger_cleanup, str(UPLOADS_DIR))`（executor 不阻塞事件循环；**显式传模块常量 UPLOADS_DIR 是测试安全关键**：存量测试只 monkeypatch server_mod.UPLOADS_DIR 不 patch paths.OUT_DIR，不显式传会扫到真实 out/ 误删真文件）+ main() 里 diskclean.start_startup_cleaner()（daemon 线程不阻塞启动；**不放模块级 import** —— TestClient 导入 app 会起线程扫真实 out/）；⑤ 分层 —— diskclean 模块级仅标准库 + ..paths，sessions/strategy 函数内延迟 import，禁 import server/cli（AST 守卫 + 源级哨兵）；⑥ tests 17 例（新旧混合/混龄整对保留/孤儿双侧/保护集三源含 AC 核心「A 活跃 + B 过期但 marker 在 → B master 不删，marker 移除后才可清」/dry-run 只列不删/单条目失败 warn 继续/trigger 吞异常/env 解析 reload/路由级 commit 触发清理 + 清理抛异常不影响 200 + 清理范围=显式传入 UPLOADS_DIR/AST 守卫）；**测试隔离坑（记档）**：无 sid commit 路由测试会 _reload_pieces_state 原位改写共享 _PIECES_STATE，commit_clean_env fixture 必须快照/恢复，否则污染 test_ws_stop（其 _smallest_loaded_size 从已加载状态动态取码）→ 首版 1mm² 微型裁片 stub 让 solve 报 error 连坐 3 个 ws 测试；⑦ 冒烟 python -m materialsorting.web.diskclean = 临时目录 13 项场景自检（PASS 13/13）+ 真实 out dry-run 只读打印将删清单（本机实清出 34 项超龄：17 对 dxf+pieces）；全量 pytest 537 绿（+17）；文档同步 5 处：web/AGENTS.md（diskclean.py 行 + 「多会话 US-006 关键约定」节 7 条）、tests/AGENTS.md（17 例行）、agent-file-map（diskclean.py 行 + server/sessions 行补 US-006）、agent-api-reference（session 生命周期第 9 条 + commit 副作用第 6 条）、CLAUDE.md（web 模块列表补 diskclean 句）；US-007（端到端验收与文档同步）留待后续 session

- [US-007] 端到端验收与文档同步（web 多会话第七块收官，运行时代码零改动，产出 = 验收 harness + 三处文档同步 + 收口）：① scripts/us007_e2e_verify.mjs（Playwright + chrome channel headless，范本 us005_session_verify：dismissTour/modalText/frameText 帧载荷形状归一等工具沿用）双相位 —— 主相位（默认 TTL=300 服务器，前置必须重启 ms-web 保注册表 4 空席）25 项：P1 双 browser context（模拟两设备）各自上传不同母版（A=5336 / B=M1787）并行 commit；P2 ptypes 互不串台双层取证（各自 sid 直取响应体对比 + 全部 /api/ptypes 请求带各自 X-Session-Id Header）；P3 核心反压场景 = B 求解中（150s 预算）A commit 第三母版 5156 → B 无 error 收 final（density 0.8649）、末帧 placed_items pid 全属 B manifest（无外来）、B ptypes 响应体不漂移、全程无阻断弹窗；P4 A 求解出帧→停止（stopped reason=user_requested 进程终止不误伤会话）；P5 B 导出 DXF（200 + 请求带 sidB + content-disposition M1787 前缀文件名取证）；P6 高级运行双会话并发（B 在 A running 时 start 202 跨会话不 409、策略请求全带各自 sid、A 终止→结果态「已终止·保留终止前最优」后 B 仍进度态不受影响）；P7 第 5 窗口页面加载即弹「当前使用用户过多（最多 4 人同时在线），请稍后尝试」+ 第 3/4 窗口无弹窗；P8 default 回归（无 Header POST /api/session → 200 sid=default / 裸 GET /api/ptypes 200 有 representatives / GET / 响应头 Cache-Control: no-cache）；生命周期相位（--expire，需 MS_SESSION_TTL_SEC=6 独立服务器）9 项：E0 sid 落库；E1 求解中不误杀（TTL=6 下 20s 求解照常 final —— ws 钉住 ws_open>0 扫描跳过 + 回调 touch；求解后开高级配置弹窗读路径不弹过期；commit 期间测试脚手架每 3s POST /api/session 保活 —— commit 非被测活性场景只为抵达已 commit 态）；E2 策略轮询中不误杀（2s status 轮询即 touch 活性，15s > TTL 观察仍在进度态，终止 → 结果态「已终止 87.49%」）；E3 空闲 10s > TTL → 点导出（apiFetch 路径）→ 弹「会话已过期（5 分钟无操作），请刷新页面」→ ms_sid=null（弃 sid）→ 点刷新 → 新 32hex sid ≠ 旧 + 无弹窗（干净新会话，绕开 1h 墓碑）；② 2026-08-27 实跑全绿：主相位 25/25 + 生命周期 9/9 + 全量 pytest 537 绿 + vitest 741 绿 + ms-web 启动冒烟（GET / 200）零回归；③ 排障记档（关键运维知识，已写进 component-map US-007 节）：**上 session 残留 shell watchdog（while true; do python -m materialsorting.web.server; sleep 5; done）会在每次杀服务器后 5s 复活 TTL=300 实例** → TTL=6 验证相位服务器全部 bind 10048、探测打到错误服务器（症状 = 过期相位永远只过 E0）；清理必须连 bash 包装进程一起杀（只杀 python 被复活）；--expire 相位必须独立服务器（TTL env 只在进程启动读入）；Windows 下孤儿孙进程可继承 listen socket（杀父后 netstat 仍 LISTENING，须按 PID 追杀）；④ 文档同步四处：agent-api-reference 新「多会话 sid 传递与错误码速查（US-007 汇总）」节（各端点 sid 通道表：POST /api/session 唯一注册入口 create=True / commit 写路径 / 四读端点+策略 create=False / WS ?sid= query / GET / no-cache；401 session_expired·429 session_limit·400 无 code 错误码表；WS error 帧 code 键；per-doc intermediate 落盘 + marker `.web_strategy_active[_<sid>].json` + run_name `web_[<sid6>_]<mode>_<rand6>` + cfg `out/uploads/strategy_cfg_[<sid6>_]<stamp>.json` 命名口径 —— 全部对代码复核过）、README 新「多会话机制」节 + 环境变量表（MS_SESSION_MAX=4 / MS_SESSION_TTL_SEC=300 / MS_UPLOAD_TTL_DAYS=14，非法值 warn 回退）、web/AGENTS.md 新「多会话机制总览（US-001~007）」节、CLAUDE.md web 行收口句；component-map 新 US-007 收官节（harness 两相位明细 + 运维知识 + 文档落点表）；⑤ 多会话特性 US-001~007 全部落地

- [US-001] CLI --extreme 糖衣旗标（极限运行第一块，run_config.py 单文件 +112 行 + 新 tests/test_cli_extreme.py 24 例）：① 旗标与常量 —— `--extreme`（store_true）+ `--extreme-budget N`（int，缺省 EXTREME_BUDGET_S=600）；模块级常量 EXTREME_SOLVER_OPTS={'exploration_pct':0.7,'early_termination':False,'num_workers':4}（quadtree_depth 不写=缺省 4，方案 §2.6 A/B 已否决调优）+ EXTREME_BUDGETS=(600,1200)（2400s+ 门判别力失效硬边界 §2.3）+ __all__ 导出三常量；② 裁决块（--target 校验后、PC-006 solver_opts 块前，配置错误全在 new_run_dir 前拦下不留空目录）—— 互斥矩阵（--extreme 与 --strategy/--kill/--solver-opts/--rotate-opts/--se-screen/--se-extend/--race-budget/--race-gate 任一同给 → 退出 1 中文报错点名冲突旗标）→ --extreme-budget 从属旗标单独给出退出 1 → 值域非 600/1200 退出 1 → **args 原地展开**（args.strategy='race'/args.race_budget=档/args.race_gate=RACE_GATE_TAU —— 下游 strategy 守卫（--time 必填/--kill 互斥天然满足）/race_plan 预算不足（600 档最低 905s=602.5+302.5）/decide_race_kill 门杀/strategy.json/种子流补齐零改动自然生效）；③ PC-006 块加 `if args.extreme:` 首分支（复用现行 solver_opts_for fixed 回调机制，互斥已保证 args.solver_opts=None/rotate=False 不打架）设 fixed_solver_opts=dict(EXTREME_SOLVER_OPTS)；④ race 启动行后追加 `[extreme] 极限运行：race 门杀 × 实验结论参数（预算档 Ns/seed，quadtree_depth 用缺省 4）` 标注行（--quiet 也打，同「改求解编排的开关不静默」口径）；⑤ run_stats config 段 additive `extreme:{budget:B}`（class_key 组成不变与历史 run 可比，θ₀ 校准分布不被割裂；无 --extreme 行不加键零回归）；⑥ 测试 24 例（test_cli_extreme.py）：展开等价性（--extreme --time 905 与手敲三件套 --strategy race --race-budget 600 --race-gate 0.5 --solver-opts 三键的 result.json/strategy.json（started_at 除外）/kill_decisions.jsonl 逐字段一致 —— **result.json 内嵌 run_dir/pieces_dir/intermediate/config.path 归一占位后对拍**，fake solve 记录每轮收到固定三键 opts 且 solve 记录回显同构；唯一合法差异=run_stats config.extreme 键）、互斥矩阵 10 例、--extreme-budget 域 4 例+从属 1 例、1200 档展开（race_budget=1200/门 600s/统计回显）、预算下限 3 例（T=904/600/100 走 race_plan StrategyBudgetError）、无 --time 走策略守卫、非 extreme run_stats 行无键、常量契约、--help 子进程冒烟；⑦ 最小真跑冒烟（5336 生产配置 --extreme --time 905 --quiet，exit 0）：strategy.json mode=race/gate 300s/种子流[0,1]、kill_decisions.jsonl 两行 R5_race_gate（seed0 豁免放行 88.85%、seed1 门值 89.94% 破 bar 纪录续跑）、result.json 两 seed solver_opts 三键正确无 quadtree_depth、portfolio.race kept=[0,1] gated=[]、run_stats 末行 config.extreme={'budget':600}、best real_density=90.70%（seed1 帧级 incumbent，用布 7216mm）；全量 pytest 568 绿（+24）；⑧ 文档同步 3 处：agent-file-map（run_config.py 树行+表行各追加 US-001 段）、agent-component-map（新「极限运行 US-001 落地」节：改动文件/6 条关键不变量/实现要点记档 args-展开-vs-本地变量 与 --quiet 口径）、tests/AGENTS.md（test_cli_extreme.py 行）；US-002（web /api/extreme 四路由）留待后续 session
