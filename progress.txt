# Ralph Progress Log
Started: 2026-08-09











- [US-004] 前端参数链路 + 布局设置 UI（前后幅下拉，band US-013 同模式移植；上一 session 代码已大部就位，本次收尾验证+文档+提交）：① params.ts —— FormState 加 prefix_enabled/prefix_front/prefix_back（DEFAULT false/''/''）+ collectPrefix 三态（关 / 开未选或 front==back 或非 ^g\d+$ → null 静默降级；开且有效 → {enabled:true,front,back}）+ prefixEligibleSizes（2+2 资格码本地预检，与后端 _parse_prefix 同口径：missing→0 非 bandMemberCount 的 1、'null' 通用码跳过、sizes 过滤、返回升序码列表）+ defaultPrefixLabels（决策⑤默认预选：每 label 取全部码片 polygon shoelace 面积最大值降序前二、平手 g 码字典序；5336 = g02/g03；不足两 label/doc=null → null）+ StartContext.prefix；② types/ws.ts —— PrefixConfig{enabled,front,back}（无 size 键，决策②）+ StartPayload.prefix? + StageMsg 扩 stage:'prefix'（size/holes 专属键回显）；useSolveRun —— StartConfig.prefix 透传（缺省序列化 null）+ stage='prefix' 复用 case stage 通道（rec.stage+onStage 不 finish）；③ PerTypeOverridesModal「布局设置」第二行「起始端成套前后幅」（band 两键之后）：PrefixFormValue{enabled,front,back} 草稿+confirm 同 band saveAndClose 通道（取消丢弃、未勾选 front/back 保留）/ 前后幅下拉（band 同模式 80×80 缩略图+徽章 testid prefix-thumb-{label}，值域 orderedLabels∪当前选中，未勾选 disabled）/ handlePrefixToggle 勾上且两码均空才写启发式缺省（用户改过不覆盖）/ 说明文案「满足 2+2 的尺码将自动选取」/ 警示两态（front==back 优先「须为不同 g 码」，其次 prefixEligibleSizes 空集「当前数量无 2+2 资格码」—— 均不阻塞确定，权威拦截在后端结构化 error）；ControlPanel —— prefix 启动闸门（prefixMissingLabel/prefixSameLabel → #start 置灰+StatusLine prefix 段文案+handleStart 兜底；无资格码不置灰）+ prefix×策略互斥 v1（strategy-btn disabled+title「起始端成套与策略运行互斥…」；prefix 与 band 可同开）+ PerTypeOverrides 透传 prefix/onPrefixChange；NestingPage —— onStage 分支 stage==='prefix' → 状态行「起始端成套构造中（尺码 {size}）…」（不进 phase 五态）+ handleStart 透传 cfg.prefix；style.css —— .per-type-prefix-note/.per-type-prefix-warn/.per-type-band-thumb:disabled 三段（暗色同系，无 CSS 框架）；④ 测试 +26（共 709 绿）：params.test（collectPrefix 三态+DEFAULT_FORM/collectStartContext.prefix/prefixEligibleSizes 0-1-2-3-缺码-'null'-sizes 过滤/defaultPrefixLabels 面积预选+平手字典序+不足两 label+doc null）+ useSolveRun.test（prefix 透传三态/双开独立键/stage(prefix) 分发 size 回显不 finish）+ PerTypeOverridesModal.test prefix 分区（未勾选 disabled/勾选预选 g02·g03/警示两态/取消丢弃/confirm 回写/无资格码不阻塞）+ ControlPanel.test prefix 接线（闸门两态置灰文案/payload prefix 三键/策略互斥 title）+ 三处既有 fixture 补 prefix 键；⑤ 浏览器验证 scripts/us004_prefix_verify.mjs（CDP headless Chrome 无依赖，范本 us006_verify；WS stub 录全程消息 + __setQty/__setVal React 受控写值助手）28/28 PASS：弹窗暗色主题（#26282e 实测）/未勾选 disabled/勾选即预选 g02·g03（5336 面积最大两片）/默认数量警示→设量后消失/front==back 警示/取消丢弃草稿/确定回写 #start 解灰/策略互斥 title/stage 序 ['stage:prefix','manifest',…]+size∈{32,33,34,35,38} 回显/状态行「起始端成套构造中（尺码 38）…」/PS_ 零泄漏（manifest pieces+帧序列；final.prefix.pid 设计内回显在案 pin.skipped=true a=0.074）/末帧 4 成员守恒 g02×2+g03×2/interleave 序 g02→g03→g02→g03/rotation 交替 180°-0°-180°-0°/polygon bbox min_x=0.07mm ≤6/竖排贴触 gaps 全负（y 交集>0 交错咬合）/y 跨度 1568<Σh 1802 竖向叠放/density 86.31% 84 片 width 7028mm；截图 out/us004_prefix_verify/ 六张（01 弹窗暗色 ~06 布头列放大）；验证踩坑记档：a) routes_ws 转发的 final 剥掉 placed_items（前端权威渲染=末帧——守恒/形态判据读末帧而非 final）；b) pin 触发场景（小负载 24 片 25s 解 a=1206.8）final 置换布局不进帧=FR-7「帧不置换」设计口径，浏览器目检面向 P0 常态（真实 7 码数量 60s → 自然锚定 a=0.074mm）；c) translation 是 [x,y] 数组非 {x,y}，且 rot180 成员局部原点≠bbox 原点（rotation 交替判据按 placed 构造序=interleave 序，不可按 translation.y 排序）；d) 未切「超排」Tab 时双页同挂 DOM display:none → svg rect 0×0 截图 0 高度；e) heredoc 写含 \s 正则会被降级为字面 s（evalJs 表达式里 split(/s+/) 静默错分 → bbox 退化单顶点）——几何解析改 SVG points DOM API（Array.from(p.points)）；⑥ 后端零改动回归 pytest 449 passed；文档同步 materialSorting-web/AGENTS.md（文件分工五处+新增「起始端成套 US-004 关键约定」节 7 条）/agent-component-map（头行+新增落地节含不变量 5 条）/agent-api-reference（prefix 校验节补前端侧 collectPrefix/预检注记）；清理：ms-web/Chrome 全杀、us004_debug.mjs 临时探针删除
- [US-005] A/B 验收与形态判据闭环（验收器 web/prefix_accept.py 782 行 + tests 12 项 + 浏览器终验 19/19，结构镜像已删的 US-014 band_accept）：① 验收器 python -m materialsorting.web.prefix_accept 单命令五判据（报告 out/config_runs/_probes/prefix_accept_report.json + 三格式导出同目录）：判据① 密度 A/B run_arm off/on × seed{0,1,2,3}（solve_with_callback_proc 真实产品管线；density=主进程 raw-width 生产口径直读）+ 判据② 形态 prefix_form（成员识别走副本守恒不变量——exclude_pids 扣减后 final 中 pid∈{f'{front}_{size}',f'{back}_{size}'} 只能来自 PS_ 展开，数出 2+2 即守恒；四子判据 same_code 2+2 / min_x≤6mm / 竖排贴触（相邻 y 区间交集>0 且缝≤1mm，原始轮廓世界几何口径）/ 相邻 rot 差≈180°±5° + interleave 交错序保留构造序）+ 判据⑤ 双开档 band_only vs band+prefix 另报列（不置换，带位 band_pos 记录 + P0 基准 89.33/90.05 对照）+ 判据③ 确定性同 seed 重跑四对拍 + 判据④ 导出 export_verify（PNG/R12-DXF/PLT + _WarningCapture「导出跳过：pid」哨兵 + DXF/PLT 字节 grep b'PS_' 双查 + DXF 头 AC1009/POLYLINE + off 臂同管线=HEAD 行为对照）；工件 diff 按 *_PS_{front}+{back}@*.json mtime 快照（MS_OUT_DIR 优先，spawn 子进程 env 继承测试隔离）；② **口径关键发现（两轮全跑实测入档）**：web 默认 params 全 0 口径下 60s 解不收敛（无平台期）→ 墙钟截断落点漂移使帧数 ±5% 逐帧不等、密度均值噪声 ±0.5pt（两次 13 臂全跑 mean +1.056/+1.049pt 均卡 1.0pt 线外，off 臂自身跨跑也漂 86.143 vs 86.039）——而 PRD 全部定量锚点（−0.14pt / 89.33%/90.05%）出自 P0 探针配置 5336_coded_really.json 的 per_type；终验绑定 P0 口径（p0_per_type() 直接读 JSON fail-fast，不 import cli 保分层；config quantities 限 7 码后与 accept_quantities() 逐字段对拍相等）；30s/60s 背靠背诊断实证 P0 口径下同 seed 956==956 / 1038==1038 帧逐帧全等——「逐帧相等」判据只在收敛口径物理可达；UI 侧经布局设置弹窗逐码填同值闭环互证；③ 确定性判据物理化重设计：frame_series_equal = 核心轨迹（前 min(n)-1 帧）逐帧相等硬判 + 帧数差 ≤12% 相对护栏（求解器迭代内容确定、截断帧位随机器速率漂移——13 臂连跑热态实测 972/1036=6.6% 内容仍逐帧对齐）；final_best_equal = best-so-far 帧密度重现 ≤0.1pt（final 是截断快照随落点漂移，off 臂同款墙钟物理——final_equal 降为信息字段不 gate）；artifact_replay_equal = 构造回放对拍（排除墙钟 ts/stage_elapsed + 主解结局 pin/band_pos/width_mm）；④ 终验数字（generated 2026-08-25T19:48:31，accept 全 PASS）：密度 off 87.097/86.715/89.734/86.446 → on 88.597/88.678/89.128/86.289，均值 87.498% vs 88.173% **−0.675pt 反超**（最差 seed2 +0.606pt 线内；4/4 pin skipped 置换零成本兜底；组合片 fill 83.4/83.6% holes 0）；形态 4/4（min_x −2.09~−1.86mm——erode d=2 贴边语义 |min_x|≤6；贴触缝隙全 0；y 咬合交集 43~100mm；rot 差全 180°；interleave）；双开 band_only 90.047/90.566 vs dual 89.864/90.353（劣化 0.183/0.213pt，P0 基准同量级反超，带位 WB_g05 min_x 1101~1171/距布尾 ~5.25m 在案）；确定性 1042==1042 帧逐帧全等 + 选码 38==38 + final 逐字段全等 + best 88.633==88.633 + 工件构造段逐键回放；导出 on/off 三格式成功零泄漏（on 853,110/2,380,700/468,087B）；⑤ 浏览器终验 scripts/us005_prefix_verify.mjs（CDP headless 无外部依赖，范本 us004）：上传 5336 → P0 全表数量 159 片 + 7 码 → 弹窗勾选 prefix（默认预选 g02/g03）+ 逐码填 P0 per_type（d-{g}/tol-{g} 受控输入）→ 60s 求解 **19/19 PASS**：stage 序 stage:prefix→manifest→frames、状态行「起始端成套构造中（尺码 38）…」、PS_ 零泄漏（manifest+帧）、4 成员守恒、interleave、180° 交替（g02@180→g03@0→…）、min_x 0.02mm、贴触 gaps 全负=咬合、**UI 密度 88.597% == 验收器 on 臂 seed0 88.597%（Δ0.000pt 口径闭环）**、final.prefix.pin {skipped,a:0.022}；截图 .docs/business/us005_prefix_final_full.png + us005_prefix_head_column.png；服务器生命周期 ms-web PID 17340 记录、终验后 Chrome+ms-web 全杀端口确认释放；⑥ tests/test_prefix_accept.py 12 项：prefix_form 五态（交错咬合全过/dx=50 布头偏移/成员散落/同朝向/副本不齐+size 不符——合成矩形 y 恰邻接退化注释记档）+ p0_per_type（真实配置 g02/g03 d=2 + 坏文件/坏条目 fail-fast）+ 签名忽略墙钟 + frame_series_equal 速率规则（1038==1038/972 vs 1036 通过/核心帧分歧 FAIL/50% 超护栏/12% 边界两态）+ final_best_equal 快照物理（best 一致 True/差 0.15pt False/空帧 False）+ artifact_replay_equal（墙钟+主解结局字段排除 + size 分歧 False）+ export_verify（三件产物 + PS_ 泄漏双查）+ run_all 秒级冒烟（合成 g02/g03 两码 2+2 + g05 双开，MS_OUT_DIR 隔离，全臂跑通 + 形态/确定性/带位刚性断言 + prefix_runs 工件 ≥3）；⑦ 质量门：pytest 461 passed（449+12）零回归（test_cli_strategy 一次瞬态失败复跑消失——standalone/full 均绿非本次改动引入）、npm run build ✓、npm test 709 passed；⑧ 文档：CLAUDE.md 数据流主线 prefix 支线句、.docs/business/起始端成套前后幅_AB验收报告_US005.md（新，口径声明/五判据/浏览器终验/复现）、确认清单 v4+§8 终验回写对照表、business-overview prefix 行 ✅ 收官、agent-file-map（树+模块行 782）、agent-component-map（US-005 收官节）、web/AGENTS.md + tests/AGENTS.md 模块行

- [US-001] 后端会话注册表与生命周期（web 多会话隔离第一块，纯后端无 UI 改动）：① 新增 web/sessions.py（422 行，仅标准库+同包 runtime，禁 import server——AST 守卫 test_web_sessions.py 源级哨兵双查）：SessionRegistry（OrderedDict[sid→SessionState] 单例 registry，SessionState={sid, state(pieces 快照 dict 同构 _build_pieces_state), doc_id(US-002 占位), last_active, ws_open, strategy_busy(US-004 占位)}）——单一解析函数 resolve(sid, create)（缺省/空串→DEFAULT_SID='default' 豁免一切；非法→InvalidSidError 400 无 code 键；墓碑命中/惰性超时(当场逐出为墓碑)/未知合法 sid 读路径→SessionExpiredError 401；存活刷 last_active 幂等；create=True 未知→容量闸门 SessionLimitError 429）；容量 MS_SESSION_MAX=4（env 可调，仅计活跃 default 不占额，文案 f-string 插值缺省逐字=PRD）；TTL MS_SESSION_TTL_SEC=300 惰性检查+30s daemon 扫描线程 start_scanner/stop_scanner（Event 可中断，server.py 文件尾启动；ws_open>0 跳过=WS 钉住不误杀；扫描异常只 warn）+ scan_once 可单测；墓碑 {sid,ts} deque(maxlen=FIFO≤128)、存活 1h（purge 后 sid 视为全新可新建）；ws_acquire(resolve 语义+计数)/ws_release(no-op finally 友好)/touch(GIL-safe float 写)；SessionError 族自带 status/code/payload()（401/429 带 code additive 旧前端可忽略）；SID_RE=^[0-9A-Za-z]{1,128}$ 单一真相源在本模块（sessions 不能 import server，server.py 反向 re-export 为 _DOC_ID_RE 同一编译对象，US-010 commit 校验旧名零改动）；时钟可注入 _FakeClock（测试/冒烟共用）；`python -m materialsorting.web.sessions` 冒烟=打印配置+default 身份 is 判+私有 registry 模拟建会话/超限 429/惰性过期/墓碑阻挡/ws 钉住/1h 重建/非法 400 全生命周期 11/11 PASS；② server.py：顶部 from .sessions import（SID_RE as _DOC_ID_RE/SessionError/registry as session_registry，sessions→runtime 单向无环）+ 删本地 re.compile 定义与 import re + POST /api/session 路由（读 X-Session-Id Header，strip 空串归 None→default；resolve(create=True) + except SessionError → JSONResponse(e.payload(), e.status) 一行映射；200 {ok,sid}/429 session_limit/401 session_expired/400 {error:'sid 非法'}）+ 文件尾 session_registry.start_scanner()（daemon 兜底：已死会话不再发请求名额只能靠扫描回收）；③ tests/test_web_sessions.py 16 例全绿（TestClient API 契约×6 + registry 单元×7 + default 身份/豁免×2 + AST 守卫）：关键设计——autouse fixture 先 stop_scanner() 再 reset()（防真扫描线程在 monkeypatch 时钟窗口并发逐出 flaky）、TTL/墓碑全 _FakeClock 注入零墙钟等待（daemon 线程用例 0.02s 周期+5s deadline 轮询）、default 会话 state is runtime._PIECES_STATE 三方 is 判等锁死（resolve(None)/resolve('default')/server re-export 同对象，US-002 commit 双写依赖此不变量）、满员测试用 registry.max_sessions 动态循环（env 旋钮下不断言）、'default' 与 uuid4 hex 结构性不可碰撞记档；④ 质量门：pytest 全量 491 passed + 16 新增（唯一 1 失败 test_cli_strategy::test_replay_regression_t3600_gain_and_miss_max 为已知瞬态——单测/整文件复跑均绿、上一 session US-005 progress 已记档同款瞬态，与本次改动无关）；python -m materialsorting.web.sessions 11/11；import 链无环（sessions 独立 import 冒烟过）；分层依赖未反向（AST 守卫 + 手查 sessions 仅 import runtime）；⑤ 文档同步 6 处：.docs/technical/agent-api-reference.md（路由表 +POST /api/session 行 + 新专节：请求/响应表/生命周期语义 5 条/关键不变量 4 条）、agent-file-map.md（web 树 +sessions.py 行 + server.py 行更新、文件表 +sessions.py 422 行行）、web/AGENTS.md（文件分工表 server/sessions 行 + 新「多会话 US-001 关键约定」节 9 条：单一解析/default 豁免三件事/错误类型即 HTTP 语义/惰性+扫描兜底/墓碑/SID_RE 真相源/测试隔离/env 旋钮）、tests/AGENTS.md（+test_web_sessions.py 行）、CLAUDE.md（web 层 bullet 补 sessions.py 一句）；US-002~005（commit 双写/读路由+WS 接入/策略会话化/前端接入）留待后续 session
