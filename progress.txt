# Ralph Progress Log
Started: 2026-08-09




















34. **[2026-09-11] prd-state-file US-004 — 恢复编排与端到端冒烟**：状态文件收官故事（前端恢复编排 applyRestorePayload + applySyntheticRun 共享抽取 + run-provenance 来源小字 + 33 检查端到端冒烟；E2E 冒烟实测揪出两处后端 400 根因一并修复）。①新建 `src/store/synthRunStore.ts` —— 合成 run **store-signal 两层拆分**：`applySyntheticRun(manifest, frameLike, seed, origin?, note?)` 模块级落笔（清场 runRegistry.clear + editStore.invalidate + setSeekTime(-1) → `deepCopyPlaced` 深拷贝（translation 拷断 + mirror omit-when-false，US-003 起保存侧同实现不再各持一份）→ 单条 done RunRecord（frames=[合成帧 phase='final']/lastFrame/双口径密度/viewBoxMaxW/origin additive）→ `signal()` bump token）；NestingPage `useEffect([synthToken])` 消费（seeds/phase/status 是本地 state 只能走信号；信号只载 UI 参数 seed/note/origin，几何真相在 runRegistry）。**lastSynthTokenRef 守卫**：token 未变（含挂载初跑）即 return —— 修模块级 store 常驻导致的 stale-token 重放（NestingPage.test 三连挂根因）+ StrictMode 幂等。`provenanceText(origin, seed)` 四枚举 + config 括号（extreme time_total_s→"(Ns)"/strategy minutes→"(Nmin)"，缺席/未知键无括号）。②`src/lib/stateFile.ts` 增 `applyRestorePayload(res)` 恢复编排（顺序敏感单一实现）：uploadStore.setState（done/doc=parse 载荷/activeSize=最小码/commit 回 idle）→ qtyStore.hydrate 默认物化 → **hydrateFlat 实值覆盖**（新 API：{label:{sizeKey:N}} clampQty 归整、null/空 no-op、多出 sizeKey 并入、多出 label 不新建行——矩阵行集以 doc 为准）→ formStore.hydrate(form, token=res.doc_id)（先于 React 提交，ControlPanel resetForDoc 匹配 token 水合保留）→ ptypeStore.invalidate()（commit-done 同口径）→ run 块在场 applySyntheticRun（manifest={type:'manifest',...res.manifest} + origin=run.provenance??{kind:'solve'} + `run.final` 缺席走 finalizeFromLayout 客户端现算兜底：computeLayoutStats 物理口径，density_sparrow/elapsed=0 展示参考）+ setNestingEnabled(true) + setTab('nesting')；无 run 纯配置档不合成不切 Tab。③`NestingPage.tsx`：applyStrategyResult 改委派 applySyntheticRun（行为对齐 + 深拷贝强化——修旧实现直共享 result.best.placed_items 被 applyToRun 原地写回穿透污染策略 store 的别名 bug）；`originOfStrategyResult(result)`（mode 定族：'extreme'→extreme kind + extremeStore.lastStart.time_total_s、'se'/'race'→strategy kind + strategyStore.lastStart.minutes；mode null→undefined、lastStart null→无 config 段）+ handleStart setProvenance(null)（WS 普通求解小字退场）。④`ControlPanel.tsx` 增可选 prop runProvenance（undefined 不渲染）+ `div.run-provenance[data-testid=run-provenance]` 小字。⑤`useParseDxf.ts` .msn 成功路径：applyRestorePayload(res) + toast「状态文件已恢复：{源名}（含排料结果）」（替换 US-003 占位；status=done doc 已写入）。⑥**后端修复（E2E 冒烟实测根因，两处）**：(a) `solver.py` `_pf` + `_resolve_d_tol` 重写 —— per_type 值字符串形态容错（前端 form 按 input.value 原样入档 `{g码:{d:'1',tol:''}}`，''/None/非数值 → 继承全局档 base；此前 `float('')` ValueError 打穿 state-save/state-restore 400「form.sizes/per_type/quantities 形态非法」；与前端 collectPerType「空串=继承不写键」同口径，手改文件非法值同 no-op）；(b) `statefile.py` `_form_gate_mm` —— 恢复 manifest gate_mm 取 `form.gate`（cm 字符串 ×10，与求解路径 routes_ws 前端 gate_mm 覆盖 + lib/params.parseGate 同口径）而非 doc.gate_mm（母版 commit 快照）：改幅宽求解后保存（180cm 求解 + 175cm 母版）恢复 manifest/导出幅宽须 1800 与布局一致，缺省/非法/非正回退 doc.gate_mm。⑦新建 `materialSorting-web/scripts/smoke_state_file.mjs` **33 检查端到端冒烟**（prod :8000，模板 smoke_drag_snap.mjs：Edge 通道/addInitScript ms.tour.*/WS framereceived 捕获/页内 fetch 包装抓 POST /export 载荷/pointer 事件流拖片）：S1 上传 5336 → 数量矩阵 g01@30=2（总片数 110 默认+1=111）→ gate 180.00 + per_type g01 d=1 + 腰头成带 g05（缩略非错误态）→ 勾 32/33/34 5s 求解 final placed=30（Σdemand 守恒，band WB_ 组合片已 expand 回成员）→ S2 编辑弹窗左键拖片 +40mm 保存（料长 1798mm）→ S3 导出 .msn Node gunzip 断言（顶层键 schema_version=1/app='materialsorting'/无 provenance 键/placed=30 守恒/恰 1 条被移动 dx≈+40 其余逐位全等/form sizes·gate·time·band·per_type d='1' 字符串原样/quantities g01@30=2+g01@31=1 全量扁平/final.width_mm=编辑态料长）→ S4 全新 context（新 sid）上传 .msn：toast「状态文件已恢复…（含排料结果）」+ 来源小字「来源：普通求解 · seed 0」+ nest-label 密度 85.47%/料长 179.80cm 与 .msn final 全等 + 毛版 polygon[data-label]=30 + 预览 Tab 总片数 111/cell 0-0='2'/列头 10/净版层 ≥1 + 表单回填 gate/time/sizes + 高级配置 band/per_type 回填 + 编辑弹窗基线料长全等/裁片 30 + PLT 导出 placed 深全等 + gate 1800/width/density 一致 + 重解新 final 86.61% + 小字退场；报告 out/smoke_state_file/report.json。**踩坑备档**：toast 不自动消失右上栈拦截 overlay 按钮（读断言后逐条 ✕）；数量矩阵 data-cell="列-行"按码集动态（5336 首行=30 非 32）；主画布 polygon 计数须 `polygon[data-label]`（net/collide 层同名标签无 data-label）。⑧测试：前端 **+26 例**（vitest 1125→**1150 passed** 68 文件）：synthRunStore.test 8 新建 + stateFile.test +8（applyRestorePayload 全量五 store 落笔/run 合成切 Tab/纯配置档/provenance 缺省 solve/深拷贝解耦/finalizeFromLayout 兜底）+ qtyStore.test +6（hydrateFlat）+ NestingPage.test +4（run-provenance race·minutes/extreme·time_total_s/mode null 无小字/lastStart null 无括号）+ useParseDxf.test 恢复成功断言重写；后端 **+3 例**（pytest 818→**821 passed**）：test_solver_label 字符串形态容错 + test_web_statefile 字符串 per_type 保存/恢复双端 200 + manifest gate 取 form 非 doc（含 None/'abc'/'0'/'-5' 回退四路）。⑨验证：`npx tsc --noEmit` 干净 + vitest 1150 + `npm run build` 过（316.22KB/gzip 103.77KB）+ pytest 821 + `python -m materialsorting.web.statefile` 自检 18/18 + 冒烟 smoke_state_file.mjs **33/33 SMOKE PASS**（prod :8000）+ 最小冒烟 smoke-state-file.mjs 9/9（dev :5173）；验证后 ms-web 已杀（:8000 释放）、探针脚本已清。⑩文档同步（/sync-docs 口径）：`.docs/technical/agent-component-map.md` 文末「状态文件 US-004 落地」节（文件表 6 行/关键不变量 6 条/验证记录）+ `agent-api-reference.md`（restore 专节不变量 4 补 _form_gate_mm 与 _pf 口径 + 测试计数 47→72 + US-004 前向引用落地注记）+ `agent-file-map.md`（solver.py 行 _pf + statefile.py 行 _form_gate_mm）+ `materialSorting-web/AGENTS.md` 文末「状态文件 US-004 关键约定」节（synthRunStore 两层拆分/lastTokenRef 守卫/深拷贝防别名/applyRestorePayload 顺序/冒烟四坑备档）+ `web/AGENTS.md`（solver 行 _pf + statefile 行 _form_gate_mm）+ 根 `CLAUDE.md` web 段 US-004 落地与两修复一句。prd US-004 passes:true。
35. **[2026-09-12] prd-state-file US-005 — CLI intermediate 入口打通策略重跑（后端收官）**：状态文件末故事（恢复会话无需母版重发策略/极限 run；设计依据 = solve 阶段只吃 run_dir intermediate、DXF 仅 commit 期消费一次）。①`cli/config.py` 第 10 键 `intermediate`：数据源键与 master_dxf **二选一互斥**（同给→「互斥」/同缺→「必填」，消息含两键名）、相对路径 CWD→仓库根双候选解析（`_resolve_input_path(name, raw)` 单函数复用原 master_dxf 逻辑）、config 层只验路径存在（schema 校验留 commit 短路段）、NestRunConfig.intermediate: Path|None；②`cli/pipeline.py` commit 短路：commit_from_config 首行判 cfg.intermediate → `_commit_from_intermediate`（源校验「可解析+pieces 非空」两路 RuntimeError→退出码 1；浅拷贝 doc 改写 gate_mm=float(cfg.gate_mm)——**solve 阶段 gate 取自 run_dir intermediate 非 config，不覆盖则 config 门幅静默失效**（实证：1980 源+1800 cfg→落盘 1800）+n_pieces/total_area 按 pieces 现算；run_dir.mkdir 显式（母版路径经 pieces_dir.mkdir 隐式建）；pieces/ 目录不生成（无下游消费面）；摘要 source=doc.source/pieces_dir=None/n_written_dxf=0）；solve/LNS/策略族零改动；③`cli/run_config.py`：commit 块上移先于 stats、`stats_source = str(cfg.master_dxf)（母版路径历史口径零回归）| str(commit.source)（=doc.source 母版身份文件名——**非临时 intermediate 路径**，web 恢复会话 int 文件带 stamp+rand6 每轮唯一会打散 class_key、θ₀ 校准无法累计）`，result.json config 回显数据源键二选一；④`web/strategy.py` `_start_run` 数据源分支：母版在盘→master_dxf（正常会话零变化）；母版失盘+doc 带 pieces（恢复会话特征，statefile 校验链保证）→ state['doc'] 写 `out/config_runs/web_[<sid6>_]int_<stamp>_<rand6>.json`（**写在 _cleanup_stale_web_artifacts 之后**落本会话前缀清理面）+ config `intermediate` 键替代 master_dxf、不写 uploads 不碰 INTERMEDIATE；doc 无 doc_id 且无 pieces 才 422（旧口径）；极限族 _start_run 共享分支。测试 +14（test_cli_config 4：intermediate 合法/相对路径双候选/互斥双缺/形态错误；test_cli_pipeline 3：短路摘要+gate 覆盖+源文件不改写/坏源三路/短路产物 solve 与母版路径逐字段一致；test_cli_run_config 3：intermediate 端到端产物同构+run_stats source/class_key 同源对拍/master 路径 stats 零回归/两键同给退出 1；test_web_strategy 4：恢复会话 start 落盘对拍不写 uploads/极限族共用分支/sid 前缀 int 清理面/真 .msn restore→start→done→result E2E）—— **835 passed 全量零回归**。真 CLI 实跑验证：intermediate config legacy 83.23% → se 策略 → race 策略 → ms-run-config.exe console script 链路全通，run_dir 产物同构（strategy.json/result.json/best_frame*/curve*/kill_decisions.jsonl/pieces_intermediate.json、无 pieces/）。文档同步：agent-file-map（config/pipeline/run_config/strategy 四条目）+ agent-api-reference（路由表两行+strategy/extreme start 422/202 段+intermediate schema 节）+ CLAUDE.md（cli 段/运行方式段 10 键 schema）+ README.md（配置表 master_dxf/intermediate 二选一行+batch 引用）+ web/AGENTS.md（strategy 行+start 校验序+sid 命名段+测试段）+ tests/AGENTS.md（四测试文件行）+ agent-component-map（extreme 10 键引用）。

36. **[2026-09-13] prd-session-expiry-auto-recovery US-001 — 后端 checkpoint 内存存储与写入/清除端点**：会话过期自动恢复首故事（服务端按 sid 维护工作台状态内存快照 + statefile 重建段抽取共享函数，行为零变更）。①新建 `web/checkpoint.py`：`_CheckpointStore`（`OrderedDict[sid→{data:gzip bytes,ts}]` + threading.Lock + **惰性 TTL 清理**（put/get 双入口清超龄，无 daemon 线程，`now-ts>ttl` 严格不等 = sessions 墓碑同款）+ **FIFO 容量上限**（超 max_entries 逐 OrderedDict 首键；重复 put 同 sid 先弹出再尾插 = 刷新新鲜度，防旧条目被误逐出）+ 时钟可注入；进程内单例 `store` 测试经 `reset()` 隔离）；env `MS_CHECKPOINT_TTL_SEC`（缺省 7200 = 恢复窗：过期后墓碑 1h + 会话 TTL 10min + 余量）/`MS_CHECKPOINT_MAX`（缺省 16 ≈3MB 内存），复用 sessions `_env_int`/`_env_float` 容错回退。②`POST /api/state-checkpoint`：**peek 口径绝不 resolve**（FR-1：不刷 last_active、不建会话名额 —— FakeClock 单测断言；惰性逐出前超龄未触发会话 peek 仍命中照常快照且不触发逐出）；body = state-save 同形（save_as 容忍忽略）；闸门序 = sid 非法 400（SID_RE）→ peek None 401 `code=session_expired`（死会话无快照可打且旧 sid 已入墓碑）→ 会话空 200 `{stored:false,reason:'empty'}` → 载荷形态 400（state-save 同文案同判据：body 非 dict/form 缺失/quantities·quantities_base·run 形态/run.placed 条目 id 缺失）→ run 在场守恒（`check_placed_conservation` 复用）失败 200 `{stored:false,reason:'conservation'}` **且先前好快照字节不变（last-good FR-2：put 未发生）**→ 成功 `build_state_document`+`serialize_state` 入库 200 `{stored:true}`；无 run 纯配置档不做守恒（改数量中间态照常快照）。③`DELETE /api/state-checkpoint`：幂等清除（条目不在也 200 `{ok:true}`，F5 干净重置 US-004 消费）；不触碰会话注册表。④`_store_checkpoint(sid,payload)→(body,status)` 业务内核（handler 只做 HTTP 壳 = sid 头 + JSON 解析；测试/__main__ 冒烟直调不经 HTTP）+ `register_checkpoint_routes(app)`（server.py 文件尾注册，statefile 同模式）；模块禁 import server（AST 守卫 ast.walk 覆盖函数内延迟 import，strategy 禁 cli 同写法）+ allowed 顶层 import 白名单。⑤`statefile.py` 重构：state_restore 内联重建段（doc_id 铸新 + `_state_from_doc` + sid/default 双分支写入 + `build_pid_meta` manifest 重算（`_form_gate_mm` 口径）+ `_DocPieceView` parse 载荷 + `edit_hold.refresh` + 响应组装）抽取为 `rebuild_session_from_document(document, sid, *, filename_fallback='')`——经 `registry.peek` 取回调用方刚 resolve 的 SessionState（同一对象，行为零变更）；state_restore 改调它 + docstring 步骤 3 指向共享函数；US-002 `/api/state-recover` 同复用（响应同形 + additive `recovered_from` 由调用方补）。⑥`__main__` 冒烟 17 项：store 假时钟生命周期（字节对拍/FIFO/重复 put/TTL/DELETE 幂等）+ 内核全路径（合法入库→gunzip+parse_state_document 逐字段对拍/save_as 容忍/conservation last-good 字节不变/empty/非法 sid/死会话/缺 form/peek 不刷 last_active——单例 registry 换 FakeClock + finally 复原）。⑦测试 `tests/test_web_checkpoint.py` **28 例**：store 单元 6 + peek 口径 3 + 写入往返 5（含 default 会话键 'default'）+ empty 1 + conservation 3 + 闸门载荷 2 + DELETE 4 + 接线 3（POST+DELETE 方法白盒/env reload 容错回退 + 结束复原/AST 守卫）+ rebuild 直接单元 1。**踩坑备档**：checkpoint placed 条目形态校验只查 id 非空（state-save 同契约镜像）——缺 rotation/translation 不是形态错误而是落守恒判定（单测期望写错首跑揪出）；env 常量测试走 `importlib.reload`（复用同模块 dict，app 路由函数 __globals__ 原位更新不脱钩，测试文件统一 `checkpoint_mod.store` 属性访问防 from-import 旧引用）。⑧验证：pytest **880 passed 全量零回归**（含 test_web_statefile 79 例全绿 = 重构零回归锚点）+ `python -m materialsorting.web.checkpoint` 17/17 + `python -m materialsorting.web.statefile` 20/20 + server app 导入冒烟 + 分层依赖未反向（AST 守卫）。⑨文档同步（/sync-docs 口径）：agent-api-reference（端点数 20→22 + 路由表两行 + 「会话 checkpoint 内存快照」专节：错误码表 + peek/last-good/纯内存/env 四不变量）+ agent-file-map（checkpoint.py 行 + statefile.py 行补 rebuild 抽取）+ web/AGENTS.md（checkpoint.py 行 + server.py 行注册点 + 环境变量行补 MS_CHECKPOINT_TTL_SEC/MAX + statefile.py 行）+ tests/AGENTS.md（test_web_checkpoint.py 行）。prd US-001 passes:true。
