# Ralph Progress Log
Started: 2026-08-09




























50. **[2026-09-21] prd-machine-nesting-api US-002 — machine solve 提交端点**：`POST /api/machine/solve` 落地（YL 排料机器任务提交：multipart 母版 DXF + config JSON → 三档模式烘焙 spawn → 202 task_id），机器任务正式进 strategy 家族第三成员。①`web/strategy.py`（1299→1317 行）微扩：`MACHINE_MODE = 'machine'` 常量 + `_MODE_LABELS`（409 单飞文案 mode→运行名映射，缺省「策略运行」兼容无 mode 旧 marker）——`_spawn_run_process`/`_kill_tree`/`_discover_run_dir`/`_cleanup_stale_web_artifacts` 四函数本就模块级零改动，machine.py 经**模块属性访问**复用（签名不变零行为变更，tests monkeypatch `strategy_mod._spawn_run_process` 对 machine 同一生效点）。②`web/machine.py` 重写（63→347 行）：`RUN_MODE_SPECS` 三档（normal {time:180, args:[]} / advanced {time:1200, args:['--strategy','race']} / extreme {time:7200, args:['--extreme']}，race-budget/race-gate/extreme-budget 全 CLI 缺省 web 零重复定义）+ `_FORBIDDEN_CONFIG_KEYS=(time,seeds,band,prefix)` 零暴露红线；`_validate_config` fail-fast 形状校验（gate_mm 必填正整数含 bool/非整浮点拒 / run_mode 值域 / sizes 非空 int 列表 / per_type·quantities 数值形状 / client_ref str≤128）——g 码正则等深度校验仍归 cli/config.py 单一真相源；solve 流程：form 解析（缺 file/非 UploadFile/非 .dxf→400，>`UPLOAD_MAX_BYTES` 20MB→413）→ config JSON 字符串（缺失/坏 JSON/非对象→400）→ `_machine_ref_inflight` 同 client_ref 在飞（mode='machine' 且非终态）→ 409 返回既有 task_id → 铸 sid `'m'+时间戳+rand`（SID_RE 防御校验，task_id==sid）→ 母版落 `uploads/<doc_id>.dxf` → **复用 server parse+commit 管线**（函数内延迟 `from . import server` + 模块属性调用时取值 = monkeypatch 生效点；executor 跑 `_commit_to_nesting_sync`，坏 DXF→422 中文）→ `registry.resolve(sid, create=True)` 建任务会话 + `_build_pieces_state(per-doc)` 快照只挂本 sid（default `_PIECES_STATE` 零触碰）→ cfg 落 `uploads/machine_cfg_<sid>_<stamp>.json`（time/seeds 烘焙 + master_dxf 绝对路径 + truthy 才写可选键）→ spawn cmd `[sys.executable, -m materialsorting.cli.run_config, <cfg>, --name, machine_<task6>_<rand6>, *策略段, --time, <烘焙秒>, --quiet]` → marker 恰 5 键 mode='machine' → 状态槽（strategy schema + machine 专属 `run_mode`/`client_ref`）→ `202 {task_id, run_name, started_at}`。③`tests/test_web_machine.py` 重写（133→476 行 15 例）：US-001 三守卫 5 例保留（路由在场断言改 `/api/machine/solve` 挂载）+ `machine_env` fixture（server.UPLOADS_DIR/paths.OUT_DIR/CONFIG_RUNS_DIR/INTERMEDIATE/tempfile.tempdir 四路 monkeypatch + 状态/registry 清零）+ `_yl_master_bytes`（ezdxf R12 合成 YL 风格母版走真 parse+commit）+ 三模式 spawn cmd **逐参数金标**（策略段+烘焙 180/1200/7200、cfg 键集对拍、marker 5 键、会话快照）+ default `_PIECES_STATE` doc_id 前后不变对拍（含 `is` 锁死）+ 错误矩阵（缺 file/坏扩展名 400、20MB+ 413、config 形状 400、gate_mm 六态 400、run_mode 值域与四禁键 400、sizes/per_type/quantities/client_ref 八态 400、坏 DXF 422 不写 marker/cfg）+ client_ref 在飞 409（返回既有 task_id 不二次 spawn）终态放行。④验证：pytest `test_web_machine.py` 15/15 + 全套件 **1106 passed**（1091 存量 + 15，181.84s 零回归）；`python -c "from materialsorting.web import machine"` 导入过；真机冒烟（:8012）——合成母版+完整 config POST → 202 三键体 + run_dir `machine_m20260_fb3099_20260921-182952` 落盘含 commit 产物、result.json config 回显（gate_mm 1750.0/time 180 烘焙/seeds [0]/normal 无 strategy 键）、同 client_ref 二发 → 409 原task_id；solve 子进程 6.5s 自然完成（real_density 57.88%）后 PID 清理、服务器/临时产物清理完毕。⑤发现备注（US-003 消费）：plain（normal 模式）run 的 `portfolio.incumbent` 为 null 且 `per_seed` 空 —— status/result 端点须从 `best`/`solve` 数组取 incumbent。⑥文档同步（/sync-docs 6 文件）：agent-api-reference（路由表 +machine/solve 行+状态段）/agent-file-map（目录树+web 章表 machine.py 347 行+strategy.py 1317 行+复用注记）/web AGENTS（machine.py 行 US-002 全契约）/tests AGENTS（test_web_machine.py 15 例）/CLAUDE.md（machine.py bullet）/business-overview（功能表 US-002 落地）。prd US-002 passes:true。
51. **[2026-09-21] prd-machine-nesting-api US-003 — status/stop/result 三端点**：`web/machine.py`（347→559 行）+ `web/strategy.py`（1317→1348 行）三公共实现参数化 + `tests/test_web_machine.py`（476→889 行 25 例）。①strategy.py：`_status_common`/`_stop_common`/`_result_common` 增 keyword-only `sid`（显式 sid 覆盖 X-Session-Id 头派生 —— machine task_id 即 sid 走路径参数）+ `gate`（False 跳过会话闸门 —— 机器会话可能已被 TTL 逐出，仍可读内存态/marker）+ `_result_common` 的 `drift`（False 跳过母版漂移 warning —— 机器 manifest 恒以 start 快照组装无共享画布）；缺省参数 = 策略/极限八路由旧调用形与行为零变化。②machine.py 三端点：`_machine_task_ctx` 公共闸（SID_RE 非法→400 marker 路径拼接安全闸 / 未知或外族非 machine 槽→404 —— default 共享槽与 strategy 槽均不可冒领）+ 三端点 `gate=False` + `session_registry.touch(task_id)` 轮询刷活性（no-op 容错）；**GET /{task_id}/status**：`_machine_status` 收口恰 10 键 `_STATUS_KEYS`（state/mode:'machine'/run_mode/total_budget_sec/elapsed_sec/incumbent/current/per_seed/error/exit_code）无 placed_items 控载荷、绝不读运行中 curve_s*.json（非法 curve 在场仍 200 由测试锁死）；incumbent 恰 4 键 {density,width_mm,seed,frame_index} 物理毛版包络口径 = base incumbent 摘要优先 → `_incumbent_from_best_frames` 边车 density 最大回落（运行中无 result.json / plain normal 档 portfolio 段恒空双覆盖，US-002 发现的 plain 档 gap 由边车回落解决且顺带覆盖 running 态 —— 满足「running 期 status 含 incumbent.density」）；current 收窄 {seed,density,ext}；orphan 收口 = MS 重启后内存态空 + marker 在：pid 死且超 `RUN_DIR_GRACE_SEC` 30s → state='error' + stderr 尾 `_STDERR_TAIL_CHARS` 2000 字符 + 附加 alive/pid 键；run_mode/total_budget 经 `_orphan_mode_budget` 反查 machine_cfg time 恢复（marker 恒 5 键不带该信息；RUN_MODE_SPECS 时间三档 180/1200/7200 恰互异反查无歧义；延迟 import server 防环）。**POST /{task_id}/stop**：`_stop_common(label='机器排料任务')` 树杀 taskkill /PID /T /F（孙进程无残留实测）+ run_dir 保留 + orphan marker 可 stop/清理 + 终态 400。**GET /{task_id}/result**：`_result_common(drift=False)` 收窄恰三键 {manifest,best,summary}；running→409；best.placed_items demand>1 的 g 码发 N 条绝不按 pid 去重（==Σdemand 守恒）；portfolio.incumbent 优先 → stopped/plain 档 best_frame 边车 density 最大回落（stopped 后 result 仍可读）+ density_sparrow 边车补；manifest = build_pid_meta(起始快照 pieces) 与 /ws/solve 同形，键集含 raw_polygon/d_mm/demand/color。③测试（+10 例）：normal running 金标（恰 10 键 + 运行中非法 curve 在场 200 + incumbent 边车回落 0.5788）/ race（advanced done 1200，portfolio incumbent 0.88 > 边车 0.86 证来源优先级 + marker 清理）与 extreme（7200，0.917）档状态金标各一 / error 态 stderr 尾（'run 目录'+种子文本 2000 截断）/ task_id 三端点闸矩阵（非法 400、未知 404、外族 strategy 槽 404）/ solve 全流程 starting→running→done + **registry.reset() 模拟会话逐出后 status 仍 200**（AC 内存态路径）/ orphan 双路径（pid 存活→orphan+alive/pid + cfg time 7200 反查 extreme；pid 死 started_at 超 30s→error 宽限文案含 pid）+ orphan stop {stopped,pid,orphan:true} / stop 树杀 taskkill /T /F cmd 金标 + run_dir 保留 + stopped result best seed1 0.62+density_sparrow 0.64 / result done 恰三键 + manifest.pieces 键集超集 {id,size,color,area_mm2,polygon,raw_polygon,d_mm,label,demand} + demand g01_28:2 placed ['g01_28','g01_28','g02_28'] 三条不去重 / running result 409 + 终态 stop 400。④验证：pytest `test_web_machine.py` 25/25 + test_web_strategy 45/45 + 全套件 **1116 passed** 零回归；`py -3 -c "from materialsorting.web import machine"` 导入过（分层未反向，双 AST 守卫仍在）；真实服务器冒烟（:8013，`out/machine_smoke_us003/smoke_us003.py`）——normal 档全生命周期：202 → 轮询 status 恒 10 键、running 期 incumbent.density=0.701039 可见（边车回落实证）→ done ~34.6s → result placed 条数 4==Σdemand 4 + manifest 键集齐 + summary 三键；advanced 档 stop：running budget 1200 透传 → stop {stopped:true,pid:38188} → 孙进程 PowerShell 扫描无 machine_/run_config 残留 → stopped result 200 best_frame 回落；服务器 :8013 PID 树杀确认 DOWN（:8010 为他人预置实例未动）。⑤文档同步（/sync-docs 6 文件）：agent-api-reference（概要行 + status/stop/result 三路由行含 10 键契约/orphan 语义/N 条不去重 + 注册注记）/agent-file-map（strategy.py 1348 参数化 + machine.py 559 US-003 全契约 + 目录树行）/web AGENTS（machine.py 行 US-003 段）/tests AGENTS（test_web_machine.py 25 例⑤段）/CLAUDE.md（machine.py bullet）/business-overview（功能表 US-003 落地注记）。prd US-003 passes:true（US-002 已归档 prd_archive）。
52. **[2026-09-21] prd-machine-nesting-api US-004 — export 端点会话无关导出**：`web/machine.py`（559→754 行）+ `tests/test_web_machine.py`（889→1132 行 30 例）。①`POST /api/machine/export`：body JSON `{task_id, fmt?, placed?, table?}` 最小请求仅 `{task_id}` → `application/plt` 附件字节流（Content-Disposition 中文/ASCII 双写同 /export 约定，前缀 = run_name + sizes 快照 + _clean/_毛版 后缀）。fmt ∈ plt-clean（缺省，`_EXPORT_FORMATS` 值域其它 400）| plt（全量版）；placed 缺省 `_best_layout`（portfolio.incumbent 优先 → best_frame 边车 density 最大，与 result 端点 best 同源同序；width_mm/density/seed 同帧取，title 复用 /export PLT 分支 ASCII 格式），显式传入须为 `[{id,rotation,translation}]` 对象列表（形状 400）、全未命中 → 400「placed 的 pid 均未匹配到原始轮廓」(/export 既有兜底)；table 缺省服务端全算（`parse_table_payload({})` 6 手输默认值 A料/0.0%/0.0%/空/noname/空 + `build_info_table` 8 自动字段既有管线 —— **表格恒在场**，与 /export 缺省无表格刻意不同），显式传入经 parse_table_payload 校验非法 → 400 中文。②**会话无关核心 `_pieces_by_id_for_export` 三源兜底链**：run_dir `pieces_intermediate.json` 直载（`load_pieces`，solve 事实源在盘 → 会话被 TTL 逐出后仍可导出、与 run 存活期逐字节一致）→ `registry.peek` 会话快照（非抛式）→ 状态槽 `pieces_snapshot` start 快照，各源带 gate（doc gate 与 st gate 同值）；三源皆空/无 run_dir/无任何布局帧/gate=0 → 409。③复用 `web/export.py` 门面全部函数（placed_to_world/parse_table_payload/build_info_table/write_marker_plt，模块级 import 无环 —— export 不 import server/machine；门面与 export_plt/plt_table 零改动，clean=False 旧输出逐字节红线不动）；状态推进内部走 `_status_common(sid=,gate=False)`（done 识别 + run_dir 发现写回内存态，不要求客户端先轮询；orphan 用 marker run_dir）；task_id 闸复用 `_machine_task_ctx`（非格式 400/未知 404/外族 404）+ touch 刷活性。④测试（+5 例）：默认档逐字节金标（`_export_setup` 基座 = done 任务 + run_dir 三产物 + 会话注册 + `_FrozenDT` 冻结 plt_table.datetime 防跨分钟抖动；`_expected_plt` 门面直算对拍 + 6 手输默认值/8 自动字段非空断言 + 无表格版 ≠ 默认档证表格在场 + CD 双写）+ **registry.reset 显式逐出后导出仍 200 逐字节一致**（AC 核心）+ fmt=plt 全量版/显式 table 合法值对拍与非法 400 中文 + placed 子集对拍（片数随之变）/全未命中 400/形状 400 + 三源兜底链逐级（run_dir 无 intermediate→会话快照→pieces_snapshot 同字节、三源全空 409）+ 闸矩阵（task_id 三态/fmt 值域/非 JSON·数组 body·缺 task_id/starting 无 run_dir 409/run_dir 无帧 409）。⑤验证：pytest test_web_machine 30/30 + 全套件 **1121 passed**（1116 存量 + 5，141s 零回归）；`py -3 -c "from materialsorting.web import machine"` 导入过（双 AST 守卫仍在，分层未反向）；真实服务器冒烟（:8014，`out/machine_smoke_us004/smoke_us004.py`）——合成母版+g01×2/g02×1 quantities POST → 202 → done（incumbent 57.88%）→ 最小请求 {task_id} 导出 200 78942B 毛版 PLT（CD 含 _clean 后缀）→ 二次导出逐字节一致 → fmt=plt 42403B 全量版 ≠ 毛版 → 显式 table 200 → 错误矩阵（fmt 值域/table 非法/placed 全未命中/task_id 非法）全 400；服务器 :8014 PID 18740 树杀确认 DOWN、无 machine_ 孙进程残留。⑥文档同步（/sync-docs 6 文件）：agent-api-reference（+export 路由行全契约/概要行/注记段）/agent-file-map（目录树+web 章表 machine.py 754 行 US-004 段）/web AGENTS（machine.py 行 US-004 段）/tests AGENTS（test_web_machine.py 30 例⑥段）/CLAUDE.md（machine.py bullet US-004）/business-overview（功能表 US-004 落地 + 1121 口径）。prd US-004 passes:true。
53. **[2026-09-21] prd-machine-nesting-api US-005 — 幂等清理 token 与契约文档（二期收官）**：`web/machine.py`（754→925 行）+ `tests/test_web_machine.py`（1132→1335 行 37 例）+ `.docs/technical/agent-api-reference.md` 契约专节。①**DELETE `/api/machine/solve/{task_id}` 幂等清理**（任务生命周期终点，与 stop 分工：stop 保 run_dir 供续读、DELETE 全清）：在飞（starting/running）或 orphan marker 存活 pid 先 `_kill_tree` 树杀（同 stop 口径防 solve 孙进程白烧，响应 `killed:true`）→ `shutil.rmtree(run_dir, ignore_errors=True)` + 清 marker + 弹 `_STRATEGY_STATES` 条目 + glob 删 `machine_cfg_<sid>_*.json`（orphan run_mode 反查依赖一并回收，best-effort 吞 OSError）→ `200 {ok:true, task_id, run_dir, killed}`；**二次 DELETE 也 200** —— 内存墓碑 `_DELETED_TASKS: set[str]`（首删后状态槽/marker 均不在，墓碑区分「已清理」（200 no-op `{run_dir:null,killed:false}`）与「从未存在」（404）；400 格式错与墓碑不相交原样透传；MS 重启丢墓碑 → 已删任务二次删回落 404 = 任务不存在语义，消费端 404 一律视同已清理）。②**X-Machine-Token 认证** `_machine_token_error(request)`：env `MS_MACHINE_TOKEN`（`_MACHINE_TOKEN_ENV`）设置时全五端点强制请求头 `x-machine-token`（`_MACHINE_TOKEN_HEADER`）—— `secrets.compare_digest` 常量时间比较（两侧 `.encode('utf-8')` bytes，规避 str 版仅 ASCII 限制），缺失/不等 → 401 `{'error':'缺少 X-Machine-Token 请求头或 token 不正确（服务端已启用 MS_MACHINE_TOKEN 认证）'}`，**认证闸先于一切业务校验**（五 handler 首行）；env 请求时 `os.environ.get` 读取（部署期设置即刻生效、tests monkeypatch 即注入），未设置放行（loopback 同机部署假设）。③**machine_* run_dir 7 天机会式清理** `_cleanup_stale_machine_run_dirs`（`_MACHINE_RUN_TTL_SEC = 7*86400`）：每次 solve start 在校验 + client_ref 幂等闸全过后、铸 sid 前触发（本轮必建新任务且前缀互斥无误删面）—— `config_runs/` 下 `machine_*` 前缀目录 mtime>7 天 rmtree（extreme 档最长 7200s 超 7 天必是死任务残留），**非 machine 前缀结构性不动**（浏览器 web_*/手工 ms-run-config run），`_STRATEGY_STATES` 在册 run_dir（任何 mode）保护集跳过（时钟回拨/长暂停进程防御），OSError 全吞 best-effort 不阻塞 start。④client_ref 幂等完善（US-002 已实现复核）：同 ref 在飞 409 返回原 task_id（`_machine_ref_inflight` 非终态判据）/ 终态后同 ref 放行新任务。⑤**契约文档落册** agent-api-reference.md 新增「机器对接 /api/machine/* — YL 后端对接契约」自洽专节（插在极限桥接与 WS 节之间，11 小节：0 基址认证/1 任务状态机图/2 solve multipart+config 键表（per_type 全 0 与不发语义全等注记、time/seeds/band/prefix 禁键）+run_mode 三档映射表（180/1200/7200+spawn 参数）/3 status 恰 10 键字段表+密度物理口径警示（real_density=原面积/(width×gate) 物理毛版包络，不可与历史 erode 口径混比，对账以 width_mm 为锚）/4 stop（run_dir 保留 vs DELETE 全清分工）/5 result（demand>1 发 N 条绝不按 pid 去重——YL 前端建 N 副本承接）/6 export（{task_id,fmt?,placed?,table?} 最小仅 task_id）/7 DELETE 墓碑/8 错误码汇总表（401/400/404/409/413/422+消费端处理建议）/9 坐标系（Y 向上，消费端 SVG 需 scale(1,-1)）/10 对接推荐流程/11 运维注记（7 天清理+重启语义+产物定位）—— **YL 侧开发者可仅凭该节对接**，评审口径达成）。⑥测试（+7 例）：DELETE done 任务（产物四清断言 + 清理后 status 404 + 二次 DELETE 200 墓碑 no-op 形态 + 未知 404/格式 400/外族槽 404）/ DELETE 在飞（kills==[pid] 树杀金标 + killed:true + 二次幂等）/ DELETE orphan marker（pid 999 存活树杀 + cfg 回收）/ 7 天机会式清理（os.utime -8d 伪造 machine_/manual/web_ 三目录 → solve 202 → machine 超窗被清、非 machine 前缀与未超窗 fresh 不动）/ `_cleanup_stale_machine_run_dirs` 直调保护集单测（在册超窗不清、未认领被清 removed==1）/ token 设置时五端点无/错 401 + solve 401 不 spawn + 对 token 放行（status 200 + solve 202 + spawn 恰 1 次）/ 未设置放行显式 delenv 用例；`machine_env` 增 `_DELETED_TASKS` setup/teardown 清零 + machine_paths 断言补 DELETE 路径。⑦验证：pytest test_web_machine **37/37** + 全套件 **1128 passed**（1121 存量 + 7，166.31s 零回归）；`py -c "from materialsorting.web import machine"` 导入过（双 AST 守卫仍在，分层未反向）；真实服务器冒烟（:8015，`MS_MACHINE_TOKEN=smoke-token-us005`，`out/machine_smoke_us005/smoke_us005.py` 14 检查 ALL PASS）——无 token solve/status/delete 401×3、错 token 401 → 伪造 mtime -8d machine_/manual 目录 → 对 token solve 202 轮询至 running → DELETE 在飞 200 killed:true + run_dir 盘上消失 → 二次 DELETE 200 墓碑 + status 404 → 再 solve 202（顺手清 stale machine_ 目录）→ stale_manual 未动 → task2 DELETE 收尾；服务器 :8015 PID 20044 树杀确认 DOWN、PowerShell 进程扫描无残留、stale_manual 夹具清理。⑧文档同步（/sync-docs 6 文件）：agent-api-reference（状态段五端点全落地 + DELETE 路由行 + 注记段 + **契约专节 11 小节**）/agent-file-map（目录树+web 章表 machine.py 925 行 US-005 段）/web AGENTS（machine.py 行 US-005 段）/tests AGENTS（test_web_machine.py 37 例⑦段）/CLAUDE.md（machine.py bullet US-005）/business-overview（功能表 ✅ 五端点全落地 + 1128 口径）。prd US-005 passes:true —— prd-machine-nesting-api 五故事全部完成。
