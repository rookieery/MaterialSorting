# Ralph Progress Log
Started: 2026-08-09










14. **[2026-09-04] prd-edit-nesting-layout US-005 — 端到端冒烟与文档闭环**：编辑排料 PRD 收口，前端/后端代码**零改动**（组件/样式/store 全沿用 US-001~004 成品），交付物 = 冒烟锁死 + 四红线入册。① `scripts/smoke_edit_layout.mjs`（新，repo 根，范本 smoke_plt_table_preview.mjs，Playwright + chrome channel headless，prod :8000 真打后端不拦路由，断言全走 DOM/WS/路由抓包）—— 27 检查七相位：S1 上传 5336 → commit（g01..g10）→ 3 码 32/33/34（显式 `#sz_*` 不碰全选框）20s 短求解 30 片 final 84.60%/1868mm + **基线三锚**（solver 终帧 placed / final.density / 主视图快照 points+viewBox）；S2 完整版 5 层按 stroke 色+display 断言（毛版 fill-opacity 0.55 ×30 / 净版 #33cc33 ×30 / 内部线 #ff8c1a ×99 / 刺口 #ffd700 ×138 / 布纹线 #e53e3e ×30 全可见）↔ 毛板纯轮廓（4 工艺层全 display:none 毛版恒显）可逆切回 + 状态条初值 = 主视图利用率（**ceil 取整伪影上界 = density×≤1mm/料长 动态 tol 对拍** —— 首跑 0.03pt 差被 0.02 静态 tol 误杀，1868.4→ceil 1869 固有取整非 bug，EditLayoutModal 头注释口径）+ Δ +0.00pt；S3 拖右端片 +300mm 超界（1869→2169mm/84.57→72.88%）+ 点选 DOM 首位片（被拖片提层后在末位 ⇒ 必不同片；5336 全片带布纹线）拖柄自由转角（points+布纹线端点双变化）→ 指标面板三值（area 219164.2mm²/depth 185.3mm/**rot 73.3°>10**）+ 脚注「按算法碰撞口径」；S4 保存 → 主视图**恰两片** points 重绘（被拖片 maxX 增≥250 锁定 + 旋转片）+ NestLabel 84.60→72.88%/186.84→216.90cm + viewBox/fab=2169 三方同锚；S5 导出 PLT 抓包（默认 **plt-clean 毛版**，2026-08-31 起现场主交付 —— 首跑硬断言 fmt==='plt' 被判死改 plt||plt-clean 同族口径）POST /export 200 .plt 附件 + placed diff=2/30 非空 + density 72.88%<84.60% 非空 + width=2169；S6 ✕ 直关（非 dirty 无确认层）→ 重开右缘**逐片**左移腾空（US-003 同 ceil 桶教训循环拖，1 片即回缩）→ 74.77%/2114mm → 保存画布收缩 vb 2169→2114；S7 弹窗外重置（confirm PRD 原文案）→ NestLabel/全 30 片 points 逐串/viewBox 逐一回算法基线 → **再导出抓包 placed diff 回零（0/30 逐片 ε 对拍）+ density==final.density（1e-9）**；报告落 out/smoke_edit_layout/report.json（27 results + base/s4/s6/s7 摘要）+ 5 截图，退出码 0。② 文档：materialSorting-web/AGENTS.md 新增「编辑排料关键约定（US-005 端到端红线四条）」节 —— ①禁 ESC/遮罩关闭（唯一关闭 = ✕/保存，全站惯例有意偏离）②多副本按 placed_items 数组寻址保序写回（pid 非身份）③width_mm 随编辑包络双向伸缩 + density 族 real 口径 total_area/(width×gate) 单一真相源重算（density_sparrow 不动、PLT 表格/导出标题自动跟随）④重合指标按 erode 几何口径（物理毛版最多大 ~2·d_g）+ 冒烟口径备注（5336/3 码/两形态 stroke 色断言/ceil 伪影上界/同桶逐片拖）；agent-component-map.md 补 US-005 落地节（文件表 + 冒烟即红线执行器/fmt 断言口径/基线三锚三不变量 + 验证记录）。验证 = 冒烟 **27/27 PASS**（首跑 25/27，两 FAIL 皆断言口径非代码 bug：S2d 静态 tol→动态伪影上界、S5b fmt==='plt'→plt||plt-clean，修正后复跑全绿）+ vitest 全量 **936 passed**（61 文件零新增用例）+ typecheck 干净 + build 过（296.59KB/gzip 96.89KB）；验收后 Playwright browser.close()、ms-web 已杀（port 8000 free，残余 chrome 进程核实为用户默认配置文件会话非 Playwright 残留不动）。编辑排料 PRD 至此全 5 故事收官。
15. **[2026-09-05] prd-edit-polish US-001 — 引擎层 polish 核心模块（诊断+去旋转+去重叠+守卫+报告）**：编辑排料「智能微调」PRD 首故事，纯引擎层零 web/前端改动。① 新建 `materialSorting-server/src/materialsorting/nesting_engine/polish.py`（730 行）—— `polish_layout(placed, pieces_by_id, gate_mm, *, exclude=None, compact=False) → (placed_new, report)` 纯函数无 RNG，四部分：**诊断**（物理毛版轮廓口径 = pieces_by_id 原始 polygon 与 /export `placed_to_world` 同源非 eroded；bbox 预筛两两重合（面积阈值 0.1mm² 与守卫同口径）+ 穿透深度（顶点+**边中点**采样到对方边界最大距离 —— 与前端 editGeometry.penetrationDepth 同语义，共边平贴重合顶点恰落边界恒 0，中点补采样才量出真实压入深度，开发期实测等宽矩形叠 5mm 报 0 而发现）+ 旋转偏差审计 dev=min(rot mod 180, 180−rot mod 180)）；**去旋转**（dev 降序平手下标，`_derotate_ladder` = 当前 dev 充当 tol 调 `constraints.discretize_orientations`（同 1°/5° 步进规则）只取最近基线一侧严格降 dev 候选 —— 先试基线逐步回退；质心锚定 `t'=c_world−R(rot')·c_local`（世界几何质心即锚，仿射保质心）+ 邻域候选（质心原位 + 障碍 bbox 棱边 8 类对齐 + 门幅底/顶/布头 x=0）按 (位移,dx,dy) 升序取首个过守卫位）；**去重叠**（穿透深度降序平手 (i,j)，`_sep_translate` 镜像 `_slide_touch` 二分（lo 恒碰撞/hi=bbox 分离界恒自由）+ **1nm 防贴死微抬** ⇒ 终态交集面积精确 0.0，方向 ±y/−x 按 (量级,±y 优先) 择一，一片失败换动另一片，都失败记 residual）；**报告**（before/after 七指标：overlap_pairs/max_penetration_mm/total_overlap_area_mm2/rotated_pieces/rotation_dev_sum_deg/width_mm/density —— density=real 口径 Σ(原面积×副本数)/(width×gate) 百分数，width=maxX−min(minX,0) 布头外凸同计）+ moves（index/pid/kind derotate|separate/from/to/detail）+ residual（终态重合对+旋转残留如实上报）+ excluded + elapsed_sec）。**逐 move 五道守卫**：y∈[0,gate]（GATE_EPS 1e-6 数值容差）/ 全图物理包络不增（≤ width_before+0.5mm 且 minX<0 同计 —— 包络双向，开发期紧密夹具实证：R1 −x 分离 2mm 落 x=−2，maxX-only 口径会放行贴出布头）/ 位移片 vs 全图零新重合（≤0.1mm² shapely 精确复核）/ pid 多重集守恒（同 pid N 条**按数组下标**逐实例寻址绝不 pid 去重，出口 Counter 终检）/ exclude（labels/pids 双键 over-conservative 同 pid 全副本）永不动仍作障碍。**无改进返回输入 list 原对象**（逐字节不变量，LNS 同款）；有 move 时新列表全量新 dict（未动片字段值不变）；compact=US-005 预留键位恒 no-op。开发期修 2 bug：`_sep_translate` x 轴 bbox 分离界反号（free(−x)=mb[2]−ob[0]，单测锁死回归锚）+ 穿透深度中点补采样。② 新建 `tests/test_polish.py`（20 项全合成夹具零 intermediate/spyrrow 依赖）—— AC 八项夹具（空白旁斜片/可分离重合对交集精确 0/紧密 no-op `out is placed`/守卫越门幅四片全堵/守卫包络增长 rot0-bbox-更宽构造/多副本 index 寻址/排除集 labels+pids 障碍语义（B 朝 A 的 +y 被 A 挡下改走 −y）/确定性双跑 elapsed_sec 剔除口径）+ 结构（report 键集/density 手算对拍/空 placed/未知 pid PolishError/纯函数输入零变异/compact additive）+ 单元（dev 归一/ladder 首元素=基线严格降 dev/sep −x 符号）+ AST 分层纯度 + 性能预算 120 片 ≤5s + `main([])` exit 0。③ 冒烟 `python -m materialsorting.nesting_engine.polish` = 合成夹具八项自检 PASS exit 0；`--demo [--demo-pieces N]` 真实母版几何演示（对齐 prefix --pin-demo 形态：intermediate 前 N 片确定性带病布局（横排叠 3mm+尾行 ±25° 斜置）→ 前后对比，无 spyrrow 依赖）—— 实测 5336 110 片：重合对 74→10（尾部链堵 residual 如实）、斜片 2→0、料长 44569.904 不变、密度 13.48% 不降、45 moves、**0.516s**（预算 ≤5s）、双跑全等。验证 = pytest 全量 **714 passed**（基线 694+20）+ vitest/前端零改动零影响 + 分层 import 白名单 AST 守卫。文档 = nesting_engine/AGENTS.md（启动行+文件分工表 polish 行）、.docs/technical/agent-file-map.md（树行+## polish.py 节：常量表+函数表）、tests/AGENTS.md（test_polish 行）、CLAUDE.md（nesting_engine 模块清单 polish.py）。遗留 US-002+：POST /api/edit-polish 会话族端点（run_in_threadpool+edit_hold.refresh+pid 全匹配 400）。

16. **[2026-09-05] prd-edit-polish US-002 — POST /api/edit-polish 会话族端点**：编辑「智能微调」web 接线，纯后端零前端/引擎改动（polish_layout US-001 成品直用）。① `web/server.py` 新增 `POST /api/edit-polish`（紧邻 `/api/edit-hold` 同款样板）：读 `X-Session-Id`（缺省 default）→ `session_registry.resolve()`（`create=False` 缺省：过期/墓碑 401 `session_expired`、非法 400，`SessionError` 统一 `JSONResponse(e.payload(), e.status)`）→ 载荷校验 fail-fast 全家桶（body 非 JSON/非对象、placed 空/缺/非列表、条目缺 id/translation 非 2 元、**pid 全匹配才跑**——任一 pid 未匹配会话 `pieces_by_id` → 400 文案含「母版已变更？请重新求解/上传」不做部分降级（与引擎 PolishError 同文案双保险）、gate_mm 字符串/NaN/payload 与 state 两处皆无 → 400、exclude 非 dict → 400）→ gate_mm 缺省回退会话 `state['gate_mm']`（`float(payload or state)` /export 同法，NaN 用 `not x > 0` 拦）→ exclude（labels/pids 双键）/compact（US-005 预留缺省 false）原样透传引擎 → **polish 构造段 `run_in_threadpool` 执行**（prefix-preview 先例防阻塞事件循环；`from ..nesting_engine.polish import` 函数级 import——monkeypatch 引擎模块即生效）→ PolishError/ValueError/TypeError 兜底 400 `微调失败：…`（数值字段非法不炸 500）→ 成功顺手 `edit_hold.refresh(sid)`（编辑钉住与心跳同语义；`if sid:` default 不进钉住表，/api/edit-hold 同口径）→ `{ok:true, placed, report}`（placed 条数+pid 多重集守恒 = 引擎出口 Counter 终检 + 路由入口全匹配双保险）。② 新增 `tests/test_web_edit_polish.py` 14 例（镜像 test_web_edit_hold 套路 + AC 六项）：200 全链路真引擎（test_polish AC#2 可分离重合对夹具 → 重合对 1→0/report 四段七指标/恰一片最小分离 ~5mm/payload 不带 gate_mm 同证 state 回退放行守卫）、多副本 Counter 守恒、default 不钉住+sid 成功续期 hold_until≈clock+EDIT_HOLD_SEC、sid 会话状态隔离（sid 只注册 sx_30 → default 的 g01_30 在 sid 下 400「母版已变更」/default 下 200）、FakeClock 过期 401 失败不续期、非法 400、载荷 400 全家桶（NaN 走原始 JSON 字面量——httpx `json=` 序列化拒 NaN，开发期实测）、gate 回退 vs payload 优先 + exclude/compact 透传（spy 捕获引擎实参）、**run_in_threadpool 运行时线程级断言**（spy 包装 `server_mod.run_in_threadpool` 记录事件循环线程 + 引擎侧记录执行线程 → 非同一线程，比源码级断言更强的 AC 满足形态；开发期修正两处测试口径：分离 mover 是 (i,j) 下标序先试 g01 −y 非固定 g02 +y）。验证 = pytest 全量 **728 passed**（基线 714+14）+ `python -m materialsorting.web.server` 实起（端口被上一会话残留 PID 12140 占用致首试 bind 失败误报「Not Found」——taskkill 后重起）真打端点：invalid sid 400/空 placed 400/unmatched pid 400 文案逐字 + 真实母版 5336 前两片（g01_30 rot25 + g02_30 叠放）→ 200 重合对 1→0、rot 25°→0°、width/density 不变、0.027s；验收后 ms-web 已杀（port 8000 free）。文档 = agent-api-reference.md（HTTP 路由表 +edit-hold 补漏行/+edit-polish 行 + 会话速查表合并行 + /api/edit-polish 专节：请求/响应/错误表/四不变量，红字口径差异注记留给 US-004 其交付物）、agent-file-map.md（web 表 server.py 行 425→581+会话族三路由归并 + 目录树行尾）、web/AGENTS.md（server.py 行）、tests/AGENTS.md（test_web_edit_polish 行）、CLAUDE.md（web 模块清单 server.py 条目）。遗留 US-003+：前端「智能微调」按钮+对比卡+一级撤销（exclude best-effort 组装 band label/prefix pids）。
