# Ralph Progress Log
Started: 2026-08-09
































58. **[2026-09-23] prd-se-ext-top3 US-003 — web 状态面多候选透传**：`web/strategy.py`（1348→1368 行，行为改动仅 `_parse_plan` 一处）+ `tests/test_web_strategy.py`（1388→1494 行 45→51 例）。①**`_parse_plan` 四新键 additive**：se 段计划态 `ext_top_n`/`ext_band`（开跑即写）+ 实际态 `ext_seeds`/`extra_rounds`（首个延长轮启动时 run_config 补写，R0/中断保持计划态）——`for key in (...): if sj['se'].get(key) is not None` 在场才加键；**判据用 `is not None` 而非 truthy**：m=1 单冠军的 `extra_rounds=0` 是合法值必须透传（truthy 判据会误吞）；旧 run/race/legacy 零新键 → plan 结构逐键不变（前端隐藏相关 UI、渲染零变化）。status 载荷经既有 plan 摘要透传（前端用 `ext_s × extra_rounds` 算实际额外秒数，如 180×2=360s）；**机器对接三端点（gate=False 家族）零额外改动**——machine.py 早已走同一 `_status_common(sid=,gate=False)`，additive plan 键自动继承；orphan 分支同函数同继承。②**`_parse_events` 多候选核验 = 零结构变更**：`best_frame_s{seed}_ext.json` 按 seed 天然多文件（US-001 装载点参数化）→ 既有 glob 逐文件产出 m 条 extension 事件，`sorted(Path)` 文件名**字典序**确定稳定（实测发现两位数 seed 非数值序：'s11'<'s2'，事件序 [11,2,7] —— 前端候选推导以 plan.ext_seeds 为权威、事件只标记「该 seed 有延长产物」，PRD 定案零结构变更锁定现行为）；尾窗按 `len(ext_events)` 联动收缩前段（keep = 20−m，≤0 时空前段）→ 全部延长事件恒存活（2026-09-20 修复的单候选豁免语义自然泛化）。docstring 补多候选段。③测试（+6 例，`_write_strategy_json` 辅助 = 旧形态 se 段 + additive 键注入）：实际态全键透传（四键 + 既有键不受扰）/ m=1 `extra_rounds=0` 不被吞 / 计划态无两实际态键 / 旧 se·race 产物 plan 逐键零变化（additive 红线）/ status e2e（plan.ext_s×extra_rounds=360）/ 3 候选事件序列（执行序 [3,1,4] → 事件序文件名字典序 [1,3,4]，3 条 seed_done + 3 条 extension 恒置尾）/ k=21 长跑尾窗联动豁免（17 条 seed_done + 3 条 extension 恒存活、`len(events)==_EVENTS_TAIL`）。④验证：pytest 全套件 **1156 passed**（1150 存量 + 6，144.61s 零回归）；`py -3 -c "from materialsorting.web import strategy"` + `py -3 -c "import materialsorting"` 导入过；AST 守卫（禁 import ..cli.*）与 server 延迟 import 惯例随套件续锁。⑤文档同步（/sync-docs 3 文件）：agent-api-reference（strategy status `plan` 行补 warm 透传（2026-09-19 漏记一并补）+ 多候选四键 + `events` 行多候选豁免；§桥接段 stale「键白名单不透传 warm」修正）/ agent-file-map（strategy.py 行 1348→1368 + prd-se-ext-top3 US-003 段）/ tests AGENTS（test_web_strategy.py 行 39→51 例 —— warm 6 例先前漏更一并补记 + US-003 电池段）。prd US-003 passes:true。
59. **[2026-09-23] prd-se-ext-top3 US-004 — 前端提示与多候选进度展示**：`types/strategy.ts`（+21 行）+ `StrategyRunModal.tsx`（823→963 行）+ `ExtremeRunModal.tsx`（+26 行）+ 双测试（StrategyRunModal 20→27 例 / ExtremeRunModal 22→25 例）。①**类型 additive**：`StrategyPlan` 四可空键 `ext_top_n/ext_band/ext_seeds/extra_rounds`（**判据 `!= null` 不能 truthy —— m=1 的 extra_rounds=0 是合法值**）+ `StrategySeSummary.ext_seeds?`（result portfolio.se 镜像）；旧 run 无键 → 相关 UI 自然隐藏（渲染零变化红线）。②**提交前文案单一实现**：`seExtHint(extSec)` =「筛选密度与冠军相差 ≤0.5pt 的 seed 会一并顺延（至多 3 个），最多多花 2×延长时长（约 N 分钟）」，N = `fmtMinutes(extSec*2)` 动态算（与 CLI 启动行「2×{se_ext}s」同口径，**不写死文案**）：高级运行 `STRATEGY_SE_EXT_S=180` → 约 6 分钟、极限 `EXTREME_SE_EXT_S=600`（web 不带 --extreme-budget 恒默认档）→ 约 20 分钟（1200 档 CLI-only → 40 由纯函数测试对拍覆盖）；band/top-N 常量 `SE_EXT_BAND_PT/SE_EXT_TOP_N` 与后端 portfolio.py 镜像。配置态 hint 行：strategy-ext-hint（mode==='se' 才渲染）/ extreme-ext-hint（SE 臂）。③**进度面多候选**（`plan.ext_seeds` 候选权威源，`extSeeds!==null && length>1` 才走多分支）：chips m 条「{seed} 延·冠军/延·候选 i + ✓/●/待定 + 密度」（完成取 per_seed `phase='extension'` 该 seed 入账、进行中取 current.ext 命中、待定灰）；阶段行 rank1=「延长中 · 候选 1/m · 冠军 seed X」/ rank≥2=「候选 i/m（seed X）」（+静默时长既有后缀）；`extra_rounds>0` 时 `strategy-ext-extra-hint` 实际值行「实际 m 个候选（额外 m−1 轮延长）· 预计多花 ~fmtMinutes(ext_s×extra_rounds)」；**m=1/无键 → 旧单冠军路径逐字不变**（三源冠军推导原样保留 —— `--se-ext-top 1` 哨兵 UI 全等）。④**事件行**：`fmtLastEvent(ev, extSeeds?)` 第二参 additive，extension 事件 rank≥2 标「候选 i/m · seed X 进入延长」（rank1/单候选/无键仍「冠军 seed X 进入延长」）。⑤测试（+10 例 → 全量 1236）：纯函数档位对拍（fmtMinutes 6/10/20/40 分钟+2 小时 / seExtHint 180/600/1200 / 常量镜像 180·600·3）/ 配置态 hint 显隐 + 逐字 / m=3 全链（chips 8 条结构 + 阶段行候选 2/3 + 额外行 ~6 分钟 + 事件行候选名次）/ rank1 冠军轮 ● / m=1 哨兵（旧文案逐字 + extra_rounds=0 无额外行）/ 旧载荷·race 零新增行（红线）/ 极限 SE 臂 m=2（~20 分钟 hint + 候选 2/2 + 额外 ~10 分钟 = 600×1）/ 旧 SE 臂载荷零变化。⑥验证：vitest 全量 **1236 passed**（71 文件，8s）+ `tsc --noEmit` + `npm run build` 过；**浏览器验证（dev 模式 :5173 proxy :8010，playwright 一次性脚本跑完即删）**：5336 母版 se 10min 真跑**实触发 m=2**（seed1 88.525% vs seed2 88.42% 差 0.10pt ≤ 0.5pt 带）—— 阶段行「延长中 · 候选 1/2 · 冠军 seed 1」+ chips「1 延·冠军 ● 88.52%」「2 延·候选 2 · 待定」+ 额外行「实际 2 个候选（额外 1 轮延长）· 预计多花 ~3 分钟」+ API status plan 四键对拍全中；冠军延长轮入账 0.887211 后候选 2 延长中经 API 停止收口（截图 out/us004_ext_verify_*.png；环境清理：杀 ms-web/vite 双进程 + playwright browser.close，杀前确认无遗留监听）。⑦文档同步（/sync-docs 3 文件）：materialSorting-web/AGENTS.md（prd-se-ext-top3 US-004 关键约定节）/ agent-component-map（US-004 落地段：文件表 + 4 条不变量 + 验证留档）/ agent-api-reference（result summary 行补 se.ext_seeds additive + 前端镜像注记）。CLAUDE.md/README se 段多候选收官**留给 US-005**（其验收明确列出，不越界）。prd US-004 passes:true。
60. **[2026-09-23] prd-se-ext-top3 US-005 — 端到端验收与文档收官（纯验证 Story，代码零改动）**：新增 `data/configs/5336_qty1_gate1750.json`（复刻 web_2538fb 触发单：5336 coded 母版、码 30-40 ×1、gate 1750）+ `5336_band_g05_gate1750.json`（+band g05 组合视角场景）+ `tasks/prd-se-ext-top3.md` 验证速查表（9 行，AC-6）。①**多候选真跑 m=2**：`--strategy se --time 600` 筛选 88.525(s1)/88.421(s2)/87.775/84.74 → s2 间隔 **0.104pt** 入带触发（s0 0.75pt 出带）；延长轮头「筛选冠军」「候选 2/2」、strategy.json ext_seeds[1,2]/extra_rounds 1、_ext 产物独立成对、best=seed1 frame13 **88.7211%**=两延长曲线帧级最大（s2 ext 峰 88.7080 未反超）、warm 两轮真顺延零回退、run_stats se_ext={top_n:3,band,candidates[{1,0.88525,0.0},{2,0.884207,0.104}]} 与 result 逐键一致、elapsed 606.8s。②**哨兵对拍**：`--se-ext-top 1` m=1（ext_seeds[1]/extra_rounds 0，seed2 入带仍不延长）且 best 与多候选单全等（单调性实证）；旧代码 = **git worktree @5328293 + PYTHONPATH 指 worktree src**（比 stash 干净 —— 不触工作区、out/ 天然隔离到 worktree），背靠背 stdout 681 行归一化**逐字节全等**（零帧漂移，密度/宽度逐帧相同；豁免仅 run_dir 路径/时间戳、逐帧 elapsed 墙钟小数、se 启动行多候选附注）+ strategy/result 旧键全等（solve 五轮、best 四元组、portfolio.se/config.strategy 旧键）+ 新键纯 additive（strategy 四键/ext_warm_rounds/ext_seeds）。③**极限 SE 臂**：band g05 + `--extreme --extreme-strategy se --time 960`（糖衣展开 300/600+p0.7/et0/w4）跑通 —— 计划态 ext_top_n:3 默认继承、ext_seeds[0]/extra_rounds 0（k=1 恒单冠军）、**band 组合视角 warm 装载成功**（ext_warm_rounds warm:true、零回退行）、run_stats extreme{budget:600,strategy:'se'}+se_ext 落键、best 89.22%。④**全量回归**：pytest **1156 passed**（首轮 2 例 test_cli_strategy replay 负载敏感 flake = §57 备案同款，隔离 21/21 绿 + 全量二轮全绿）；vitest **1236 passed**；race/无旗标新旧背靠背 —— 无旗标 148 行归一化逐字节全等（全帧密度零差异）、race 末行/best 86.29%/kill 判定全等 + seed1 门段 3 行帧漂移（num_workers 豁免口径）、run_stats 四行键集同构零新键。⑤**smoke-extreme-run.mjs 零改动复跑 PASS**（断言不涉 se 行 —— 默认 race 臂；commit/预设轮数/启动/首帧 80.39%→stopped 全过；环境清理：playwright browser.close + Stop-Process 杀 ms-web PID 39880，确认 :8010 无监听零残留）。⑥**文档收官**：CLAUDE.md（运行方式旗标签名 + se 段多候选句）/ README.md（SE 筛延段多候选语义+超支设计+哨兵+三面可观测 additive 键全景）/ 极限方案 v1.2 §6 多候选注记（0.5pt/top-3/超支 + se_ext 观测校准回路）/ agent-file-map（portfolio.py+run_config.py 行 US-005 验证留档；api-reference/strategy.py 行已随 US-003/004 落档复核无缺）。⑦验证留档全量数据进 tasks/prd-se-ext-top3.md 验证速查表。prd US-005 passes:true —— prd-se-ext-top3 五故事全部完成。
