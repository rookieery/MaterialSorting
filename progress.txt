# Ralph Progress Log
Started: 2026-08-09















24. **[2026-09-05] prd-edit-keyboard-transform US-005 — 键盘变换交互（L/K/空格/O/I）**：US-001~003 mirror 地基之上的编辑键盘交互层（已定案 2026-09-05：L=顺时针 +1°、K=逆时针 −1°、Shift+L/K=±10°、空格=180° 掉头、O=水平镜像=toggle mirror（rot 不变）、I=垂直镜像=toggle mirror+rot+180（diag(1,−1)=R(180°)·diag(−1,1) 复合律共用单 mirror 标志）；零后端、零渲染公式改动，全复用 US-003 落地链路）。①EditCanvas：window keydown 挂**指针交互同一 effect**（复用闭包 commitDragPlacement/refreshMetrics/updateHandle，监听器仍只挂一次 —— PRD 文字「新增 window keydown effect」按精神落为同 effect 内新 window 监听，理由头注备档）；守卫链三段任一命中零变换：`interactionRef.current` false 总闸 → target ∈ INPUT/SELECT/TEXTAREA/BUTTON 或 isContentEditable（键盘归表单控件，含画布左上形态 select；tagName 大写比较防 SVG 小写 tagName）→ 无选中片；单字符键统一小写（Shift+L 的 'L'/CapsLock 同键命中）；L/K 放行 e.repeat（auto-repeat 每击一档 = 按住连转）、幂等键（空格/O/I）忽略 e.repeat 防抖、空格 preventDefault 防 body 滚动；`applyKeyTransform(index, rot, mirror)` 质心锚定唯一出口：`t' = c_world − R(rot')·M(m')·c_local`（c_local/c_world = base/当前世界多边形顶点均值；质心是仿射量 ⇒ 零平移变换后质心恰 = R·M·c_local，片原地变换不漂移）→ clampPlacement（带 mirror 参，Y∈[0,gate]/minX≥0 与拖动同口径，右界永不钳）→ commitDragPlacement 泛化路径（DOM 5 层 + setWorkingItem + 池增量 + 指标/手柄，O(单片)）；commitDragPlacement 加可选 `mirrorPatch?: boolean`（缺省 undefined = 不改 mirror，指针会话零回归；键盘 O/I 显式传目标值）；新 prop `interactionEnabled?: boolean`（缺省 true，`interactionRef` 渲染期回写 modeRef 先例 —— 监听器不重挂、确认层开关即时生效）；R 键分支已留（return 空）接线归 US-006；指南卡新增键盘行「键盘：L/K 微转 · 空格 180° · O 水平镜像 · I 垂直镜像 · R 重置此片」（「形态」「保存」反向锁不破）。②EditLayoutModal：传 `interactionEnabled={!confirmDiscard}` —— ✕ dirty 确认层打开 = 画布全键禁用（确认层按钮/回车不被画布键劫持；指针交互不受影响，确认层 overlay 已挡 pointer 热区）；编辑弹窗仍不挂 ESC 关闭（仓库红线不变）。③editStore：setWorkingItem patch 扩可选 `mirror?: boolean`（US-002 节预告「届时扩 patch」落地）：显式 true 带键 / 显式 false 键消失（omit-when-false）/ 缺省保持现值。④单测 +11（1005→1016）：EditCanvas 键盘 describe 10（L/K/Shift 精确值 + 质心不漂移（世界多边形顶点均值回 c_world）+ DOM points 手算 r2 对拍 + 整型往返回原位 / clamp 两界（minY 上抬 + minX 右推闭式 500·sin1°）+ 旋转手柄 cy 同帧随质心上移 4.325 / 空格 rot+180 + defaultPrevented + 同足印 / O toggle 双向 + 大写 O 同键 + 'mirror' in w[0] 键消失 / I 复合律 (600,750) / 键盘 O 池增量实时指标 164000→200000 mm²（锁 applyEditPlacement 显式传 mirror 接线）/ e.repeat 规则（L×2 → 2°；空格/O/I repeat 全忽略）/ 守卫链四条：真实形态 select + input/textarea/button/contentEditable（Object.defineProperty isContentEditable）五类控件零变换、无选中零变换、interactionEnabled=false 全键禁用 + 恢复 true 后同键生效（ref 读现值））+ editStore patch.mirror 三态 1。⑤验证：`npx tsc --noEmit` 干净 + vitest 全量 **1016 passed**（1005+11，64 文件）+ `npm run build`（tsc+vite）过（304.77KB/gzip 99.65KB）、static/ 已重建；**浏览器 dev 模式 26/26**（out/usk005_keyboard_verify/verify2.mjs + report2.json + 3 截图：P1 上传 5336 → 3 码 30 片短求解 + 弹窗 + 指南键盘行在场无回潮；P2 真实 trusted 按键 L/K/Shift+L/Shift+K —— 页面内动态 import editGeometry/editStore 纯函数直算期望 placement，DOM↔纯函数逐字节对拍（内容匹配恰 1 片）+ 指标面板旋转角同帧刷新；P3 空格×2 掉头往返/O×2 镜像双向/I×2 垂直镜像复合往返（v2_p3c_mirror.png/v2_p3e_vmirror.png 归档）；P4 连发 20 次 repeat:true L → +20° 耗时 78.1ms（<3000ms 不卡顿）+ 空格/O/I repeat 全忽略；P5 守卫链：形态 select 聚焦/无选中/✕ dirty 确认层全键禁用/取消确认层后 L 恢复生效（interactionRef 渲染期回写，前轮失败点修复）；P6 全程无 pageerror）。⑥文档同步：materialSorting-web/AGENTS.md（store 树行 + edit 树行 dated fragment + 文末「edit-keyboard US-005 关键约定」节：keydown 同 effect/守卫链顺序/interactionRef 回写/e.repeat 分界/小写归一/质心锚定唯一出口/mirrorPatch 三态/指南行反向锁/验证门）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-keyboard US-005 落地」节：文件表/四不变量/验证记录含浏览器 26 检查项明细）；agent-file-map.md 后端专属不适用、agent-api-reference.md 无 API 改动不适用（US-001 同款口径）。prd US-005 passes:true。
25. **[2026-09-05] prd-edit-keyboard-transform US-006 — R 键片级重置**：US-005 键盘层收官键（定案：R 键非右键菜单；重置 = 恢复算法基线，会抹掉该片已保存编辑 —— 定案接受，安全网 = ✕ dirty 确认 + 全局重置 + 片级重置；本期不建撤销栈）。①EditCanvas R 分支接线（位置**移到幂等键 repeat 闸之后**——R 与空格/O/I 同为幂等键 `if (e.repeat) return` 后分发；守卫链与 US-005 六键同链：总闸/表单控件/无选中任一命中无效）：调 `editStore.resetItem(selRef 下标)`（同 pid 多副本按下标寻址绝不按 pid）→ 返回 false（store 层 baseline/越界/id 错位守卫命中）静默不动不炸 → 成功后重读 `working[index]` 按基线值走 refreshPieceView 单点同帧刷新。②`refreshPieceView(index, entry, rot, tr, mirror)` 自 commitDragPlacement **拆出共用视图段**（applyEntryPlacement DOM 5 层 + 签名登记 + applyEditPlacement 池增量 + refreshMetrics/updateHandle，不含 store 写入）；commitDragPlacement 重构 = setWorkingItem + refreshPieceView（指针/键盘变换路径行为不变——R 的 store 写入唯一出口 = resetItem，不重复 setWorkingItem）。③单测 +5（1016→1021）：正常恢复（拖动+L+O 后 R → working 逐字段回基线/mirror 键消失/其余片与 lastFrame 不动/毛版 points 手算回基线/指标面积 90000→50000 精确重算 + 旋转偏离 1.0°→0.0° + 手柄 cy 767.5→567.5 同帧——主渲染 effect 不触指标手柄，精确断言锁 refreshPieceView 接线）+ e.repeat 忽略后单击生效 + 未选中无效（空白点击取消后）/重新选中生效 + 确认层 interactionEnabled=false 禁用/恢复生效 + resetItem 返回 false（人为 baseline id 错位、pidSeq 不变直达守卫）静默不炸 DOM 不动。④验证：`npx tsc --noEmit` 干净 + vitest 全量 **1021 passed**（64 文件）+ `npm run build` 过（304.95KB/gzip 99.74KB）、static/ 已重建；**浏览器 dev 模式 26/26**（out/usk006_reset_verify/verify.mjs + report.json + 3 截图：P1 上传 5336 → 30 片短求解 + 指南 R 行；P2 L+1°→**保存并关闭**（handleSave→closeModal，前轮脚本踩坑）→重开承接已存编辑→O 镜像→R 回**算法基线而非已存值**（「连已保存编辑一起抹掉」语义锁 + lastFrame 不动 + 全 30 片 points 多重集回基线 + 指标同帧 0.0°）；P3 指针拖片 5（down 直发 polygon/move-up 冒泡 svg——真实 hit-testing 会被叠片抢 target）+ Shift+L + O 复合编辑 → R 镜像片复位逐字段回基线；P4 repeat×3 忽略 + 单击复位；P5 守卫链（无选中 / ✕ dirty 确认层 R 禁用（dirty 制造踩坑：单按 L 恰复刻已存态 → 连按两次）/ 取消后恢复）；P6 无 pageerror）。⑤文档同步：materialSorting-web/AGENTS.md（edit 树行 US-006 fragment + US-005 节 R 前向引用改「已随 US-006 接线」+ 文末「edit-keyboard US-006 关键约定」节：repeat 闸位置/下标寻址/静默守卫/store 唯一出口/refreshPieceView 单一真相源/验证门）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-keyboard US-006 落地」节：文件表/四不变量/验证记录）。验证后 ms-web/vite 已杀（8000/5173 释放）。prd US-006 passes:true。
26. **[2026-09-05] prd-edit-keyboard-transform US-007 — 端到端冒烟扩展与文档同步**：键盘/镜像/重置收官 —— 零组件/零后端/零渲染公式改动，把新不变量锁进 E2E 冒烟 + 按仓库 dated fragment 惯例同步三份文档。①smoke_edit_polish.mjs 新增 **S8 键盘/镜像段 17 检查（29→46）**：S7 compact 保存 → 镜像前基线 DXF 导出（placed 守恒 + 自研 R12 解析器 parseDxfOutlines：严格 code/value 成对步进，**门幅边框无 layer 键落缺省 '0' 不入 layer1 列表**）→ 选片决策（eroded 质心锚定预测不触门幅 ≥2mm + 镜像可辨位移 ≥20mm + 非带排除片，k=12 g06_32 margin=700.6mm disp=635mm）→ 重开承接（points 内容匹配恰 1 片）→ 合成 pointerdown 选中（selectByPoints 按内容寻址 + 直发 polygon —— 真实 hit-testing 被叠片抢 target；blurActive 后 trusted keydown 防按钮聚焦命中守卫②）→ **O 键镜像**（DOM 多重集差恰 1 出 1 进 + 新 points = 质心锚定反射对拍 reflErr=0.0059mm ≤0.05）→ 保存 → 导出 PLT/DXF：**POST placed 恰一项 mirror:true**（下标=选中片 + rot 不变 + eroded 质心锚定补偿 anchorErr=0 ≤1e-6 + 其余 29 项与镜像前逐位全等 + PLT/DXF 载荷一致）+ **正文几何镜像坐标对拍**（闭式公式 期望=R(θ₁)·M·R(−θ₀)·(镜像前 DXF layer1 顶点−t₀)+t₁，θ/t 取两次导出 POST placed[k] —— 不依赖 intermediate 磁盘文件；PLT err=1.41unit ≤2（lead 偏移自门幅框 stroke 首点推导）/ DXF err=0.0095mm ≤0.05，非镜像反事实 50801unit/1270mm = mirror 键真实驱动几何）→ 重开承接镜像片 → **微调 mirror 逐位透传**（请求/响应恰一项同下标同 pid，其余 29 项无 mirror 键，请求项 byteEqual）→ **R 键重置**（恰改一片 + points 逐字节 = S6 末帧算法基线串 + ∈ 首开快照 m6Points + 基线 pid 对位）。配套纯 Node 算子：pointsStrJs（lib/geometry.ts 逐字节复刻 —— prod 构建页面内无法 import 源码模块；Math.round(x*100)/100 同 V8 语义）/ parsePltStrokes（PU 起新笔/PD ≤10 点分块拼接，stroke[0]=门幅框、stroke[k+1]=placed[k]）/ multisetDiff（恰改一片判据）。②文档同步：materialSorting-web/AGENTS.md（edit/ 树行 US-007 fragment + 冒烟节 29→46+S8 bullet + 文末「编辑键盘/镜像关键约定」汇总节：六键语义/质心锚定（O 锚 eroded、导出画 raw，口径差由锚定 translation 吸收）/omit-when-false（恒发 mirror:false 会红精确锁键集用例）/resetItem 下标守卫/46 检查端到端门）+ .docs/technical/agent-component-map.md（头部覆盖行 + EditLayoutModal 行 dated 注记（零改动、S8 锁保存→重开承接/透传不变量）+ 文末「edit-keyboard US-007 落地」节：文件表/四不变量/验证记录）+ .docs/technical/agent-api-reference.md（/export placed mirror 块补端到端几何对拍注记 + /api/edit-polish mirror 键补透传锁注记 + 冒烟覆盖行 29→46）。③验证：冒烟 **46/46 PASS**（既有 29 复跑全绿 = mirror=false 零回归门；报告 out/smoke_edit_polish/report.json mirror 段全指标在案）+ vitest 全量 **1021 passed**（64 文件）+ 后端 pytest **745 passed** + `npm run build` 过（304.95KB/gzip 99.74KB）、static/ 已重建（冒烟跑在重建后 prod 构建）。④/sync-docs 已跑（改动映射：前端 E2E 脚本 → component-map、API 契约注记 → api-reference；无后端改动 agent-file-map 不适用）。验证后 ms-web 已杀（:8000 释放）。prd US-007 passes:true。
