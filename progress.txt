# Ralph Progress Log
Started: 2026-08-09






6. **[2026-09-02] 修复 — prefix 无 per_type 求解必炸「solver 返回 None」（门幅安全余量 + panic 透传）**：用户报 web 运行算法 seed 0 报「求解失败: 求解失败: solver 返回 None」（US-004 ⑥-a 移交问题的真实触发，当时验收全走 per_type d=2 路径未修）。根因链（本地 5336 无 per_type 100% 重现）：① d_g=0（web 上传默认无 per_type）时 `select_prefix_plan` 近满幅搜索选出贴线组合（PS_g02+g03@34+g02@38 H=1979.69 / gate 1980，residual 0.307mm），组合片碰撞轮廓不做 erode 收缩 ≈ 条带高；② spyrrow 放置器放不下 → Rust panic `strip-width is running away (item 68 = PS_ 组合片)`，探针实证片高 ≥ strip_height（含恰好等高）必 panic、留余量即 OK；③ pyo3 `PanicException` 是 **BaseException 子类**，`solve_worker._solve` 的 `except Exception` 捕不住 → 求解线程静默死亡、holder 空 → 走「理论上不可达」防御兜底误报「solver 返回 None」（内层 worker:209 + 外层 routes_ws:344 双层包装）。修复（用户确认 A+B、margin=10）：① `prefix.py` 新增 `PREFIX_GATE_MARGIN_MM=10.0`——`select_prefix_plan` 可行线 `limit = gate_nest − 10`（取次高安全候选，residual ≥ 10 恒成立）+ `build_prefix_plan` 竖排高守卫同口径（安全条带界 = 门幅−10，兜底 4 片路径同样防贴线；d_g>0 时 erode 对非水平边缘收缩 <2·d_g 故不按 d_g 折算）；② `solve_worker._solve` 与 `solver.solve_with_callback`（threading 兼容版）`except Exception` → `except BaseException` 字符串化（带异常类型前缀）——下次任何 Rust panic 都能看到真因而非误导兜底文案（daemon 线程收不到 KeyboardInterrupt，捕获无副作用）。测试 +4：test_prefix.py（贴线候选拦下取次高 / 全贴线兜底 / 兜底基座贴线 PrefixError「安全条带界」）+ test_solve_proc.py（2200 高片段条带 1000 → err 含 PanicException+strip-width 且不含「solver 返回 None」）。回归：pytest 全量 680 passed；无 per_type CLI（事故路径）跑通 86.68%/119 片（修复前必炸）；带 per_type CLI 87.87% 跑通（margin 10 < erode 4mm 缓冲的既有 per_type 场景零影响）。既有独立问题（本次未动、修复前同败）：`python -m materialsorting.nesting_engine.prefix` 冒烟在 grouped 备档构造 FAIL（最大缝隙 932.64mm > 1mm，PrefixError 形态守卫）——与 margin 无关，另行排查。
7. **[2026-09-02] 修复② — 需求2「弧线相切」：补片朝向 FR-3 委派（prefix 选码 rot 不再按 max-H 枚举）**：用户反馈顶部异码补片搁在下片弧峰上留楔形空隙，应取 x/y 双镜像（点反射 ≡ rot180，布纹 {0,180} 合法且保手性）相切嵌入。根因：PRD FR-3（rot 按 union bbox 面积增长最小择优）在 `_place_members` 已实现但被搜索层绕过 —— `select_prefix_plan` 显式枚举 (A×片型×B×rot) 后只按 max-H 排序，而嵌入贴合使 H 变矮、被近满幅判据反向惩罚 ⇒ 系统性选中浅搁朝向（5336 旧胜者 @34+g02@38 rot180：嵌入仅 7.9mm、楔形 472,870mm²；同组合 rot0 嵌入 42.2mm、楔形 432,764mm² 省 ~40,000mm²）。修复：① 搜索层 rot 不枚举，每组合 `_place_members(extra_pid=…)`（extra_rot=None）委派内建 FR-3（面积增长最小、平手取 0.0），选定朝向 `placed[-1][1]` 回收，组合间仍 max-H 近满幅（定案②不动）；② `_place_members` rot 候选循环改逐 rot 贴触校验容错（新助手 `_assert_touch_gaps`：单 rot 失败记错继续、全败才 raise 首个错，保持「任一 rot 可行即组合可行」语义）。测试：n_candidates 计数减半（24→12 / 2→1 / 8→4 / 4→2）+ tie_break 用例 rotation 重锁 180.0（L 形 FR-3 面积更小侧朝外）+ 新增 `test_select_prefix_plan_fr3_rot_nesting` 回归锁（凸台/凹腔夹具手算精确：rot0 嵌入 H=1320、补片底面 1180 < 凸台顶 1220 嵌入 40mm 胜出，旧口径 rot180 浅搁 H=1360 可证伪）。验证：pytest 全量 681 passed；模块冒烟新胜者 = 套装@35 + 顶部 g02@38 rot0 H=1952.4mm residual=27.6mm fill=81.0%（旧 79.5%）gaps 全 0.00 holes=0；5336 全场 60 组合（旧 120）一致全取嵌入朝向。UI 冒烟 smoke_prefix_extra.mjs 29/29（预览/求解/形态/守恒/导出全绿；顺带修 5h 判据免费副本误报 = 候补加「≥50% 宽度在基座列 x 区间内」—— 免费副本贴列右缘只蹭 bbox 边缘 ~1% 宽）。prefix_accept --seeds 0,1 --time 30：判据① on/off 劣化 0.163pt PASS（早前口径 0.556pt）、形态/确定性/导出全 PASS；判据② 双开 1.466pt FAIL 为既有现象非本次所致 —— 同会话 stash 背靠背对照：旧代码双开同样 FAIL 且更差（1.666pt），早前 0.297pt 属短预算跨会话漂移带（band_only 臂本场走强到 90.141%），双档漂移备案不改代码。文档：CLAUDE.md prefix 条目修复②段 + 数据流主线口径、确认清单 §9.5。
8. **[2026-09-03] prd-prefix-member-order US-001 — prefix 成员序切 paired 同型成对堆叠 + 测试重锁**：用户依生产参考件（同片型 180° 头尾互锁、刀缝级贴合）改判 FR-10 interleave 定稿，成员序从 [F@0,B@180,F@0,B@180] 改 [B@0,B@180,F@0,F@180]（自下而上，3 条缝从全异型变 2 同型自吻合 + 1 异型）。改动全收敛 prefix.py：① `PREFIX_ORDERS=('paired','grouped','interleave')`（paired 默认定案，interleave/grouped 备档逐字节不变）+ `_member_spec` paired 分支显式 BBFF（等价性陷阱：4 片级 BBFF ≡ grouped 翻转但带顶部补片不等价 —— 补片蹬 F@180 vs B@180，实测 fill 83.33% vs 82.86%，不可复用 grouped 分支）；② `build_prefix_plan`/`select_prefix_plan` 默认 order 切 paired（worker/预览/CLI/策略/极限五入口单点继承，协议零变化）；③ 死区透明化：新助手 `_dead_area_of`/`_dead_area_of_chunk`（焊接外环面积 − solid 面积），select info 与 build 冒烟 additive `dead_area_mm2`（只落工件/冒烟，不进 WS stage 白名单）——solve_worker `_build_prefix`/`_prefix_record` 顺落 prefix_runs 工件；④ prefix_accept 形态判据随改判改名 interleave→`member_order`（「前后交替」→「先序后段精确分段 [后,后,前,前]」，强度不放松）。测试重锁：新锁 `test_prefix_orders_locked_and_default`（三序序列 + inspect 默认值）；成员序/兜底 parity（+dead_area 0.0）/best_fit/preview WS/artifact 用例改 BBFF 序；FR-3 凸台凹腔夹具重手算 —— 凸台从后幅 g03(body 250) 移到前幅 g02(body 300)（基座总高同 1100+120，m4 F@180 凸台顶 1220/补片底 1180 嵌入 40mm，H=1320 数字不变）；竖排超高守卫块 gate 1980→1890（paired 基座矮 1628→1416.5，原门幅不再触发）+ 正向对照；interleave 封闭腔用例保留 + `holes_paired==1` 如实断言。验证：pytest 全量 682 passed（+1 新锁）；冒烟胜者 = 套装@38+g02@36 rot0 H=1956.2 ≤ gate−10、residual 23.8、fill 84.4%（+3.42pt）、gaps 全 0.00、holes 3、dead 47,236mm²（与 PRD 预测逐字一致）；paired 4 片 @38 bbox 1175×1513/fill 86.0%/2 腔 45,811mm²；确定性双跑 to_dict 全等 + --pin-demo PIN≡FREE Δ0.00pt。文档：prefix.py 模块 docstring、agent-file-map（模块节/常量/签名表/历史修订注记/两行数）、agent-component-map（UI 冒烟 5a 判据失配备注 US-002 收口）、CLAUDE.md prefix 条目改判段、确认清单 §6 决策① 改判指针。遗留 US-002：on/on' stash 背靠背 A/B ≤1.0pt 门线、UI 冒烟 smoke_prefix_extra.mjs 判据 5a 随新序更新、文档五处（确认清单 §10/README/memory）。
9. **[2026-09-03] prd-prefix-member-order US-002 — paired 成员序端到端 A/B 验收 + 文档闭环（定案合入）**：on/on' 同会话背靠背 A/B（`python -m materialsorting.web.prefix_accept --seeds 0,1 --time 30`，旧代码臂 = `git checkout ff5a43c -- prefix.py` 后同款复原，solve_worker 对旧 info 兼容 `.get('dead_area_mm2',0)`）：paired on 均值 **88.820%** vs interleave 88.223% = 均值劣化 **−0.597pt（paired 更优）≤1.0pt PASS 合入**；on/off −0.434pt 反超（seed0 −1.515 / seed1 +0.647）；双开 band+prefix 90.453% 反超 band_only 90.141%（−0.313pt PASS，interleave 同场 1.466pt FAIL 的既有短预算漂移被 paired 顺带修复）；off 臂两臂逐字一致（88.038/88.733% = off 路径零改动产品级证据）；判据②形态 2/2（码均 38、min_x −2.029/−1.947）、判据④导出 on/off 双臂 PS_ 零泄漏。**判据③帧轨迹 FAIL 归因定案（探针 out/tmp_probe/prefix_us002_det_probe{,2}.py 三连对照）**：paired@4w 背靠背 1221/1201 帧内容分歧（首分歧帧 idx≈989、best Δ0.018pt）+ arm1 非相邻对 Δ0.264pt；**paired@1w 798==798 逐帧全等**；interleave@4w 同场 1243==1243 全等 ⇒ paired 组合片更大（1175×1513）+ 3 封闭腔触发 spyrrow `num_workers=4` 并行放置并列候选竞速（merge 序随墙钟），构造层恒确定（选码一致 / chunk.to_dict 双跑全等 / 工件回放 / PIN≡FREE），best 带宽 0.02~0.27pt 与既有「跨会话漂移 ±0.2~5pt」同桶备案 —— 确定性断言只锁构造段，非 paired 代码缺陷不阻塞。UI 冒烟 smoke_prefix_extra.mjs **29/29**（判据三处随新序更新不放松强度）：5a 改「paired 同型成对序（后后前前）」（帧序 g03_38→g03_38→g02_38→g02_38 精确分段 + 5b rots 0/180/0/180）；4a `PS_` 零泄漏加前端 DOM 探针（五处：manifest/帧序列/final 无 placed 键/DOM polygon/导出）；4b 顶异码守恒改按矩阵量 `SIZE_QTY`（paired 胜者补片 g02@36 矩阵量 3 → placed 3；旧断言硬编码 2 只对 qty=2 码成立，首跑 28/29 唯一 FAIL 即此校准）；胜者 套装@38+顶部 g02@36 residual 23.761mm、min_x 0.07mm 锚定（pin skipped）、placed=105=Σdemand（front 2+back 2+extra 3 部分扣减不丢片）、工件 gaps [0,0,0,0]、导出 PLT 478,062B 无 PS_。前端放大层文案对齐定案（「4 片同码 paired 同型成对竖排贴靠」原 interleave 字样失配；PerTypeOverridesModal 3 行 + 2 处测试断言 + ws.ts holes 注释，协议零改动）。全量门：后端 pytest **682 passed**（首轮 1 fail test_cli_strategy::test_replay_regression_t3600 系环境 flake —— 单跑 1.16s 过 + 整套复跑绿，0xC0000005 同桶）、前端 vitest **815 passed**、npm run build 过（新 bundle hash）。文档五处：确认清单 §10（需求改判 + 四项决策 + on/on' A/B 定案表 + 确定性边界备案 + UI 冒烟守恒 + 判定 accept）、CLAUDE.md（prefix 条目定案段 + 数据流主线 paired 注记）、prefix.py 模块 docstring 定案注记、README prefix 行（成员序 + A/B 数字）、memory prefix-member-order-paired 新条目 + MEMORY.md 索引。
