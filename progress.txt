# Ralph Progress Log
Started: 2026-08-09












- [US-001] 后端会话注册表与生命周期（web 多会话隔离第一块，纯后端无 UI 改动）：① 新增 web/sessions.py（422 行，仅标准库+同包 runtime，禁 import server——AST 守卫 test_web_sessions.py 源级哨兵双查）：SessionRegistry（OrderedDict[sid→SessionState] 单例 registry，SessionState={sid, state(pieces 快照 dict 同构 _build_pieces_state), doc_id(US-002 占位), last_active, ws_open, strategy_busy(US-004 占位)}）——单一解析函数 resolve(sid, create)（缺省/空串→DEFAULT_SID='default' 豁免一切；非法→InvalidSidError 400 无 code 键；墓碑命中/惰性超时(当场逐出为墓碑)/未知合法 sid 读路径→SessionExpiredError 401；存活刷 last_active 幂等；create=True 未知→容量闸门 SessionLimitError 429）；容量 MS_SESSION_MAX=4（env 可调，仅计活跃 default 不占额，文案 f-string 插值缺省逐字=PRD）；TTL MS_SESSION_TTL_SEC=300 惰性检查+30s daemon 扫描线程 start_scanner/stop_scanner（Event 可中断，server.py 文件尾启动；ws_open>0 跳过=WS 钉住不误杀；扫描异常只 warn）+ scan_once 可单测；墓碑 {sid,ts} deque(maxlen=FIFO≤128)、存活 1h（purge 后 sid 视为全新可新建）；ws_acquire(resolve 语义+计数)/ws_release(no-op finally 友好)/touch(GIL-safe float 写)；SessionError 族自带 status/code/payload()（401/429 带 code additive 旧前端可忽略）；SID_RE=^[0-9A-Za-z]{1,128}$ 单一真相源在本模块（sessions 不能 import server，server.py 反向 re-export 为 _DOC_ID_RE 同一编译对象，US-010 commit 校验旧名零改动）；时钟可注入 _FakeClock（测试/冒烟共用）；`python -m materialsorting.web.sessions` 冒烟=打印配置+default 身份 is 判+私有 registry 模拟建会话/超限 429/惰性过期/墓碑阻挡/ws 钉住/1h 重建/非法 400 全生命周期 11/11 PASS；② server.py：顶部 from .sessions import（SID_RE as _DOC_ID_RE/SessionError/registry as session_registry，sessions→runtime 单向无环）+ 删本地 re.compile 定义与 import re + POST /api/session 路由（读 X-Session-Id Header，strip 空串归 None→default；resolve(create=True) + except SessionError → JSONResponse(e.payload(), e.status) 一行映射；200 {ok,sid}/429 session_limit/401 session_expired/400 {error:'sid 非法'}）+ 文件尾 session_registry.start_scanner()（daemon 兜底：已死会话不再发请求名额只能靠扫描回收）；③ tests/test_web_sessions.py 16 例全绿（TestClient API 契约×6 + registry 单元×7 + default 身份/豁免×2 + AST 守卫）：关键设计——autouse fixture 先 stop_scanner() 再 reset()（防真扫描线程在 monkeypatch 时钟窗口并发逐出 flaky）、TTL/墓碑全 _FakeClock 注入零墙钟等待（daemon 线程用例 0.02s 周期+5s deadline 轮询）、default 会话 state is runtime._PIECES_STATE 三方 is 判等锁死（resolve(None)/resolve('default')/server re-export 同对象，US-002 commit 双写依赖此不变量）、满员测试用 registry.max_sessions 动态循环（env 旋钮下不断言）、'default' 与 uuid4 hex 结构性不可碰撞记档；④ 质量门：pytest 全量 491 passed + 16 新增（唯一 1 失败 test_cli_strategy::test_replay_regression_t3600_gain_and_miss_max 为已知瞬态——单测/整文件复跑均绿、上一 session US-005 progress 已记档同款瞬态，与本次改动无关）；python -m materialsorting.web.sessions 11/11；import 链无环（sessions 独立 import 冒烟过）；分层依赖未反向（AST 守卫 + 手查 sessions 仅 import runtime）；⑤ 文档同步 6 处：.docs/technical/agent-api-reference.md（路由表 +POST /api/session 行 + 新专节：请求/响应表/生命周期语义 5 条/关键不变量 4 条）、agent-file-map.md（web 树 +sessions.py 行 + server.py 行更新、文件表 +sessions.py 422 行行）、web/AGENTS.md（文件分工表 server/sessions 行 + 新「多会话 US-001 关键约定」节 9 条：单一解析/default 豁免三件事/错误类型即 HTTP 语义/惰性+扫描兜底/墓碑/SID_RE 真相源/测试隔离/env 旋钮）、tests/AGENTS.md（+test_web_sessions.py 行）、CLAUDE.md（web 层 bullet 补 sessions.py 一句）；US-002~005（commit 双写/读路由+WS 接入/策略会话化/前端接入）留待后续 session
- [US-002] commit 双写与会话绑定（web 多会话第二块，纯后端 server.py 单文件改动 + 路由级测试）：① server.py 新增 `PIECES_INTERMEDIATE_NAME` 常量 + `_per_doc_intermediate(doc_id)` 路径单一真相源（`out/uploads/<doc_id>_pieces/pieces_intermediate.json`，与切片/manifest 同目录同生命周期，commit 整目录重写天然 idempotent；读路由 US-003 与磁盘清理 US-006 复用同一约定；依赖模块级 UPLOADS_DIR → 测试 monkeypatch `server_mod.UPLOADS_DIR` 经 `__globals__` 生效）；② `_commit_to_nesting_sync` 双写：**主写 per-doc**（会话快照数据源，多会话互不覆盖的磁盘锚点）→ **镜像写 paths.INTERMEDIATE**（单文档时代兼容面：无 sid 启动 reload / CLI 工具链 / 人工排查），同一 doc dict 两个文件顺序先 per-doc 后镜像；镜像整块 try/except 失败仅 warn（stderr `[server] intermediate 镜像写失败`）+ 响应 additive `mirror_error` 键回显，不影响 per-doc 落盘/会话注册/200；`.bak` 备份行为保留镜像侧（per-doc 目录整体重写无需 .bak）；③ commit 路由读 `X-Session-Id`：带 sid → 会话解析**先行**（`resolve(create=True)` fail-fast：过期 401/超限 429/非法 400 均不跑 CPU 管线不落盘；合法未注册 sid 可直接建会话——commit 数据自带，服务重启场景无需先 POST /api/session），成功后 `_build_pieces_state(str(_per_doc_intermediate(doc_id)))` 快照挂会话（`st.state` 赋值 + `st.doc_id = doc_id`）**不触碰 default 内存**；无 sid → default 会话更新（`st.doc_id` 同步 + 显式传 `paths.INTERMEDIATE` 调 `_reload_pieces_state`，default 会话 state 即 `runtime._PIECES_STATE` 同一 dict 自动跟随）；漂移语义定稿：default = 最后无 sid commit 者，镜像 = 最后 commit 者，允许漂移；**踩坑入档**：`_reload_pieces_state(intermediate_path=paths.INTERMEDIATE)` 缺省参数在 import 时绑定——monkeypatch `paths.INTERMEDIATE` 后裸调仍读启动期真实路径（路由级测试暴露的存量隐患），路由层必须显式传参（调用时取模块属性），`_build_pieces_state` 同款签名但 sid 路径恒传显式 per-doc 不受影响；④ tests/test_commit_sessions.py 9 例（TestClient 路由级 + 双档合成母版「不同码号×不同几何」构造不同 doc）：AC1 带 sid 双写逐字段一致 + 会话快照/doc_id 绑定 / AC2 双会话 A/B 先后 commit 互不串台（pid 集/gate_mm/面积互证）+ 两份 per-doc 并存 + 镜像=最后 commit 者 / AC3 带 sid 后 default 不刷新不 rebind + `resolve(None).state is _PIECES_STATE` is 锁死 + 镜像已漂移 / AC4 无 sid 回归 + default=最后无 sid commit 者语义 / AC5 过期 401 fail-fast 不落盘 + 非法 400 + 未注册合法 sid 建会话 / AC6 镜像父路径被文件占住 → warn-only（`mirror_error` 回显 + 200 + 会话不受影响）；基础设施 = autouse registry 隔离（test_web_sessions 套路）+ monkeypatch UPLOADS_DIR/INTERMEDIATE（test_commit_pipeline 套路）+ `_FakeClock` 过期注入；⑤ 质量门：pytest 全量 **501 passed**（492 存量零改动 + 9 新增，test_commit_pipeline/test_cli_pipeline/test_web_sessions/test_web_strategy 全绿零改动）、`from materialsorting.web.server import app` import 链无环、sessions 无 server import（分层未反向，AST 守卫存量）、`python -m materialsorting.web.sessions` 冒烟 11/11；⑥ 文档同步 5 处：agent-api-reference（路由表行 + commit 节：请求 Header 说明/响应 mirror_error/错误表 401·429·400 sid 行/副作用双写+备份镜像侧重排/快照注册分叉 + session 节生命周期 4·5 条补 commit 写路径 create=True 语义）、agent-file-map（web 树 server.py 行 US-002 段 + 文件表 server.py 425 行重写 + 数据流主线 per-doc 主写/镜像两行）、web/AGENTS.md（server.py 行 + US-010 管线/ reload 两条改写 + 新「多会话 US-002 关键约定」节 6 条：双写口径/路由分叉/漂移语义/缺省参数坑/per-doc 落盘布局/测试）、tests/AGENTS.md（+test_commit_sessions.py 行 + test_commit_pipeline 行补零改动回归注记）、CLAUDE.md（数据流主线 commit 句补双写）；US-003~005（读路由+WS 接入/策略会话化/前端接入）留待后续 session
- [US-003] 读路由与 WS 接入会话（web 多会话第三块，纯后端 routes_views/routes_ws 两文件 + 新测试）：① routes_views.py —— 四读端点（GET /api/ptypes、POST /api/band-preview、POST /api/prefix-preview、POST /export）统一接 `_resolve_session_state(request)` 新单一解析点（`X-Session-Id` Header strip 空串归 None → `session_registry.resolve(sid).state`，**读路径 create=False**：未知合法 sid（服务重启）同过期 401 不静默重建不占名额、非法 400、SessionError → `JSONResponse(e.payload(), e.status)` fail-fast 先于请求体解析/业务校验；缺省 → default 会话 state 即 `runtime._PIECES_STATE` 同一 dict 无 sid 行为逐字节不变）；`_get_pieces_state` import 从本模块删除（不再直读进程级全局）；GET `/` 补 `Cache-Control: no-cache` 响应头（FileResponse headers 参数；防部署新 bundle 后旧 index 引用已删 hash 资源、旧前端滞留 default 语义的迁移窗口，/static hash 资源自身长缓存不受影响）；export 过期 sid → 401 `{code:'session_expired'}` **JSON 响应非文件流**（解析先于一切导出逻辑）；② routes_ws.py —— `/ws/solve` 读 `?sid=` query（浏览器 WS 不能自定义 Header；缺省/空串 → default）：accept 后立即 `ws_acquire(sid)` 钉住（resolve 语义 + ws_open+=1，扫描线程对求解中会话不逐出），SessionError → `{'type':'error','message':e.error}` + 有 code 才加 `'code'` 键（session_expired/session_limit additive 旧前端忽略，US-005 前端据此弹阻断弹窗）→ 显式 close 不发 manifest；accept 阶段 state 快照改自**会话**（会话 state 从不原位 mutate——commit 整体 rebind st.state——「整连接一次快照」不变量保持）；`on_manifest`/`on_report` 回调顺手 `registry.touch(sid)` 刷活性（求解中客户端不发消息不误杀，GIL-safe float 写）；整函数体包 try/finally `ws_release(sid)`（任何退出路径 final/error 早退/stop/断开 ws_open 归零，acquire 失败路径不进 try 不多减）；**设计定稿：WS 超限分支不可自然触达**——读路径 create=False 下 resolve 的 not-create 检查先于容量检查，未知 sid 满员也落 session_expired；容量闸门（429）只在 create 路径（POST /api/session / commit）把关，US-005 前端第 5 窗口页面加载即弹「用户过多」由 HTTP 层满足，错误帧格式对两 code 通用（白盒单测锁定）；③ tests/test_read_routes_sessions.py 13 例：AC1 四读端点双会话 A/B（60×300 vs ×1.4 两档合成裁片，`st.state` 直接注入绕过 commit 管线）互不串台（ptypes representatives 逐字段 / band 成带 bbox+成员单片宽 60/84 / prefix 组合形态 / export placed 匹配会话 pid + 无 sid 同 pid → 400 均未匹配）+ 无 sid default 回归（_PIECES_STATE 原位注入+teardown 恢复）；AC2 export 过期 401 JSON + 四端点未注册 401/非法 400；AC3 GET / no-cache；AC4 WS 合法 sid 全流程 manifest（pids=会话裁片）→frames→final + ws_open 钉住 1/断开轮询归零 / 无 sid default / 过期 error 帧+close / 超限帧契约白盒（monkeypatch registry.ws_acquire 抛 SessionLimitError）/ 求解中 _FakeClock 超 TTL scan_once 不逐出·断开归零后才逐出；④ 质量门：pytest 全量 **514 passed**（501 存量零改动 + 13 新增，test_band_preview_api/test_prefix_preview_api/test_ws_stop/test_waist_band_ws/test_prefix_ws/test_commit_*/test_web_sessions/test_web_strategy 全绿）；`python -m materialsorting.web.sessions` 冒烟 11/11；import 链无环；ms-web 真实启动冒烟（curl：GET / cache-control:no-cache ✓ / POST /api/session 200 / 注册 sid ptypes 空快照隔离 ✓ / 未注册 sid 401 code ✓ / 非法 400 ✓ / 无 header default 真实 intermediate ✓，验后杀进程清端口；顺手清掉前 session 滞留 :8000 的旧 ms-web）；⑤ 文档同步 5 处：agent-api-reference.md（路由表 5 行 + /export·ptypes·band·prefix·WS ?sid= 专节 + session 节生命周期第 6 条读端点接入 + 错误帧契约）、agent-file-map.md（web 树 routes_views/routes_ws 行 + 文件表两行重写 396/431 行 + 数据流 web 句）、web/AGENTS.md（文件表两行 + US-020 节快照口径三条改写 + 新「多会话 US-003 关键约定」节 8 条）、tests/AGENTS.md（+test_read_routes_sessions.py 行）、CLAUDE.md（web bullet 补 routes_views/routes_ws 一句）；US-004~005（策略会话化/前端接入）留待后续 session
- [US-004] 策略长跑会话化（web 多会话第四块，纯后端 strategy.py 单文件 + 测试，本 session 从中断工作树续完：实现/测试已在位，补齐文档同步 + 修复 agent-file-map 被中断写坏的 strategy.py 表行（新行尾部残留整段旧文）+ 质量门全量复跑）：① strategy.py —— 每会话策略状态 `_STRATEGY_STATES[sid]`（`_states(sid, create=)` 单一取用点；default = 旧名 `_STRATEGY_STATE` 同一对象，29 存量测试直改零改动兼容）；四路由公共 `_route_sid`（X-Session-Id strip 空串 → default）+ `_session_gate`（非 default sid → `resolve(create=False)` 刷 last_active——**status 轮询即活性**，长跑会话不被扫描误杀；SessionError → JSONResponse(e.payload(), e.status) fail-fast 先于 409/422/一切校验）；marker 分文件 `.web_strategy_active[_<sid>].json`（default 旧名单一名保路径兼容；orphan 按本 sid marker 判定，用户过期后同 sid 回来仍能发现/清理遗留 run——状态槽与 marker 均不随会话逐出清理）；run_name/cfg/stderr 嵌 `_sid6`（sid[:6]，default 空串沿用旧命名）；**run_dir 认领改确定性前缀 glob `<run_name>_*`**（run_name 嵌本会话唯一 rand6，完全并发下只命中自己 spawn 的 run —— 修 PRD 指定必修 bug：旧「快照 diff + mtime 最新」多会话并发 starting 必互相认错；diff 保留为存量内存态回退路径且 `_other_session_claims` 排除他会 run_name 前缀与已认领 run_dir）；409 单飞收窄为每会话（本会话内存态非终态或本会话 marker 在；跨会话完全并发放开接受 CPU 争抢，不加全局闸门）；`_cleanup_stale_web_artifacts(sid)` 按前缀互斥（sid 会话只清 `web_<sid6>_*`/`strategy_cfg_<sid6>_*`/`web_strategy_err_<sid6>_*` 零删除竞争；default 清全部 web_* 但 `_other_sid6s`（在册 sid ∪ 磁盘 marker sid）保护并行会话前缀，sid 会话不清 legacy 由 US-006 TTL 兜底）；stop 只树杀本会话 pid、result 只读本会话 run_dir、warning 对本会话当前画布 doc_id（sid → 会话快照 / default → `_pieces_state()` 打桩点不变）；② tests/test_web_strategy.py 新增 6 例（`dual_env` fixture：strat_env + registry 隔离 + `_STRATEGY_STATES` 清零；`_register_session` 注快照落 master；`_spawn_capture_multi` 多轮 pid）：双会话 start 均 202+run_name sid6+status 前缀 glob 各认各（B 目录 mtime 更新不串台）+marker/槽各归各 / 清理范围（B start 不删 A run 目录·cfg·stderr；default 清 legacy 保护并行 + 手工 CLI run 不碰）/ A stop 只杀 A pid（B marker·starting 原封）+A result 读 A 产物+B running 409 / 同 sid 二次 409·跨会话·default 不 409+未知 401 带 code·非法 400 均不 spawn / orphan 本 sid marker（B idle·stop 400）/ `_FakeClock` 过期四路由 401（start 401 证闸门先于 409）+墓碑过龄同 sid 重建 → 内存态续读 starting·stop 可清理；③ 质量门：pytest 全量 **520 passed**（514 存量零改动 + 6 新增）；`from materialsorting.web.server import app` import 链无环 + 四路由在场；`python -m materialsorting.web.sessions` 冒烟 11/11；AST 守卫（strategy 禁 import cli）存量不变；strategy.py 940 行无 print 残留；④ 文档同步 5 处：agent-api-reference.md（路由表 4 行补多会话 + session 节第 7 条策略四路由接入 + 策略桥接节头部/多会话 7 bullet/四端点 401·409 语义/不变量 5·6 条）、agent-file-map.md（web 树 strategy.py 行 + 文件表 940 行重写 + **修复中断写坏的表行**）、web/AGENTS.md（strategy.py 行 + US-004 节 start/快照·前缀 glob/marker/stop·orphan/result 六 bullet 会话化改写 + 新「多会话 US-004 关键约定」节 8 条）、tests/AGENTS.md（test_web_strategy.py 行 35 例重写）、CLAUDE.md（运行方式 web 策略入口句补多会话一句）；US-005~007（前端接入/uploads TTL/端到端验收）留待后续 session
