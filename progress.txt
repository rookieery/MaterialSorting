# Ralph Progress Log
Started: 2026-08-09








- [US-014] A/B 验收与形态判据闭环（verdict accept，四判据全过）：① 判据②成对口径修正 —— pair_adjacency 改边距权威（shapely Polygon.distance ≤ PAIR_ADJ_EPS_MM=10mm，单一真相源 waist_band.PAIR_ADJ_EPS_MM；PRD 中心距公式为同朝向并排特例，FR-8 头尾翻转 180° 成对实测边距 1.5mm/中心距 713mm 会误判，中心距降级对照字段 worst_center_mm；g05_34 回归锁单测）；② 成对形态重试选解（根因：spyrrow 不按喂入序聚排，size-major 输入序假设被 3 seed 实测否定 2/3 散落 400mm+；同码全成对解出现率 ~50%/次）——build_band_plan 固定派生 seed 序列 crc32(band_seed|try{k}) 重试 ≤BAND_MAX_TRIES=6 取首个 _pairs_complete 解（预算均摊 max(1,total//6)），6 败走 _slot_fallback 确定性 bbox 槽位兜底（矩形 fill~100% 即最优、弧形由 fill≥45 下限拦截）；带内幅宽钳 min(gate,PLOT_SAFE_MAX_Y_MM)（组合片 1979/1980>1910 会主解不可置，test 断言改 ≤PLOT_SAFE_MAX_Y_MM）；曾试分码分块拼接（成对 100% 但均值密度 −3.75pt，宽 1818×1271 块主解难包）已否决；③ 判据③确定性尾帧规则 —— frame_series_equal（核心前缀 min(n)−1 帧逐帧相等 + 帧数差 ≤FRAME_TAIL_TOLERANCE=8；wall-clock 预算截断使截止快照漂 1~3 帧，实测 2005/2008 与 1167/1166 两形态均 final 相等、分歧仅在短列末帧；核心分歧或大面积漂移 FAIL，三态单测锁死）；④ A/B 终验（band_accept run_all，uploads 源/web 默认 g05 d=0.0/无 kill 口径三差异只与同配置 off 对照，main 120s×2×3seed + 重跑 + 导出）：密度 off 85.69/86.16/87.86 vs on 87.53/86.87/87.15，均值劣化 −0.614pt=提升（最差 seed2 +0.71 ≤1.0 线）PASS；形态成对率 100%×3（13/13 多副本、最差边距 0.2~0.76mm；off 臂对照 15.4/30.8/38.5% 证明非自然涌现）、span 1178~1208×1479~1515mm 全过；确定性 frames/final/artifact 三重全等（本次恰 2008/2008 逐帧相等）；导出 on/off 双臂 PNG/R12-DXF/PLT 全成 + WB_ 三重字节哨兵零命中；⑤ 浏览器终验 scripts/us014_verify.mjs（Playwright，修复 waitForFunction 二参陷阱：{timeout} 被当 arg 全部落默认 30s，final 120s 求解超时 → 改三参 undefined,{timeout}）11/11 PASS：上传→P0 表→超排 120s→成带 g05+ack 硬警告流→stage/final 截图 .docs/business/→终值对拍 UI 87.51% vs A/B on 臂 seed0 87.531%（差 0.021 ≤0.05 确定性交叉印证）→UI 导出三格式经真实 HTTP /export→前端 WS 1987 消息 WB_ 零命中→服务端日志「导出跳过：pid」零出现；⑥ 报告落盘 .docs/business/腰头成带_AB验收报告_US014.md + 3 截图（stage/final/export_on）；测试：test_band_accept 新增 10+1 项（边距口径/尾帧规则/冒烟 main 6s —— 2s 档 wall-clock 截断小实例帧列漂移 6s 收敛）全量 pytest 395 passed、前端 build+vitest 675 passed 零回归；文档同步 business-overview（no-go 行翻案为 US-014 accept）/README 索引/agent-file-map/web+nesting_engine+tests AGENTS/CLAUDE.md 求解支线
- [US-016] v2 构造性链构造（版师形态：开口朝左+最大码在最右，建议1 落地；建议2 带置布料最右实测结构性右置 83.98%=−2.10pt 用户拍板暂缓）：① 根因（用户报「成带后布局乱象」，out/tmp_probe/exp_*.py 三臂对照+带内 fill 天花板实验闭环）——v1 spyrrow StripPacking 带内目标是最短用布 X 非贴触 → 48.2% 对角阶梯坏带形（预算×30 仅+4.1pt 结构性卡死；US-014 成对重试 _pairs_complete 每 pid 单副本空真无牙口；fill 48.2% 勉强过 45% 下限）；紧带组合片（607×1326）进主解实测 87.51% > OFF 86.08% ⇒ 劣化是坏带形的代价非成带固有代价；版师指正真实构造 = N 条单副本异码链（每码第 k 副本一条链、链内片片贴触、缝隙只在链间，无需同码成对——此前「同码成对前提」已证伪）；② waist_band.py 第 2 步重写 —— chain_k 链拆分（链内 size 降序）→ _chain_nest 构造性滑移贴靠贪心（首片锚定，后续 rot{0,180}×5 y 对齐 × _slide_touch 右起 20mm 粗扫+40 次二分贴触取 union bbox 增长最小，纯 shapely 无 RNG 毫秒级）→ _flip_chain 整链点对称翻转（rot+180/tr 取负，合法布纹无镜像；开口方向与构造顺序绑定：升序+右滑=开口右，降序+翻转=开口左+最大码右）→ 形态自检（_chain_gap≤CHAIN_GAP_EPS_MM=1.0 贴触口径=版师验收非 bbox fill + _opening_side∈{left,flat} 质心偏移反侧判据旧链校准 + 最大码质心 x 最大程序断言）→ _stack_chains 多链堆叠（不翻链保开口）+ 带高守卫 ≤min(gate_nest,PLOT_SAFE_MAX_Y_MM)（链构造无 strip 约束显式校验）；删除 _pairs_complete/_slot_fallback/BAND_MAX_TRIES/BAND_NUM_WORKERS 与 spyrrow import；time_budget/fill_floor/PAIR_ADJ_EPS_MM/DEFAULT_BAND_TIME_BUDGET_S deprecated 保留（solve_worker/routes_band/band_accept import 兼容）；第 3-5 步管线（原始轮廓 union→焊接→erode(d_g)→归一化）与 BandChunk/expand_placements/band_seed_for 契约零改动；③ _slide_touch 平移记账 bug 修复（返回 dx=x_left−mb[0] 真实平移量：真实片归一化到原点 rot0 时误差 0.4mm<容差侥幸通过、rot180 候选被选则差 ~180mm——合成弧原始坐标 x≈1550 暴露）；④ 测试 —— test_waist_band.py 弧形 fixture _arc_piece（5336 g05 真实几何族 r=1550+40·码/thick68/half19°、凸侧 −X 与真实 rot0 质心偏移 −18~−22 朝向一致——凸侧 +X 时翻转后开口判 right）+ 单链形态/双链全链路 2 用例、fill_floor 阈值 103→130（v2 贴触构造矩形 fill≈116% 确定性）；test_waist_band_ws stop 用例换 7 码×8 副本弧片撑 ~2s 构造窗口（v2 毫秒级矩形停不进 band 窗口；弧片长宽比 6.9>6 硬警告须 ack:true——初版缺 ack 结构化 error 早退是断连根因非 stop 竞态）；⑤ 实测：5336 g05 6 码×1 fill 71.8%（537×1326）链内缝隙 0.00mm 225ms、P0 14 副本 79.5%（1153×1271）647ms、开口均左、同 seed 两跑 to_dict JSON 相等；端到端 A/B（生产 WS 全链路 30s seed0，out/tmp_probe/band_v4_e2e_ab.py）ON 88.35% vs OFF 86.08%（+2.27pt，v1 −3.81pt 翻转）、stage fill/bbox/elapsed 与直测逐字段一致（71.77%/537×1326/0.21s）、无 WB_ 泄漏、守恒 6/6、成员紧贴链形态（质心 x 递增、相邻 x 区间交集 108~120mm）；pytest band 四套件 60 项全绿（test_waist_band 14 + ws 25 + band_accept + gate）；文档同步 CLAUDE.md（waist_band bullet+数据流主线）/nesting_engine+web+tests 三处 AGENTS.md；COMPOSITE_ORIENTATIONS 仍 (0,180)（FR-8 版师确认整块头尾调换合法——主解选 180° 时最终布局带开口朝右属已知边界，与建议2 同属后续位置控制议题）
- [US-015] v1.1 填料混带（任意 g 码多选；版师确认无白名单，仅数量上限 + 不可选主 g 码）：① 协议/校验 —— routes_ws._parse_band 并入 fillers 护栏（数组项 ^g\d+$ / ≠主码 / 去重后 ≤_BAND_MAX_FILLERS=3 / 各自存在母版 + quantities>0；填料不做硬警告 ack——填料本就是小片；重复静默去重放行；返回 dict 恒含 fillers 键缺省 [] 旧 payload 不变）；② 引擎 —— waist_band build_band_plan 加 fillers/filler_ds 参数（逐 filler _resolve_d_tol 在 web 层裁定传入）：链堆叠后 _fill_gaps 贪心塞隙（每副本 y 候选 = 已排 bbox 棱边 ∪ 各已放几何 bbox 棱边 × rot{0,180} × 双滑向 from_left/right，x 右起粗扫 + FILLER_BISECT_ITERS=24 二分，代价 = _bbox_area bbox 并集解析式免 unary_union，prep() 预处理几何 bbox 预筛降 28s→4.9s）；混带带内碰撞多边形在成员基准 d 之上补腐蚀到 BAND_INNER_D_MM=2.0（版师口径 2~4mm 保守端：肋间切口端部开到小件可入宽度；关键语义 erode(e) 贴触 ⇒ 原轮廓允许交叠 ≤2e 非分离——弧形夹具实测混带 83.24→87.96 且 bbox 收窄；组合片外轮廓 erode 仍 d_g、纯腰路径逐字节不变、erode 失败回退原轮廓安全方向）；BandChunk.fillers 进 to_dict 工件回放；③ web 编排 —— solve_worker._build_band 透传 fillers/filler_ds，主实例 exclude_labels={label,*fillers}（同一扣减路径，manifest on/off 一致性不变）；routes_band 预演 body 可带 fillers（fill 分子 = 腰+填料，含主码 422 不设 hard_warning）；④ 前端 —— types/ws.ts BandConfig.fillers?（可选键清洗后非空才附）、lib/params.ts BAND_MAX_FILLERS=3 镜像 + FormState.band_fillers + collectBand 清洗（trim/^g\d+$/过滤/去重/剔除主码）、PerTypeOverridesModal 填料多选 chip 行（候选 = orderedLabels−主码同源缩略图 48×48、.on+aria-pressed、满 3 未选置灰已选可反选、切主码自动剔除同名、fillersKey join(',') 做 effect 依赖防数组引用重跑、BandFormValue 扩 fillers confirm 写回）、ControlPanel bandFillerZeroLabel 闸门（填料码数量全 0 置灰+文案，与 bandZeroQty 同构）；⑤ A/B 终验（scripts/us015_band_ab.py 复用 band_accept 探针套件，g05+g07、3 seed×120s，报告 out/config_runs/_probes/us015_ab_report.json）：带 fill 79.5%→86.52%（bbox 1153×1271→1097×1333 收窄 55mm，三 seed stage_bbox 逐字节一致=无 RNG）≥62.4 线；全局 off 85.704/86.164/87.856 vs on 87.766/86.628/86.933 均值劣化 −0.534pt（两 seed 提升）≤1.0pt；守恒 g05=14+g07=7、无 WB_ 泄漏、verdict=accept；填料选择依据 g06（711×30 长条）单混 fill 44.4% 触发 BandQualityError 灾难守卫、g07 实测最优；⑥ 浏览器验证 scripts/us015_verify.mjs 17/17 PASS（Playwright 系统 Chrome：上传 5336→P0 表→成带 g05+ack 纯腰预演 79.5%→填 g07 多选 86.5%==A/B on 臂 86.52%→预演 body {"enabled":true,"label":"g05","ack":true,"fillers":["g07"]}→满 3 置灰/反选恢复→求解终值 87.77%==A/B 87.766% 确定性对拍→末帧守恒+2171 消息 WB_ 零命中；截图 .docs/business/us015_filler_multiselect.png/us015_mixed_band_stage.png/us015_mixed_band_final_seed0.png）；⑦ 测试：后端 test_waist_band 20 项（+6：混带守恒/to_dict.fillers/包络 rot0,180/交叠深度口径 buffer(-half).disjoint ≤2·BAND_INNER_D_MM+0.5 与填料↔腰 ≤BAND_INNER_D_MM+d_f+0.5/弧形混带收益 mixed>pure+bbox 不膨胀+填料在带/同 seed 确定性/filler==主码与 0 副本 ValueError 兜底）+ test_waist_band_ws 38 项（+8：fillers 校验八参数矩阵（4 项凑满触发 cap 先于存在性）/去重放行/混带 e2e stage→manifest→final g05+g06 守恒+工件 fillers/混带 on-off manifest 一致/预演带 fillers 200 与含主码 422）全量 pytest 416 passed 零回归；前端 PerTypeOverridesModal.test +6（多选/预演 body/满 3 置灰反选恢复/切主码剔除/confirm 回写取消丢弃/props 初值过滤）+ params.test +3 + 既有 literal 补 fillers:[]，vitest 684 passed + npm run build 通过；⑧ 文档同步 CLAUDE.md/nesting_engine+web+tests 三 AGENTS/agent-file-map（waist_band 节 v2+US-015 重写+build_band_plan 签名）/agent-api-reference（band.fillers 协议+预演 body+solve_worker exclude_labels）/agent-component-map（US-015 节+树）/腰头成带_落地方案 ✅ 注/business-overview/README/新报告 .docs/business/腰头成带_AB验收报告_US015.md
