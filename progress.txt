# Ralph Progress Log
Started: 2026-08-09












18. **[2026-09-05] prd-edit-polish US-004 — 端到端冒烟与文档闭环**：新建 `materialSorting-web/scripts/smoke_edit_polish.mjs`（Playwright Edge 通道，模板对齐 smoke_edit_layout/smoke_prefix_extra：msedge 头选 + chrome 兜底、rawFetch/WS framereceived 抓帧、dismissTour）+ 文档同步五处。① 冒烟 24 检查七相位：S0 sid 落库 → S1 上传 5336 母版 → commit → per_type 全 g 码 d=3/tol=30（制造工艺余量重合与旋转，polish 有实事可做）→ 3 码 32/33/34 × Σdemand=30 片 20s 短求解出 final（density 0.9004）→ S2 编辑弹窗微调（请求 200 placed=30 gate=1750 无 exclude 键 + 报告四段 before/after/moves/residual + 四守恒不等式：overlap_pairs 42→38 严格下降、rotΣ 210°→190°、width 1762.039→1761.778mm 不增、density 89.707→89.72% 不降 −1e-6 容差 + moves=4 residual=61 + 对比卡六指标前→后渲染含口径脚注与撤销按钮）→ S3 撤销（画布 points 逐片回微调前 + 卡清空）→ S4 再微调确定性双跑（placed 深相等 + report 除 elapsed_sec 全等）→ S5 保存 → 导出 PLT（plt-clean 128057B PU≥100）+ DXF（542749B 含 POLYLINE 无 LWPOLYLINE）placed 双双=30 且与微调响应 placed 深相等（守恒）→ S6 band on g05 重解（30 片含 g05×3）→ 弹窗微调请求 exclude.labels=['g05'] + 带形态区域 g05 毛版 points 前后逐一不变 + report.excluded=[6,7,8] 恰覆盖 g05 实例。报告落 `out/smoke_edit_polish/report.json`（24/24 PASS exit 0）+ 截图 4 张。② 实现三坑记档：Playwright Response 无 `.postData()`（须 `r.request().postData()`）；/export 附件 body 被页内 fetch→blob 消费后网络层拿不到（exportOnce 页内 fetch 包装 `res.clone().arrayBuffer()` 捕获，__exportOrigFetch 守卫 + 用后还原）；done 相位 SolveControls 只有 `#restart`（`page.click('#restart, #start')` 兼容两态）；连续冒烟 edit-hold 钉住会话触发 429（每轮跑前重启 ms-web 清会话表）。③ 质量门 = pytest 全量 **728 passed** + vitest 全量 **962 passed**（零新增用例——纯冒烟脚本无组件改动）+ `npm run build` 过（302.70KB/gzip 98.86KB）+ `python -m materialsorting.nesting_engine.polish` 冒烟 exit 0。④ 文档同步五处 + 加一：agent-api-reference.md（/api/edit-polish 专节顶部**红字口径差异注记**：编辑画布=erode 后轮廓（碰撞可行性口径，数值恒≤物理口径）、polish 报告=物理毛版轮廓（与 /export 导出同源）；同一布局两套数值并存是设计非 bug，前端对比卡脚注同文案锚点；本期不切换画布口径 = PRD 非目标 + 节首补冒烟脚本指针）+ agent-file-map.md（web 树补 edit_hold.py 缺行——US-003 时漏档）+ agent-component-map.md（EditLayoutModal 微调区 dated 注记 + 文末 US-004 落地节：smoke 文件表/四守恒不变量/双格式导出/band exclude 锁/验证记录 + 头部覆盖行补 edit-polish）+ materialSorting-server/src/materialsorting/web/AGENTS.md（启动校验块补 /api/edit-polish curl 例含口径差异注记指针）+ CLAUDE.md（polish.py 行尾补冒烟脚本与红字注记指针）+ materialSorting-web/AGENTS.md（文末 US-004 端到端冒烟收官节：24 检查清单摘要 + 两实现坑）。验收后清理：ms-web 已杀（port 8000 无 LISTENING）、Playwright browser.close()（残余 chrome 核实为用户默认会话不动）。
19. **[2026-09-05] prd-edit-polish US-005 — 压缩回收档（compact pass，可选低优先级）**：引擎 pass ④ + 端点旗标补语义 + 前端 checkbox + 冒烟 S7，全链 additive（缺省 false = US-001~004 行为逐字节不变）。① 引擎 `nesting_engine/polish.py`：pass ④ 压缩回收（compact=True 才跑）—— 按 (minX,下标) 升序**级联**处理（左片先贴墙、右片贴新位），每片 `_slide_west_touch`（新助手函数：`COMPACT_SCAN_STEP_MM`=20 粗扫 + 40 轮二分到首个碰撞界 + `SEP_NUDGE_MM` 1nm 回退自由侧 = 贴触零交；起点已碰撞→0 / 全程自由→t_wall 贴布头 x=0；滑程墙 = 自身 minX）逐 move 走同款五道守卫（kind='compact'，`_move_ok` 复用：障碍集 = 滑移路径 x 可达 + y 带重叠的其余片，exclude 片永不动仍作障碍）+ **pass 级守卫**：收尾全图 maxX 严格变小（`COMPACT_GAIN_EPS_MM`=1e-6）才接受，否则整体回滚（items/geoms/bounds/moves 四快照恢复 = 无空隙可收时逐字节不变，AC ② 实现根基）；冒烟夹具 ⑨（三 100×160 @ x=0/130/260 gate=160 → width 360→300 + moves 全 kind=compact + 两两交集面积精确 0.0）⑩（98/196 紧密链 → compact=true 与 false 输出逐元素相同（elapsed 剔除口径）），八项自检 → 十项。② 端点 `web/server.py`：docstring 补 compact 语义（US-002 已预埋接线 `bool(payload.get('compact') or False)` 透传引擎，本故事零路由代码改动）。③ 前端：`lib/editPolish.ts` `PolishPayload.compact?: boolean` + `buildPolishPayload(working, run, compact=false)` 第三参（false 省略键 / true 才发 compact:true）；EditLayoutModal `polishCompact` state（默认 false）受控下发 EditCanvas `EditPolishUi.compact/onCompactChange`；对比卡内 density 行与 foot 之间 checkbox「回收空隙缩短料长（下次微调生效）」（testid edit-polish-compact，title 悬浮说明滑贴语义；`.edit-polish-opt` pointer-events:auto = 卡内第二个交互点（撤销按钮外），勾选态跨多次微调保持、撤销/关窗随卡清零）；style.css `.edit-polish-opt`（inline-flex gap 5 / input accent #3f8f5f / 11px #aab）。④ 测试：test_polish.py +6 例 25 项（AC① 三片 30mm 缝 → width ≤ before−29 + 落点 x≈100/200 + 几何级零重合 + 确定性双跑；AC② 紧密夹具逐元素相同（原「预留键位恒 no-op」占位测试按 AC 语义重写——旧夹具两片自由空间真有可收空隙，compact 落地后滑到墙 maxX 480→200 故必须换紧密夹具）；exclude 片挡滑移路径 → 后片贴它非贴墙；无包络收益整体回滚；纠缠片跳过；`_slide_west_touch` 三单元）+ test_web_edit_polish.py +1 例 15 例（`_PIECES_STATE` 换三片带缝夹具 try/finally 还原 → 200 + width ≤ before−29 + moves 全 compact + pid 序守恒）+ vitest +4（editPolish.test compact 省略/true 两例 + EditLayoutModal.polish.test checkbox 默认不勾无键在卡内 / 勾选→compact:true 受控保持撤销随卡清空，13+13 项）。⑤ 冒烟 S7 相位 5 检查（S7a 默认不勾 / S7b 受控切换 / S7c compact:true 载荷+placed 30 守恒+报告四段 / S7d width ≤ 非 compact 档（S6 结果）+1e-6 / S7e 本轮守恒不等式 width 不增·密度不降·重合对不增）+ s7_compact.png + report.json compact 段 → **29/29 PASS**（该 band 布局本无空隙可收：compact 1748.918 = 非 compact 1748.918 相等，AC 不等式成立）。⑥ 验证：`python -m materialsorting.nesting_engine.polish` 十项自检 exit 0；pytest 全量 **734 passed**（728+6）—— **全量必须 `PYTHONDONTWRITEBYTECODE=1 python -B -m pytest` 跑**：`test_cli_strategy.py::test_replay_regression_t3600_gain_and_miss_max` 在普通全量跑下必挂（`_at` 的 `for pt, d in curve:` 处随机炸 int unpack/空表解包/'float' context manager），单跑绿 / 纯 python 永绿 / `PYTHONHASHSEED=0` 确定性挂 / `--assert=plain` 或 `-p no:cacheprovider` 绿 —— pytest 9.1.1 断言重写 .pyc 缓存 + hash-seed 环境级 flake，**git stash 干净基树同样复现 = 预存与 US-005 无关**，-B 规避已入档；vitest 全量 **966 passed**（962+4）；`npm run build` 过（303.04KB/gzip 99.08KB）；浏览器 DOM bbox 几何目检 PASS（checkbox 在卡内 density 行下 foot 上、与撤销按钮/foot/指南卡零交集、pointer-events:auto、点击受控切换 —— Read 工具读截图仅回 CDN 链接不可直读，Playwright bbox 断言替代目检）；验收后 browser.close() + ms-web 已杀（port 8000 free）、throwaway 脚本已删。⑦ 文档：agent-api-reference.md（compact 键位节改落地语义 + moves kind 补 compact + 冒烟 29 检查 + 测试 15 例）+ agent-file-map.md（polish.py 行/`_slide_west_touch` 行/两常量/树行 + server.py 两行 compact 措辞）+ agent-component-map.md（文末 US-005 落地节：文件表/三不变量（additive 缺省零变化/pass 级严格变小/checkbox 生命周期=卡）/验证记录含 -B flake 备档）+ nesting_engine/web/tests 三处 AGENTS.md + materialSorting-web/AGENTS.md（lib/edit 树行/关键约定第 13 项 compact 档/US-004 冒烟节 29+S7/style.css 行）+ CLAUDE.md（polish.py 行 compact 落地 + server.py 行 + 冒烟 29）。prd 收官：US-004 归档 prd_archive.json、US-005 passes:true（prd-edit-polish 五故事全闭环）。
20. **[2026-09-05] prd-edit-keyboard-transform US-001 — mirror 数据模型与前端纯函数层**：镜像（det=−1 反射，{id,rotation,translation} 表达不了）进数据模型，additive 可选键 `PlacedItem.mirror?: boolean`（types/piece.ts；语义 = 局部 x 翻转 `world=R(rot)·diag(−1,1)·p+t`，展开 `x'=−c·x−s·y+tx`/`y'=−s·x+c·y+ty`，实现统一「旋转前一行式取负 `x = mirror ? -p[0] : p[0]`」）。①纯函数层三处贯穿：`lib/geometry.ts pointsStr(poly,rot,tr,mirror=false)` 第 4 参（渲染出口）+ `lib/editGeometry.ts transformPolygon` 同款（全精度计算出口，与 pointsStr mirror 分支同公式对拍锁死）+ `lib/overlap.ts` EditPiece 扩**必填** `mirror: boolean`（内部计算池用明确 boolean——omit-when-false 只约束 wire/store 键集）/ `precomputeEditPieces(FromItems)` 读 `it.mirror === true` 初始化镜像几何展开 / `applyEditPlacement(ep,rot,tr,mirror=false)` 增量覆写（**覆写语义**：镜像片走增量必须显式传标志，缺省覆回 false —— US-003 拖动帧与 US-005 键盘帧的接线点）。②硬约束两条入档：**omit-when-false**（PlacedItem 序列化只在 true 时带键——store 写回/微调载荷/导出 placed 一律「有镜像才带键」，恒发 mirror:false 会红掉 EditLayoutModal.polish 精确锁键集用例；solver 原生帧永无此键）；**缺省 false 逐字节一致**（零回归红线）。③测试 +15 项（geometry +4：手算方块 x 取负（mirror 只翻 x 不翻 y——首版笔误连 y 也取负当场红掉即改）/rot=90 公式手算/x-预取负逐字节等价恒等式互证 6 组/mirror=false 显式=缺省=legacy 三方对拍；editGeometry +5：手算方块/与 pointsStr mirror 逐点对拍 7 组/x-预取负全精度逐点相等/全精度+不改入参/mirror=false 显式=缺省；overlap +6：FromItems 读键+镜像几何手算/mirror+rot90 手算（同 tr 落点与无镜像不同 y 带——质心锚定补偿根因注记）/缺省与 mirror:false 同构/frame 版同读键/applyEditPlacement 覆写三态/镜像片照常参与 computeOverlap）；两处测试自身坑备档：独立两次 precompute 的 key 同为 0 会被 computeOverlap 按 key 跳「自身」（须 {...e, key:1} 改 key）；px=0 时 -px=-0 与 +0 被 Object.is 区分（既有「+0 归一化」同款处理）。④验证：vitest 全量 **981 passed**（966+15，64 文件；既有用例零改动——三测试文件 diff 全为纯新增）+ `npm run build`（tsc+vite）过（302.72KB/gzip 99.02KB）、static/ 已重建；零组件/零后端改动，浏览器验证与冒烟归 US-003/US-005/US-007（本 story 无 UI 面，缺省路径逐字节不变）。⑤文档同步：agent-component-map.md 文末「edit-keyboard US-001 落地」节（文件表/四不变量：公式三方锁步·omit-when-false·EditPiece.mirror 必填·x-预取负恒等式/验证记录）+ materialSorting-web/AGENTS.md types/ 与 lib/ 两树行 dated fragment + 文末「edit-keyboard US-001 关键约定」节（七条口径：mirror 语义与垂直镜像复合律预告/omit-when-false/=== true 判定/缺省逐字节/EditPiece 覆写语义/三方锁步/验证门）；CLAUDE.md 无前端 lib 引用行零改动；prd US-001 passes:true。
21. **[2026-09-05] prd-edit-keyboard-transform US-002 — editStore mirror 贯穿与 resetItem**：US-001 立数据模型后的 store 层接线。①五处显式字段 map 全补 mirror 透传（此前逐字段重建对象，新增字段会被静默丢弃）：`deepCopyItems`/`applyToRun`（save·reset 落盘）/`setWorkingItem`（cur.mirror 透传，rot/tr 增量更新不掉镜像）三处序列化一律 `...(it.mirror === true ? { mirror: true } : {})` **omit-when-false**（applyToRun 对象整体重建 ⇒ 关镜像保存后 lastFrame 旧键自然消失）；`itemsEqual` 归一布尔比较 `(a.mirror===true)!==(b.mirror===true)`（undefined/false 同义，布尔差异 → dirty，open 的 savedDirty 重算自动继承）；`computeLayoutStats` 传 `it.mirror === true` 第 4 参（镜像改变包络 maxX，料长/密度对镜像片正确）。②新增 `resetItem(index): boolean` 片级重置：守卫链 = baseline 在案 + 下标双侧（working/baseline）在界 + id 对齐（同 pid 多副本**绝不按 pid 寻址**，下标错位防御）→ 命中 = working[index] ← 基线项深拷贝（复用 deepCopyItems 单元素）、**只写 working 草稿不写 run**（保存才落盘）、不 bumpRenderTick（working 引用变化即重渲染与 setWorkingItem 同款）；miss = false 且草稿原样。重置语义定案：恢复算法基线（本会话首次 open 的算法原始布局，与全局 reset 同锚点 ⇒ 会抹掉该片已保存编辑，定案接受）。③单测 +13（24→35）：mirror 贯穿 8（open 快照/save 写回 mirror===true+无镜像项无键/关镜像保存清键/setWorkingItem·replaceWorking 透传/reset 全局落盘/itemsEqual 布尔差异/computeLayoutStats 三角形不对称夹具 maxX 800→500 手算）+ resetItem 5（正常恢复只写 working+深拷贝解耦/镜像随基线清零与回填双向/越界三态/id 错位（swapped 草稿防御）/baseline 缺席）。④验证：vitest 全量 **994 passed**（981+13，64 文件，既有用例零改动）+ `npm run build`（tsc+vite）过（303.08KB/gzip 99.11KB）、static/ 已重建；零组件/零后端改动，浏览器验证与冒烟归 US-003/US-005/US-007（store 无 UI 面，US-001 同款口径）。⑤文档同步：materialSorting-web/AGENTS.md（store 树行 dated fragment + 文末「edit-keyboard US-002 关键约定」节：omit-when-false/setWorkingItem 透传非覆写（US-005 翻转 mirror 写入入口本故事未建）/itemsEqual 归一布尔/computeLayoutStats 传参/resetItem 只写 working+三守卫）+ .docs/technical/agent-component-map.md（文末 US-002 落地节：文件表/四不变量/验证记录）；agent-file-map.md 后端专属不适用（US-001 同款）。prd US-002 passes:true。
