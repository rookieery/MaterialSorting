# Ralph Progress Log
Started: 2026-08-09

## 2026-08-20 — US-004 web 后端策略桥接四路由（strategy PRD）
- `web/server.py`：`_commit_to_nesting_sync` doc dict 首键增 `'doc_id': doc_id`（母版原件定位键 uploads/<doc_id>.dxf）；旧 intermediate 无此键 → /api/strategy/start 422「母版信息缺少 doc_id，请重新上传并 commit」；commit 响应体不变。文件尾 `from .strategy import register_strategy_routes` + `register_strategy_routes(app)`（尾置防循环导入）。
- `web/solver.py`：片级管线抽纯函数 `build_pid_meta(pieces, *, sizes=None, per_type=None, quantities=None, params=None) -> tuple[dict, float, int]`（demand 判定 quantities[label][str(size)] 0 跳过 → per_type[label][sizeKey] 命中覆盖 d/tol → erode/clean → pid_meta 构造 + total_area 累计；**不 import sparrow**，/api/strategy/result 建 manifest 复用）；新增共享 `_resolve_d_tol(label, pdef, per_type)` 单一真相源（pid_meta 蚀边与 Item orientations 同一 tol，min(d, MAX_OVERLAP_MM)/min(tol, MAX_ROTATION_TOL_DEG)）；build_instance 改调它后按 sizes 过滤循环建 Item（demand=0/退化片 continue），输出与提取前逐字段一致（对拍护栏）。
- 新增 `web/strategy.py`（689 行）`register_strategy_routes(app)` 四路由：**POST /api/strategy/start**（校验序 409 单例「策略 run 已在执行」—— 内存态非终态或孤儿 marker 在，先于一切 → 422 状态「请先上传母版并 commit」—— pieces 空/doc 缺 doc_id/母版 DXF 文件不在 → 400 参数 —— mode∈{se,race}/minutes∈{10,20,30,60}/seed 整数；7 键 config（master_dxf 绝对路径/sizes/quantities/per_type/gate_mm 请求值回退 state/seed/time）可选键 truthy 才落，写 out/uploads/strategy_cfg_<stamp>.json；**快照先于 spawn** 防竞态 → `[sys.executable, '-m', 'materialsorting.cli.run_config', cfg, '--name', f'web_{mode}_{rand6}', '--strategy', mode, '--time', str(minutes*60), '--quiet']`（stdout=DEVNULL、stderr=临时文件、POSIX start_new_session=True 供 killpg）→ marker out/config_runs/.web_strategy_active.json {pid,run_dir,doc_id,mode,started_at} → 202；_STRATEGY_STATE 存 pieces_snapshot/sizes/per_type/quantities/seed/gate_mm start 时口径）。**GET /api/strategy/status**（无状态惰性轮询：内存空+marker 在 → orphan 分支 pid 探活 + ISO elapsed；poll proc → run_dir 快照差分发现（写回 marker+st）→ starting→running→done/stopped/error 状态机 + **state 回写 st**（终态后可再 start）；来源白名单 strategy.json/result.json/best_frame_s*.json/kill_decisions.jsonl，**curve_s{seed}.json 不读**（中途非法 JSON）；run_dir 静默 >30s 且进程死 → error + stderr 尾部 2000 字符；字段 {state,mode,total_budget_sec,elapsed_sec,run_dir,plan{planned_seeds+race.gate_seconds|se.k_screens/screen_s/ext_s},incumbent{density,width_mm,seed,elapsed}（无 placed_items 控载荷）,current（最新 mtime best_frame 的 seed+density+ext 位）,per_seed,events（R5 门杀/extension/seed_done 尾部 20 条）,error,exit_code}，缺文件降级 null；终态清 marker 保留内存态）。**POST /api/strategy/stop**（树杀 Windows `taskkill /PID <pid> /T /F`、POSIX `os.killpg` 防求解孙进程白烧 CPU；孤儿清理分支；内存空 → 400）。**GET /api/strategy/result**（done/stopped 才 200，running → 409/idle → 404；best = result.json incumbent（完整 placed_items）缺失回落 best_frame_s*.json 密度最大（stopped 中断交付物）；density_sparrow 从 best_frame 副车补；manifest 用 start 快照走 build_pid_meta 与 /ws/solve manifest.pieces 同形（id/size/color/area_mm2/polygon 五层/label/demand）+ gate_mm/gate_nest_mm/total_area_mm2/n_eroded；summary {per_seed, mode, race{gate_seconds,kept_seeds,gated_seeds}|se{k_screens,screen_s,ext_s,champion}}；marker.doc_id≠当前 state doc_id → 附 warning「母版已变更，应用结果可能与当前画布不一致」）。`_pieces_state()` 惰性 `from .server import _get_pieces_state` 防循环导入（server 只文件尾 import strategy）。
- 测试 `tests/test_web_strategy.py`（**22 例**）：commit 落 doc_id、409 单例 ×2（内存态/marker 在）、422 ×3（空 pieces/缺 doc_id/母版文件不在）、400 ×3（mode/minutes/seed）、happy path（config 7 键逐键对拍 + master_dxf 绝对路径 + spawn cmd 参数序 + marker 5 键 + start 快照存参）、run_dir 发现 + plan 解析、发现写回 marker（完整 start 流）、30s 超时 error + stderr 尾部、fixture 各字段解析、缺文件降级 null、done 清 marker 留内存态、stop taskkill 参数 + 清理、orphan status + stop、result manifest 与 build_pid_meta 对拍 + build_instance 三元组一致性（提取回归护栏）、doc_id 漂移 warning、stopped best_frame 回退、idle/running 拒绝、**AST 守卫**（web/strategy.py 全模块不得 import ..cli.*，镜像 test_cli_portfolio.py 写法）、TestClient 四路由在场。
- 真实端到端验证（非 mock）：`MS_OUT_DIR` 指临时目录（子进程同 env 继承 → 父子 run_dir 一致）start 202 → 真子进程 spawn → status 轮询发现 run_dir（web_race_dae48b_*）→ 快速失败路径 error 态 + stderr 中文尾巴（坏 master_dxf rc=1「配置错误」）→ marker 清理闭环；`python -m materialsorting.web.strategy` 导入冒烟零副作用。
- 验证：pytest 全量 **337 passed**（315→337，0 failed）；无 --strategy 既有链零回归；分层零反向（strategy.py 模块级仅标准库+fastapi+.paths，cli 交互全走子进程边界）；TestClient 探测四路由 200/4xx 正常。文档同步：`web/AGENTS.md`（文件表三行 + US-004 策略桥接关键约定段）、`tests/AGENTS.md`（test_web_strategy 行）、`.docs/technical/agent-api-reference.md`（9 端点 + 路由表 + 策略桥接契约节 + solver 表 build_pid_meta 行 + intermediate doc_id 422 注记）、`.docs/technical/agent-file-map.md`（web 树 strategy.py 行 + 详表 server/solver 行更新 + strategy.py 新行）。本 story 纯后端（无 SVG/前端改动），浏览器验证不适用（TestClient 探测替代）。
