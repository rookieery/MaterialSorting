# Ralph Progress Log
Started: 2026-08-09

























44. **[2026-09-19] prd-warm-start-phase1 US-001 — warmstart 引擎模块（校验+序列化+能力探测）**：warm-start 一期首个故事（se 真顺延的载荷构造地基；契约 = `.docs/business/sparrow-ms侧需求规格_warm-start暴露_2026-09.md` §3.2 跨项目单一真相源）。①新建 `materialsorting/nesting_engine/warmstart.py`（约 340 行，仅 import 标准库 importlib.metadata/json/math/re/sys/collections，无 spyrrow/shapely 运行时依赖）：`build_initial_solution(placed, demand_map, strip_width_mm) → dict` —— 全链路统一 placed 条目 `{id, rotation(度), translation:[x,y]}`（solver 帧 `_emit` / `best_frame_s{seed}.json` 边车 placed_items / 编辑器 editStore 同构）→ `{"strip_width": float, "placed_items":[{id, rotation, translation}]}`（字符串 pid jagua u64 映射不外漏；纯 JSON dict = Windows spawn pickle 安全，`json.dumps` 留给 US-002 worker 最终消费点 FR-7；placed_items **保持输入顺序** = restore 确定性前提；条目多余键白名单丢弃输出恒三键，`mirror: False` 是 omit-when-false 显式形态放行）。②校验矩阵全 fail-fast 中文 `ValueError`（pyo3 把 Rust panic 转 PanicException、release 无 validate，Python 层前置是主防线）：载荷形态（placed 非列表/demand_map 非字典/demand 非正整数含 bool 拒/条目非对象/缺三键/id 非字符串/rotation·translation 非数值且 bool 拒/translation 非 list·tuple·2 元/元素非数值/**NaN·Inf 拒**——过不了 Rust serde 的 JSON 解析/strip_width 非正有限）；未知 pid（第 i 条带下标消息）；**每 pid 条数 ≠ demand = F2 完整解硬约束**（sparrow `SPProblem::restore()` 只按 placed 扣减 demand 不补放新片、exploration/compression 只有 move/shrink ⇒ 部分解与超量解同拒，同 pid N 条 N==demand 合法与「demand>1 spyrrow 发 N 条 placed_items」既有不变量对齐）；**mirror 真值拒绝**「含镜像片，无法热启动（sparrow 姿态不含反射，请先取消镜像）」（真值判定 mirror=1 脏数据同拒；为二期 A4 编辑器入口预埋）。③`warm_start_supported() → bool`：`importlib.metadata.version('spyrrow')` 经 `_LOCAL_TAG_RE=r'\+ms(\d+)'` 解析 local tag 且 **N≥1 才 True** —— **对原 PRD AC 的修订点**（prd.json 已按修订口径写）：2026-09-19 联调前实勘 0.9.0+ms0 已装入 .venv（纯重建 wheel 无 initial_solution 参数），纯 '+ms' 子串判定会误报 True 致 worker 层硬 TypeError，故 ms0 必须判 False（`+msfoo`/`+local`/`+MS1` 大小写敏感同不认；`0.2.0+ms3` 版本号无关只看 tag）；包不存在/任何异常 → False 绝不抛（except Exception 兜底）。④`main` 冒烟 = 合成夹具自检（合法构造精确结构对拍含多副本+乱序+int→float 归一/JSON round-trip/空解/部分解/超量解/未知 pid/mirror 拒绝/畸形 14 项/能力探测不抛 + 打印当前装载态诊断行）全过 PASS exit 0。⑤新建 `tests/test_warmstart.py` **63 例**（全合成夹具零依赖）：合法 round-trip 精确结构 + 多副本 + 空解 + 多余键白名单 + 输入不可变（deepcopy 快照对拍）/ 完整解三向（部分解·同 pid 欠量·超量）/ 未知 pid / mirror 拒绝×2 / 畸形矩阵参数化 placed 21 + demand_map 7 + strip_width 8（含 bool/NaN/Inf/字符串/元数错）/ 形态先于条数校验 / `warm_start_supported` 版本串多态 10 态（**`0.9.0+ms0` 误报回归锁** + ms1/ms2/ms10/0.2.0+ms3 通过 + msfoo/local/MS1/无 tag 不认，monkeypatch `importlib.metadata.version` 共享模块对象同款套路）+ 包缺失 PackageNotFoundError + RuntimeError 异常态 + 真实装载态返回 bool / **AST 分层守卫**（仅 stdlib 白名单 + imported 非空哨兵防守卫自失效 + 源级 web/cli 六哨兵，镜像 test_polish 套路）/ 冒烟 main exit 0 且输出无 FAIL。踩坑 1：测试 helper `_pl` 初版 `tr=` 走 `**extra` 成多余键（7 例 DID NOT RAISE 假绿）—— 改 `tr` 为一等参数直写 translation 后修复，教训：参数化脏数据用例的 helper 必须让脏字段真正落进目标键。⑥验证：pytest 全量 **1001 passed**（含新 63；0:02:21）+ `python -m materialsorting.nesting_engine.warmstart` exit 0 + `python -c` 相对导入 ok + 当前态探测 spyrrow 0.9.0+ms0 → False（符合 N≥1 修订口径）；本轮零 web/前端改动，无浏览器验证面。⑦文档同步（/sync-docs 口径）：`.docs/technical/agent-file-map.md`（目录树 polish 行改 ├── + 新增 warmstart 行 + nesting_engine 章新增 `### warmstart.py` 三函数表节）/ `.docs/business/business-overview.md`（当前状态表新增「warm-start 热启动一期」行，🚧 US-001 落地 + 回归红线注记）/ `nesting_engine/AGENTS.md`（文件分工表新增 warmstart.py 行 + 启动校验 import 列表补 warmstart）/ `tests/AGENTS.md`（文件表新增 test_warmstart.py 行）/ `CLAUDE.md`（nesting_engine 模块 bullet 追加 warmstart.py 段，含 +ms0 修订口径与 US-002~005 后续故事注记）。prd US-001 passes:true。
45. **[2026-09-19] prd-warm-start-phase1 US-002 — initial_solution 求解链透传（pipeline→solver→worker）**：warm 载荷通道全链打通（US-001 构造点 → 求解进程消费点），纯透传零解释、缺省 None 逐字节不变、PyPI 0.9.0（ms0）态全程可跑可测。①`cli/pipeline.py` `solve_pieces(..., initial_solution: dict | None = None)` 纯透传 kwarg → `solve_with_callback_proc(initial_solution=...)`（零校验零序列化 —— 装载/校验的单一装载点在 US-003 portfolio 编排层，本层加逻辑会违反单一职责）。②`web/solver.py` `solve_with_callback_proc` 同名 kwarg → **Process args 元组 6→7 位**（`args=(pieces_snapshot, gate_mm, solve_params, result_queue, band, prefix, initial_solution)`）：载荷作**纯 JSON dict 原样**跨 spawn（pickle 安全），本函数零解释零序列化。③`web/solve_worker.py` 顶层签名加第 7 位 `initial_solution=None`（Windows spawn 可 pickle 不变量保持），warm 载荷**最终消费点**：入场三道防御闸门（双保险之二，其一 = US-003 编排层）—— 闸门序刻意定为「band/prefix 同传 → 硬 error 先于能力探测」（组合非法是编排层缺陷，静默降级会掩盖它：只投 `{kind:error,'band/prefix 与初始布局暂不支持同开'}` 不投 manifest、solve 不被调）→ `warm_start_supported()` False（当前 0.9.0+ms0 装载态即此）→ 丢弃载荷 + `_log.warning` 降级**普通重放**（不重跑不炸轮，manifest→frame*→final 全链照常，探测在 solve 之前）→ 序列化失败防御降级（超出 PRD 两闸门的兜底：`json.dumps` try/except TypeError/ValueError → 同②降级 warn，理论不可达 —— US-001 构造点保证纯 JSON，「不炸轮」精神）；过闸后 `json.dumps` **只在最终消费点**做（FR-7）传 `instance.solve(config, progress=progress, initial_solution=<JSON 串>)`；`warm_start_supported` 走函数内延迟 `from ..nesting_engine.warmstart import ...`（web→nesting_engine 分层合规 + 测试可按调用时 monkeypatch）。**缺省 None 字节一致红线**：`_solve` 闭包 if/else 二分 —— payload 缺席时 `instance.solve(config, progress=progress)` **绝不带 initial_solution 关键字**（PyPI 0.9.0 的 solve 不认未知 kwarg → 带了就 TypeError），既有调用形逐字节不变。④新建 `tests/test_warmstart_chain.py` **9 例**（PRD AC 四向全覆盖，**PyPI 0.9.0 态可跑**：monkeypatch `sys.modules['spyrrow']` 假模块（Item/StripPackingInstance（solve 捕获 `**kwargs` 并投 ExplFeas+Final 两帧）/StripPackingConfig/ProgressQueue/ReportType 桩）+ `_set_warm` 假能力探测）：worker 直调 5 例（None 缺省 solve 调用形零变化 `capture==[{}]` + 消息序 manifest→frame*→final / 载荷在场 dumps 恰在最终消费点 solve 收到 `json.dumps(payload)` 字符串 / 不支持丢弃降级 warn + 普通重放不炸轮 / band·prefix 同传参数化两向只投一条 error 且 solve 未被调 / set 混入序列化失败降级）；`solve_with_callback_proc` 2 例（`_FakeProcess` start() 当前进程内同步执行 target + `_FakeQueue` 同步 list —— **Windows spawn 不继承 monkeypatch，闭环必须留在当前进程**：args 元组恰 7 位、第 5/6 位不动、第 7 位载荷**原样 dict 非 JSON 串**、缺省 None 零变化）；`solve_pieces` 1 例（dict 原样透传 / None 缺省）。**踩坑（flaky 根因）**：worker 直调初版用真 `multiprocessing.Queue()`，其 put 走后台 feeder 线程异步落管，`get_nowait()` 在全套件 CPU 争抢下与 feeder 竞态 → 单跑恒过、全套件随机 3/9 失败（两次失败用例都不同），换同步 `_FakeQueue` 后 warmstart+chain 双序组合 + 全量三跑稳定全绿 —— 教训：multiprocessing.Queue 的 put 不是同步操作，测试断言队列内容必须用同步假队列。⑤PRD 任务5（同步既有直调 solve_worker 桩的 args 元组长度）实勘为 no-op：全套件 grep 无任何测试直调 `solve_worker` 位置参数（全部经 `solve_with_callback_proc`，其 fake 均 `**kw` 兼容）。⑥验证：pytest 全量 **1010 passed**（1001 存量 + 9 新，零逻辑改动零回归）+ `python -m materialsorting.nesting_engine.warmstart` exit 0 + AST 分层核查（solve_worker.py 恰 1 处 warmstart import = web→nesting_engine 合法；solver.py/pipeline.py 0 处）+ 模块导入冒烟 ok；本故事纯后端通道（web 前端/SVG 零改动），无浏览器验证面。⑦文档同步（/sync-docs 口径）：`.docs/technical/agent-file-map.md`（目录树 solver.py/solve_worker.py/pipeline.py 三行尾注 + web 章表 solver.py 673→720、solve_worker.py 567→657 含三闸门全段 + warmstart 节 US-002 落地注记）/ `.docs/technical/agent-api-reference.md`（solve_with_callback_proc 与 solve_worker 两表行签名 `+ initial_solution=None` + 闸门/消费点/缺省逐字节一致描述）/ `web/AGENTS.md`（文件分工表两行更新 + 新增「US-002 warm 透传关键约定」节）/ `tests/AGENTS.md`（新增 test_warmstart_chain.py 行含 feeder 竞态踩坑）/ `CLAUDE.md`（cli pipeline bullet + web solver bullet + nesting_engine warmstart 尾注 US-002 落地）。prd US-002 passes:true。
46. **[2026-09-19] prd-warm-start-phase1 US-003 — se 延长轮真顺延接线（portfolio + CLI 旗标）**：se 策略冠军延长段灌入筛选轮冠军解真顺延（`--se-warm {on,off}` 默认 on），延长段 180s 全部花在增量搜索而非重放已知结果；warm 不可用自动回退现状重放 + warn 一行，绝不炸轮（当前 0.9.0+ms0 装载态默认即回退 unsupported，ms1 wheel 到位后默认路径自动翻真 warm）。①`cli/portfolio.py` 延长轮编排 + 前置判定：新增 `se_warm_plan(cfg, enabled)` 纯函数（判定序 off → unsupported → band/prefix —— unsupported 更根本：wheel 不支持时 band 关了也没用，报 band_prefix_on 会误导用户去关 band；三类可装桶回退 → `(False, reason)`，全过 `(True, None)`；`warm_start_supported` 函数内延迟 import = cli→nesting_engine 分层合规 + 调用时解析可 monkeypatch；cfg.band/prefix 经 getattr 容错直构合成 cfg 无此二属性）+ `SE_WARM_REASONS` 五元枚举（off/unsupported/band_prefix_on/no_best_frame/invalid_best_frame，后两类装载点产生、此处列全量供文档与测试对拍）；`PortfolioController` 加 `se_warm` 旗标态 kwarg（缺省 False —— 直接构造控制器的既有测试与 legacy/race 路径零回归）+ `se_warm_state` 归档属性（None=延长轮未跑即 R0 提前停/中断；否则 `{'warm':bool,'reason':str|None}`）；`run_serial_portfolio` 延长轮：前置判定通过才给该轮 solve 传 `warm_best_frame=True` 策略标志（**回退不传该键 —— solve 调用形与无 warm 逐字节一致，零回归红线**），实际状态归档 se_warm_state（前置回退用早期 reason；装载点回退经 solve 记录 warm/warm_reason 带回），任一回退 → `_notify_line` warn 一行（--quiet 也打，不静默不炸轮）。②`cli/pipeline.py` 单一装载点：`_load_warm_payload(run_dir, seed, pid_meta, band, prefix)`（warm 载荷装载与校验的 MS 侧唯一边车→载荷转换处之二，与 warmstart.build_initial_solution 联合）：读 `run_dir/best_frame_s{seed}.json` **筛选轮边车（无 artifact_suffix —— 延长轮自己产物带 _ext 后缀防覆盖，warm 源是冠军筛选解）** → placed_items + width_mm → demand_map（pid_meta demand 投影）→ `build_initial_solution` 构造载荷 → 经既有 initial_solution 透传通道下传（显式 initial_solution 同给时以显式载荷为准）；回退矩阵**全降级不抛**（判定序与 solve_worker 闸门同理 —— 组合非法先于能力探测）：band/prefix 开 'band_prefix_on'（编排层已拦此处防御复核）/ warm_start_supported False 'unsupported'（防御复核）/ 边车缺失·损坏·缺键·投影失败 'no_best_frame' / build 校验 ValueError 全矩阵 'invalid_best_frame'；solve_pieces 加 `warm_best_frame=False` kwarg，返回记录 additive 附 `warm`（+失败时 `warm_reason`），缺省 False 记录无 warm 键零回归。③`cli/run_config.py` CLI 旗标：`--se-warm {on,off}`（默认 on，不走 argparse choices 手工值域校验 —— choices 外退出码是 2，本入口统一以 1 呈现配置错误；值域外/无策略/race 模式下给出=从属笔误/与 --extreme 同给均退出 1，new_run_dir 前拦下不留空目录）；se 启动行附 warm 计划态注记（「延长轮 warm 真顺延」|「延长轮 warm 回退重放（<reason>）」，--quiet 也打）；strategy.json se 段 additive 记**计划态** warm/warm_reason（三类前置回退开跑即知）；run 结束后 result.json config.strategy 段与 run_stats 行 config 段 additive 记**实际灌入态**同键（se_warm_state 非 None 才加键 —— 中断/R0 未进延长无从 claim；class_key 组成不变与历史 run 可比）；race/legacy 路径零新增键。④`tests/test_cli_strategy_wiring.py` 28→47 例（19 新增）：autouse 钉 `warm_start_supported=False`（测 CLI 接线而非 wheel 检测，防未来 ms1 wheel 入 .venv 翻转默认路径炸断言；需 True 的用例内 monkeypatch 后写胜出）+ iso_env 补 `RUN_STATS_JSONL` 隔离（此前本文件测试会向真实 out/run_stats.jsonl 追加行）；旗标裁决 3 例（值域外/无策略·race 从属笔误参数化/--extreme 互斥）；se_warm_plan 纯函数矩阵（判定序注记）+ SE_WARM_REASONS 枚举闭包；透传 1 例（supported=True → warm_flags=[False]×5+[True] 仅延长轮带 + 计划态/实际态 additive 同键 + true 时无 warm_reason 键 + run_stats 行断言）；回退 3 例（默认 unsupported 全轮不带键+warn 行+计划态·实际态三面 / off 显式 / band·prefix on 参数化探测 True 也不接线）；装载点 7 例（`_FakeProc` 桩 solve_with_callback_proc + 真 build_instance + 真 commit：载荷精确对拍含 int→float 归一与顺序保持 / 五类装载回退参数化（边车缺失·损坏·部分解·不支持·band on —— band 本身照常下传只退 warm）/ 缺省不传旗标记录 9 键逐集零回归）；零回归哨兵 2 例（无旗标 legacy 运行 stdout **逐字节对拍** —— 行序 run_dir→配置→多seed→commit→轮次头/进度→digest→best→末行全可先验推演，时间戳目录与 commit 数值事后读回拼入，任何输出增量即炸；--help 含 6 策略族旗标子进程冒烟）。⑤验证：pytest 全量 **1029 passed**（1010 存量 + 19 新增，0:02:21，warmstart+strategy_wiring 双序组合稳定全绿）；真 CLI 冒烟 `ms-run-config --strategy se --time 20 --se-screen 5 --se-extend 10` rc=0 + warn 行「se 延长轮 warm 回退 → 现状重放（warm_reason=unsupported）」+ strategy.json 计划态/result.json config.strategy/run_stats 行实际态三面全中（冒烟产物已清理）；AST 分层核查（portfolio.py/pipeline.py 各恰 1 处函数内延迟 warmstart import = cli→nesting_engine 合法、run_config.py 0 处、warmstart.py 零相对导入）；本故事纯后端 CLI（web 前端/SVG 零改动），无浏览器验证面。⑥文档同步（/sync-docs 口径）：`.docs/technical/agent-file-map.md`（目录树 pipeline/portfolio/run_config 三行 US-003 段 + warmstart 树行与 warmstart 节消费方落位注记 + cli 章表 portfolio/run_config 两行 US-003 段 + 护栏行 47 例）/`.docs/technical/agent-api-reference.md`（策略桥接 spawn 段注记：不传 --se-warm、se 缺省 on 自动回退 + _parse_plan 键白名单不透传零感知）/`CLAUDE.md`（cli bullet portfolio 段补入 + pipeline US-003 装载点 + 运行方式旗标串 --se-warm 与策略段 US-003 注记 + nesting_engine warmstart 尾注 US-003 落地）/`tests/AGENTS.md`（test_cli_strategy_wiring 行 US-003 全段含 autouse 钉死与逐字节哨兵口径）/`.docs/business/business-overview.md`（warm-start 一期行 US-001~003 落地 + 回退口径 + US-004~005 后续）。prd US-003 passes:true。
