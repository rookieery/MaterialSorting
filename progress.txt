# Ralph Progress Log
Started: 2026-08-09

















28. **[2026-09-06] prd-edit-drag-snap US-002 — 松手吸附引擎纯函数（retreat + attract）**：右键拖动松手贴附 PRD 第二故事（纯前端零后端零组件，消费 US-001 几何算子的引擎层）。①新建 `materialSorting-web/src/lib/snap.ts`：常量收口顶部 —— `ATTRACT_MAX_GAP_MM=10`（2026-09-06 定案，导出供测试/文档锁值）/ 粗扫步 20mm / 二分 ≤14 轮 / `SEP_NUDGE_MM=1e-9` / `COLLIDE_AREA_EPS_MM2=1e-9`（后两者与后端 polish.py 同名常量同口径；另 CLAMP_EPS_MM=1e-6 同 GATE_EPS_MM、ZERO_DISP_EPS_MM=1e-9 零位移判定）。核心 `computeSnapCorrection(dragged, rawTr, sess, others, opts)` → `{tr, kind:'free'|'retreat'|'attract', partnerKey}`：**P0 retreat**（落点违谓词时沿 lo=sess.lastSafeTr → hi=rawTr 线段参数化 t(mm)，20mm 粗扫括出**首个**穿越小段——凹形自由区间不连通、朴素二分会跳过窄障碍隧道穿越（后端 _slide_west_touch 同款机器）→ 小段内二分 ≤14 轮 → 自由侧回退 1nm，终态布尔交精确 0；锚点 null/路径零长/锚点自身违谓词/粗扫不可达均防御回落 raw）；**P1 attract**（落点合谓词时沿末帧拖动位移方向 rawTr−lastSafeTr、位移 <1nm 零位移帧退化逐邻居质心连线，用 US-001 firstContactDistance 解析取首触 t，t≤10 才吸、吸附位=落点+(t−1nm)·dir；多邻居 t 最小、平手 key 最小（与 others 数组顺序无关））；**谓词** = computeOverlap(pieceAt(拖片,位), others).areaMm2 ≤ sess.startOverlapMm2+1e-9（复用 overlap.ts oracle，起手存量重叠不恶化不误清）；**clamp 守卫** = 候选 minX≥−1e-6/minY≥−1e-6/maxY≤gate+1e-6 否则弃用回落 raw（引擎不重钳 raw）；**fail-open** = 整体 try/catch 任何内部异常返回 raw；**只改 translation 永不动 rot/mirror**；邻居寻址全程 EditPiece.key 逐个枚举绝不 pid 去重。②新建 `src/lib/snap.test.ts`（PRD 指定扁平路径，vitest include 覆盖）22 项：retreat 基础夹具（解析对拍 0.01mm + 布尔交 ≤1e-9 + 分离 ≥1e-9，1nm 断言容 1e-12 ulp 消去噪声并注释）+ 隧道穿越夹具（窄障碍 150-155 中途 + 远墙 400-500 终点：朴素二分收敛 300 vs 粗扫正确回 50）+ 基线不恶化（恶化位 900>500 retreat 回锚点、终态 = 基线不清零）不误清（改善位 300/持平位 500 均不动）+ 5 副本同 pid 求和锁（各 50 ≤ 基线 200、合计 250 > 200 → retreat 到解析位 54，去重实现会误判 free；边界五副本同面积平手 → partnerKey=key 最小）+ attract 6mm 吸（10−1nm 精确）/ 15mm 不动 / 零位移质心退化（rawTr===lastSafe 与 null 锚点两变体，东 4mm 北 8mm 取东）/ 平手 key 小（东 key9 数组首位 vs 北 key2，取 2）/ 同 pid 寻址锁（t=6/8 取 6 的后位副本）/ 背离方向不吸 / clamp 三变体（gate 顶越界、minX、minY）+ retreat 候选违 clamp 同回落 / getter 注入 basePolygon 抛异常 fail-open 不外抛 / 确定性双跑 JSON 逐位全等 / 空邻居（tr 拷贝不别名入参）· 无锚点 · 含自身 key 跳过。③验证：`npx tsc --noEmit` 干净 + vitest 全量 **1060 passed**（65 文件，1038+22）+ `npm run build` 过（305.07KB/gzip 99.81KB，snap.ts 尚未进组件图 —— US-003 接线后入包）、static/ 已重建；零后端零组件改动（纯函数层无 SVG/UI 变更，浏览器验证不适用）。④文档同步：materialSorting-web/AGENTS.md（lib/ 树行 US-002 fragment + 文末「edit-drag-snap US-002 关键约定」节：职责边界红线/retreat 机器/attract 机器/谓词基线/clamp 与 fail-open/多副本红线/常量口径含 1nm 断言浮点余量缘由/改引擎须同步 22 项测试）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-drag-snap US-002 落地」节：文件表/五不变量/验证记录）。prd US-002 passes:true。
29. **[2026-09-06] prd-edit-drag-snap US-003 — EditCanvas 右键会话接线与视觉反馈**：右键拖动松手贴附 PRD 第三故事（US-002 引擎接进编辑画布，纯前端零后端）。①`src/components/edit/EditCanvas.tsx` 接线六件套：**按键门控** —— onPointerDown 顶部 `button!==0 && button!==2` return（中键/后退/前进不起任何会话，顺手修掉「非主键也能拖片/平移/转柄」隐性怪癖）+ 旋转柄与空白分支内 `button!==0` return（右键不转柄、右键空白不平移不取消选中）+ 毛版分支 `snap = button===2` → MoveDrag 带 `snap:true` 且 `snapSessRef = {lastSafeTr:null, startOverlapMm2:Infinity}`（基线占位，紧随 selectPiece→refreshMetrics 首帧落真实值；左键显式置 null）；**contextmenu 吞除** —— svg native listener preventDefault（右键已被贴附手势占用，无自定义菜单）；**松手单次求解** —— endDrag flushFrame 后 `applySnapOnRelease`：会话态先取后清 → manifest/entry/pool/working/dragged 五重防御 → rawTr = clampPlacement（幂等）→ computeSnapCorrection → kind==='free' 直接 return（落点原样）→ 否则 commitDragPlacement **唯一落笔出口**（只改 translation，rot/mirror 读 working 现值原样透传）+ flashPartner；引擎只被 snap:true 会话 pointerup 调用，applyKeyTransform/applyRotateFrame/左键路径逐字节不变；**lastSafeTr 免费跟踪** —— refreshMetrics try 分支 sess 非空时首帧落基线、谓词 `areaMm2 ≤ 基线+COLLIDE_AREA_EPS_MM2` 成立续写 lastSafeTr（拖动帧零新增计算、rAF 合帧管线不变；degraded 帧保守跳过，全程 degraded = 无锚点 retreat fail-open 回 raw）；**伙伴片高亮** —— ensureUiLayers 第三子层 partnerG（子层序 [partnerG,overlapG,handleG]，pointer-events:none）绿色虚线 #2ea06c（旋转手柄同主题辅助色，与红色重合告警语义区分）+ 淡填充，PARTNER_FLASH_MS=900 常驻 → PARTNER_FADE_MS=300 opacity 淡出 → 移除，重复吸附重置计时，deselect/骨架重建/卸载 cancelPartnerFlash；**指南卡**新增「贴附：右键拖动松手贴附」行。②`src/lib/snap.ts` 扩一行：COLLIDE_AREA_EPS_MM2 改 export（原值不变）—— 跟踪谓词与引擎判定必须同 eps，否则跟踪位可能落在引擎谓词违例侧 → fail-open 白白退化吸附。③单测 +12（1060→**1072**，EditCanvas.test 61→73 右键贴附 describe + firePointer 加 button 参缺省 0 既有用例零改动 + UI 子层断言 2→3）：attract 6mm 缝（x≈500+1e-9+指标 0.00+伙伴 points/stroke+rot/mirror 原值）/ retreat 单跳（中途 50000 如实 → 松手归 0）/ 两帧拖动 lastSafeTr 锚点精确回 [500,0] / 缝 60>10 free / 左键重叠原样 [400,0] 回归红线 / 吸附后左键拖离不回吸 / rot180+mirror 片只改 translation / 非主键门控 / contextmenu 吞除 svg+裁片 / 高亮 1400ms 消退 / 右键无位移同款选中（远邻不吸附）/ 指南卡文案。④验证：`npx tsc --noEmit` 干净 + vitest 全量 **1072 passed**（65 文件）+ `npm run build` 过（309.68KB/gzip 101.34KB，snap.ts 随接线进组件图）+ 零后端改动；**浏览器验证 17/17 PASS**（Playwright+Edge headless 真实 5336 母版 → 20s 求解 30 片 → 编辑弹窗）：期望值由 Node 导入**真实引擎 esbuild bundle**（snap.ts+overlap.ts 临时 bundle 到 out/）在帧数据上预计算 —— B0 左键+30 原样（points 串全等）/ B1 右键 5mm 缝 attract（DOM points == 引擎 tr、伙伴轮廓 == partnerKey 世界轮廓、指标 0.0）/ B5 高亮 ~1.2s 消退 / B6 contextmenu 吞除（synthetic+trusted click 双验、无吸附位移）/ B4 右键拖离远端 free 原样+指标 0.0 / B2 右键深重叠 retreat（points == 引擎 tr、回退 393.9mm、全邻居布尔交 0.0mm²、指标 0.0）/ B3 左键深重叠原样 8027mm² 永不吸附 / B7 指南卡 / B8 dirty 丢弃确认重开回保存版 multiset 全等不炸（选片用引擎预模拟筛选 k/m 对 —— attract/click-free/far-free/retreat 全预测通过才采用，全流程确定性不靠碰运气）。⑤文档同步：materialSorting-web/AGENTS.md（lib/ 树行 US-003 fragment + edit/ 树行 US-003 fragment + 文末「edit-drag-snap US-003 关键约定」节：按键门控红线/contextmenu 吞除/松手求解链/免费跟踪与零位移帧推论/伙伴高亮/指南卡/改接线须同步 12 项测试）+ .docs/technical/agent-component-map.md（头部覆盖行 + 文末「edit-drag-snap US-003 落地」节：文件表/六不变量/验证记录含浏览器 17/17 明细）。验证后 ms-web 已杀（:8000 释放）。prd US-003 passes:true。
30. **[2026-09-06] prd-edit-drag-snap US-004 — 冒烟脚本与文档同步**：右键拖动松手贴附收官故事（端到端双护栏 + 吸附语义文档化，零后端零 src 改动）。①新建 `materialSorting-web/scripts/smoke_drag_snap.mjs` **31 检查点**（模板 smoke_edit_polish.mjs 流程骨架 + smoke-band-preview.mjs 的 addInitScript 预置 ms.tour.version='7'/seen.* 防 tour-overlay 拦截；Edge 通道 Chrome 兜底）：**S1** 上传 5336 → 3 码（32/33/34）20s 求解 30 片 → 基线 PLT 导出（placed 与 WS 末帧逐位全等 = 对拍基准端）；**S2** 编辑弹窗 + **真实引擎预模拟选片对**（esbuild 即时 bundle `src/lib/{snap,overlap,editGeometry}.ts` 到 out/smoke_drag_snap/engine_bundle.mjs —— 非复刻非 snapshot；Node 在 WS 帧数据上跑 predictSnap(S,R) 单跳会话模型 `sess={lastSafeTr: ovAt(R)≤基线+EPS?R:S, startOverlapMm2:ovAt(S)}` 与浏览器事件流逐帧一致，attract/far-free/retreat/final-attract 四路全预测通过才采用 = 全流程确定性不靠碰运气；末次 attract 推距梯度搜索 [0.5..3.2] 自适应 —— solver 墙钟终止布局每轮不同不能写死步长）；**B 右键行为段**（B0 左键 +30 基准原样 / B1 右键 5mm 缝 attract：DOM points == 引擎 tr 数学锚点串全等（pointsStr r2 复刻，键盘用例同法）/ B2 伙伴高亮轮廓 == partnerKey 世界轮廓 / B3 指标面板 0.0 / B4 高亮 ~1.2s 淡出 / B5 trusted 右键无位移 click：contextmenu 被吞 + points 不动 + 选中保持 / B6 拖动中合成 contextmenu 被吞 + 拖离远端 free 原样 / B7 深叠 retreat：points == 引擎 tr + B8 独立布尔交 <50mm² vs 全邻居复核）；**R 左键零回归红线两档**（R1 深叠压线原样 + 重叠 >1000mm² 如实显示 + 无伙伴永不 retreat / R2 同小缝落点原样 attract 永不发生）；**C 全精度贯通**（C1 末次 2mm 缝 attract 回贴 / C2 保存 / C3 导出 / C4 placed[KI].translation vs 引擎 resF.tr **|Δ|≤1e-6 双轴** + 亚 0.01mm 精度未截断（trOut≠r2(trOut)）+ 终态接触几何在导出值上复核（firstContactDistance 沿质心方向 ≈1nm + 全邻居布尔交 0）+ rot/mirror 原样 / C5 其余 29 项与求解末帧逐位全等 / C6 PLT 正文在案 PU≥100 + 笔画数 |Δ|≤12 宽松带（笔画漂移属预期差分）/ **C7 未动片轮廓区（stroke[k+1]=placed[k]，k≠KI）逐位全等 = 零回归判据** / C8 被拖片轮廓 = 基线笔画平移吸附位移 ±2 HPGL unit x/y 双轴）；报告 out/smoke_drag_snap/report.json + 逐相位截图 + 基线/末态 PLT dump。**实现坑三处备档**：①attract 沿质心连线（y 分量非 0）→ 吸附链期望坐标必须继承 res1.tr[1]（R1f=[R1[0],res1.tr[1]]，写死 t0[1] 首跑全链错位）；②PLT 笔画数漂移 = 门幅框随料长/被拖片标注 gate 裁剪分裂/右侧表格字宽联动，未动片轮廓区全等才是判据；③连跑多轮新 sid 撞 MS_SESSION_MAX=4/TTL 600s → 429 session_limit 上传卡死 → 重启 ms-web。②文档：materialSorting-web/AGENTS.md 文末「edit-drag-snap US-004 关键约定」节 —— **吸附语义六条单一真相**（触发=仅右键会话/求解=松手一次/效果=只改 translation/永不吸附路径回归红线/ATTRACT_MAX_GAP_MM=10/erode 口径脚注**引用 overlap.ts 文件头原文**只引用不复制防漂移，2026-09-06 定案）+ 冒烟运行口径与三坑备档；.docs/technical/agent-component-map.md 头部覆盖行 + 文末「edit-drag-snap US-004 落地」节（文件表/六不变量/验证记录）；.docs/technical/agent-api-reference.md /export placed 块补全精度贯通锁注记（/sync-docs 映射）。③验证：`npx tsc --noEmit` 干净 + vitest 全量 **1072 passed**（65 文件零新增用例）+ `npm run build` 过（309.68KB/gzip 101.34KB 与 US-003 同 = 零 src 改动佐证）、static/ 已重建；**冒烟 31/31 PASS × 3 轮**（典型轮：选片 k=3 m=29 pid=g03_34 缝 21.5mm、attract 吸拢 1.930mm、retreat 回退 393.874mm、末次吸拢 0.500mm、C4 dxErr/dyErr 双 0.0、C8 位移 dx=4.071/dy=0.948mm err 1.00unit、密度 84.60%→84.39%）。验证后 ms-web 已杀（:8000 释放）。prd US-004 passes:true。

31. **[2026-09-11] prd-state-file US-001 — 状态文件序列化模块与保存端点（后端）**：状态文件保存恢复首故事（POST /api/state-save + 纯函数模块 + runtime 提取，纯后端零前端零 UI = 浏览器验证不适用）。①新建 `materialSorting-server/src/materialsorting/web/statefile.py`：常量 `STATE_SCHEMA_VERSION=1`/`STATE_MAX_BYTES=20MB`（对齐 server.UPLOAD_MAX_BYTES）/`STATE_EXTENSION='.msn'`；`build_state_document(state, form, quantities, run)`（doc 块 = 会话 state['doc'] 原样含 5 层；run 缺省/空对象 → 整块省略 = 纯配置档「端点容忍但 UI 不可达」契约）；`serialize_state`（json.dumps ensure_ascii=False → utf-8 → gzip 恒开）；**守恒校验单一真相** `check_placed_conservation(placed, pieces, sizes, per_type, quantities)`——内部 `expected_demand_map` 走 `build_pid_meta` 完整同口径流水线（sizes int 集合过滤 + quantities (label,str(size)) 查 N/缺省 1/0 跳过/erode 退化石天然两侧一致），`StateConservationError.kind ∈ unknown_pid|count_mismatch`（detail 人类可读差异，保存端统一 400 指路文案、恢复端 US-002 按 kind 分别文案——两端复用同一判定函数）；`POST /api/state-save` handler：`_resolve_session_state`（自 routes_views import，读路由同口径 default 回退）401/400 结构化 JSON 先行 → 会话空（无 doc/pieces）422 → body 非 JSON/form 缺失/quantities·run·placed 条目形态非法 400 → run 在场保存期守恒 fail-fast（违者 400「数量矩阵/尺码选择与当前结果不一致，请先重新求解或改回数量再保存」防 A 产出他人无法恢复的内部不一致文件；build_pid_meta 的 ValueError/TypeError 形态异常同 400）→ 200 `application/gzip` 附件，文件名 `<source 去 .dxf>_状态_<yyyymmdd-HHMMSS>.msn` + Content-Disposition 中文/ASCII 双写（ASCII 侧 `_state_` 后缀 + 含中文 source 回退 `nesting` 前缀，routes_views.py /export 同法 RFC5987 quote）；`register_statefile_routes(app)`（strategy.py:1126-1139 同模式）。②`web/runtime.py`：自 `_build_pieces_state` 提取 `_state_from_doc(doc_dict)`（doc dict → pieces state 纯内存构建；gate_mm=float(doc['gate_mm'])/pieces=doc['pieces'] 与 load_pieces 返回同源同值 → 行为零变化，原函数 `doc,_,_ = load_pieces(...)` 后共享后者；US-002 恢复端重建会话复用）。③`web/server.py` 文件尾 `register_strategy_routes(app)` 之后 `from .statefile import register_statefile_routes` + `register_statefile_routes(app)` 一行（无 hook 无顺序约束）。④`__main__` 合成夹具自检 7 项（_state_from_doc 构建 → build 带/不带 run 双路 → gzip 魔数 → gunzip 往返逐字段（doc 5 层原样）→ 守恒通过/篡改 quantities count_mismatch/母版外 pid unknown_pid 三路）。⑤新建 `tests/test_web_statefile.py` **23 例**：200 往返（gzip 魔数/media_type/双名 regex + RFC5987 percent-encode 对拍/gunzip 后 schema_version=1 + doc.pieces 5 层逐字段 + placed 原序含 mirror）+ ASCII source 双名同前缀 + 无 run/`run:{}` 纯配置档 + 空态 422 + **FakeClock 先装再建会话**（建完换钟得负龄假新鲜 —— 修正首版失败教训，套路同 test_web_edit_polish）过期 401 结构化 JSON 非文件流 + 非法 sid 400 + sid 会话快照隔离保存 + 守恒三路 400（改数量 g01 demand 3≠placed 2 / sizes=[32] 码选过滤 demand 全空 / placed 越界 pid=母版已变更）+ 多副本 g01×2+g02×1 精确通过 + 载荷校验全家桶（缺 form/quantities 非 dict/run 非 dict/placed 空/条目缺 id/body 非 JSON）+ 路由在场白盒 + `_state_from_doc` 提取回归（tmp intermediate 与 _build_pieces_state 逐键全等）+ 守恒函数单元（quantities=None demand 全 1/两 kind）。⑥验证：pytest 全量 **771 passed**（748 存量 + 23 新增零回归）+ `python -m materialsorting.web.statefile` 自检 7/7 + 分层依赖未反向（相对 import 仅 .routes_views/.sessions/.solver/.runtime(函数内)，web 兄弟层无环，standalone import ok）；纯后端无 SVG/坐标/可视化改动 → 浏览器验证不适用（UI 入口属 US-003/US-004）。⑦文档同步（/sync-docs 口径）：`.docs/technical/agent-api-reference.md`（状态段 15→19 端点计数修正 + 路由表 +/api/state-save 行 + 新专节：请求/响应/错误表/七条关键不变量含「端点容忍无 run 但 UI 不可达」契约注记与守恒单一真相；修复编辑期间误吞的 GET /api/ptypes 节标题）+ `.docs/technical/agent-file-map.md`（web 树新增 statefile.py 行 + runtime.py 行补 _state_from_doc 提取）+ `web/AGENTS.md`（启动校验段 +state-save curl 与 `python -m` 自检两行 + 文件分工表 statefile.py 行 + server.py 行文件尾注册注记 + runtime.py 行补提取说明）+ `tests/AGENTS.md`（test_web_statefile.py 行）+ 根 `CLAUDE.md` web 段一句（/api/state-save 与守恒复用注记）。prd US-001 passes:true。
