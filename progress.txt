# Ralph Progress Log
Started: 2026-08-09


























46. **[2026-09-19] prd-warm-start-phase1 US-003 — se 延长轮真顺延接线（portfolio + CLI 旗标）**：se 策略冠军延长段灌入筛选轮冠军解真顺延（`--se-warm {on,off}` 默认 on），延长段 180s 全部花在增量搜索而非重放已知结果；warm 不可用自动回退现状重放 + warn 一行，绝不炸轮（当前 0.9.0+ms0 装载态默认即回退 unsupported，ms1 wheel 到位后默认路径自动翻真 warm）。①`cli/portfolio.py` 延长轮编排 + 前置判定：新增 `se_warm_plan(cfg, enabled)` 纯函数（判定序 off → unsupported → band/prefix —— unsupported 更根本：wheel 不支持时 band 关了也没用，报 band_prefix_on 会误导用户去关 band；三类可装桶回退 → `(False, reason)`，全过 `(True, None)`；`warm_start_supported` 函数内延迟 import = cli→nesting_engine 分层合规 + 调用时解析可 monkeypatch；cfg.band/prefix 经 getattr 容错直构合成 cfg 无此二属性）+ `SE_WARM_REASONS` 五元枚举（off/unsupported/band_prefix_on/no_best_frame/invalid_best_frame，后两类装载点产生、此处列全量供文档与测试对拍）；`PortfolioController` 加 `se_warm` 旗标态 kwarg（缺省 False —— 直接构造控制器的既有测试与 legacy/race 路径零回归）+ `se_warm_state` 归档属性（None=延长轮未跑即 R0 提前停/中断；否则 `{'warm':bool,'reason':str|None}`）；`run_serial_portfolio` 延长轮：前置判定通过才给该轮 solve 传 `warm_best_frame=True` 策略标志（**回退不传该键 —— solve 调用形与无 warm 逐字节一致，零回归红线**），实际状态归档 se_warm_state（前置回退用早期 reason；装载点回退经 solve 记录 warm/warm_reason 带回），任一回退 → `_notify_line` warn 一行（--quiet 也打，不静默不炸轮）。②`cli/pipeline.py` 单一装载点：`_load_warm_payload(run_dir, seed, pid_meta, band, prefix)`（warm 载荷装载与校验的 MS 侧唯一边车→载荷转换处之二，与 warmstart.build_initial_solution 联合）：读 `run_dir/best_frame_s{seed}.json` **筛选轮边车（无 artifact_suffix —— 延长轮自己产物带 _ext 后缀防覆盖，warm 源是冠军筛选解）** → placed_items + width_mm → demand_map（pid_meta demand 投影）→ `build_initial_solution` 构造载荷 → 经既有 initial_solution 透传通道下传（显式 initial_solution 同给时以显式载荷为准）；回退矩阵**全降级不抛**（判定序与 solve_worker 闸门同理 —— 组合非法先于能力探测）：band/prefix 开 'band_prefix_on'（编排层已拦此处防御复核）/ warm_start_supported False 'unsupported'（防御复核）/ 边车缺失·损坏·缺键·投影失败 'no_best_frame' / build 校验 ValueError 全矩阵 'invalid_best_frame'；solve_pieces 加 `warm_best_frame=False` kwarg，返回记录 additive 附 `warm`（+失败时 `warm_reason`），缺省 False 记录无 warm 键零回归。③`cli/run_config.py` CLI 旗标：`--se-warm {on,off}`（默认 on，不走 argparse choices 手工值域校验 —— choices 外退出码是 2，本入口统一以 1 呈现配置错误；值域外/无策略/race 模式下给出=从属笔误/与 --extreme 同给均退出 1，new_run_dir 前拦下不留空目录）；se 启动行附 warm 计划态注记（「延长轮 warm 真顺延」|「延长轮 warm 回退重放（<reason>）」，--quiet 也打）；strategy.json se 段 additive 记**计划态** warm/warm_reason（三类前置回退开跑即知）；run 结束后 result.json config.strategy 段与 run_stats 行 config 段 additive 记**实际灌入态**同键（se_warm_state 非 None 才加键 —— 中断/R0 未进延长无从 claim；class_key 组成不变与历史 run 可比）；race/legacy 路径零新增键。④`tests/test_cli_strategy_wiring.py` 28→47 例（19 新增）：autouse 钉 `warm_start_supported=False`（测 CLI 接线而非 wheel 检测，防未来 ms1 wheel 入 .venv 翻转默认路径炸断言；需 True 的用例内 monkeypatch 后写胜出）+ iso_env 补 `RUN_STATS_JSONL` 隔离（此前本文件测试会向真实 out/run_stats.jsonl 追加行）；旗标裁决 3 例（值域外/无策略·race 从属笔误参数化/--extreme 互斥）；se_warm_plan 纯函数矩阵（判定序注记）+ SE_WARM_REASONS 枚举闭包；透传 1 例（supported=True → warm_flags=[False]×5+[True] 仅延长轮带 + 计划态/实际态 additive 同键 + true 时无 warm_reason 键 + run_stats 行断言）；回退 3 例（默认 unsupported 全轮不带键+warn 行+计划态·实际态三面 / off 显式 / band·prefix on 参数化探测 True 也不接线）；装载点 7 例（`_FakeProc` 桩 solve_with_callback_proc + 真 build_instance + 真 commit：载荷精确对拍含 int→float 归一与顺序保持 / 五类装载回退参数化（边车缺失·损坏·部分解·不支持·band on —— band 本身照常下传只退 warm）/ 缺省不传旗标记录 9 键逐集零回归）；零回归哨兵 2 例（无旗标 legacy 运行 stdout **逐字节对拍** —— 行序 run_dir→配置→多seed→commit→轮次头/进度→digest→best→末行全可先验推演，时间戳目录与 commit 数值事后读回拼入，任何输出增量即炸；--help 含 6 策略族旗标子进程冒烟）。⑤验证：pytest 全量 **1029 passed**（1010 存量 + 19 新增，0:02:21，warmstart+strategy_wiring 双序组合稳定全绿）；真 CLI 冒烟 `ms-run-config --strategy se --time 20 --se-screen 5 --se-extend 10` rc=0 + warn 行「se 延长轮 warm 回退 → 现状重放（warm_reason=unsupported）」+ strategy.json 计划态/result.json config.strategy/run_stats 行实际态三面全中（冒烟产物已清理）；AST 分层核查（portfolio.py/pipeline.py 各恰 1 处函数内延迟 warmstart import = cli→nesting_engine 合法、run_config.py 0 处、warmstart.py 零相对导入）；本故事纯后端 CLI（web 前端/SVG 零改动），无浏览器验证面。⑥文档同步（/sync-docs 口径）：`.docs/technical/agent-file-map.md`（目录树 pipeline/portfolio/run_config 三行 US-003 段 + warmstart 树行与 warmstart 节消费方落位注记 + cli 章表 portfolio/run_config 两行 US-003 段 + 护栏行 47 例）/`.docs/technical/agent-api-reference.md`（策略桥接 spawn 段注记：不传 --se-warm、se 缺省 on 自动回退 + _parse_plan 键白名单不透传零感知）/`CLAUDE.md`（cli bullet portfolio 段补入 + pipeline US-003 装载点 + 运行方式旗标串 --se-warm 与策略段 US-003 注记 + nesting_engine warmstart 尾注 US-003 落地）/`tests/AGENTS.md`（test_cli_strategy_wiring 行 US-003 全段含 autouse 钉死与逐字节哨兵口径）/`.docs/business/business-overview.md`（warm-start 一期行 US-001~003 落地 + 回退口径 + US-004~005 后续）。prd US-003 passes:true。
47. **[2026-09-19] prd-warm-start-phase1 US-004 — spyrrow 双源切换助手 + rev 钉板 + 构建手册**：本机「PyPI spyrrow 0.9.0 ↔ spyrrow-ms 私有 wheel（0.9.0+msN）」一条命令切换可复现可审计，ms1 未交付前日常开发生产零影响（ms0 与 PyPI 行为全等已归档）。①新建 `scripts/spyrrow_wheel.py`（314 行，仅标准库 argparse/importlib.metadata/json/re/subprocess/sys/pathlib 零新依赖；sys.path 自引导对齐 embed_piece_codes.py 惯例）：`status`（**缺省命令**恒 exit 0）= 解释器路径自证（**pip 永远 `sys.executable -m pip` 不假设 PATH**，被哪个解释器跑就切哪个环境）+ 已装版本 + 安装源判别（`_LOCAL_TAG_RE +ms(\d+)` 命中即私有源，与 warmstart 同款正则但**判源 N≥0 / warm 能力 N≥1 口径分工**）+ `warm_start_supported()` 探测（函数内延迟 import 本仓 warmstart）+ rev 钉板四字段与一致性三态判读（一致 / 钉板落后需按 spyrrow-ms 台账更新 / 回线上临时态属正常组合）；`use-pypi` = `pip install --force-reinstall spyrrow==0.9.0`（PYPI_VERSION 常量，0.2.0 升级独立后续项届时改此）；`use-local <wheel>` = `pip install --force-reinstall --no-deps <wheel>`（**只换 spyrrow 一个发行版不动 shapely 等依赖**——force-reinstall 全量重装依赖既慢又可能漂移版本）+ 相对路径按 CWD 解析取绝对 + 错误矩阵（路径不存在/非 .whl/文件名非 `spyrrow-` 开头）全部 exit 1 + stderr 中文清晰报错**绝不触碰 pip**（装错发行版 force-reinstall 会破坏 .venv）；装后读回验证 = `importlib.invalidate_caches()` 再查版本（**同进程内 pip 改了 site-packages 元数据查找有缓存，不失效读到旧版**——踩坑点）+ 与 wheel 文件名版本（`_parse_wheel_version` 按 wheel 命名规范取第二段）不符打警告。②rev 钉板口径（对 PRD「提示更新/自动更新」的裁决取**提示更新**）：`materialSorting-server/spyrrow_build.json` 四字段单一真相源在 spyrrow-ms 侧交付台账（规格 §4 每出 wheel 同步写），MS 侧 use-local 装入版本 ≠ 钉板时打 `[提示更新钉板]` 块（四字段 JSON 模板 + 新版本号）**不自动改写**——spyrrow_ms_commit/sparrow_rev/built_at 三字段 MS 侧无从得知，半自动拼装会造无法审计的假钉板；use-pypi 回线上是临时态钉板不动（记录私有 wheel 交付谱系）。③新建 `.docs/technical/spyrrow私有wheel构建与升级手册.md`（MS 侧消费视角）：双源语义表（ms0=纯重建全等/ms1 起=含 initial_solution）/ 三命令快速上手 + status 输出实例判读 / 钉板四字段与更新流程 / 切换场景表（含与 8000 在线后端隔离说明）/ **0.2.0 升 rev 流程预留**（sparrow-ms checkout main → 出 wheel → 换钉板 → **确定性基线重标**（#149 改帧轨迹跨时代不可混比）→ 全量回归 → 回滚路径）/ troubleshooting 六条（wheel 在 `target/wheels/` 非 `dist/`（maturin 默认产物目录——本次实勘纠偏）/ PyPI 清华源 / 读回不变查解释器 / ms0 warm False 属预期 / 钉板损坏降级 / 后端隔离）；工具链/源码/构建命令本体指向 spyrrow-ms 侧文档不重复。④新建 `materialSorting-server/tests/test_spyrrow_wheel.py` **27 例纯桩**（不真跑 pip 不动 .venv；被测对象是 repo 根脚本非包内模块 → `importlib.util.spec_from_file_location` 唯一名加载）：纯函数 16（wheel 文件名解析 8 态 / 判源 7 态含 `+msfoo` 非数值 tag 不算 / 钉板入库态四字段可读）+ status 2（真环境**结构性断言** exit 0 + 关键行齐，不锁具体版本串防 US-005 装 ms1 后翻转 + 无参缺省=status）+ 错误矩阵 3（`no_pip` 哨兵 monkeypatch subprocess.run 直接 raise，错误路径绝不装东西）+ 桩剧本 6（`_FakeRun` + monkeypatch `importlib.metadata.version` 共享模块对象 test_warmstart 同款：pip 参数精确对拍 / 读回 invalidate / warm 探测跟随假 ms1 翻 True / 钉板漂移提示块 / 同版本无提示 / pip rc 透传 2 例）。⑤验证：pytest 全量 **1056 passed**（1029 存量 + 27 新，0:02:22）；`status` 当前态（0.9.0+ms0 已装）repo 根直跑 exit 0 输出全对；错误矩阵三路 exit 1 清晰报错；**真 happy path 实装走查**：`use-local D:/code/spyrrow-ms/target/wheels/spyrrow-0.9.0+ms0-*.whl` 同版本重装（真 pip uninstall+install、终态与钉板一致、warm False 预期提示、exit 0）——刻意不装 ms1（US-005 集成验收的故事面），.venv 终态复核 0.9.0+ms0；本轮零 web/前端改动，无浏览器验证面；git 核查仓库零 Rust 构建产物零 wheel 落盘。⑥文档同步（/sync-docs 口径）：新建手册（上述③）+ `scripts/AGENTS.md`（新建：sys.path 自引导三形态惯例 + GBK 控制台 reconfigure 要早于 argparse --help + spyrrow_wheel 条目）/ `.docs/technical/agent-file-map.md`（目录树 spyrrow_build.json 行入册 + scripts 表 spyrrow_wheel.py 行）/ `.docs/business/business-overview.md`（warm-start 一期行 US-004 落地）/ `tests/AGENTS.md`（test_spyrrow_wheel.py 行）/ `CLAUDE.md`（nesting_engine warmstart 尾注 US-004 段）。另：前会话遗留的 US-003 prd 归档未提交改动（prd.json 删行 + prd_archive.json 追加）随本故事 commit 一并入库。prd US-004 passes:true。
48. **[2026-09-19] prd-warm-start-phase1 US-005 — warm-start 一期集成验收收官（跨仓联调：spyrrow-ms 侧 US-005/006 先行实施 + MS 侧六项验收全绿）**：spyrrow-ms 仓本会话完成 A1 实装交付（commit aaebfe8f2c41/1a9bd169e84a）：`instance.solve(config, progress=None, initial_solution=<JSON 字符串>)` 第三关键字参数（`parse_initial_solution` 第二道校验闸 fail-fast ValueError：JSON 解析/strip_width 正有限/未知 id/每 id 条数≠demand；pid→item_id=items 下标与导出侧同源互逆；rotation 度直传 f64→f32 收窄；占位字段零填）→ `import_solution` → `optimize(..., Some(&sol))`，红线 = None 路径零变化（golden 对拍 ms1 不传参与 ms0 逐字节全等 + 上游 13 测试零改动全绿）；`tests/test_warmstart.py` 3 用例（首帧宽度==灌入 strip_width 实测 delta=0 / placed 守恒==Σdemand / None 路径==0a golden 逐字段全等 + 错误路径 4 例）；ms1 wheel 重建（清掉 9/7 陈旧版本演练产物与 venv 毒态）经 `scripts/spyrrow_wheel.py use-local` 装入 MS .venv（`warm_start_supported()=True`）+ 钉板 `materialSorting-server/spyrrow_build.json` 四字段同步（ms1/aaebfe8f…/881cdcb…/2026-09-19T11:36:17+08:00）+ spyrrow-ms 台账 §7。MS 侧六项验收：①前置 gate = 0a 行为全等对拍已归档 PASS 直接引用（三跑 best 全等 0.880385 + 152 帧语义轨迹逐帧全等）+ ms1 None 路径 golden 逐字节全等双保险；②warm 端到端（`--strategy se --time 275 --se-warm on`）：strategy.json/result.json 均 `warm:true`（真灌入无回退）、**延长轮首帧宽度与灌入 strip_width 精确全等 delta=0.0000mm**（判据 ±1mm，首帧 density==冠军帧 = restore 起点即冠军解）、末帧 placed 119==Σdemand、run_stats 行 additive warm 键；③se A/B 判据（`--time 600` 同总预算，on×5/off×6 共 11 轮 —— 编排脚本经会话切换后原实例存活+恢复接力双实例并发，恰跨臂对称且各轮耗时 7.9~8.8min 与独跑一致、工作站核余量争抢弱，数据有效；champion 恒 seed 3）：**on 90.4784%×5 零方差 vs off 均值 90.4185%（90.436×3/90.4009×3 双吸引子）= +0.0599pt ≥ −0.1pt → 保默认 on**（逐对比较 on 严格>off 最大值，符号稳健）；背靠背确定性双跑 = 三对 on 轮（含独跑 vs 并发窗口）`curve_s3_ext.json` 5 帧语义轨迹逐帧全等 + warm 输入源冠军帧六跑全等；④既有验收复跑：`prefix_accept --seeds 0,1 --time 30 --intermediate <5336 run_dir intermediate>` accept（①on/off 均值 −0.026pt ②形态 2/2 ③确定性 frames 132==132 逐帧全等+工件回放全等 ④导出无泄漏）+ pytest 全量 **1056 passed** + vitest 全量 **71 files/1217 tests passed**（前端零改动符合预期）；⑤文档收官：CLAUDE.md「关键技术决策」sparrow 条目翻页 = spyrrow-ms 私有 fork 单一真相源（起步锚 881cdcbd/双源切换/0.2.0 独立后续项）+ solver/pipeline warm 通道注记（US-002/003 已带）、盘点文档 §2.A 一期落地状态注记、agent-file-map warmstart 节收官注记、规格文档 MS 真相源副本与 spyrrow-ms 工作副本双侧同步（§2.1/2.3/2.4/3.1/3.2/3.4 六处状态注记 + 「MS 侧已对接」）；⑥验证速查 10 项表入 `tasks/prd-warm-start-phase1.md`。附带修复 `tests/test_spyrrow_wheel.py` 2 例环境耦合误报（钉板同步 ms1 后暴露 ms0 时代写死假设：桩只 mock 已装版本未 mock 钉板文件，加 `_pin` 临时钉板 monkeypatch PIN_PATH 隔离，27/27 恢复全绿）。验收报告归档 `.docs/business/warm-start一期AB验收报告_2026-09.md`。prd US-005 passes:true —— **一期五故事全链闭环**（warmstart 载荷构造 → 求解链透传 → se 真顺延 → 双源助手 → 集成验收），默认 on 有 +0.06pt 实测背书。
