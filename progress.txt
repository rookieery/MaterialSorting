# Ralph Progress Log
Started: 2026-08-09

















- 真实冒烟（data/configs/5336_coded_really.json，rc 均 0）：race `--strategy --time 240 --race-budget 60 --race-gate 0.5` → seed0 豁免 86.98%、seed1/2/3 于 ~30s 门杀（86.33/86.43/85.59% ≤ bar 86.93%）、seed4 续跑 89.57%、预算收口 1 seed 未启动（222.5/240 名义）、best=seed4 89.61%、决策 5 行 theta=null；se `--strategy se --time 300 --se-screen 30 --se-extend 60` → k=7 筛选 + 冠军 seed4 延长 89.65% ≥ 筛选最大 89.48%、_ext 边车在场、portfolio.se {k_screens:7,screen_s:30,ext_s:60,champion:4}。
- 验证：pytest 全量 **306 passed**（278→306，0 failed）；文档同步 `.docs/technical/agent-file-map.md`（cli 节头/叙述段/pipeline·portfolio·run_config 树行+详表行）与 `tests/AGENTS.md`（test_cli_strategy_wiring 行）+ README kill_decisions 过载备注。

## 2026-08-20 — US-003 simulate 双档扩展与文档同步（strategy PRD）
- `cli/calibration.py`：SIM_STRATEGY_GRID 增 se180/race180 策略档（网格 8→10 档，kind='strategy'）。配对曲线基础设施：`paired_curves`（base/{short,full} 同 seed 值配对；变体 = variant_*/ 池合并）、`_eligible_pairs`（short 原生 ≥90s 且 full 原生 ≥180s 才合格，预算外不外推）。`simulate_strategy_scenario` 单场景回放 —— race：`race_plan(T)` 计划、名义记账收口（spent+SEED_UNIT_S≤T 才启下一 seed）、逐帧 R0 先判后 `decide_race_kill`（US-001 单一真相源；首 seed 豁免建 bar、严格破纪录续跑、被杀交付 best-so-far；per_seed outcome full/kill/r0 + false_kill=「full 曲线 180s 帧内本可达标」）；se：`se_plan(T)` k 筛 + 冠军延长恰一次；跨 fork 诚实口径 = 筛选读 short 截断 90s 终值、延长/续跑读 full 截断 180s 帧。`evaluate_strategy_tier` 产出与 kill 档同构 ETT 字段（判据同现有，场景序列长 = race/se/uniform 三计划 max 保证跨档可比）+ E[max] 口径（delivered_mean/σ、oracle_max_mean、miss_max_rate、配对 uniform90 基线 gain_vs_uniform90、started_mean、n_kills、false_kill_rate）；总预算 <275 或无配对曲线 → metrics None + 中文 note 降级行。`recommend_strategy` 候选扩到 `_RECOMMEND_KINDS`（kill/θ/strategy），策略档 params 含 strategy/time/race_budget+race_gate 或 se_screen+se_extend；控制台主表 k/B None 安全（'-'）+ 第二张「策略双档 E[max] 口径」表（base/变体 E[max]、漏 max、对 uniform90 增益）；报告 pools 增 pairs 键。
- 测试 `tests/test_cli_simulation_strategy.py`（9 用例）：se 筛+延结构（se_plan(600)=(4,180)、冠军 argmax、delivered=max(筛选,延长)、wall=4×90+180、同 seed 不二次延长）、race 门杀省时再投资（race_plan(600)=(5,90) 启动 4 > 600//182.5=3、full/kill 交替 t_stop 180/90）、首 seed 参照豁免、每 seed 门评估恰一次（spy）、R0 优先（race 续跑中停 / se 筛选中跳延长）、tier 指标+CLI 端到端（metrics 键集、plan、base bootstrap+变体全枚举 4096、race kills>0 fkr=0、除 generated 逐字节一致）、DRY 断言（decide_race_kill/se_plan/race_plan 与 portfolio 同一对象 + spy 证明回放真调）、推荐含策略档（race180/se180 胜出 params 旗标、fkr 0.06 淘汰）、降级路径（T<275『最小配置』/空配对『配对』/short-only tag CLI）。`tests/test_cli_simulation.py` 3 处适配（10 档名集、策略档 k=None+note 降级、pools.pairs、策略档豁免 base 非空断言）。
- 验证：pytest 全量 **315 passed**（306→315，0 failed）；CLI 冒烟合成配对 tag rc=0（主表 se180/race180 ETT 行 + E[max] 副表 + 确定性重跑一致）；运营产物 `out/portfolio_calibration/5336_coded_really/` 不在本机（AC 条件不满足，非测试依赖）。文档四处同步：README「配置驱动求解」新增策略双模式块（词汇表 race 门杀/SE 筛延/门时刻/bar/冠军/名义记账/确定性重放 + 用法示例 + 四档速查表 + 现场对跑 88.38 平局结论）+ simulate 条目扩双档、CLAUDE.md 运行方式（旗标签名 + US 策略双模式/双档条目）、`.docs/technical/agent-file-map.md`（calibration 树行+详表策略双档段）、`tests/AGENTS.md`（test_cli_simulation_strategy 行 + test_cli_simulation 行更新）。
