# CLAUDE.md — MaterialSorting

## 项目定位

牛仔裤排料（marker making）引擎与可视化工作台。从 `D:\Pattern_Making` 迁移而来，与打板模块完全解耦。核心目标：把 M1787 直筒款 8 码套排利用率做到 90%+（版师认可的行业生死线）。

## 分层架构（依赖方向单向，禁反向）

```
cli  →  web  →  nesting_engine  →  nesting_bounds  →  dxf_parser
              (sparrow_experiments → sparrow_baseline)
```

- `cli`：最上层编排者（`ms-run-config` 配置驱动求解，见下「运行方式」）。只 import 底层原语与 `web.solver` 求解封装，**绝不 import `web.server`**（FastAPI 服务进程副作用）。产物只落 `out/config_runs/`（cli 子包主要可写目录）+ `out/portfolio_calibration/`（标定管线）+ `out/run_stats.jsonl`（PC-009 run 统计库单文件），物理隔离 web 事实源。`config.py`（9 键 JSON 配置严格校验，模块级仅标准库）、`pipeline.py`（commit 管线镜像 `server._commit_to_nesting_sync` + `solve_pieces` 单 seed 求解封装，2026-08-22 起透传 `cfg.band` → `solve_with_callback_proc(band=...)` worker 进程内成带；2026-08-25 起同款透传 `cfg.prefix` → `prefix=...` 起始端成套前后幅）、`run_config.py`（入口：逐 seeds 串行多轮 + best 汇总）。
- `dxf_parser`：底层 DXF 读写。`reader.py`（ezdxf recover + GBK 块名 + R12 POLYLINE）、`geometry.py`（纯几何算子，无 ezdxf）、`model.py`（PieceOutline dataclass）。仅标准库 + ezdxf，不依赖任何兄弟包。
- `nesting_bounds`：`load_pieces.py` 把切片目录（`pieces_manifest.json` 驱动）逐文件 → 布纹对齐水平 → 归一化到原点（US-001 v2：**无镜像展开**，每文件恰一条 `NestPiece`，`pid=f'{label}_{size}'`）。定义 `GATE_MM=1750`（**单一幅宽口径**，2026-09-04 起默认 1750mm=175cm（仅默认值变更，仍可任意输入）；2026-08-28 版师定案：输入的幅宽就是实际幅宽、不做任何缩减 —— 求解约束带 / 密度分母 / band·prefix 守卫 / PLT 导出边界 / UI viewBox / 导出外框全用 gate_mm（web/solver 与 CLI 引擎同口径）；历史 `PLOT_SAFE_MAX_Y_MM=1910` 与 `NEST_GATE_MM=min(两者)` 的 70mm 钳制已整体移除 —— 当时撞机系个别机器无法处理 1980 幅宽，幅宽受限的设备由用户直接输入更小门幅）、`DEFAULT_SIZES`（8 码跳 32）。
- `nesting_engine`：sparrow 求解。`constraints.py`（重合/旋转**全局**上限 `MAX_OVERLAP_MM=10` / `MAX_ROTATION_TOL_DEG=45`（2026-08-17 起不再按片型钳制，版师按片型的工艺参考值在 `.docs/business/排料规则_详细版.md`）+ 位图腐蚀 + 合法性校验（US-002 起成对齐套校验与 `PAIR_TYPES` 已删））、`sparrow_baseline.py`（基线 + **共享层**：`SIZE_PALETTE`/`size_color`（2026-08-20 起**尺码** → 16 色循环表**单一真相源**，锚点 `SIZE_ANCHOR=DEFAULT_SIZES[0]=28` 稳定绝对映射、同码同色跨片型；solver manifest / PNG / DXF ACI / CLI SVG 四处同源取色；此前 US-002~2026-08-19 为 g 码键 `label_color`）/_clean_polygon/solve_with_progress，被 solver/export/sparrow_experiments 复用）、`sparrow_experiments.py`（旋转/重合公差实验；US-002 起内片集合改 `--internal g04,g07` 命令行参数）、`labeling.py`（**g01+ 编号单一真相源**：`assign_codes` 统一赋码（顺序模式 = `sequential_sort_key`（group_key 前置保跨码同号 + 几何稳定序）；母版 block 名自带 `g/G/#`+数字编号且每码内唯一时全量复用），被 parse 赋号/label 代表裁片两处共用；导出 PNG/DXF 逐片叠印 g 码，PLT 永不加文字）、`waist_band.py`（US-009 腰头成带核心，**v2 2026-08-21 构造性链构造重写**替换 v1 spyrrow 带内子求解：`build_band_plan` 按版师真实构造「N 条单副本异码链」—— 链内滑移贴靠（`_slide_touch` 粗扫+二分，无 RNG 毫秒级）**按弧片手性自适应定向**（2026-08-27，M1787 g10 排查定案：凸左弧片 5336 朝向 = 降序+整链点对称翻转；镜像凸右弧片 M1787 g10 朝向 = 升序+不翻转，终态同形态）⇒ **开口朝左+最大码在最右**（建议1 定稿形态）；**v3 2026-08-28 增横向弯腰头支**（3069 g11 族：长轴横置 ⌣ 扁弧片、布纹沿弧长 0°、开口朝上/下，分派 = 非 flat 且全员 `_long_axis_x`）—— T-转置 (x,y)→(y,x) proxy 帧复用全部链机器（T·R(r)·T=R(−r) 共轭保成员 {0,180} 布纹合法；90° 旋转 proxy 两头皆错已实测否决：±90° 非法 / 片横躺端对端串 11m）+ 单一朝向 rots=(0,) 逐层竖排居中、大码打底、多副本链间互扣（版师参考图形态），闸门 `_check_chain_y` = 贴触+大码在底+旋转合法（开口不设闸，⌣/⌢ 皆母版原向，无手性分支），实测 10 码 demand1 1272×793mm / fill 77.2% / 贴触 0.00；flat / 弧形各族分派与行为零变化；贴触判据 `_chain_gap`≤1mm + 开口/码序程序自检 → 原始轮廓 union→erode(d_g)→归一化组合片（`WB_*` pid 永不出现在 manifest/frame/导出）+ `expand_placements` 展开权威式（offset 减号）；fill<45% 降为灾难形态守卫（**2026-08-22 起唯一守门人**：US-015 填料混带 / 预演 / ack 硬警告 / go-no-go 闸门等旁路功能已整体删除，band 收敛为「勾选+选 g 码」极简主流程）、带高守卫 ≤gate_nest（2026-08-28 起即门幅原样）；实测 6 码×1 链内缝隙 0.00mm、端到端 A/B **+2.27pt**（88.35% vs OFF 86.08%）；**禁 import web**，AST 守卫在 tests/test_waist_band.py）、`prefix.py`（US-001 起始端成套前后幅核心构造：`eligible_sizes` 2+2 资格码（两 g 码 demand 均 ==2）→ `pick_prefix_size` seeded 随机（crc32 派生）→ `build_prefix_plan` 4 片同码竖排贴靠组合片（**成员序 2026-09-03 用户依生产参考件改判 `paired` 同型成对堆叠 [后@0,后@180,前@0,前@180]，`PREFIX_ORDERS=('paired','grouped','interleave')`、interleave（FR-10 旧定稿）/grouped 备档逐字节不变**，PRD tasks/prd-prefix-member-order.md，US-001 引擎层落地；同型对 180° 曲率自吻合互锁 ⇒ 封闭腔死区以 select_prefix_plan info additive `dead_area_mm2`（顺落 prefix_runs 工件/冒烟）如实观测不设闸，全局净效应 US-002 定案（2026-09-03 同会话背靠背 `--seeds 0,1 --time 30`：on/on' 均值 paired 88.820% vs interleave 88.223% = **劣化 −0.597pt paired 更优 PASS 合入**，双开 band+prefix −0.313pt 反超 band_only、顺带修复 interleave 时代 1.466pt 短预算双开漂移；确定性边界 = 主解帧轨迹在 num_workers=4 下随 spyrrow 并行 merge 序漂移（组合片更大带 3 腔 ⇒ 并列候选竞速，num_workers=1 逐帧全等、构造层 chunk.to_dict/工件回放/选码恒确定），best 密度带宽 0.02~0.27pt 备案），prefix_accept 判据同步改名 member_order；`PS_*` BandChunk 同构直接喂 expand_placements，rot180 负坐标记账 tr=(xoff−b0,yoff−b1) 单测锁死；冒烟 `python -m materialsorting.nesting_engine.prefix` 对拍 P0：paired 4 片 @38 1175×1513/fill 86.0%/贴触 0.00/封闭腔 2 死区 45,811mm²、选码胜者 套装@38+g02@36 H=1956.2/fill 84.4%/3 腔 47,236mm²（interleave 时代 = 1155×1458/83.3%/0 腔））+ **US-002 段置换钉位/驱逐重插**：`permute_pin`（组合片 min_x ≤ skip_at_head=6 已锚定即跳过（P0 常态 4/4 seed a≈0.2mm 零成本）；否则 c1 硬切/c2 软切（flex=400 取跨线片最少、eps=5 防 d 包络外凸误判）A/C/B 三组刚性重组钉布头，x 区间不交无新重叠）→ `reinsert_evicted` 三优先序（home+微调梯 → `_slide_touch` 滑贴（x<0 钳壁）→ 尾追加兜底 width_growth 超 50 warn，面积降序无 RNG）→ `pin_prefix_layout` 纯函数编排（validate 全图复检失败回滚；三守卫参数 inspect 单测锁死；`--pin-demo` 冒烟 PIN≡FREE Δ0.00pt）+ **2026-09-02 顶部异码补片与联合选码**（prd-prefix-extra-piece：US-001 引擎层 + US-002 web 接线均落地）：`build_prefix_plan` additive `extra_pid`/`extra_rot` 异码补片第 5 成员同款贴触滑移（pid 追加 `+{label}@{size}`，extra_pid=None 逐字节回归）+ 新 `select_prefix_plan` 近满幅联合选码搜索取代 seeded 随机 —— 遍历 (资格码×片型×异码) 取 5 片原始轮廓 union bbox 高 ≤ gate 最大者（补片朝向不枚举、每组合委派 FR-3 面积增长最小内定，见修复②），平手确定性裁决，无 RNG；全无可行兜底回 4 片 seeded（用户定案）；web 侧 `solve_worker._build_prefix` 已切此函数（worker/预览/CLI 同真相源），主解 `exclude_pids` 升级 Mapping 部分扣减（`Counter(m['pid'] for m in members)`：异码 pid 扣 1 份余量照排，placed 守恒=Σdemand）；**US-005 收官验收（2026-09-02）**：端到端全绿 —— UI 冒烟 `materialSorting-web/scripts/smoke_prefix_extra.mjs` 29/29（上传 5336 → 预览 5 片+异码标注 → 求解状态行 → 形态判据（贴触/近满幅/锚定）→ final+末帧守恒 → 导出 PLT 无 PS_）+ `python -m materialsorting.web.prefix_accept --seeds 0,1 --time 30` accept（同源短预算 on/off 均值劣化 0.556pt ≤1.0pt、双开 0.297pt；确定性 frames 1137==1137 逐帧全等 + 工件 chunk.to_dict() 回放全等；导出 on/off 零泄漏）；**2026-09-02 修复（门幅安全余量 + panic 透传）**：近满幅选码在 d_g=0（web 上传默认无 per_type）时会选出贴线组合（5336 实测 residual 0.307mm），组合片碰撞轮廓≈条带高 ⇒ spyrrow 放置器 Rust panic `strip-width is running away`，而 PanicException 是 BaseException 子类、worker `except Exception` 捕不住 → 求解线程静默死亡、对外误报「求解失败: solver 返回 None」（US-005 验收全绿系全部走 per_type d=2 路径）；修复 = `PREFIX_GATE_MARGIN_MM=10`（`select_prefix_plan` 可行线 H ≤ gate−10 取次高安全候选、`build_prefix_plan` 竖排高守卫同口径；d_g>0 时 erode 对非水平边缘收缩 <2·d_g 故不按 d_g 折算）+ `solve_worker._solve`/`solver.solve_with_callback` 改 `except BaseException` 字符串化（带异常类型）透传 panic 真因；**2026-09-02 修复②（需求2「弧线相切」—— 补片朝向 FR-3 委派）**：用户反馈顶部补片搁在下片弧峰上留楔形空隙、应取 x/y 双镜像（≡rot180）相切嵌入朝向；根因 = 搜索层显式枚举 rot 再按 max-H 排序，嵌入贴合使 H 变矮被近满幅判据反向惩罚 ⇒ 系统性选中浅搁朝向（5336 旧胜者 @34+g02@38 rot180 嵌入仅 7.9mm/楔形 472,870mm²；同组合 rot0 嵌入 42.2mm 省 ~40,000mm²）；修复 = rot 候选不枚举、每组合委派 `_place_members` 内建 FR-3（union bbox 面积增长最小、平手取 0.0，选定朝向 `placed[-1][1]` 回收）+ `_place_members` rot 循环逐 rot 贴触校验容错（单 rot 失败不炸组合、全败才 raise）；n_candidates 减半（5336 120→60），新胜者 @35+g02@38 rot0 H=1952.4/residual 27.6/fill 81.0%，回归锁 test_select_prefix_plan_fr3_rot_nesting（凸台/凹腔夹具：嵌入 H=1320 vs 旧口径浅搁 1360 可证伪）；验证 = pytest 全量 681 passed + UI 冒烟 29/29（顺带修 5h 判据：免费同码副本贴列端打包误报，候补加「≥50% 宽度在基座列 x 区间内」）+ prefix_accept on/off 0.163pt PASS / 双开 1.466pt FAIL 为既有现象（同会话 stash 对照旧代码 1.666pt 更差，短预算跨会话漂移备案）；**禁 import web/cli**，AST 守卫在 tests/test_prefix.py）、`polish.py`（prd-edit-polish US-001 编辑排料「智能微调」引擎层确定性后处理：`polish_layout(placed, pieces_by_id, gate_mm, *, exclude=None, compact=False)` 纯函数无 RNG —— 诊断+去旋转（discretize_orientations 同款离散集向最近基线 {0°,180°} 步进、质心锚定 t'=c_world−R(rot')·c_local）+去重叠（最小分离平移 ±y 优先/−x 不增料长、二分贴触+1nm 微抬 ⇒ 交集面积精确 0）+报告（before/after 七指标 real 口径密度 + moves/residual/excluded）；**物理毛版轮廓口径**（原始 polygon 与 /export 同源非 eroded，编辑画布红字是腐蚀口径数值可能偏小，差异文档级约定）；逐 move 五道守卫（y∈[0,gate]/包络不增 +0.5mm 含 minX<0 布头外凸/零新重合 ≤0.1mm²/pid 多重集按数组下标寻址绝不 pid 去重/exclude 片永不动仍作障碍）+ 无改进返回输入 list 原对象（LNS 同款逐字节不变量）；compact=US-005 压缩回收档（2026-09-05 落地 additive：缺省 false = pass ④ 整段跳过 US-001~003 行为逐字节不变；true 时自布头方向逐片 −x 滑贴收空隙进料长 —— (minX,下标) 升序级联左片先贴、`_slide_west_touch` 20mm 粗扫+40 轮二分贴触+1nm 回退、move 走同款五道守卫 kind=compact + pass 级「全图 maxX 严格变小否则整体回滚」四快照恢复）；实测 110 片 0.5s（预算 ≤5s）；`python -m materialsorting.nesting_engine.polish` 冒烟 = 合成夹具十项自检 + `--demo` 真实几何演示（无 spyrrow 依赖）；端到端回归冒烟 `materialSorting-web/scripts/smoke_edit_polish.mjs`（US-004 2026-09-05 24 检查 + US-005 S7 compact 档 5 检查 = 29：微调四守恒/撤销/确定性双跑/PLT+DXF 导出 placed 守恒/band exclude 抽验——带形态区域不动/compact:true 载荷+width ≤ 非 compact 档+守恒不等式）；**口径差异红字注记**（编辑画布 = erode 后轮廓、polish 报告 = 物理毛版）在 .docs/technical/agent-api-reference.md /api/edit-polish 专节；禁 import web/cli，AST 守卫在 tests/test_polish.py）。
- `web`：`server.py`（FastAPI + WS 路由编排；2026-08-20 拆分：state/线程池在 `runtime.py`、parse 响应在 `parse_payload.py`、视图路由在 `routes_views.py`、WS solve 在 `routes_ws.py`，server.py re-export 全部拆出符号且 `_commit_to_nesting_sync` + 上传路由留守；**POST /api/edit-polish（prd-edit-polish US-002 2026-09-05）**：编辑排料「智能微调」会话族端点 —— placed 随 body（/export 同模式布局态后端不存）→ 会话 `pieces_by_id` 物理毛版轮廓口径经 `run_in_threadpool` 跑 `nesting_engine.polish_layout` → `{ok, placed, report}`（placed 条数+pid 多重集守恒）；pid 全匹配才跑（任一未匹配 → 400「母版已变更？请重新求解/上传」不部分降级）、placed 空/条目形态/gate_mm 两处皆无/exclude 非 dict 400 fail-fast、gate_mm 缺省回退会话 state（/export 同法）、exclude（labels/pids）+ compact（US-005 压缩回收档，缺省 false additive）透传引擎、成功顺手 `edit_hold.refresh(sid)`（default 不进钉住表）；契约详见 .docs/technical/agent-api-reference.md 专节，测试 tests/test_web_edit_polish.py）、`sessions.py`（多会话 US-001 2026-08-27：`SessionRegistry` 单一 `resolve()` + 容量 `MS_SESSION_MAX=4` + 空闲过期 `MS_SESSION_TTL_SEC=600`（惰性检查 + 30s daemon 扫描，`ws_open>0` 跳过；2026-08-30 alive hook `_is_expired` 三段短路：strategy.py 注册 `_run_alive_hook`，策略/极限 run 存活滚动钉住 + 终态宽限窗 `MS_RESULT_GRACE_SEC=7200` 内不逐出 —— 睡眠/关页/跑完挂机不丢结果，钉住仍占名额）+ 墓碑 FIFO≤128 存活 1h；default 会话豁免一切且其 state = `runtime._PIECES_STATE` 同一 dict 对象；禁 import server，AST 守卫在 tests）、`edit_hold.py`（编辑排料会话钉住 2026-09-04：编辑弹窗纯前端无任何请求，10min 空闲过期会在长编辑中途逐出会话 → 保存后导出 401 刷新丢全部编辑成果；`sid → hold_until` 滚动钉住表 `refresh` = now+`MS_EDIT_HOLD_SEC`（缺省 2h）+ `_edit_hold_hook` 豁免源 + `install(registry)` 把 strategy 的 `_run_alive_hook` 包成组合体（任一豁免源未来时间戳即钉住、取 max；server.py 文件尾 strategy 注册**之后**调用，顺序反了被覆写丢编辑豁免）；前端 `lib/editHold.ts` 弹窗打开期间每 4min 滚动 `POST /api/edit-hold` 续期（server.py 会话族路由：resolve 闸门过期 401 不给死会话续命、default no-op）—— 编辑中睡眠 ≤2h 唤醒恢复、关窗后自然留 2h 宽限（镜像策略终态宽限，宽限内仍占 `MS_SESSION_MAX` 名额）；禁 import server/cli，AST 守卫在 tests/test_web_edit_hold.py）、`diskclean.py`（多会话 US-006 2026-08-27：uploads 磁盘 TTL 清理 `MS_UPLOAD_TTL_DAYS=14` 按 mtime —— 超龄 `dxf`+`_pieces` 成对删（混龄整对保留）/孤儿单边/超龄 strategy_cfg；保护集 = 活跃会话 doc_id ∪ marker 内 doc_id ∪ 未超龄；commit 尾 + `main()` 启动 daemon 线程双触发，best-effort 吞异常仅 warn）、`routes_views.py` + `routes_ws.py`（多会话 US-003 2026-08-27：四读端点 `X-Session-Id` → `_resolve_session_state` 会话快照（读路径 create=False，过期/未知 401 带 `code` fail-fast）；WS `/ws/solve` `?sid=` query → `ws_acquire` 钉住（扫描不逐出）+ 回调 `touch` 刷活性 + finally `ws_release`，错误帧 `{'type':'error','code':...}` additive；GET `/` 响应头 `Cache-Control: no-cache`）、`solver.py`（build_instance + 子线程求解回调）、`export.py`（导出门面；2026-08-20 拆分：`export_geometry/png/dxf/plt` 四模块，PNG + R12-DXF marker + PLT；PLT 2026-08-31 起含毛版变体 `fmt='plt-clean'` → `write_marker_plt(clean=True)`：裁片只画毛版轮廓 + 尺码\*数量标注（无净版线/内部线/刀口/布纹杆羽）、带表格时唛架左右各一份同内容表格（X 引导扩 56mm），`clean=False` 逐字节不变；命名当日由「净版」更名与裁片「毛版轮廓」统一，协议值不变，前端默认导出格式同日切 `'plt-clean'`））、`plt_text.py`（2026-08-30 PLT 矢量文本引擎，v5 起**单线字默认路径**：汉字笔画中线 `hanzi_medians.txt`（hanzi-writer-data 9574 字，Arphic PL；**文件保留 MMaH 源 y 向下坐标（与 JHF 同款约定），加载期在身体范围 [139,1120] 内镜像翻 y** —— 构建期并未翻 y，v5 上线首日用户报告汉字逐字上下颠倒即此因，回归锁=test_plt_text 语义探针「上」长横在盒下半「下」顶横在盒上半）+ 仿射归一到 [0.06,0.94]em 排印盒+ ASCII `hershey_rowmans.txt`（Hershey Roman Simplex 92 字符；**JHF y 自字顶向下增长，加载期以 'H' 竖笔底为基线整体翻 y**——不翻则 ASCII 逐字垂直镜像，v5 目检对拍生产件发现已修）+ `normalize_text`（NFKC 全角→半角 + 残留符号 —·。、→ASCII）统一在 text_width/text_strokes 入口；两库未覆盖字符回退 Noto 轮廓路径（fontTools BasePen 子类展平 CFF 三次/TTF 二次贝塞尔为折线，De Casteljau 递归、容差 2 字体单位、内建 TrueType 隐含 on-curve 处理——RecordingPen 手排有 '0' 尖刺前车之鉴）+ 每字符一次 warn；`text_strokes` (u,w) 基变换必须右手系 det>0 否则 raise，防镜像文字回归；cmap 未命中画豆腐框 + warn 不静默；shrink-to-fit 后按 advance 尾截断；单线笔画开放 closed=False、轮廓回退 closed=True；单线资源缺失降级空表 + warn 回退 Noto 不硬炸导出；字体 = 仓库捆绑 `resources/fonts/`（`paths.FONT_DIR`，环境变量 `MS_FONT_DIR` 可重定位；Noto Sans SC Regular OFL + 两单线库 + 双许可证文件））、`plt_table.py`（2026-08-30 PLT 唛架信息表格 **v5 key/value 两行网格 + 四项细节定稿**（v4 两行网格结构保留；v3 14 行标签表被否：方案名称 36mm 大字异常 + 「标签:值」单行混排不对，对标前端「裁片设置」表格 = 第一行 key 第二行 value 的两行网格，逆时针旋 90° 后即生产视图正立表；v5 细节 = 外框左缘与唛架右边框**共用一条线**（TABLE_GAP_MM 20→0，空隙移除）/ 列 0 自唛架右下顶点**垂直向上 3cm** 起（TABLE_Y_START_MM=30，不再贴布边）/ 单元格**内容居中** + 左右 ≥1cm 内衬（CELL_PAD_MM 3→10）/ 文字**单线矢量字**（plt_text v5 默认路径，对拍生产件一笔单线观感））：**14 字段 = 14 列**画在排料图**外围**（边框右侧 table_x0=width+0 共线、表宽 `TABLE_W_MM=36mm`=两条 18mm 行带：key 带 [x0,x0+18] 靠唛架 + value 带 [x0+18,x0+36]），**不占排料区不计入用料**（仅在 PLT 图纸展示，实际裁布不处理；边框恒到 width_mm 不延伸、PS 纸长覆盖表格区（v5 共线后 =(width+66)×40））；**版式 = 文字旋转 90°**（基线沿世界 +y 门幅方向、字顶朝 −x 向唛架，基 `u=(0,1)/w=(-1,0)` 右手系直接生成过防镜像守卫；生产排料软件视图 = 切割视图逆时针旋 90°，此方向文字水平正立、key 行在上 value 行在下）+ **列沿 +y 自 y=30mm 起排开**（列 0=方案名称 → 列 13=备注，生产视图自左向右）+ 闭合外框 [x0,x0+36]×[30,30+Σ列宽] + 1 条沿 y key|value 行分隔线 + 13 条沿 x 列分隔线；**列宽自适应** = max(key 宽,value 宽)+2×10mm 内衬、**表长自适应** = Σ列宽（用户定案不与门幅等长，实测 7 码 ≈1020mm @12mm 字高/10mm 内衬）≤ gate−20−30，超限先全表缩字高（**统一 12mm** 含方案名称，下限 7mm）再等比压列宽（单元格尾截断兜底）+ warn；6 手输字段（床次/经纱缩水/纬纱缩水/排料师/样板号/备注，前端 ExportInfoModal 填、localStorage `ms_export_table` 跨导出记忆；默认 A料/0.0%/0.0%/空/noname/空，全自由字符串无格式校验）+ 8 自动字段（方案名称=`_plan_name_and_sets` 按系数分组拼式如 `(30+34+35)+(31+32+33)*1.5+(36)*0.5=8套`，每码系数=该码**面积最大裁片**同 pid 计数÷2（前后幅数量恒相等取最大片即套数）/本床包含套数/利用率=real_density×100/幅宽(m)=gate÷1000/料长(m)=width÷1000 **不含表格**/每套用料=料长÷套数/片数=len(world)/绘图时间无秒；幅宽/料长/每套用料 3 位小数）；`write_marker_plt(info_table=...)` additive，None 时输出逐字节不变——表格文字是文件级元数据 PU/PD 笔画，「PLT 永不加文字」口径指 g 码不进 PLT，与此不冲突且仍无 LB/VS 指令）；**2026-08-31 增 `preview_rows`**（`_ROW_META` (key,manual) 槽位元数据与 `_row_texts` 渲序一一对齐）→ `POST /api/plt-table-preview`（`routes_views.plt_table_preview`，导出弹窗 ExportInfoModal 按最终表格列序只读展示 14 字段的数据源：载荷 = /export 几何子集 `{gate_mm,width_mm,density,placed}`、手输槽位渲染默认值，前端预览失败降级 v2 六手输形态、确认导出永不被预览阻塞；纯 additive 零回归）。**多会话特性 US-001~007 已整体落地（2026-08-27 端到端对拍验收，harness = `scripts/us007_e2e_verify.mjs`：双浏览器各自上传不同母版 → ptypes/求解/停止/导出/高级运行/超限/default 回归互不串台 + 生命周期相位（`--expire`，TTL=6 服务器）求解中/策略轮询中不误杀 + 空闲过期弹窗 + 刷新干净会话）；README「多会话机制」节 + `web/AGENTS.md` 总览节是运维/环境变量速查入口。**

## 路径约定

所有数据/产物/前端目录集中在 `materialsorting/paths.py`，优先环境变量，默认相对包位置上溯到 repo 根。**不要在代码里硬编码 `..` 上溯或绝对路径**，一律 `from .. import paths` 后用 `paths.XXX`。

## 启动顺序约束

`ms-web` 的 `server.py` 在**模块顶层**调用 `load_pieces()` 读 `out/sparrow_baseline/pieces_intermediate.json`，并 `app.mount('/static', ...)` 指向 `materialSorting-web/static`（前端构建产物）。因此：
1. intermediate 由 **Web 上传母版 → `/api/commit-to-nesting`** 生成；首次启动 `_PIECES_STATE` 为空属正常（不崩），前端上传母版 commit 后自动 reload；
2. **prod 模式**：`materialSorting-web/static/` 必须先 `cd materialSorting-web && npm run build` 生成（产物已 gitignore，不入库；旧版 vanilla 三件套已删除）。
3. **dev 模式**：`npm run dev` 启 Vite dev server (:5173)，经 Vite proxy 转发 `/export` 与 `/ws` 到后端 :8000；**不需要 build 产物**（但仍建议先跑一次 `npm run build` 让 `static/` 存在，避免 FastAPI mount 空目录报错）。

## 关键技术决策

- **DXF 导出走 R12 + POLYLINE**（非 LWPOLYLINE）：ET2008 读 LWPOLYLINE 轮廓会消失。导出 marker、单裁片均如此。
- **sparrow 不改源码**：作为 pip 包（spyrrow）引用，v0.3 服装约束（重合/旋转/布纹线）在外层 `constraints.py` + `solver.build_instance` 包装实现。
- **坐标系**：spyrrow 世界坐标 X=用布长度(0..width)，Y=门幅(0..gate)，Y 向上；前端 SVG `scale(1,-1)` 翻转后与 PNG 一致。
- **密度口径**：版师/90% 生死线用**原面积·输入幅宽**口径 `real_density = total_area/(width*gate_mm)`（2026-08-28 版师定案：输入幅宽即实际幅宽，分母不再 min(gate,1910) —— 同一布局较 1910 分母口径 **−~3.5pp**，与历史 A/B 数据跨口径不可直接比；单一换算点 `web.solver._apply_density_dual`），erode 后 sparrow 自报密度仅作参考（density_sparrow）。前端 NestSVG 红虚线与 manifest `gate_nest_mm` 字段已随口径统一删除。
- **前端已迁移到 React 18 + TypeScript 5 + Vite 5**（US-001~US-008 落地）。源码在 `materialSorting-web/src/`（Zustand 状态管理 + 命令式 SVG 渲染逃逸 React reconciliation），`npm run build` 产出到 `static/`（gitignore，prod 模式前必须先 build）。旧 vanilla 三件套（index.html + 主脚本 + style.css，原 `legacy/` 归档）已删除，React 应用是唯一真相源。**不引入 CSS 框架**（沿用迁移自旧版的 `style.css`）；**坐标系翻转 `scale(1,-1)` 必须保留**，与 PNG / R12-DXF 导出口径一致。**多会话 US-005（2026-08-27）**：HTTP 一律走 `lib/api.ts` `apiFetch`（唯一裸 fetch 点 —— 注入 `X-Session-Id` + 会话先行门 + 401/429 `code` → 全局阻断弹窗，阻断期间请求拦截不发）；sid 由 `lib/session.ts` 管（localStorage `ms_sid`，刷新不变；session_expired 时弃旧 sid 铸新，绕开后端墓碑）；WS 由 `lib/ws.ts` 拼 `?sid=`。

## 数据流主线

上传母版 → `/api/parse-dxf`（解析预览，每片 g 码 label + 5 层字段；「裁片 × 尺码」数量矩阵编辑 quantities，随求解 WS start 按码下发）→ `/api/commit-to-nesting`（US-001 v2：`assign_codes` 最先赋 g 码 → 切单裁片 `{label}_{size}.dxf` + `pieces_manifest.json` 到 `out/uploads/<doc_id>_pieces/` → `load_nest_pieces` manifest 驱动归一化（无镜像）→ US-002 双写 intermediate：主写 `out/uploads/<doc_id>_pieces/pieces_intermediate.json`（会话快照数据源）+ 镜像写 `out/sparrow_baseline/pieces_intermediate.json` 事实源，条数 = 母版轮廓数；带 `X-Session-Id` 的 commit 快照只挂自己会话不碰 default 内存）→ `ms-sparrow-*` / `ms-web`。求解支线：WS start 带 `band` 时腰头 g 码成员在 worker 进程内聚排成 `WB_*` 组合片（waist_band.build_band_plan，v2 构造性链构造：N 条单副本异码链按弧片手性自适应定向 —— 凸左降序+整链翻转 / 镜像凸右升序不翻转 ⇒ 开口左+最大码右，2026-08-21/27）→ 主解排除该 label、组合片展开回成员 placement 后才发帧（solve_worker._emit_placed 单点，`WB_` 永不出进程；US-011）；带 `prefix` 时前/后幅 g 码经 `select_prefix_plan` 近满幅联合选码（资格码×片型×异码取 union bbox 高 ≤ gate 最大者、补片朝向 FR-3 面积增长最小内定（弧线相切嵌入，2026-09-02 修复②），无 RNG；全无可行兜底 seeded 4 片，2026-09-02）构造 4 片同码基座 + 可选顶部异码补片贴靠 `PS_*` 组合片自由进主解（prefix.build_prefix_plan，成员序 **paired 同型成对** [后@0,后@180,前@0,前@180] 180° 互锁（2026-09-03 用户依生产件改判 FR-10 interleave 定稿、on/on' A/B −0.597pt 定案合入，确认清单 §10），跨界共同填充；5 片时 pid 追加 `+{label}@{size}`），主解 `exclude_pids` 双形态扣减（iterable=整 pid 跳过 / Mapping=部分扣减，worker 传 `Counter(m['pid'])`：异码 pid 余量照排、placed 守恒=Σdemand，US-002）、final 置换挂钩 min_x≤6mm 常态跳过、双开只记录带位（US-001~005，验收报告 `.docs/business/起始端成套前后幅_AB验收报告_US005.md`；2026-09-02 异码补片收官验收 = 确认清单 §9 三项定案 + UI 冒烟 smoke_prefix_extra.mjs 29/29 + prefix_accept 短预算 accept 0.556pt）；高级运行（`/api/strategy/start` → `ms-run-config --strategy`）2026-08-25 起 band/prefix 均支持——经各单一校验点写进 9 键 config 由子进程长跑（链路见下「运行方式」）。详见 [README.md](README.md)。

## 运行方式

重构为正经包后，不能直接 `python file.py`（相对导入）。用 console_scripts（`ms-*`）或 `python -m materialsorting.<sub>.<module>`。5 个入口定义在 `pyproject.toml`：`ms-explore` / `ms-sparrow-baseline` / `ms-sparrow-exp` / `ms-web` / `ms-run-config`。

**配置驱动求解（无需浏览器）**：`ms-run-config <config.json> [--name RUN_NAME] [--time N] [--quiet] [--target P] [--params FILE] [--kill shadow|off|on] [--strategy [se|race] --se-screen 90 --se-extend 180 --race-budget 180 --race-gate 0.5]` 一条命令跑完「commit → 求解」。配置是 9 键 JSON schema（`master_dxf` / `gate_mm` 必填；`sizes` / `time` / `seeds` / `per_type` / `quantities` / `band` / `prefix` 可选，字段语义与 WS StartPayload 1:1；`band={'enabled':true,'label':g码}` 腰头成带——2026-08-22 起与 `--strategy` 策略模式兼容：web 策略入口（`/api/strategy/start` 复用 `routes_ws._parse_band` 单一校验点）把 band 写进 config，`solve_pieces` 经 `solve_with_callback_proc(band=...)` 在 worker 进程内成带+展开；`prefix={'enabled':true,'front':g码,'back':g码}` 起始端成套前后幅（front≠back，须均匹配 `^g\d+$`）——2026-08-25 起同款链路接入策略模式：`/api/strategy/start` 复用 `routes_ws._parse_prefix` 单一校验点（含 2+2 资格码全量校验，start 期 400 早退防长跑空跑）写进 config，`solve_pieces` 经 `solve_with_callback_proc(prefix=...)` 在 worker 进程内构造（`select_prefix_plan` 近满幅几何选码无 RNG、跨进程确定性重放，2026-09-02 起；seed 仅兜底 4 片路径消费；手写 config 的存在性/资格码校验由 worker 求解期 fail-fast）；band/prefix on 时 `--lns` 自动 warn 跳过（波段重排会拆散带形态/布头钉位）；示例 `data/configs/`，加载校验见 `cli/config.py`）。`seeds` 是**串行**种子列表（缺省 `[0]`；≥2 个时逐 seed 串行求解）；取代旧 `seed`/`multi_seed`/`seed_count` 三键。产物只落 `out/config_runs/<run_name>_<时间戳>/`（pieces/ + pieces_intermediate.json + result.json **逐轮重写** + curve_s\{seed\}.json / best_frame_s\{seed\}.json 逐帧轨迹 + kill_decisions.jsonl），**不触碰 web 数据**（不写 `out/sparrow_baseline/` 与 `out/uploads/`，可与 ms-web 并行运行互不干扰）。PC-001 起 `solve_pieces` 走 `solve_with_callback_proc` 多进程求解（主进程先 build_instance 取 meta），支持逐帧 `should_stop` 中止（terminate 杀子进程、best-so-far 帧交付，记录加 `killed`/`kill_reason`）；Ctrl-C 退出码 130 不丢已完成轮。PC-002 起多 seed 串行循环经 `cli/portfolio.py` 控制器转发（incumbent banking 帧级全局最优 + R0 达标即停 `--target`，`best`/`portfolio` 段语义见 README「配置驱动求解」）。PC-003 kill 引擎 `--kill`（默认 shadow 只记 `kill_decisions.jsonl` 不真杀；on 需 `--params` 标定就绪 `calibrated: true` 否则降级 shadow；引擎仅 `--target` 给定时激活，seed 1 永不 kill，R0 恒用真 target）。PC-004 标定管线 `python -m materialsorting.cli.calibration`（batch 跑批 base 28 seed 曲线 / variants 确定性订单邻域变体 ±1 抖动 4 个 / analyze 聚合出 `controller_params.json`（包络 S(τ) + kill 参数推荐，小样本拒绝下发）+ 泛化报告；产物只落 `out/portfolio_calibration/<tag>/`，真实跑批 ≈2h 属运营步骤）。PC-005 simulate ETT 离线仿真器：同总预算（k×B 恒等）回放策略网格（单 seed 基线/best-of-k/kill 三档/θ 衰减两档/US-003 策略双档 se180·race180 —— 走 base/{short,full} 同 seed 配对曲线、跨 fork 诚实口径、判据复用 US-001 单一真相源，另出 E[max] 口径副表：delivered 期望/漏 max 率/对配对 uniform90 基线增益）→ `analysis/simulation_report.json` + 推荐参数档。PC-006 起 `build_instance`/`solve_pieces` 可选 `solver_opts` （spyrrow 旋钮白名单 exploration_pct/quadtree_depth/num_workers/early_termination——et 仅收严格 bool、2026-08-29 入白名单并显式透传，缺省 True 行为不变；清洗单一真相源 `web.solver._normalize_solver_opts`）经 `--solver-opts`（全 seed 固定档）/ `--rotate-opts`（内置 4 档池逐 seed 轮换去相关，含默认空档；两旗标互斥，坏 JSON 退出 1）透传，WS 协议与 web 前端零改动（base 与变体 held-out 双达标：ETT 均不劣于单 seed、误杀率均 <5%，`params` 键与 `--params` 同构）；截断轨迹终值用「kill 时刻 best + 条件期望增量」插值（下界 ≥ best-so-far，无 oracle）；`--shadow-log` 统计真实 would-kill 假阳性。PC-007 起 `cli/lns.py` LNS 波段重排核心（`ms-lns --run-dir <dir> --time --rounds [--band-width]`：对 run_dir 最优布局做波段级 ruin-and-recreate，段内同口径子实例多进程重解、新段更窄才接受、validate+y 双复检失败回退、无改进输出逐字节不变；产物 result_lns.json + lns_compare.svg 落 run_dir）。PC-008 起 `ms-run-config --lns [--lns-time 30] [--lns-rounds 5]` 把该后处理接入编排：portfolio 跑完（含 R0 提前停）自动对 incumbent 跑 `lns.postprocess_run_dir`（与 ms-lns 同一代码路径），**严格更优才回写** result.json（incumbent 三字段更新 + `lns` 段前后对比/轮次明细，best 同步），不优则逐字节不变（明细仍写 result_lns.json）；回写一次性整体重写，Ctrl-C 不半写（退出码 130）；从属旗标单独给出/值 <1 退出 1，LNS 输入错误降级 warn 跳过。PC-009 起 run 结束（含 R0 提前停/kill 路径）追加一行 JSONL 到 `out/run_stats.jsonl`（`{ts, source, sizes, class_key, seeds, target, best_density, n_killed, elapsed_total, config{time,per_type,quantities,band?,prefix?}}`，class_key=sha1(source+sizes+quantities+per_type) 10 位短哈希（band/prefix on 时各追加组件成新 key（band label / `front+back`）——None 不加组件与旧口径逐字节一致，避免 ±2pt 级密度差混同 θ₀ 分布）；写失败只 warn 不阻塞），`--target` 模式启动读该库做 θ₀ 校准（同 class_key ≥5 条 → `θ₀=min(target, 历史最大 best_density+0.003)`，只降 kill 门槛、R0 恒用 --target）。US-001/002 起策略双模式 `--strategy [se|race]`（给定总预算拿更高利用率；race 门杀默认档：每 seed 180s 预算、90s 门处严格破 bar 才续跑、省出预算再投资、首 seed 豁免；se 筛延：k×90s 筛选 + 冠军同 seed 全新 180s 延长，边车 best_frame_s{seed}_ext.json；`--time` 策略模式 = 总预算必填 T≥275、`--kill` 与 `--strategy` 同给退出 1、`--target` 共存 R0 优先；名义记账 92.5s/182.5s；确定性重放保证两模式期望等价 —— 现场对跑 88.38%=88.38% 精确平、20min 起比均分 +0.2pt；产物 strategy.json；无旗标零回归）。**极限运行 `--extreme`（2026-08-30，方案 `.docs/business/极限运行功能方案_race门杀.md`）**：糖衣旗标 = `--strategy race` + `--extreme-budget 600|1200`（缺省 600；2400s+ 门判别力失效是硬边界）+ `--race-gate 0.5` + 固定 solver_opts `EXTREME_SOLVER_OPTS`（p0.70/early_termination=False/workers4，quadtree_depth 用缺省 4 —— 5336 pctgrid 实验结论固化非可调项）——与 --strategy/--kill/--solver-opts/--rotate-opts 及策略从属旗标任一同给退出 1、`--time`=总预算必填（600 档最低 905s）；目标从「期望最优」换「右尾最优」（best-of-k 长跑）；run_stats config 段 additive `extreme:{budget}`（class_key 不变）；band/prefix 经 9 键 config 照常生效（worker 进程内成带/构造）。web 端 `/api/extreme/*` 四路由（strategy.py mode='extreme' 家族分支 `_start_run/_status_common/_stop_common/_result_common`，与高级运行共用每会话状态槽 ⇒ 同会话 409 单飞互斥——三臂并行实测 CPU 争抢截断墙钟预算）+ 前端独立「极限运行」按钮/弹窗（载荷透传 band/prefix，2026-08-30 解禁）；同总预算 4h 三臂对拍 extreme 91.7107% ≥ race 默认 91.3114%（验收报告 `.docs/business/极限运行_AB验收报告.md`）。web 策略入口（`web/strategy.py` spawn `ms-run-config --strategy` 子进程）2026-08-22 起 start 时 best-effort 自动清理上一轮 web 产物（磁盘收敛；前端结果态不再展示 run_dir 服务器路径）；**多会话 US-004（2026-08-27）**：四路由读 `X-Session-Id`（缺省 default），每会话一份状态槽 + 每会话 409 单飞（跨会话并发放开），run_name 嵌 sid 短缀 `web_<sid6>_<mode>_<rand6>`、run_dir 认领 = 确定性前缀 glob（并发安全），产物清理/树杀/orphan 均按本会话 sid 前缀隔离。详见 README「配置驱动求解」。

## 已知问题（待清理，勿在迁移中扩大改动）

- `sparrow_baseline.py` 职责混合（既是 CLI 入口又是共享层），未拆分 `engine_core.py`。

## 规则与方案文档

排料 v0.3 规则、各阶段（0/1/1c/2）规划、DXF 解析架构、工作台实现/导出方案均在 [.docs/](.docs/)（技术速查/代码地图在 `technical/`、业务规则/方案/反馈在 `business/`）。权威约束 spec 是 `.docs/business/排料规则_详细版.md`。
