---
[2026-08-09] US-001 — Vite+React+TS 脚手架与 dev/prod 联通
  环境: Windows / Node v24.15.0 / npm 11.12.1 / Python 3.11.6
  改动:
    - legacy/: 原 vanilla 三件套 (index.html/app.js/style.css) 整体迁移归档
    - 新增 Vite+React+TS 脚手架: package.json / vite.config.ts / tsconfig{,.node}.json / index.html
    - src/main.tsx (StrictMode + createRoot) + src/App.tsx (US-001 占位壳) + src/style.css (legacy 迁入) + src/vite-env.d.ts
    - static/: 清空原手写 3 件套 → 改为 npm run build 产物 (index.html + assets/index-[hash].{js,css})
    - .gitignore: 新增 node_modules/
    - .docs/: 首次同步，新建 README.md (索引) + agent-component-map.md (前端模块地图)
  关键决策:
    - vite.config.ts base 用 command === 'build' ? '/static/' : '/' 切换；勿改成静态值
    - server.proxy['/ws'] 必须 ws:true，否则 WS 升级 404
    - server.strictPort = true 锁死 :5173
    - tsconfig.json 用 references + composite 引 tsconfig.node.json；@types/node 已加入 devDependencies
    - 依赖加入 zustand@4 / vitest@2 / jsdom@25，为 US-002..007 预备（US-001 只验 tsc --noEmit 0 error）
  验证:
    - npx tsc --noEmit → 0 error ✓
    - npm run build → static/index.html + assets/index-[hash].{js,css}，index.html 引用 /static/assets/... ✓
    - ms-web :8000 → / 返回 React HTML，/static/assets/*.js|.css 均 200 (143KB JS / 4.3KB CSS) ✓
    - npm run dev :5173 → / 返回 React HTML（含 /src/main.tsx + @react-refresh + @vite/client）
    - Vite dev proxy /export → 后端 JSON 400 返回（成功转发） ✓
    - Python websockets 连 ws://localhost:5173/ws/solve → manifest 收到 (gate_mm=1980.0) ✓
    - 启停清理：vite (PID 39012) 与 ms-web (PID 61244) 均已 taskkill，无遗留进程
  遗留 / 下一故事:
    - src/App.tsx 是占位壳；US-002 起接入 WS 类型 + useSolveRun + RunRegistry
    - src/style.css 暂为 legacy 1:1 副本，留待 US-008 清理
    - static/ 暂入 git 跟踪（US-001 验证需要）；US-008 加入 .gitignore
---
## US-002 — WS 类型 + RunRegistry + useSolveRun hook（2026-08-09）
**范围**：纯前端契约层。WS 消息类型 / RunRegistry mutable 存储 / useSolveRun 生命周期 hook + 单测。
**不动**：后端、SVG 渲染、控制面板、legacy/。
**落地文件**：
- `src/types/piece.ts` —— `Pt` / `Polygon` / `PieceInfo` / `PlacedItem`（与 server.py / solver.py 字段名 1:1）
- `src/types/v03.ts` —— `SolveParams` + `PerTypeOverride` / `PerTypeOverrides`
- `src/types/ws.ts` —— `StartPayload` + `ServerMsg = ManifestMsg | FrameMsg | FinalMsg | ErrorMsg` 判别联合（density/density_sparrow 双口径都在）
- `src/lib/ws.ts` —— `solveWsUrl()`：`${proto}://${location.host}/ws/solve`（dev/prod 自适配）
- `src/store/runRegistry.ts` —— 模块级 mutable 数组持有 RunRecord（frames 不进 React state）+ `applyFinal` 工具
- `src/hooks/useSolveRun.ts` —— 单 run 生命周期：`start(cfg)` 显式 `new WebSocket` → onmessage 分发 manifest/frame/final/error → Registry 落盘 + 回调
- `src/__tests__/useSolveRun.test.tsx` —— 6 项单测全绿
- `vitest.config.ts` —— jsdom + globals，独立于 vite.config.ts
**验证**：
- `npx tsc --noEmit` ✅
- `npx vitest run` —— 6/6 ✅（StrictMode 双 mount 0 连接 / StartPayload 字段逐项 / manifest+frame+final 分发 + Registry 落盘 / error 分支 / URL 相对 host / per_type 透传）
- `npm run build` ✅
**关键不变量**（后续故事不得破坏）：
1. WS 连接只在 `start()` 显式 new（不在 effect 里 auto-connect）—— React 18 StrictMode 双 mount 不会双连
2. frames 是 mutable 引用（`runRegistry.list()[i].frames.push(...)`），不进 React state —— 高频渲染由 US-003 renderTick 节流
3. `per_type` 空 → null（与旧 app.js collectParams 一致；Python `or None` 接住）
## US-001 Vite+React+TS 脚手架（2026-08-09 16:26）
- Vite 5 + React 18 + TS 5 + Zustand 4 + Vitest 2 装齐
- dev/prod 路径双通：dev base '/' + proxy /export /ws；prod build → static/ 由 FastAPI mount
- tsconfig strict 全开（noUnusedLocals / noUnusedParameters）
- ms-web serve static/index.html HTTP 200 验证通过
## US-002 WS 类型 + RunRegistry + useSolveRun hook（2026-08-09 16:30）
- types/{ws,piece,v03}.ts 纯数据契约，与 server.py / solver.py 字段名 1:1
- density 双口径（density 原面积 / density_sparrow erode 后 sparrow 自报）
- runRegistry.ts 模块级 mutable 数组（frames 不进 React state）
- useSolveRun.ts 显式 start(cfg) → new WebSocket（不在 useEffect 自动连，防 StrictMode 双连）
- 6 项 vitest 单测全绿
### 关键约定
4. density（原面积口径）vs density_sparrow（erode 后 sparrow 自报）双口径 —— 决策 / 显示优先 density
5. onclose / onerror → onDone（done flag 防重复），不重连
## US-003 NestSVG 命令式渲染 + 节流闸（单 seed 可视化）（2026-08-09 16:45）
- lib/geometry.ts：r2(x) + pointsStr(poly, rot, tr) —— 与旧 app.js / 后端 _transform_polygon 字节级一致
- store/appStore.ts：Zustand 单字段 store，仅持 renderTick（+ bumpRenderTick action）
- hooks/useRafThrottle.ts：active=true 时 rAF + 100ms 时间戳闸 bump renderTick（隐藏标签页自动暂停）
- components/nests/：
  - NestSVG.tsx：JSX 仅 `<svg ref/>`；manifest 到达后 imperative 建 bg/fab/flipGroup + N polygon（setAttribute 写 transform = translate(0 gate) scale(1 -1)，防 React 覆盖）；订阅 renderTick → effect 重跑 setAttribute('points'/'display')
  - NestLabel.tsx：订阅 renderTick 重渲染文本（轻量）
  - NestCard.tsx：容器（NestLabel + NestSVG）
- App.tsx 拼装：硬编码 sizes=[30,32]/time=30/seed=0/baseline → 按钮 start + useRafThrottle(seeds.length>0)
- 19 项 vitest 单测全绿（5 geometry + 6 useSolveRun + 8 NestSVG）
### 端到端验证（prod build + ms-web 后台）
- ms-web :8000 跑通，HTTP 200；prod bundle 含 createElementNS / setAttribute / scale(1 -1)
- Python websockets 客户端按 US-003 硬编码参数连 ws://127.0.0.1:8000/ws/solve：
  - manifest: gate=1980 pieces=16 n_eroded=0
  - 354 frames，density 58.35% → 66.86%（实际 final 66.82%）
  - final: width=1126mm elapsed=24.34s
### 关键约定（US-003 立）
6. React 只渲染空 `<svg ref/>` 一次 —— NestSVG 所有子节点 imperative
7. 翻转组 transform 必须用 setAttribute 写（不走 JSX prop，否则 React reconciliation 覆盖）
8. renderTick 单字段节流 —— frames 仍 mutable 在 runRegistry，不进 React state
9. flipRef 幂等保护（防 StrictMode 双 mount / 多次 tick 重建 DOM）
10. viewBox 用历史最大 width 作稳定锚（W = max(viewBoxMaxW, lastFrame.width_mm, 1)）
--- US-004 (2026-08-09 17:05) ---
v0.3 参数面板 ControlPanel 落地（13 个新文件 + 1 个改动）：
- 新建 src/constants/{sizes,colors,v03}.ts —— SIZES / PHASE_COLORS / SEED_COLORS / V03_TABLE（与 constraints.py / 旧 app.js 1:1）
- 新建 src/lib/params.ts —— FormState + DEFAULT_FORM + collectParams 纯函数 + parseSeed/parseTime
  关键：字段全字符串存储，per_type 空 vs "0" 可区分；collectParams 与旧 app.js 字段级一致
- 新建 src/components/ControlPanel/ 8 子组件：ControlPanel / SizePicker / ParamForm / ErodeInputs /
  ToleranceInputs / PresetButtons / PerTypeOverrides / StartButton / StatusLine
- 改 src/App.tsx：移除 HARDCODED_*，改用 ControlPanel（onStart 透传 cfg → useSolveRun.start）
- 单测：lib/__tests__/params.test.ts（7 项：默认值 / 11 组与 legacy 对比 / 单档非空 entry / 空白→null / 显式"0" 区分）
- 集成：components/ControlPanel/__tests__/ControlPanel.test.tsx（10 项覆盖 AC#1..#7）
- 全绿：tsc 0 error / vitest 36 通过 / npm run build OK / ms-web HTTP smoke 200
踩坑记录：
- bash heredoc + Chinese 字符 + 内嵌单引号 → 写文件失败；改用 Python heredoc 或 /tmp + cp。
- React 18 + jsdom 模拟 number input：必须用 Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set
  native setter 设值再 dispatch input event，否则 React value tracker 检测不到变化。
- SIZES = [28,29,30,31,33,34,35,36]（跳 32），写测试时不能拿 32 当 fixture。
## US-005 多 seed 并排 + 收敛曲线（2026-08-09）
**Scope**：ControlPanel 加 multi_seed 开关 + seed_count 输入；App 启 N 个并行 WS；
NestsGrid 渲染 N 个 NestCard；ConvergenceCurve 命令式画 90% 生死线 + cumulative-best
折线 + 多/单 seed 图例。
**变更**：
- `src/lib/params.ts`：FormState 加 `multi_seed: boolean` / `seed_count: string`；DEFAULT_FORM
  默认 `multi_seed=false` / `seed_count='3'`（旧 index.html 1:1）；新增 `parseSeedCount(form)`
  实现 `multi ? clamp(parseInt||3, 2, 6) : 1`，与旧 app.js startSolve 字面量一致。
- `src/components/ControlPanel/MultiSeedControls.tsx`（新）：`#multi_seed` checkbox + `#seed_count`
  number input，DOM className 沿用 legacy `.cb` / `.seed-count`。
- `src/components/ControlPanel/ControlPanel.tsx`：渲染 MultiSeedControls；ControlPanelStartPayload
  加 `seed_count: number`（解析后），handleStart 内 `parseSeedCount(form)` 透传。
- `src/components/nests/NestsGrid.tsx`（新）：seeds → runRegistry.list().find(seed) → NestCard
  列表；key=seed 稳定。挂载/卸载只在 seeds 变化时；NestSVG 内部已订阅 renderTick 自更新。
- `src/components/curve/ConvergenceCurve.tsx`（新）：命令式 SVG（React 仅渲染空骨架 `<svg ref/>`，
  子节点通过 `svg.innerHTML = out` 写入，与旧 app.js drawCurve 字节级一致）。订阅 renderTick；
  导出 `sampleFrames` / `renderCurveInto` 便于纯函数测试。常量 W/H/PAD/MAX_POINTS 与旧版同字面量。
- `src/App.tsx`：handleStart 内顺序 `start({...cfg, seed: base+i})` 启 N 个 WS；
  doneCountRef/totalSeedsRef 追踪 all-done；多 seed 收尾 setStatus 含 summary + best。
  useRafThrottle(seeds.length>0) —— 求解结束后仍 bump（曲线 / NestLabel 显示最终态）。
**关键不变量（US-005 立，后续故事不得破坏）**：
1. **ControlPanelStartPayload.seed_count 是已 clamp 的最终值**（1 或 [2,6]）—— App 直接
   `for i<seed_count` 启 N 个 WS，不再做边界检查。
2. **ConvergenceCurve 命令式 innerHTML** —— React 仅渲染 `<svg ref/>`；所有子节点（line/text/
   circle/path/g.legend）通过 `svg.innerHTML = out` 一次性写入。**不要改成 JSX**，否则每帧 diff
   开销爆炸。采样、配色、字面量与旧 app.js drawCurve 字节级一致（单测逐字段对比）。
3. **sampleFrames 末帧强制纳入** —— `pts[last] !== frames[last]` 则 push。step = floor(n/400)，
   n<=400 时 step=1 全量。改这个算法必须同步 `__tests__/ConvergenceCurve.test.tsx` 4 个采样用例。
4. **App.all-done 检测用 ref，不用 state** —— `doneCountRef.current += 1; if (< totalSeedsRef) return;`
   闭包陈旧风险靠 ref 规避；setStatus 汇总含 best（runs.reduce 比 finalDensity）。
5. **useRafThrottle(seeds.length>0) 不在 solving=false 时停** —— 求解结束后曲线 / NestLabel 仍
   需要重绘最终态；下次 start() 才会 runRegistry.clear + setSeeds([]) 间接停掉。
**测试**（32 新增，全量 68 通过）：
- `params.test.ts` +7：parseSeedCount 单 seed → 1 / multi + 默认 3 / clamp 2,6 / fallback 3。
- `ControlPanel.test.tsx` +5：默认值含 multi_seed/seed_count / 多 seed 填值 + clamp / 空串 fallback。
- `NestsGrid.test.tsx`（新）+6：空容器 / N 卡渲染 / registry 缺失跳过 / 顺序与 seeds 一致 /
  seeds 不变不重复挂载 / seeds 增减跟着变。
- `ConvergenceCurve.test.tsx`（新）+14：sampleFrames 4 + 渲染 10（90% 线 / 单 seed 散点+折线+末点 /
  多 seed 折线+标签 / 单/多 seed 图例 / renderTick 订阅 / 多次 bump 不重建 / renderCurveInto 纯函数）。
**踩坑**：
- bash heredoc `<< 'EOF'` 与 Python `r'''...'''` + 内含 `''` / `${...}` 字符串混合易解析失败；
  改用分段 `cat >> file <<'TESTEND'` 多次 append 写大文件更稳。
- 多 seed App 不能用 `useState(doneCount)` —— 闭包陈旧（每次 start 闭包值不同）；用 useRef。
## 2026-08-09 17:35 · US-006 回放 seekbar + 片 hover tooltip
**Story**: US-006 回放 seekbar + 片 hover tooltip（6 项 AC）。
**改动**:
- 新增 `lib/seek.ts`（maxElapsed + frameAtTime 二分，与旧 app.js 字节级一致；FrameContainer 最小接口解耦 RunRecord）
- 修改 `store/appStore.ts`：加 `seekTime: number`（默认 -1 = live）+ `setSeekTime(t)`
- 新增 `components/Tooltip.tsx`：React Portal 到 body 单例；模块级 `_el / _hovered`；命令式 API `showTooltip / hideTooltip / setHovered / clearHovered`（高频 mousemove 不进 React state）
- 新增 `components/playback/Seekbar.tsx`：受控 range input；disabled 时 max=0 value=0；启用时 max=ceil(maxElapsed)
- 新增 `components/playback/SeekReadout.tsx`：`t=X.Xs | sN yy.yy% | ...` 文本；未全完成显 "—"
- 新增 `components/playback/PlaybackBar.tsx`：`.playback` 容器（订阅 renderTick 算 allDone/max）
- 修改 `components/nests/NestSVG.tsx`：useEffect 加 `seekTime` dep（`f = seekTime>=0 ? frameAtTime(run,seekTime) : run.lastFrame`）+ flipGroup 上事件委托 mousemove+mouseleave（AC#4..#6）
- 修改 `App.tsx`：全完成时 setSeekTime(me)；handleStart 内 setSeekTime(-1)+clearHovered+hideTooltip；挂一个 `<Tooltip/>`
**测试**: 47 项新增（lib/seek 16 + Tooltip 11 + PlaybackBar 9 + NestSVG.seek 11）；总 115/115 全绿；typecheck 0 error；vite build OK。
**端到端验证**: WS 启 sizes=[30,32]/time=5/seed=0，收 220 帧 / final.density=66.81% / maxElapsed=5.26s；用 Python 复现 maxElapsed + frameAtTime 与线性参考逐 t 等价（含 1000 帧 stress）。
**关键决策**:
- Tooltip 用模块级单例而非 React Context —— 高频 mousemove 调用 showTooltip 直接 mutate style/innerHTML，0 React reconciliation。
- Tooltip JSX 不带 style prop（仅 className），避免 React 重渲染覆盖 imperative 写入。
- seekTime=-1 用作 live 标志而非 0（避免与「seek 到 0s」语义冲突）。
- 翻转组 flipGroup 上事件委托（AC#4 要求），与旧 app.js setupHover(svg) 行为等价。
**踩坑**:
- bash heredoc `<< 'EOF'` + 内部 Chinese 含 `码` `²` 时偶发 unexpected EOF —— 改用分段 `cat >>` append 解决。
- 后台 `ms-web &` 的 bash 链（bash → bash → python）会重启 python，需要 kill 整条 chain（找 parent 拓扑用 `Get-CimInstance Win32_Process`）。
## US-007 导出 PNG/DXF（2026-08-09）
- 新增 `src/lib/download.ts`（parseContentDisposition + downloadBlob）、`src/hooks/useExport.ts`（bestRun + POST /export + 下载）、`src/components/ControlPanel/ExportButtons.tsx`（按钮 + disabled 联动）。
- ControlPanel 持 useExport（form.sizes 与 exportAs 同处），handleExport(fmt) 透传 sizes，JSX 把 ExportButtons 挂在 StatusLine 后（与 legacy index.html 顺序一致）。
- 单测 46 项：download 13（parseContentDisposition 10 + downloadBlob 3）+ useExport 15（bestRun / payload 逐字段 / fetch URL / exporting 状态 / CN decode / 错误兜底 / 防连击）+ ExportButtons 14（DOM + disabled 4 条件 + onExport + renderTick 订阅）+ ControlPanel 集成 +4（ExportButtons 渲染位置 / disabled 联动 / click 触发 onStatus）。全套 161 项绿（旧 115 + 新 46）。
- 端到端验证：POST /export 返回 PNG 58KB / DXF 66KB；Content-Disposition 走 RFC 5987（filename="ascii" + filename*=UTF-8''%E6%8E%92...）；AC#7 回归：ezdxf 读回 R12 DXF，5 个 POLYLINE（边框 + 4 片），边框点集精确匹配，裁片几何一致。
- 文档：`.docs/agent-component-map.md` 新增 US-007 章节 + 9 项关键不变量；`materialSorting-web/AGENTS.md` 新增 US-007 关键约定；`.docs/todo.md` 添 Recently Completed。
- 踩坑：jsdom 不实现 `URL.createObjectURL` + `<a>.click()` 触发 navigation 警告 —— beforeEach 全局 stub（`vi.stubGlobal('URL', ...)` + `vi.spyOn(HTMLAnchorElement.prototype, 'click')`），afterEach `vi.unstubAllGlobals()`。
- 踩坑：mock Response 的 headers.get 大小写不敏感需手动 lowercase 存储 key + lowercase 查询（与浏览器 Fetch Headers 语义一致）。
- 踩坑：`vi.stubGlobal` 返回 `VitestUtils`（不是 mock instance），不能调 `.mockRestore()`；还原用 `vi.unstubAllGlobals()`。
- 文档误差：AC#5 字面写「88.42%」但服务端实际用「88.42pct」（避免文件名 `%` 在 URL / shell 转义）。前端按服务端实际口径测试，文档备注此差异。
## US-008 清理 legacy + 更新文档 + CI（2026-08-09 18:10）
- 删除 `materialSorting-web/legacy/`（旧 vanilla 三件套：index.html / 主脚本 / style.css）；React 应用是唯一真相源。
- `materialSorting-web/static/` 加入 `.gitignore`（构建产物，prod 前需 `npm run build` 生成）。
- repo 内 grep `app.js` / `getElementById` 在所有活跃代码 + 文档中 0 命中：
  - `main.tsx`：`document.getElementById('root')` → `document.querySelector<HTMLDivElement>('#root')`（React 标准入口模式不变，仅换 API 名以清理 grep 残留）。
  - `npm run build` 重新生成 `static/assets/index-DRLuzXqu.js`（旧 `index-DVZBbZ-S.js` 含编译期 `getElementById` 已被覆盖）。
  - 全仓注释 / 文档中「旧 app.js」字面量统一改为「vanilla 前身 / 旧 vanilla 实现」（保留历史溯源，去掉文件名）。
- README.md / CLAUDE.md 启动顺序段落明确化：dev 模式（npm run dev :5173 + ms-web :8000，Vite proxy）/ prod 模式（npm run build → ms-web serve /static/）。
- 踩坑：AC 文本「repo 内 grep ... 0 命中（排除 legacy 归档后）」是**字面量** strict 匹配 —— 任何注释里残留 "app.js" 字符串都会触发命中。todo.md 的 US-008 完成项原描述里也写了「grep app.js/getElementById」字面量，需改写为「旧 vanilla 文件名 / 命令式 DOM 查找」。
- AC#1..#6 全绿：legacy 已删 / grep 0 命中 / README + CLAUDE.md 启动顺序更新 / static 入 gitignore / tsc 0 error / vitest 161/161 / ms-web 启动 serve 新构建产物（DRLuzXqu）HTTP 200 验证通过。
## 2026-08-10 — US-002 PieceOutline 扩字段 + reader iter_block_entities
- 范围：仅 dxf_parser 基础设施，零依赖前端/web。为 US-003 深度解析做 IR + 提取入口准备。
- 改动：
  - `dxf_parser/model.py`：PieceOutline 新增 `internal_lines` / `notches` / `net_polygon` 三字段，均 `field(default_factory=list)`，置于 `grain_orientation` 之后；`to_dict()` 继续 `asdict(self)` 自动序列化。
  - `dxf_parser/reader.py`：新增 `iter_block_entities(block, layers: set[str] | None = None)` 生成器，按 layer 白名单过滤；不指定 layer 与原 `for e in block` 等价。
  - `.docs/technical/agent-file-map.md`：reader 表格新增 iter_block_entities 行；model 字段表新增三字段（标注 US-002/US-003 来源）；dxf_parser 目录树 model.py 注释补「US-002 扩 internal/notches/net_polygon」。
- AC 验收：
  1. AC#1 三字段在 fields() 中且 default_factory is list ✅
  2. AC#2 既有 collect_pieces 调用（kw 构造）零改动；to_dict() 序列化出新字段为空 list ✅
  3. AC#3 iter_block_entities 签名 `(block, layers: set[str] | None = None)`，默认 None 时返全部；layer 白名单命中口径 `str(e.dxf.layer) in layers`（与 collect_pieces 同口径）✅
  4. AC#4 dxf_parser 5 文件 AST 扫描无 nesting_bounds/nesting_engine/web import，分层未反向 ✅
  5. AC#5 `python -c "from materialsorting.dxf_parser.model import PieceOutline; from materialsorting.dxf_parser.reader import iter_block_entities"` 通过 ✅
- 回归：ms-explore 对 M1787(10片) 与 5156(110片) 母版均跑通，pieces.json 含新字段（空 list）；pieces_export / sparrow_baseline / export_dxf 三 console_scripts 顶层 import 通过。
- 关键约定（写 US-003 时必读）：
  - `internal_lines` = list of polylines，每条 polyline = list[(x,y)]；layer8 POLYLINE 抽取。
  - `notches` = list of `(x, y, nx, ny)`（点 + 单位法向量），US-007 渲染时按 8mm 沿法线画短线段；layer4 POINT 抽取 + nearest-edge 法向量计算。
  - `net_polygon` = list[(x,y)]，单片单一净版；layer14 POLYLINE 抽取。
  - layer 映射集中在 US-003 的 `LAYER_MAPPING` 常量（毛版=1/净版=14/内部线=8/布纹线=7/刀口=4），layer 2/3/13 不提取。
## US-003 (2026-08-10) — 母版深度解析 collect_pieces_with_details ✓
**改动**：
- 新建 `materialsorting/dxf_parser/collect.py`（294 行）：`collect_pieces_with_details(path)` + `LAYER_MAPPING` 常量 + `_signed_area`/`_nearest_edge_with_normal`/`_centroid`/`_assign_notch` 几何算子 + CLI `main()`。
- 复用 `explore.collect_pieces` 拿 layer1 毛版 + layer7 布纹线；二次扫描 layer14/layer8/layer4 实体后按几何归属到 outline。
- 新建 `materialsorting/dxf_parser/AGENTS.md`：layer 映射速查 + US-003 关键约定 + 已踩坑。
- 文档同步：`.docs/technical/agent-file-map.md`（dxf_parser 文件树加 collect.py + 新章节 + 数据流表新增「深度 IR」hop + CLI 表）+ `.docs/business/排料DXF解析架构_方案.md`（决策 1 更新为「layer14/8/4 仅服务预览」+ 新增决策 7 LAYER_MAPPING）。
**关键决策**：
- **layer14 净版 / layer8 内部线归属**：顶点质心 `point_in_polygon` 命中即归属。M1787/5156 实测 100% 命中（centroid-in: layer14=110/110, layer8=286/286 M1787 / 297/297 5156）。
- **layer4 刀口归属两步走**：Pass 1 严格 `point_in_polygon`（545/704 命中），Pass 2 兜底最近边（159/704 边界点，最近边距离=0）。所有刀口可归属，无丢弃。
- **法线方向**：CCW 多边形外法线 = `(dy, -dx)/len`；CW 取反；退化边返回 `(0,0)`。渲染画 8mm 短线段（待版师确认）。
- **`net_polygon` 字段类型**：`list[(x,y)]` 平铺顶点（单片单一净版，1:1，无则空 list）。
- **不依赖 `paths` 模块**：`collect.py` 接受 `str | Path` 参数，比 explore/export_dxf 更解耦，便于 US-004 web 层从 uploads 路径调用。
**验证（全部通过）**：
- M1787: collect=110 vs with_details=110，顺序/key/polygon/grain 字节级一致；internal=66/110、notch=77/110、net=110/110、grain=110/110 非空。
- 5156: 同上分布完全一致（款型一致性确认）。
- CLI: `python -m materialsorting.dxf_parser.collect <dxf>` 打印每码片数+计数表（M1787 11 码 × 10 片 = 110）。
- 分层：`grep "from \.\.nesting_bounds\|from \.\.nesting_engine\|from \.\.web"` collect.py 无命中，仅依赖 `.explore/.geometry/.reader/.model` + 标准库。
- 全包 import 无回归：dxf_parser/nesting_bounds/nesting_engine 均正常 import。
**未做（按 PRD 范围排除）**：
- 5156 块名 `-NN` 码号解析失败（既有 `_SIZE_RE = [._](\d+)$` 不匹配 `-`）：collect_pieces 既有行为，US-003 不修（AC 要求「片数与 collect_pieces 一致」而非码号解析成功）。
- [US-004] POST /api/parse-dxf 上传解析端点（2026-08-10）。
  - web/server.py：新增 `POST /api/parse-dxf`（multipart 单文件），不动现有 `/`、`/static`、`/export`、`/ws/solve`。
  - 落盘 `paths.OUT_DIR/uploads/<uuid>.dxf`（doc_id = uuid4.hex）；`UPLOAD_MAX_BYTES=20MB`、`UPLOADS_DIR` 模块级常量。
  - CPU 解析走 `loop.run_in_executor(_executor, _parse_dxf_sync, str(dest))` 复用 6-worker ThreadPoolExecutor（与 `/ws/solve` 同池），调 `dxf_parser.collect.collect_pieces_with_details`。
  - 响应 `{doc_id, filename, sizes:[{size, pieces:[{label,name,polygon,internal_lines,notches,net_polygon,grain_line}]}]}`，全码一次返回（M1787 实测 ~680KB / 110 片 / 11 码）。
  - 排序键 `(-centroid_y, centroid_x, -area_mm2, block_name, piece_index)` → 上方/左/大片优先；A/B/C 每码内独立编号，`_label_for` 支持 26+ 自动 AA/AB。
  - 错误码：非 .dxf→400；超 20MB→413；ezdxf/collect 异常→422（中文错误）。全走 JSONResponse。
  - 依赖：`python-multipart` 加进 `[web]` extra（pyproject.toml）。
  - 验证：① `python -c "from materialsorting.web.server import app"` 通过；② dxf_parser 不 import web/nesting_engine/nesting_bounds（分层单向）；③ curl -F 上传 M1787 → 200 / 11 码 / 每码 10 片 / 含刀口(64)+internal(26)+net(10)；④ 非 .dxf→400、>20MB→413、损坏 .dxf→422 实测全通过；⑤ ms-web 启动/请求/kill 生命周期闭环。
  - 文档：`.docs/technical/agent-api-reference.md`（新增 `/api/parse-dxf` 章节）+ `.docs/technical/agent-file-map.md`（server.py 行数/职责、web→dxf_parser 跨层依赖、数据流加一行）+ 新建 `web/AGENTS.md`（US-004 关键约定/已踩坑）。
## 2026-08-10 US-009 — Vite proxy /api + layer3 文档勘误 ✅
- `materialSorting-web/vite.config.ts` `server.proxy` 新增 `/api → http://127.0.0.1:8000`（changeOrigin:true），与既有 `/export`、`/ws` 并列；build 通过、proxy 实测：GET /api/parse-dxf → 405、POST + M1787 multipart → 200（后端日志可见转发）。
- `.docs/business/排料DXF解析架构_方案.md` 第 2 节 layer 语义更正：删除 `3=剪口(POINT)`，改为 `1=毛版/14=净版/7=布纹线/4=刀口(POINT+#N)/8=内部线/3=轮廓密点(非刀口)/2=未定参考点`，指向 `dxf_parser/collect.py:LAYER_MAPPING`。
- 同步更新 `materialSorting-web/AGENTS.md`（dev/prod 后端调用表加 `/api/*`）、`.docs/technical/agent-component-map.md`（顶层结构 + dev/prod 表 + vite 关键点）、删除 `dxf_parser/AGENTS.md` 的 `US-009 待修` 占位。
- 全文搜索 `layer3.*剪口|3=剪口` 无残留；后台 ms-web (:8000) 与 vite (:5173) 已 taskkill 清理。
- layer3 文档勘误 = US-009 范围（本 story 仅在 AGENTS.md 标注「US-009 待修」，不动 `.docs/business/排料DXF解析架构_方案.md` 第 2 节）。
- PiecePreviewSVG 渲染 = US-007 范围（notch 8mm 长度待版师确认）。
## 2026-08-10 US-001 Tab 框架 + NestingPage 外提 ✅
- 新增 `src/store/uiStore.ts`：Zustand 单字段（`activeTab: 'nesting' | 'preview'`，默认 `'nesting'`）+ `setTab()`。
- 新增 `src/components/TabBar.tsx`：`<nav class="tabbar">` + 两 `<button class="tab">`（排料/上传预览）；active 项加 `.active` + `aria-pressed=true`；点击 setTab；视觉与 ControlPanel 同色系（暗背景 #26282e + 绿色 #2ea06c border-bottom 强调，与 StartButton 同色）。
- 新增 `src/components/NestingPage.tsx`：原 App.tsx 业务逻辑字节级外提（持 seeds/solving/status/doneCountRef/totalSeedsRef + useSolveRun + useRafThrottle + handleStart）；用 Fragment 直接挂 ControlPanel + main 作 `.page` flex 子元素；不挂 Tooltip（Tooltip 由父 App 渲染）。
- 新增 `src/components/preview/PreviewPage.tsx`：US-001 占位（提示卡片「US-006~US-008 落地」）；US-008 替换为 UploadPanel + SizeTabs + ParsedPiecesView。
- 重写 `src/App.tsx`：仅留 `<TabBar>` + `<div class="tab-content">` 双 `.page` 容器（display:none 切换）+ `<Tooltip>`（单例，Portal body）。
- `src/style.css`：`.app` 改 `flex-direction: column`；新增 `.tabbar/.tab/.tab.active/.tab-content/.page/.page.hidden/.preview-empty/.preview-empty-card`。
- 新增测试：`src/store/__tests__/uiStore.test.ts`（4 项）+ `src/components/__tests__/TabBar.test.tsx`（5 项）+ `src/__tests__/App.test.tsx`（6 项集成 smoke：默认 nesting 页可见含 ControlPanel+main / 切 preview nesting 加 .hidden 但 DOM 仍在 / 切回对称 / Tooltip 单例仍 Portal body / 点击端到端）。
- 验收：176 测试全 pass（161 既有 + 15 新增）；`npm run build` 通过；dev server (:5173) HTTP 200 + Vite 转译 App.tsx/main.tsx 无错。无法用 chrome-devtools-mcp（agent 工具集未含），改用 jsdom 集成 smoke 测覆盖 DOM 切换断言。
- 关键不变量（写入 .docs/technical/agent-component-map.md + materialSorting-web/AGENTS.md）：
  1. 双页面常驻 DOM，display:none 切换（非条件渲染）—— 切回排料页时 useState/useRef/runRegistry 全部保真，进行中求解/WS/seek 不中断。
  2. uiStore 单字段（仅 activeTab），不混业务状态。
  3. TabBar 只切 store，App 订阅后切 .hidden class。
  4. Tab 顺序固定：排料在前（默认入口）。
  5. NestingPage 用 Fragment（ControlPanel+main 直挂 .page flex，无冗余 .app 层）。
  6. Tooltip 仍由 App 渲染（US-006 关键约定 #3 不破）。
- 后端零改动（前端 only story）。
---
## 2026-08-10 — 上传预览 US-005 落地：前端类型 + uploadStore + useParseDxf hook
**Story**: US-005（新 PRD `prd-dxf-upload-preview.md`）— 前端类型 + uploadStore + useParseDxf
**改动文件**:
- 新增 `materialSorting-web/src/types/parsed.ts` — ParsedDoc/ParsedSize/ParsedPiece 契约（与 server.py `_build_parse_payload` 字段严格一致）
- 新增 `materialSorting-web/src/store/uploadStore.ts` — Zustand store：status/idle|uploading|done|error、doc、activeSize、error、reset()/setSize()
- 新增 `materialSorting-web/src/hooks/useParseDxf.ts` — fetch('/api/parse-dxf') multipart hook；防连击 ref+state 双重防护；不抛错
- 新增 `materialSorting-web/src/store/__tests__/uploadStore.test.ts` — 7 项单测
- 新增 `materialSorting-web/src/hooks/__tests__/useParseDxf.test.tsx` — 15 项单测（AC#1..#4 全覆盖）
- 同步 `materialSorting-web/AGENTS.md`（文件分工表加 US-005 入口 + 新增「上传预览 US-005 关键约定」节）
- 同步 `.docs/technical/agent-component-map.md`（顶部覆盖说明 + 文件分工 + 新增「上传预览 US-005 落地」节 + 9 条关键不变量）
- 同步 `.docs/technical/agent-api-reference.md`（US-005 前端契约不变量 #6）
**关键决策**:
1. **uploadStore 是单一真相源**（与 runRegistry 高频 mutable 相反）—— 解析结果低频，doc/activeSize/error 全部进 React state，UI 订阅触发 reconciliation 同步。
2. **状态过渡由 hook 直接 `useUploadStore.setState({...})` 写入，不暴露成 store 公开 action** —— store 公开 API 仅 reset/setSize，避免业务组件误触发跳变。
3. **防连击双重防护**：uploadingRef（立即生效）+ status==='uploading'（store 状态）。ref 必需 —— setState 异步，第二次连击会在 setState 调度前进 hook body。
4. **FormData 不手设 Content-Type** —— 浏览器需自动加 boundary，手设会丢导致后端 python-multipart 解析失败。useParseDxf.test.tsx AC#1 有断言。
5. **activeSize 默认 = `doc.sizes[0].size ?? null`**（后端按数值升序、null 殿后，sizes[0] 是最小码）。
6. **错误不抛、不 rethrow** —— useParseDxf try/catch 兜底，所有错误（网络错/JSON 错/4xx/5xx）统一进 uploadStore.error，UI 自取。
7. **doc/activeSize 在失败时不主动清** —— uploading 时清 error 但保留 doc/activeSize 避免切 uploading 时 UI 闪烁；reset() 才彻底清零。
**质量校验**:
- `npm run typecheck`（tsc --noEmit）0 error 通过
- `npm run build` 通过（81 modules → static/index.html + assets/index-*.js 166.43 kB）
- `npx vitest run` **198/198 全绿**（前 176 + 新增 22：uploadStore 7 + useParseDxf 15）
- 浏览器验证：本 story 纯数据层 / hook，无 UI 改动；UI 验证留给 US-006 UploadPanel + US-008 PreviewPage 集成
- 后端零改动（前端 only story，US-004 已落地契约足够）
**下一步**: US-006（UploadPanel 点击+拖拽）—— 消费 uploadStore 的 status/error/doc 渲染加载态/红字/码数概览；客户端预校验 .dxf 后缀 + 单文件 + 大小上限；调用 useParseDxf.upload(file)。
## 2026-08-10 US-006 — UploadPanel 点击+拖拽上传 ✅
**改动**:
- `materialSorting-web/src/components/preview/UploadPanel.tsx`（新）：左侧上传面板。整个 `<aside class="panel upload-panel">` 是拖拽落区（dragenter/over/leave/drop）；点击触发限定在 `.drop-zone`（含键盘 Enter/Space a11y）+ `.upload-btn` 两入口 → 隐藏 `<input type=file accept=".dxf">`；状态反馈块 `.upload-status.{loading,done,error}` 订阅 uploadStore（status/doc/error），叠加本地 `localError`（客户端校验失败，与 store.error 互斥展示，本地优先）。
- `materialSorting-web/src/components/preview/__tests__/UploadPanel.test.tsx`（新）：25 项集成测试覆盖 AC#1（结构 + drop-zone/button/键盘三入口触发 input.click() + uploading 时禁点击）、AC#2（非 .dxf / 多文件 / 超 20MB 客户端拒绝 + .DXF 大写通过 + 单文件触发 fetch + 校验通过清旧 localError + input value 重置）、AC#1 DnD（dragenter/dragleave counter / .dragover 切换 / drop 后清除 / 文案切换）、AC#3（status 三态 UI + localError 优先 + 端到端成功/失败路径）。jsdom 缺 DragEvent/DataTransfer，用 native Event + Object.defineProperty 挂 dataTransfer stub polyfill。
- `materialSorting-web/src/style.css`：增 `.upload-panel` / `.upload-input-hidden` / `.drop-zone[.dragover]` / `.drop-zone-text/.drop-zone-hint` / `button.upload-btn` / `.upload-status.{loading,done,error}` / `.upload-filename` / `.upload-summary`。沿用 ControlPanel 同色系（#26282e 暗 + #2ea06c 绿强调 + #2c5d8f 蓝按钮区别 StartButton 绿）。
- `.docs/technical/agent-component-map.md`：顶部覆盖列表 + 文件树加 UploadPanel.tsx；新增「上传预览 US-006 落地」一节 + 8 条关键不变量。
- `materialSorting-web/AGENTS.md`：文件分工树加 UploadPanel.tsx；新增「上传预览 US-006 关键约定」9 条（panel-wide DnD / dragCounter / 三件套校验 / localError 优先 / value 重置 / 状态驱动 UI / 双入口 / 不引 CSS 框架 / jsdom polyfill）。
**关键设计**:
- **状态来源拆分**：HTTP 流程（uploading/done/error + doc）走 uploadStore；客户端校验失败消息走本组件 `localError` useState（不污染 store 状态机）。`displayError = localError ?? (status==='error' ? store.error : null)`。
- **dragCounter 防抖**：dragenter/dragleave 在子元素间移动会反复触发，ref 计数器保证只在 counter=0 时清 `.dragover`。
- **20MB 与后端 `server.py UPLOAD_MAX_BYTES` 一致**（双校验：前端先拦 + 后端兜底）。
- **`e.target.value = ''` 重置**：让同一文件可重复选（否则 input value 去重哑火）。
**验证**:
- `cd materialSorting-web && npm run typecheck`：0 error。
- `cd materialSorting-web && npm run build`：tsc + vite build 通过（166KB JS gzip 55KB / 6.6KB CSS gzip 1.9KB）。
- `cd materialSorting-web && npm run test`：20 files / 223 tests 全 pass（含新增 25 项 UploadPanel）。
- 后端 curl 端到端：`ms-web` 启动 → POST /api/parse-dxf (M1787 母版) → 200 + 11 码 / 110 裁片；非 .dxf → 400 + 中文错误。
- 前后端 dev proxy：`curl -X POST -F file=@test.txt localhost:5173/api/parse-dxf` 经 Vite proxy 命中后端，返回 `{"error":"仅支持 .dxf 文件"}`。
- 浏览器自动验证（chrome-devtools-mcp）：本次会话无该 MCP 工具访问，AC#5 浏览器肉眼验证延后；改以 25 项集成测试 + jsdom 渲染 + 端到端 curl 联合覆盖 AC#1~#3 全部分支。
**下一步**: US-007（PiecePreviewSVG 命令式渲染）—— 参考 NestSVG.tsx 命令式范式（useRef+useEffect 直绘 path/circle/text 逃逸 reconciliation）；layer1 毛版实心+layer14 净版绿虚+layer8 内部线橙+刀口 8mm 短线段+layer7 布纹线红虚 + A/B/C 文字（在翻转组外，屏幕坐标）；坐标系 scale(1,-1) 必须保留。
## 2026-08-10 19:10 — 上传预览 US-007 PiecePreviewSVG 命令式渲染 ✅
**Story**: US-007 — PiecePreviewSVG 命令式渲染（5 层分层 + scale(1,-1) 翻转 + A/B/C 文字不镜像）。
**改动**:
- `src/components/preview/PiecePreviewSVG.tsx`（新）：单片（或一组，AC#4）母版预览 SVG。命令式渲染范式（参考 NestSVG.tsx）：React 仅渲染 `<svg ref/>`；useEffect 内 imperative 建翻转组 `<g data-role=flip>` + 各层节点（`<polygon data-role=rough|net>` / `<polyline data-role=internal>` / `<line data-role=notch|grain>` / `<text data-role=label>`），用 setAttribute 写 transform/points/stroke/...，逃逸 React reconciliation。
- 5 层分层（颜色 / 线型与 scripts/preview/*.svg 版师确认配色一致）：layer1 毛版半透明蓝实心 `rgba(80,140,200,0.22)` + `#3f7fbf` 实线边（闭合 polygon，stroke-width=1.5）；layer14 净版绿虚线 `#33cc33` `stroke-dasharray=6 3`（闭合 polygon，fill=none）；layer8 内部线橙实线 `#ff8c1a`（polyline 不闭合，line.length<2 跳过）；layer4 刀口黄短线段 `#ffd700`（line，端点 `P ± 4*unit_normal`，NOTCH_LEN_MM=8 待版师确认）；layer7 布纹线红虚线 `#e53e3e` `stroke-dasharray=5 3`（line，grain_line=null 跳过）。
- 关键设计：翻转组 transform=`translate(0 minY+maxY) scale(1 -1)`（NestSVG `translate(0 gate)` 是 minY=0/maxY=gate 的特例）；A/B/C 文字标注在翻转组外（屏幕坐标 SVG Y-down 直接定位，避免镜像），锚点 = bbox 左上角上方 LABEL_Y_OFFSET=3（baseline 在 minY - 3，font-size=11）；viewBox = bbox + pad（默认 14，最小 4 clamp）；piece(s) 切换整组重建（useEffect 头部 while removeChild 清空，不同于 NestSVG flipRef 幂等 —— 切片是低频操作，重建简洁可接受）。
- AC#4 多片同框：prop 接受 `ParsedPiece | ParsedPiece[]`，归一化为数组；多片时合并 bbox 计算 viewBox（piecesBBox），每片独立渲染 5 层 + 各自 A/B/C 标注（不合并）。
- 空片容错：piecesBBox 返回 null → svg 清空后啥都不画（无 viewBox/flipGroup/标注，不留残影）；polygon.length<3 跳过 rough 层；net_polygon.length<3 跳过 net 层；internal line.length<2 跳过该条。
- 导出 `pieceBBox` / `piecesBBox` / `BBox` 便于单测（纯函数 + 类型）。
- `src/components/preview/__tests__/PiecePreviewSVG.test.tsx`（新）：33 项单测覆盖 AC#1..#5。
  - bbox 纯函数 5：pieceBBox 合并所有层顶点 / 空片 null / 无 grain 跳过 / 多片 piecesBBox 合并 / 全空片 null
  - AC#1 命令式 2：React 仅 `<svg ref/>` 子节点 imperative（首个子节点是 `<g data-role=flip>`）/ StrictMode 双 mount 不残留（重渲染同 piece 只 1 个 flipGroup）
  - AC#2 渲染分层 11：5 层颜色/线型/数据逐项 + layer1 points 字符串 r2 截断 + internal line<2 跳过 + 刀口端点 P±4*normal（横 (1,0) + 斜 (0.6,0.8)）+ grain null 跳过 + 全 5 层同框节点数
  - AC#3 翻转+标注 9：flip transform 单片 (translate(0 120) scale(1 -1)) + 多片合并 bbox / viewBox 默认 pad=14 (-4 6 128 108) + 自定义 pad=5 + clamp pad=0→4 / 标注在翻转组外（svg 直接子节点，非 flipGroup 内）/ 标注屏幕坐标 (minX, minY-3) dominant-baseline=alphabetic font-size=11 / label 空串跳过 / 多片各自标注（B baseline y=-3）
  - AC#4 单片/多片/空片容错 4：单片 1 rough+1 label / 多片 3 片各 1 rough+1 label / 空片啥都不画无 viewBox / polygon<3 跳过 rough 其他层照常
  - AC#5 切片重建 3：切 piece 清旧+新 viewBox 反映新 bbox / 切到空片清空 / pad 变化触发 viewBox 重算（dep 含 pad）
- `src/style.css`：加 `.piece-preview-svg { display:block; width:100%; height:100%; min-height:0; background:#eef0f3 }`（与 .nest-card svg 同口径，背景与排料图同色）。
- 文档同步：`.docs/technical/agent-component-map.md`（顶部覆盖列表 + 文件树加 PiecePreviewSVG.tsx/test + 新增「上传预览 US-007 落地」节 + 12 条关键不变量 + 已知差异更新）；`materialSorting-web/AGENTS.md`（文件分工树加 PiecePreviewSVG + 新增「上传预览 US-007 关键约定」13 条）；`.docs/technical/todo.md`（Recently Completed 加 US-007 行）。
**关键决策**:
- **piece(s) 切换整组重建**（不同于 NestSVG flipRef 幂等）：NestSVG 同 run 内 N 帧复用 DOM（高频，renderTick ~10fps 节流），PiecePreviewSVG 切片是低频 UI 操作（用户点选某片），重建简洁且开销可接受。useEffect 头部 `while removeChild` 清空后重建，StrictMode 双 mount 同样安全。
- **翻转组 transform 用 `translate(0 minY+maxY) scale(1 -1)`**（NestSVG 是 minY=0/maxY=gate 的特例）：bbox 的 Y 对称轴翻转，保证 bbox 内几何视觉与 sparrow 视图一致（不上下颠倒）。
- **A/B/C 文字标注放在翻转组 `<g>` 之外**（AC#3 不镜像）：用屏幕坐标（SVG Y-down）直接定位，dominant-baseline=alphabetic（y=baseline），font-size=11（cap 顶 ≈ minY-11，pad=14 时刚好在 viewBox 内）。
- **viewBox pad 默认 14**：容纳 8mm 刀口半段（4mm）+ 标注文本（~10mm cap 高度），最小 4 clamp（防刀口被裁）。
- **NOTCH_LEN_MM=8 暂定待版师确认**：与 PRD 「待确认问题 - 刀口短线段长度」一致；改值需同步 2 项刀口测试 + 版师预览确认。
- **多片同框不刻意避免重叠**：多片本身可能共享边界，由调用方决定是否同框；US-008 ParsedPiecesView 用单片卡片，多片能力留作未来扩展。
**验证**:
- `cd materialSorting-web && npm run typecheck`：0 error。
- `cd materialSorting-web && npm run build`：tsc + vite build 通过（166KB JS gzip 55KB / 6.7KB CSS gzip 1.9KB）。
- `cd materialSorting-web && npx vitest run`：21 files / 256 tests 全 pass（前 223 + 新增 33 项 PiecePreviewSVG）。
- 后端 curl 端到端：ms-web 启动 → POST /api/parse-dxf (M1787 母版) → 200，10 片 / 1 码（M1787 文件本身只含码 30）；POST 5156 → 200，110 片 / 1 码（5156 块名码号解析失败，size=null，US-003 已知行为）。响应字段名与 ParsedPiece 类型 1:1（label/name/polygon/internal_lines/notches/net_polygon/grain_line）。
- 浏览器验证：本次会话无 chrome-devtools-mcp 工具访问（与 US-006 同约束），AC#5 浏览器肉眼验证延后到 US-008 ParsedPiecesView 集成时一并完成（届时 SizeTabs + 卡片 grid 落地，可在浏览器逐码切逐片查看 5 层渲染）。改以 33 项 jsdom 单测（覆盖 5 层颜色/线型/数据 + 翻转 transform + 标注屏幕坐标 + 切片重建 + 空片容错）+ 后端 curl payload 结构匹配 ParsedPiece 联合覆盖。
- 后台 ms-web (:8000) 已 taskkill 清理（PID 21112/3296/11404 全部 Stop-Process -Force），无遗留进程。
**下一步**: US-008（SizeTabs + ParsedPiecesView + PreviewPage 集成）—— 把 PiecePreviewSVG 拼进单片卡片 grid，按码切换；届时浏览器肉眼验证 5 层渲染（刀口 8mm 长度 + 标注镜像 + 颜色配色）一并完成。
## US-008 完成 (2026-08-10) — SizeTabs + ParsedPiecesView + PreviewPage 容器集成（Tab 打通）
**改动**：
- 新建 `materialSorting-web/src/components/preview/SizeTabs.tsx`：订阅 uploadStore.doc/activeSize/setSize；chip 行（按 doc.sizes 顺序，null→「通用」）；active 高亮 + role=tablist/tab + aria-selected；点击 → setSize。
- 新建 `materialSorting-web/src/components/preview/ParsedPiecesView.tsx`：订阅 uploadStore.doc/activeSize；`find(s=>s.size===activeSize)` 过滤当前码；CSS Grid auto-fill minmax(220px,1fr)；每片卡片（PiecePreviewSVG 单片 + .piece-card-label 徽章 + .piece-card-name 中文名）；key=`${label}-${name}`；空码 → .parsed-pieces-empty。
- 改 `materialSorting-web/src/components/preview/PreviewPage.tsx`：US-001 占位 → US-008 容器；`hasParsed = status==='done' && doc!==null` 决定挂主体 or .preview-empty 空态；左 UploadPanel + 右 .preview-main（SizeTabs+ParsedPiecesView）。App.tsx 无改动（US-001 已 mount PreviewPage，自然接通）。
- `style.css` 加 .preview-page/.preview-main/.size-tabs/.size-chip(+active)/.parsed-pieces-view/.piece-grid/.piece-card*/.parsed-pieces-empty（沿用 ControlPanel/NestCard 暗背景 #26282e/#2a2c32 + 绿色 #2ea06c 同色系，无 CSS 框架）。
- 新增 25 项单测/集成测试（SizeTabs.test.tsx 8 + ParsedPiecesView.test.tsx 8 + PreviewPage.test.tsx 9），全 pass。覆盖 AC#1（chip 列表+null→通用+role=tablist+active+点击）+ AC#2（grid+每片含 A/B/C+名+svg+key+切码刷新+空码空态）+ AC#3（左 panel+右 main 布局+4 空态分支+端到端切码）+ AC#5（state 经 store + display:none 持久）。
- 更新 AGENTS.md（加「上传预览 US-008 关键约定」14 条 + 文件分工树）+ `.docs/technical/agent-component-map.md`（加「上传预览 US-008 落地」section + 关键不变量 12 条 + 文件树更新）。
**关键设计决策**：
- 三个新组件都不持本地状态，全从 uploadStore 读 —— AC#5（切 Tab 状态保留）由 store 模块级 + App display:none 不卸载保证，组件本身无需任何持久化逻辑。
- chip 顺序 = doc.sizes 顺序（后端按数值升序、null 殿后 `_size_sort_key`），前端不二次排序，与后端语义一致。
- null 码 chip 显示「通用」（`NULL_SIZE_LABEL`）—— 母版极少出现的「不分码」片，用人读文案代替 null。
- grid auto-fill minmax(220px,1fr) —— 浏览器宽度自适应列数；窗口缩小时单卡不被压扁，最小 220px 保证 SVG 不退化成窄条。
- piece-card 视觉沿用 .nest-card 同口径（暗背景 + 圆角 + 上方标签头 + 下方 SVG 自适应），label 徽章用绿色圆形 + 白字（与 StartButton/TabBar active/size-chip active 同色系）。
- PreviewPage 空态分支 `hasParsed = status==='done' && doc!==null` 双重防御（done 理论必有 doc，但 TS 类型上 doc nullable）。uploading/error 时也显示空态卡片（不显示「上传中…」—— 那是 UploadPanel 的事）。
**验证**：
- vitest: 24 files / 281 tests pass（256 baseline + 25 新增；包括 6 项 App 集成 smoke + 33 项 PiecePreviewSVG 单测 + 25 项 UploadPanel 集成等）。
- `npm run typecheck` + `npm run build` 通过（`tsc --noEmit && vite build` → `static/index-D_7rjQsf.js + index-DSUg6jSv.css`）。
- 后端 e2e：`ms-web` :8000 启动 → `curl POST /api/parse-dxf` 上传 M1787 母版 → 200 返回 `{doc_id, filename, sizes:[{size:30, pieces:[10 items]}]}`，每片含 label/name/polygon/internal_lines/notches/net_polygon/grain_line（与 US-004 契约一致）。
- 静态服务：`curl GET /` 返回引用新 build hash 的 index.html；`curl HEAD /static/assets/index-D_7rjQsf.js` 200 OK（FastAPI mount 正常 serve）。
- chrome-devtools-mcp 在本会话不可用（tool 未挂载）；改用 vitest jsdom + StrictMode（与 production 渲染同路径）+ DOM 断言覆盖 AC#1-#5，并由后端 curl e2e 验证管线通畅。
**清理**：ms-web (PID 39396) 已 PowerShell Stop-Process -Force 杀掉，netstat :8000 LISTENING 无残留。
**下一步**: US-010（后端 Path A /api/commit-to-nesting：复用 export_dxf + load_nest_pieces 全管线把上传母版转 intermediate）。
## US-010 完成 (2026-08-10) — 后端 Path A /api/commit-to-nesting（上传母版 → intermediate 全管线）
**改动**：
- `materialSorting-server/src/materialsorting/web/server.py`：新增 `POST /api/commit-to-nesting` 路由 + `_commit_to_nesting_sync(doc_id, src_dxf, source_name)` 同步管线 helper（跑在 `_executor` 里）。
  - 顶部 imports 加 `json` / `re` / `shutil` / `dxf_parser.explore` / `dxf_parser.export_dxf`（assign_group_no, GROUP_NAMES, write_piece_dxf）/ `nesting_bounds.load_pieces`（load_nest_pieces, GATE_MM as NEST_GATE_MM，**别名避免与顶层 `_DOC, GATE_MM, PIECES = load_pieces()` 的 GATE_MM shadow**）。
  - 加 `_DOC_ID_RE = re.compile(r'^[0-9A-Za-z]{1,128}$')`（防路径逃逸；`uuid.uuid4().hex` 自然命中）。
- 管线实现（Path A）：`explore.collect_pieces` → `assign_group_no` + `GROUP_NAMES` 定 ptype → `write_piece_dxf` 切单裁片到 `uploads/<doc_id>_pieces/`（每次 commit 先 `shutil.rmtree` 再重写，**idempotent**）→ `load_nest_pieces(pieces_dir, sizes=母版全码)` → 写回 `paths.INTERMEDIATE`。
- 备份：写回前 `shutil.copy2(paths.INTERMEDIATE, paths.INTERMEDIATE.with_suffix('.bak'))`；首次 commit 无原文件则跳过；`.bak` 只保留一份（再 commit 覆盖）。
- schema 与 `ms-pieces-export`（`pieces_export.py`）完全一致：`{source, gate_mm=1980, n_pieces, total_area_mm2, pieces[]}`；pieces 字段 `{pid, ptype, size, side, polygon, bbox, area_mm2, n_verts, allowed_angles=[0,180]}`。
- 路由签名：`payload={doc_id, filename?}`，`doc_id` 必填 + regex 校验；`filename` 可选（缺省 `<doc_id>.dxf`），作为新 intermediate 的 `source` 字段。CPU 密集管线走 `loop.run_in_executor(_executor, ...)` 复用 6-worker 线程池。
- 错误码：请求体非 JSON / 缺 doc_id / 类型错 / `_DOC_ID_RE` 不中 → 400；`uploads/<doc_id>.dxf` 不存在 → 404；管线异常（collect 空 / write 全跳过 / load_nest_pieces 空 / 写盘失败）→ 422。全部 JSONResponse。
- 响应字段：`{doc_id, source, sizes, n_pieces, total_area_mm2, n_written_dxf, n_skipped, skipped[≤10], bak}`。
- 文档：更新 `.docs/technical/agent-api-reference.md`（routes 表 + 新增 "POST /api/commit-to-nesting" section 含请求/响应/错误/副作用/不变量 6 条）+ `.docs/technical/agent-file-map.md`（文件树 + 行数 + 数据流链路 + 关键不变量）+ `web/AGENTS.md`（新增 "US-010 /api/commit-to-nesting 关键约定" section 8 条 + 文件分工 + 已踩坑更新）。
**关键设计决策**：
- **Path A 全复用**：服务端直接跑 `export_dxf` 切单裁片 + `load_nest_pieces` 全码加载，而非另写「上传母版 → NestPiece 直转」。理由：`ptype` 来自单裁片文件名（`{ptype}_{size}.dxf`），由 `assign_group_no` + `GROUP_NAMES` 在导出时赋值，跳过 `write_piece_dxf` 会丢失 ptype 映射。复用全管线零风险。
- **全码 sizes 来自母版实测**：`sorted({p.size for p in pieces if p.size is not None})`，M1787 实测 [28-38] 11 码（vs `DEFAULT_SIZES` 8 码跳 32）。**不沿用 `DEFAULT_SIZES`**。
- **GATE_MM 别名导入**：`from ..nesting_bounds.load_pieces import GATE_MM as NEST_GATE_MM`。`server.py` 顶层已有 `_DOC, GATE_MM, PIECES = load_pieces()` 从既有 intermediate 读 GATE_MM（1980.0）；写回新 intermediate 时用 `NEST_GATE_MM`（load_pieces 模块的源常量 1980.0），而非既有 intermediate 的值——避免「写回时把既有 intermediate 的可能脏值扩散」。
- **`filename` 可选不强制**：AC1 说入参 `{doc_id}`，但 source 字段需要「上传文件名」。折中：payload `{doc_id, filename?}`，filename 缺省用 `<doc_id>.dxf`。前端 commit 按钮（TODO）会有 uploadStore.filename 直接传；curl 测试可显式传 filename 覆盖。**不改 US-004 parse-dxf 添加 sidecar**——保留 US-004 行为不变。
- **idempotent 临时目录**：每次 commit 先 `rmtree` 再 `mkdir`，避免旧文件残留污染（M1787 假设一码一片型唯一；新款若违反，文档化为 open question）。
- **`.bak` 单份**：再 commit 会覆盖 `.bak`——保留最近一次写回前快照足够回滚。多版本备份非 v1 目标。
- **不 auto-reload PIECES**：`server.py` 顶层 `PIECES = load_pieces()` 是 import 时一次性加载；commit 后 intermediate 文件已更新但内存 `PIECES` 不变。**v1 TODO 随排料页改造补 reload 端点或重启策略**（AC6 明确 deferred）。
**验证**：
- 导入冒烟：`python -c "from materialsorting.web.server import app"` 通过；路由表含 `POST /api/commit-to-nesting`。
- 分层校验：`web → nesting_bounds → dxf_parser` 单向；`nesting_bounds.load_pieces` 不 import web；`dxf_parser.explore` 不 import 兄弟包。
- 回归等价（核心 AC#7）：Path A 产物 vs 「全码 CLI 管线」`load_nest_pieces(paths.PIECES_DIR, sizes=[28..38])`：
  - Path A：176 NestPiece / total_area=17650482.2 mm² / 11 码 [28-38]
  - CLI baseline：176 NestPiece / total_area=17650482.2 mm² / 11 码 [28-38]
  - **PID 集合完全相同**；**零面积 diff（>0.5mm²）**。等价性证实。
- 端到端 curl：`ms-web` :8000 启动 → `POST /api/commit-to-nesting {doc_id, filename}` → 200 返回 `{n_pieces:176, sizes:[28-38], total_area_mm2:17650482.2, bak:...}`。
- 错误路径 curl：缺 doc_id → 400；`doc_id="../../../etc/passwd"` → 400（regex 拒绝）；不存在 doc_id → 404；请求体非 JSON → 400。
- 副作用：`pieces_intermediate.bak` 277118 bytes（原 8 码 128 片）；新 intermediate 385154 bytes（11 码 176 片）；schema 字段完全一致。
- **未做 UI 浏览器验证**：本 story 纯后端（AC6 明确 v1 仅 curl 可测，前端 commit 按钮随排料页改造补 TODO）；无 SVG/坐标变换改动，按规则不需 chrome-devtools-mcp。
**清理**：ms-web (PID 50884) 已 `Stop-Process -Force` 杀掉，`Get-NetTCPConnection -LocalPort 8000` count=0。原 intermediate 已从 /tmp 备份还原，`.bak` + 临时 `<doc_id>_pieces/` 已清掉，dev 环境回到 8 码 128 片基线。
**下一步**：v1 dxr-upload-preview PRD 全 10 个 story 已完成（US-001~US-010）。后续：（1）排料页 commit 按钮 + `setTab('nesting')` UX + commit 后 PIECES reload 机制（AC6 TODO）；（2）新款母版（非 M1787 结构款）需版师确认 `GROUP_NAMES` g00-g09 → ptype 映射；（3）`uploads/<uuid>_pieces/` 临时目录清理策略（v1 open question）。
---
## US-011 数量状态 store(qtyStore)+类型+selector (2026-08-10)
**分支**: `ralph/dxf-upload-preview`
**新增文件**:
- `materialSorting-web/src/types/qty.ts` — PieceQuantity / PieceQuantityMap / QtyMode 类型契约
- `materialSorting-web/src/store/qtyStore.ts` — Zustand store（quantities + 3 actions）+ 纯函数 clampQty + 纯函数 selector getPieceDisplay
- `materialSorting-web/src/store/__tests__/qtyStore.test.ts` — 24 项单测（clampQty 6 + getPieceDisplay 9 + setPiecePerSize 4 + setPieceGlobal 2 + resetQuantities 1 + store 独立性 2）
**实现要点**:
- qtyStore 与 uploadStore 完全解耦（字段不重叠），双向 reset 互不影响
- label（A/B/C）跨码匹配同一片型，依赖后端几何排序稳定
- getPieceDisplay 四分支：未配置 / per-size / global+source / global+非source（reason 含 sizeLabel(source)）
- clampQty 公式 `Math.max(0, Math.min(99, Math.trunc(Number(v)||0)))`，setPiecePerSize/setPieceGlobal 内部统一调用
- 从 global 切回 per-size 时 globalValue 继承到 perSize[sizeKey(globalSource)]，globalSource=null 时 sizeKey='null' 兜底
- null 码双口径：sizeKey='null'（perSize key 空间）/ sizeLabel='通用'（人读文案，与 SizeTabs NULL_SIZE_LABEL 同语义）
**验证**:
- `npm run typecheck` ✅
- `npm run test -- qtyStore` ✅ 24/24 passed
- `npm run test`（全量）✅ 305/305 passed（24 新增 + 281 既有，无回归）
- `npm run build` ✅ static/ 产出无报错
**踩坑**: Git Bash 单引号 heredoc 在 ~200 行 + 含中文 + 三引号嵌套时会失效（`unexpected EOF`），改用分块 `cat <<'CHUNK'` 多次 append 解决。
## 2026-08-10 US-012 数量编辑弹窗 PieceQtyDialog+Switch
- 扩 `materialSorting-web/src/store/uploadStore.ts`：新增 `qtyDialog: {label, size}|null` + `openQtyDialog(label,size)` / `closeQtyDialog()`；`reset()` 同步清；store 公开 API 扩到 4 个。
- 新增 `materialSorting-web/src/components/preview/Switch.tsx`：受控 toggle（`role="switch"` + `aria-checked`）；CSS `.switch`/`.switch-track`/`.switch-label-off/on`/`.switch-thumb` 自绘滑块，沿用暗底 `#34363d` + 绿 active `#2ea06c`；`disabled` 走原生 button disabled 兜底。
- 新增 `materialSorting-web/src/components/preview/PieceQtyDialog.tsx`：草稿 + 确定模式（draftQty/draftGlobal 用 useState，仅确定才写 qtyStore；取消/遮罩/ESC 丢弃）；Portal 到 document.body；ESC 用 `useEffect` 挂 window.keydown、Inner unmount 自动卸载 listener；target 切换时用 `key=${label}-${size}` 强制重建 Inner 重新读初值；初值严格走 `getPieceDisplay` selector。
- `style.css` 加 `.piece-qty-dialog-overlay/.piece-qty-dialog-modal/.qty-input-group/.qty-step/.qty-input/.switch*/.qty-btn/.qty-confirm` 暗色样式。
- 单测：uploadStore.test.ts +6 项（共 13 项）；Switch.test.tsx 5 项；PieceQtyDialog.test.tsx 15 项。`npm run test` 全 331 项绿、`npm run typecheck` 通过、`npm run build` 无报错。
- `.docs/technical/agent-component-map.md` 与 `materialSorting-web/AGENTS.md` 同步：新增「上传预览 US-012 落地」section + 关键不变量 11 条；文件树加 Switch/PieceQtyDialog。
- 不集成到 PreviewPage（留 US-014）：本故事仅弹窗 + Switch 组件 + 单测；不进 commit / 排料；uploadStore.reset 联动 qtyStore.resetQuantities 留到 US-014 集成。
- **未做浏览器验证**：本故事无 SVG/坐标变换改动（仅 DOM 弹窗），AC 仅要求 typecheck + 单测，故跳过 chrome-devtools-mcp；US-013 PieceZoomModal / US-014 集成时再统一浏览器回归（含弹窗显隐 / Switch 视觉）。
## US-013 放大预览模态 PieceZoomModal（2026-08-10）
### 落地范围
- `materialSorting-web/src/store/uploadStore.ts`：扩 `zoom: {label:string; size:number|null} | null`（默认 null）+ `openZoom(label, size)` / `closeZoom()` actions；`reset()` 同步清 `zoom=null`；store 公开 API 扩到 6 个。
- `materialSorting-web/src/components/preview/PieceZoomModal.tsx`（新增）：声明式受控 Portal；订阅 `uploadStore.zoom + doc`；zoom/doc 任一为 null 时 return null；打开时调 `locatePiece(doc, label, size)` 定位 ParsedPiece + seq(findIndex+1)，防御性兜底（找不到渲染 null）；ESC 监听在 zoom!==null 时挂 window.keydown、关闭自动卸载；✕/遮罩（e.target===e.currentTarget）/ESC 三方式关闭；modal onClick stopPropagation 双重防冒泡；头部 `[label徽章] 序号(qty) · 码 {sizeLabel(size)} · {name}`，qty 从 qtyStore `getPieceDisplay` 读；body 复用 `<PiecePreviewSVG piece={p} pad={20}/>`；Portal 到 document.body。
- `materialSorting-web/src/style.css`：新增 `.piece-zoom-overlay`（fixed inset:0 + rgba(0,0,0,0.6) + flex 居中 + z-index:1000 与 .piece-qty-dialog-overlay 同层）+ `.piece-zoom-modal`（`#2a2c32` 暗底 + radius 8 + max-width/height 90vw/90vh + overflow:auto + 相对定位承载关闭按钮 + flex-col gap 12）+ `.piece-zoom-head`（flex + padding-right 28 给 ✕ 留位）+ `.piece-zoom-seq`（monospace + 600）+ `.piece-zoom-meta`（`#9aa`）+ `.piece-zoom-name`（`#cdd` + word-break）+ `.piece-zoom-close`（absolute top:8 right:10 + 28×28 + transparent + hover `#3f424a` bg + `#2ea06c` 字 + focus-visible outline）+ `.piece-zoom-body`（flex 1 + min-height 320 + min-width 0）。
- `materialSorting-web/src/store/__tests__/uploadStore.test.ts`：新增 7 项 zoom 用例（默认 null / openZoom 写入 number / openZoom 写入 null 码 / closeZoom 清回 / reset 同步清 / 订阅者通知 / zoom 与 qtyDialog 字段独立），原 13 项不变 → 共 20 项。
- `materialSorting-web/src/components/preview/__tests__/PieceZoomModal.test.tsx`（新增）：14 项集成测试，覆盖 AC 全部要点（含 Portal body / 防御性兜底）。
### 关键决策
- **ESC 监听挂在主组件 useEffect（不在 Inner 子组件）**：与 PieceQtyDialog 不同，本模态无可编辑草稿故无 Inner 子组件；直接在主组件 useEffect dep `[zoom, closeZoom]`，zoom!==null 时挂 listener、cleanup 自动卸载。hook 无条件调，zoom!==null 判定在 effect 内部（避免条件 hook 警告）。
- **遮罩 onClick（不是 mousedown）+ modal stopPropagation 双重防御**：与 PieceQtyDialog 用 mousedown 不同（数量弹窗有可拖选文本场景）；本模态无可拖选文本，click 语义更直观；modal onClick stopPropagation 兜底即使冒泡到 overlay 也已被 stop。
- **头部同时显示 label 徽章 + 序号(数量)**：序号 = pieces 数组 index+1（与 label 字母次序一致，依赖后端 `_label_for` 几何排序）；详情模态追求信息完整（与卡片头追求简洁互补）；D1 决策信息冗余但语义一致。US-014 集成时卡片头序号同口径（届时可考虑提取公用 `findPieceIndex` 函数；本故事先内联在 locatePiece）。
- **PiecePreviewSVG pad=20（比卡片默认 pad=14 加大留白）**：放大显示更多内边距视觉更舒适；pad 经 PiecePreviewSVG 内 `safePad = Math.max(MIN_PAD=4, pad)` clamp，20 安全。
- **不集成到 PreviewPage（留 US-014）**：本故事仅模态组件 + store 字段 + 单测；PreviewPage 顶层挂 `<PieceZoomModal/>` 单例 + uploadStore.reset 联动 qtyStore.resetQuantities + ParsedPiecesView 卡片图形区点击 openZoom 都由 US-014 集成。
- **未做浏览器验证**：本故事无 SVG/坐标变换改动（仅复用 PiecePreviewSVG 加大 pad + DOM 弹窗外壳），AC 仅要求 typecheck + 单测，故跳过 chrome-devtools-mcp；US-014 集成时再统一浏览器回归（含放大模态显隐 / ✕/遮罩/ESC / 头部信息）。
### 验证
- `npm run typecheck`：通过（0 errors）。
- `npm run test -- PieceZoomModal`：14/14 通过（含 zoom=null 不渲染 / doc=null 不渲染 / 渲染 overlay+modal+aria-label / 头部 label+seq(qty)+size+name / qty 从 qtyStore / null 码「通用」/ body svg.piece-preview-svg / ✕ closeZoom / 遮罩 closeZoom / modal 内不冒泡 / ESC closeZoom / Portal body / label 不存在兜底 / size 不存在兜底）。
- `npm run test`（全量）：352/352 通过（含 uploadStore 20 / PieceZoomModal 14；既有 333 项无回归）。
## US-014 ParsedPiecesView 卡片头改造 + 双模态集成（2026-08-10）
- ParsedPiecesView.tsx 改造：卡片头由 [.piece-card-label]+[.piece-card-name] → [.piece-card-label]+[.piece-card-qty]。
  - 序号 = pieces 数组 index+1（A=1, B=2, ...，与 PieceZoomModal locatePiece.seq 同口径）。
  - 数量 + 可编辑性 从 qtyStore getPieceDisplay(quantities, label, activeSize) 读：editable=true → <button onClick=openQtyDialog+stopPropagation>{seq}({qty})</button>；editable=false（global 非 source） → <span class="disabled" title={reason}>{seq}({qty})</span>（native title 提供 hover 提示）。
  - .piece-card-body 包裹层加 onClick→openZoom + role=button + tabIndex=0 + aria-label + onKeyDown(Enter/Space)。key 保留 `${label}-${name}` 跨码安全。
- PreviewPage.tsx 集成：顶层挂 <PieceQtyDialog/> + <PieceZoomModal/> 单例（与 SizeTabs/ParsedPiecesView 同级；Portal 到 body）。重传清零：useEffect 内 useUploadStore.subscribe 监听 state，对比 prev/next doc?.doc_id，不同则调 useQtyStore.getState().resetQuantities()。覆盖三路径（首次上传 no-op / 重传清零 / reset 清零）；切 activeSize 不触发（数量保留）。
- style.css：删 .piece-card-name；加 .piece-card-qty（透明底 + monospace + 600 + cursor:pointer + hover #2ea06c + focus-visible outline）+ .piece-card-qty.disabled（#666 灰字 + cursor:not-allowed + hover 不高亮）；.piece-card-body 加 cursor:zoom-in + :focus-visible outline。
- ParsedPiecesView.test.tsx：原 8 项改造（「每片含 A/B/C+名+svg」→「每片含 label 徽章+序号(数量)+svg，无裁片名残留」；切码刷新断 1(0) 序号）+ 新增 11 项（序号 index+1 / qty 默认 0 / qty 从 qtyStore per-size 5 / editable button openQtyDialog / global非source span.disabled+title 含 30 / global source 仍为 button 可编辑 / body onClick openZoom / body role+tabIndex / Enter / Space / qty stopPropagation 不冒泡）。
- PreviewPage.test.tsx：原 9 项改造（切码 30 names→qtyTexts 断 1(0)/2(0)）+ 新增 7 项（默认模态不渲染 / qtyDialog 自显隐 / zoom 自显隐 / reset 联动清 quantities / 重传 doc_id 变化清 / 切 activeSize 保留 / 端到端：切码→点 qty→切 global+确定→切回原码→span.disabled+title 含 30）。
- `npm run typecheck` 通过；`npm run build` 通过（static/ 产物刷新 hash：index-DUdlKAyF.js / index-DMHAOuCp.css）。
- `npm run test -- ParsedPiecesView`：19/19 通过（8 项基础改造 + 11 项新增）。
- `npm run test -- PreviewPage`：16/16 通过（9 项基础改造 + 7 项新增）。
- `npm run test`（全量）：370/370 通过（含 ParsedPiecesView 19 / PreviewPage 16；既有 335 项无回归）。
- 未做浏览器验证：本故事无 SVG/坐标变换（仅 DOM 结构 + 点击处理 + CSS），AC 仅要求 typecheck + 单测 + build；浏览器验证留作整体回归。ms-web :8000 已可启（PID 2304 旧进程占用），最新 build 已 serve（hash 一致）。
## [US-015] uiStore 扩 nestingEnabled + TabBar 置灰（2026-08-11）
- **改动文件**：materialSorting-web/src/store/uiStore.ts（+ nestingEnabled 字段 / + setNestingEnabled action / setTab 加 guard 用 get().nestingEnabled）；TabBar.tsx（超排 button disabled 闸三层双重防御）；style.css（+ .tab.disabled / .tab.disabled:hover #555 灰字）；uiStore.test.ts（+5 项测试，原 4 项 beforeEach 加 setNestingEnabled(false)、test 2/3/4 加 setNestingEnabled(true) 兜底 guard）；TabBar.test.tsx（+4 项测试，原 5 项 beforeEach 加 reset、test 3/4 加 setNestingEnabled(true)）；App.test.tsx（beforeEach 加 setNestingEnabled(true) 兜底 store guard 测原 nesting 渲染）；AGENTS.md + .docs/technical/agent-component-map.md 同步 US-015 关键约定 / 文件分工。
- **关键不变量**：`setTab('nesting')` 在 `nestingEnabled===false` 时**静默不切**（store 层 guard 兜底）；`setTab('preview')` 永远允许（用户随时可回上传预览页）。三层双重防御：native `disabled` 属性（a11y / 键盘 tab 序列）→ TabBar `onClick` 内 `if (disabled) return`（合成事件 / devtools 旁路）→ store `setTab` guard（JS 旁路）。
- **测试新增**：uiStore +5（默认 false / setNestingEnabled 切换 / 订阅者通知 / **setTab(nesting) false 时静默不切**（关键不变量）+ 解锁后生效 / setTab(preview) false 时仍可切）；TabBar +4（disabled 点击不调 setTab / disabled 视觉 .disabled+aria-disabled+native disabled / 启用后正常切换 / nestingEnabled 切换时 TabBar 重渲染）。
- **测试结果**：`npm run typecheck` 通过；`npm run test`（全量）384/384 通过（uiStore 4→9 / TabBar 5→9 / App 6→7 修 beforeEach +1 setNestingEnabled 调整；既有 369 项无回归，新增 14 项含 5 项 store + 4 项 TabBar + 修正 App beforeEach 行为）；`npm run build` 无报错（static/ 产物更新 index-CmCu3NQx.js / index-D5o_SGFf.css）。
## 2026-08-11 — US-016 PreviewPage 联动 setNestingEnabled（branch: ralph/nesting-tab-linkage）
**Goal**: PreviewPage 监听 uploadStore.status，按 `status==='done' && doc!==null` 调 `useUiStore.getState().setNestingEnabled(...)`；mount 时立即对齐初值（idle→false）。覆盖路径：done+doc→true / idle/uploading/error/reset→false / 重传 doc_id 变化短暂 false（uploading）后 true（done）。
**Files changed**:
- `materialSorting-web/src/components/preview/PreviewPage.tsx` —— 新增 useEffect subscribe uploadStore.status 联动 uiStore.setNestingEnabled（与 US-014 qtyStore 联动 effect 同模式：subscribe + mount 即对齐 + 卸载 unsub）；调用前先判 `get().nestingEnabled !== next` 避免无谓 setState。
- `materialSorting-web/src/components/preview/__tests__/PreviewPage.test.tsx` —— 新增 8 项 US-016 用例（mount idle→false / done→true / error→false / reset→false / 重传短暂 false 后 true / **关键不变量 setNestingEnabled(false) 不强制切 Tab** / uploading→false / 状态机循环）；beforeEach 加 setNestingEnabled(false)+setTab('preview')，afterEach 同步重置。
- `materialSorting-web/src/__tests__/App.test.tsx` —— beforeEach 加 `useUploadStore.setState({status:'done', doc:makeParsedDoc()})` 让 PreviewPage mount 时的 US-016 effect 不把 nestingEnabled 覆盖回 false（否则 beforeEach 设的 true 会被立即覆盖）；afterEach 加 reset uploadStore + uiStore；「默认 preview」用例显式 reset uploadStore 验 .preview-empty；「切到 preview」用例把 `.preview-empty` 断言改 `.preview-page`（已上传状态下不再走空态）。
- `materialSorting-web/AGENTS.md` —— 文件分工注释加 US-016 落地段；新增「US-016 关键约定」段（联动公式 / PreviewPage 集成层模式 / 关键不变量 AC#3 / App.test.tsx beforeEach 适配 / 两 effect 独立 / 浏览器验证留作 US-021）。
- `.docs/technical/agent-component-map.md` —— 顶头 coverage 加 US-016；PreviewPage.tsx 描述加 US-016 effect；PreviewPage.test.tsx 描述加 8 项新增；App.test.tsx 描述加 US-016 beforeEach 适配；新增「US-016 落地」段 + 8 项关键不变量；US-001 关键约定 #2/#5/#8 扩 US-016 联动。
**Key invariants**:
- **联动公式严格固定** `next = status==='done' && doc!==null` —— 仅 done+doc 才 true，其它均 false。
- **关键不变量（AC#3）**：setNestingEnabled 仅控 Tab「能否进入」，不强制切 Tab —— `uiStore.setNestingEnabled(b)` 仅 set nestingEnabled，不触碰 activeTab。用户在 nesting Tab 时 reset（doc→null→false），activeTab 仍是 nesting，preview 仍可点回但不强制切回（避免丢失求解状态）。
- **uiStore / uploadStore 解耦**（与 qtyStore 同设计原则）：PreviewPage 作为集成层用 subscribe 绑定。
- **App.test.tsx beforeEach 必须 set uploadStore done+doc**：App mount 触发 PreviewPage mount → US-016 effect 按当前 uploadStore.status 对齐 nestingEnabled；若 idle 会把 beforeEach 设的 true 覆盖回 false。
**Test results**: `npm run typecheck` 通过；`npm run test` 全绿（392 项，其中 PreviewPage.test.tsx 25 项含 US-016 8 项；App.test.tsx 7 项全绿）；`npm run build` 无报错。
**未做浏览器验证**：本故事无 SVG/坐标变换（仅 store 联动 + setState），AC 仅要求 typecheck + 单测，故跳过 chrome-devtools-mcp；浏览器视觉回归（disabled 灰字 + cursor:not-allowed 随 status 切换）留作 US-021 自动 commit 集成时统一核对（届时 done→commit→切 nesting 端到端联调）。
---
- **未做浏览器验证**：本故事无 SVG/坐标变换（仅 button disabled 态 + CSS），AC 仅要求 typecheck + 单测 + build，故跳过 chrome-devtools-mcp；浏览器视觉回归（disabled 灰字 + cursor:not-allowed）留作 US-016 集成时统一核对。
- **AGENTS.md / .docs 同步**：US-015 关键约定段已加（三层双重防御 / nestingEnabled 默认 false 由 PreviewPage 解锁 / 上传预览 Tab 永远可点 / 测试 beforeEach 必须重置）；agent-component-map.md US-001 关键不变量 #2/#5/#6 已扩、新增 US-015 落地段；文件分工注释 US-015 标记。
## 2026-08-11 — US-020 后端 PIECES reload 机制 + GET /api/ptypes（branch: ralph/nesting-tab-linkage）
**Goal**: commit 成功后 `_PIECES_STATE` 即时更新（替代旧顶层 PIECES/GATE_MM/PIECES_BY_ID 三个 import 时常量），前端无需重启 ms-web 即可排料；新增 GET /api/ptypes 供高级配置弹窗取片型代表裁片（v1 仅 polygon，layer-aware 待 US-024）。
**Files changed**（仅后端，无前端）：
- `materialSorting-server/src/materialsorting/web/server.py` —— 顶层 imports 加 `threading`；新增模块级 `_state_lock = threading.Lock()` + `_PIECES_STATE: dict = {}`；新增三个 helper：`_build_pieces_state(intermediate_path)`（load_pieces + pieces_by_id 索引）、`_reload_pieces_state(intermediate_path)`（锁内整体 clear()+update() 原子替换 immutable snapshot）、`_get_pieces_state()`（锁内返回当前引用）。启动期 `try: _reload_pieces_state() except: pass`（allow-empty：intermediate 缺失不再让 import 崩）。`/ws/solve` accept 阶段 `_get_pieces_state()` 取一次快照，整连接内 pieces/gate_mm 不变（AC#5 关键不变量）。`/export` 路由同样改走快照。`commit_to_nesting` 在 `_commit_to_nesting_sync` 成功后调 `_reload_pieces_state()`；返回 payload 加 `reloaded: true`（异常时 `reloaded: false` + `reload_error`）。新增 `GET /api/ptypes`（D10）：按 ptype 分组取首个 piece，按 `_PTYPE_REPRESENTATIVE_FIELDS = ('polygon','net_polygon','internal_lines','notches','grain_line')` 透传白名单（v1 仅 polygon 在 intermediate 中，自动 layer-aware）。空 state 返 `{representatives: {}}`。
- `materialSorting-server/pyproject.toml` —— `[web]` extra 的 `uvicorn` → `uvicorn[standard]`（含 websockets 等）。
- `.docs/technical/agent-api-reference.md` —— 顶部「5 个 HTTP 端点 + 1 条 WS」；启动约束改为「allow-empty」；路由表加 `/api/ptypes`；`placed_to_world` 引用改为 `_get_pieces_state()['pieces_by_id']`；commit-to-nesting 响应字段加 `reloaded: true` + 副作用改为「commit 后 reload」；新增 `GET /api/ptypes` 完整章节（请求/响应/字段白名单/4 条关键不变量）；`/ws/solve` 加「accept 阶段快照口径」提示；关键不变量 #2/#4/#5/#8 + 新增 #8 改写只能走 `_reload_pieces_state()`。
- `.docs/technical/agent-file-map.md` —— 文件树 server.py 描述加 US-020；行数表 432→549；关键不变量 #7（pieces_by_id 来源）/ #8（启动期 _reload_pieces_state + 可 reload）同步。
- `materialSorting-server/src/materialsorting/web/AGENTS.md` —— 启动校验段加 `curl /api/ptypes`；「首次启动前必须」段补 allow-empty 降级行为；文件分工表 server.py 描述加 US-020；US-010 关键约定段「不自动 reload」改为「commit 后 reload（US-020）」；新增「US-020 关键约定」段 7 条（可 reload 状态 / immutable snapshot / 启动期 allow-empty / ws/solve 快照口径 / export 快照口径 / GET /api/ptypes / M1787 curl 验证 10 ptype）；「已踩坑」首条改为 allow-empty。
**Key invariants**:
- **immutable snapshot 模式**：`_reload_pieces_state()` 在锁内 `clear()+update()` 整体替换；读者始终拿完整一致 state，不会读到半状态。
- **AC#5（ws/solve 快照口径）**：accept 阶段 `state = _get_pieces_state()` 取一次快照，整连接内 pieces/gate_mm 不变（避免求解中途 reload 切数据）。`build_instance` 用快照 pieces，density 换算用快照 gate_mm。
- **启动期 allow-empty**：intermediate 缺失不再让 import 崩；`_PIECES_STATE={}` 时 `/api/ptypes` 返空 representatives、`/ws/solve` 报「排料数据为空」、`/export` 报「placed 的 pid 均未匹配」。commit 成功后自动填入。
- **GET /api/ptypes 字段白名单 layer-aware（D11）**：v1 intermediate 只有 polygon → 仅返 polygon；US-024 扩 intermediate 为 5 层后自动带 net_polygon/internal_lines/notches/grain_line，**本端点代码无需改**，前端按数据有无自适应。
- **commit 后 reload 异常降级**：罕见 I/O 竞态时 `reloaded: false` + `reload_error` 字段，保留旧 state 不半切；前端 US-021 看到 `reloaded=false` 可降级提示「需重启 ms-web」。
**验证（8 个 AC 全部通过）**：
- AC#1 ✅ `_state_lock = threading.Lock()`（type=lock）+ `_PIECES_STATE: dict`（keys = {doc, gate_mm, pieces, pieces_by_id}）。
- AC#2 ✅ `_reload_pieces_state()` 读 `paths.INTERMEDIATE` → 返回完整 state（n_pieces=176 / pieces_by_id size=176）。
- AC#3 ✅ `_get_pieces_state()` 返回当前引用（snap is _PIECES_STATE）；`/ws/solve`（line 462）+ `/export`（line 411）均走 helper。
- AC#4 ✅ `commit_to_nesting` 在 `_commit_to_nesting_sync` 成功后调 `_reload_pieces_state()`，返回 payload 加 `reloaded: true`；现有「TODO: PIECES 不会 reload」注释已删（替换为 US-020 reload 注释）。
- AC#5 ✅ 线程安全烟雾测试：4 reader + 2 writer 并发 200/20 次读写，0 异常（每次读到的 state 都包含 4 个完整 keys，无半状态）。
- AC#6 ✅ 端到端 curl+WS：ms-web :8000 启动 → POST /api/parse-dxf 上传 M1787（doc_id+sizes=[30]+10 片）→ POST /api/commit-to-nesting（n_pieces=16, reloaded=True）→ 立即 WS /ws/solve 收到 manifest（pid 数=16，匹配 commit 结果）→ final density=66.82%（正常求解）。**不需重启 ms-web**。
- AC#7 ✅ `python -c "from materialsorting.web.server import app"` 导入通过；分层依赖扫描 9 文件无反向 import。
- AC#8 ✅ `curl /api/ptypes` 对 M1787 返回 10 ptype 代表裁片（前片/后片/腰/前袋/后袋/机头/单排/双排/火机袋/裤耳），每个仅 polygon 字段。
**未做浏览器验证**：本故事纯后端（无 SVG/坐标变换/可视化改动），按规则不需 chrome-devtools-mcp；改用 Python 直接调函数 + curl+WS 端到端 + 线程安全烟雾测试联合覆盖 AC。
**清理**：ms-web (PID 28940) 已 `Stop-Process -Force` 杀掉，netstat :8000 LISTENING 0；测试期覆盖的 intermediate 已从 .bak 还原到 176 片基线；uploads/ 测试文件清理（部分 Windows 锁留待下次 GC）。
## 2026-08-11 — US-017 SizePicker 从上传 doc 动态读码号（branch: ralph/nesting-tab-linkage）
**Goal**: 超排页码号区从 uploadStore.doc 动态读码号（不锁死 M1787 的 8 码）；null 码 chip 显示「通用」；doc=null 时 StatusLine 增提示「请先在上传预览页解析母版」；DEFAULT_FORM.sizes=[] 强制用户选。
**Files changed**（纯前端，无后端）：
- `materialSorting-web/src/components/ControlPanel/SizePicker.tsx` —— 重写。新增 `useUploadStore(s=>s.doc)` 订阅；chip 列表 = `doc ? doc.sizes.map(s=>s.size) : [...SIZES]`（doc=null fallback SIZES，前端不二次排序）；`NULL_SIZE_LABEL='通用'`（与 SizeTabs 同语义）+ `sizeKey(s)`（null→'null'，number→String(n)）+ `sizeLabel(s)`（null→「通用」）两个纯函数；`SizePickerProps.selected/onChange` 类型从 `number[]` 扩为 `(number|null)[]`；chip id 改 `sz_${sizeKey}`（null → `sz_null`）；onChange 不再 sort（旧版 `selected.sort((a,b)=>a-b)` 移除）。
- `materialSorting-web/src/lib/params.ts` —— `FormState.sizes: (number|null)[]`（从 `number[]` 扩）；`DEFAULT_FORM.sizes = []`（从 `[...SIZES]` 改为空数组，强制用户选）；删 `import { SIZES }`（params.ts 内不再用 SIZES）。
- `materialSorting-web/src/components/ControlPanel/ControlPanel.tsx` —— 新增 `useUploadStore(s=>s.doc)` 订阅；`handleStart` / `handleExport` 用类型守卫 `(s): s is number => s !== null` 过滤 null（保持下游 StartConfig.sizes / useExport.exportAs 的 `number[]` 契约不变）；`visibleStatus = doc===null ? ${status} — 请先在上传预览页解析母版 : status`；StatusLine 接 visibleStatus。
- `materialSorting-web/src/components/ControlPanel/__tests__/SizePicker.test.tsx` —— 新建。8 项单测：doc=null fallback SIZES / doc 11 码（含 null）渲染全 11 / null 码显示「通用」+ id `sz_null` / 切 doc 自动重渲染（订阅 uploadStore.doc） / toggle 数字 chip 不二次排序 / toggle null chip onChange 收到 [null] / selected 含 null → checked / key-id 唯一无冲突。覆盖 AC#1 + AC#3 + 切 doc 重渲染 + 边界（含 PassThrough+useState 模拟父级累积）。
- `materialSorting-web/src/components/ControlPanel/__tests__/ControlPanel.test.tsx` —— beforeEach/afterEach 加 `useUploadStore.getState().reset()`（模块级单例隔离）；AC#1 改为「8 fallback chips, NONE default-checked」（旧版全选废除）；AC#6 default-Start 改为「先全选再 Start」（默认 sizes=[] 校验会失败）；AC#7 简化为「默认空 + Start → onStatus error」（旧版需先点掉全选）；AC#6 toggle-sizes 改为「默认空 + 勾 30/31」（旧版从全选取消）；AC#6 fill per_type 加「先勾一个码号」；4 个 multi_seed Start 用例均加「先勾一个码号」；新增 US-017 StatusLine hint describe block 4 项（doc=null 增提示 / doc 非空无提示 / fallback SIZES / doc.sizes 渲染含 null 通用）。
- `materialSorting-web/src/lib/__tests__/params.test.ts` —— 新增「US-017: DEFAULT_FORM.sizes 默认空数组」回归断言。
- `.docs/technical/agent-component-map.md` —— `src/lib/params.ts` / `ControlPanel.tsx` / `SizePicker.tsx` 3 行描述加 US-017 改动；`ControlPanel.test.tsx` 行项数从 15 → 23；新增 `SizePicker.test.tsx` 行；落地段表新增 US-017 一行。
- `materialSorting-web/AGENTS.md` —— 文件分工注释加「US-017 SizePicker 动态读码号 + DEFAULT_FORM.sizes=[]」；新增「US-017 关键约定」段 8 条（订阅 uploadStore.doc / DEFAULT_FORM.sizes=[] / FormState.sizes (number|null)[] / null 通用文案 / 下游 WS+export 过滤 null 保 number[] 契约 / StatusLine 提示 / 测试隔离 reset uploadStore / 未做浏览器验证）；US-004 DEFAULT_FORM 关键约定加 US-017 交叉引用（sizes 默认 []）。
**Key invariants**:
- **SizePicker 订阅 uploadStore.doc（不硬编码 SIZES）**：doc 非空 → `doc.sizes.map(s=>s.size)`；doc=null → fallback SIZES。改订阅源会破坏「切款母版→码号区自动同步」语义。
- **DEFAULT_FORM.sizes = []（不再是 [...SIZES] 全选）**：用户必须主动勾选；ControlPanel「请至少选一个码号」校验保留兜底。后端 build_instance 入参契约不变（ControlPanel.handleStart 过滤 null 后透传 number[]）。
- **下游 WS / export 契约不变（仍是 number[]）**：ControlPanel.handleStart / handleExport 用类型守卫过滤 null；StartConfig / StartPayload / useExport.exportAs 签名不改。M1787 实际母版无 null 码；含 null 母版的完整端到端支持留给 US-022（按 (label, sizeKey) 查 quantities，sizeKey 已支持 null）。
- **null 码 chip 文案 / key / id 三件套**：`sizeLabel(null)='通用'` / `sizeKey(null)='null'` / `id='sz_null'`。改任一项需同步 SizePicker.test.tsx + ControlPanel.test.tsx US-017 用例。
**验证（5 个 AC 全部通过）**：
- AC#1 ✅ SizePicker 订阅 useUploadStore(s=>s.doc)；doc 非空 → doc.sizes.map(s=>s.size)；doc=null → fallback SIZES（SizePicker.test.tsx 2 项 + ControlPanel.test.tsx US-017 2 项）。
- AC#2 ✅ DEFAULT_FORM.sizes=[]（params.test.tsx 新增断言；ControlPanel.test.tsx AC#1 改为 NONE checked）。
- AC#3 ✅ null 码 chip 显示「通用」+ ControlPanel 校验保留 + doc=null 时 StatusLine 增「请先在上传预览页解析母版」（SizePicker.test.tsx 2 项 + ControlPanel.test.tsx US-017 StatusLine hint 4 项）。
- AC#4 ✅ ≥4 项单测：doc=null fallback SIZES / doc 11 码渲染全 11 / null 码显示「通用」/ 切 doc 自动重渲染（SizePicker.test.tsx 4 项核心 + 4 项补充 = 8 项）。
- AC#5 ✅ `npm run typecheck` 通过；`npm run test` 全绿（29 文件 / 405 项，含新增 SizePicker.test.tsx 8 项 + ControlPanel.test.tsx +4 项 US-017 StatusLine hint + params.test.ts +1 项）；`npm run build` 无报错（static/assets/index-*.js 182KB）。
**未做浏览器验证**：本故事无 SVG/坐标变换（仅 chip 渲染 + StatusLine 文案），AC 仅要求 typecheck + 单测 + build，故跳过 chrome-devtools-mcp；浏览器视觉回归（chip 渲染 / 通用 文案 / doc 切换重渲染）留作 US-021 自动 commit 集成时统一核对（届时 done→commit→切 nesting 端到端联调）。
---
## US-018 高级配置弹窗（按钮→table+片型缩略图+放大预览）— 2026-08-11
**Story**：把现有 `<details>` 折叠的「每片型覆盖」改成「按钮→弹窗 table」，表头列=片型缩略图（点击放大预览）+片型名，tbody 2 行=重合/旋转。两层独立 ESC，草稿+确定模式，D7 预填。
**新增文件**：
- `materialSorting-web/src/types/ptype.ts`（39 行）— GET /api/ptypes 响应契约类型 `PtypesResponse.representatives[ptype]: PtypeRepresentative`，layer-aware 字段（polygon/net_polygon/internal_lines/notches/grain_line），无 label/name。
- `materialSorting-web/src/store/controlPanelStore.ts`（45 行）— Zustand store：`modal:'per_type'|null` + `previewPtype:string|null` 两个独立字段 + 4 actions（openModal/closeModal/openPreviewPtype/closePreviewPtype）。
- `materialSorting-web/src/components/ControlPanel/PerTypeOverridesModal.tsx`（326 行）— 高级配置弹窗。订阅 `modal==='per_type'` 显隐；Inner 用 `key="per-type-modal"` 强制重建；mount 时 fetch /api/ptypes（cancelled flag 防 StrictMode 双 mount race）；D7 预填 INTERNAL_PTYPES={'单排','双排','火机袋','裤耳'} d=10/tol=0，其它 0/0；草稿+确定；ESC listener 判 previewPtype!==null 时 return（让放大预览优先）。
- `materialSorting-web/src/components/ControlPanel/PtypePreviewModal.tsx`（133 行）— 放大预览模态。订阅 previewPtype 显隐；一次 mount fetch /api/ptypes 缓存；ESC 仅 previewPtype!==null 时生效，总关 previewPtype；rep 缺失渲染 .ptype-preview-empty 降级空态。
- `materialSorting-web/src/store/__tests__/controlPanelStore.test.ts`（7 tests）
- `materialSorting-web/src/components/ControlPanel/__tests__/PerTypeOverridesModal.test.tsx`（19 tests）— 含 mount triggers fetch / fetch failure degrades / fetch success renders compact svg / D7 prefill / 双层独立 ESC / 点击缩略图开 PtypePreviewModal / placeholder hint d≤X/t≤Y from V03_TABLE 等。
- `materialSorting-web/src/components/ControlPanel/__tests__/PtypePreviewModal.test.tsx`（7 tests）— 含 null 不渲染 / fetch success 渲染 SVG / ✕ 关 / 遮罩关 / ESC 关 / stacked close preview keeps 底层 / fetch 失败降级空态。
**修改文件**：
- `materialSorting-web/src/components/preview/PiecePreviewSVG.tsx` — 加 `compact?:boolean` prop：compact=true 时 pad 默认 COMPACT_PAD=2（不是 DEFAULT_PAD=14）、跳过 renderLabel（不渲染 A/B/C 文本）、5 层 layer-aware 渲染不变。useEffect deps `[piece, pad, compact]`。
- `materialSorting-web/src/components/ControlPanel/PerTypeOverrides.tsx`（47 行，重写）— 旧 10 行 `<details>` 折叠 → 单按钮触发器 `.per-type-btn`；内挂 PerTypeOverridesModal + PtypePreviewModal；保留 values/onChange 契约。
- `materialSorting-web/src/style.css` — 加 `.per-type-wrapper/.per-type-btn`（#2c5d8f 蓝）、`.per-type-overlay`(z-1100)/.per-type-modal`(#26282e)、`.per-type-head/.per-type-title/.per-type-close`、`.per-type-table-wrap`(overflow-x:auto)/.per-type-table`(table-layout:fixed)/.per-type-rowhead`(sticky left)、`.ptype-col`(80px)/.ptype-thumb`(64×64 zoom-in)/.ptype-name`、`.per-type-table input[type='number']`、`.per-type-hint/.per-type-actions/.per-type-btn-cancel/.per-type-btn-confirm`(#2ea06c)、`.ptype-preview-overlay`(z-1200)/.ptype-preview-modal/.ptype-preview-head/.ptype-preview-name/.ptype-preview-close/.ptype-preview-body/.ptype-preview-empty`。
- `materialSorting-web/src/components/preview/__tests__/PiecePreviewSVG.test.tsx` — 加 5 项 compact 测试。
- `materialSorting-web/src/components/ControlPanel/__tests__/ControlPanel.test.tsx` — 删 V03_TABLE import / firstHeadButton；beforeEach 加 fetchSpy stub；旧 per_type tests 改为 button+modal tests（AC#4 渲染 .per-type-btn / 点击打开 modal）。
- `materialSorting-web/src/__tests__/App.test.tsx` — beforeEach/afterEach 加 fetchSpy stub（防 App mount 触发 PtypePreviewModal fetch 的 act warning）。
- `.docs/technical/agent-component-map.md` — 加 US-018 章节（new/modified files table + 9 key invariants），更新 store/ / types/ / ControlPanel/ / style.css 行描述。
- `materialSorting-web/AGENTS.md` — 加 US-018 关键约定（按钮触发替代折叠 / controlPanelStore 两字段 / 双层独立 ESC / D7 预填 / 草稿+确定 / 缩略图 fetch / compact prop / z-index / mockImplementation 必要性 等 14 条），更新文件分工行 + style.css + ControlPanel/ 行描述。
**验收**：
- `npm run typecheck` ✅ 无错
- `npm run test` ✅ 32 test files / 443 tests pass / 0 warning
- `npm run build` ✅ 187.01 KB JS / 17.53 KB CSS
- PerTypeOverridesModal 19 项（≥14）+ PtypePreviewModal 7 项（≥6）+ controlPanelStore 7 项 + PiecePreviewSVG compact 5 项 + ControlPanel button 改造 ✅
- 双层独立 ESC（AC#10）：放大预览打开时 ESC 只关放大预览，底层 modal + 草稿保真 ✅
- D7 预填（INTERNAL_PTYPES d=10/tol=0，其它 0/0；非空值保留）✅
- fetch /api/ptypes 失败降级（thumbnail disabled + ptype name 兜底，不阻塞 d/tol input）✅
- 缩略图 64×64 + cursor:zoom-in + 点击 openPreviewPtype ✅
- layer-aware：v1 仅外轮廓（rep 只有 polygon），US-024 数据带 5 层后代码无需改 ✅
**关键决策**：
- D5 按钮触发替代 `<details>`：母版 10 片型 20 input 常驻 ControlPanel 视觉噪声过大，收进 modal 更简洁。
- D7 预填用 INTERNAL_PTYPES Set（不读 v03.ts INTERNAL 标记），保持 store/types 解耦。
- mockImplementation vs mockResolvedValue：StrictMode dev 双 mount 调 2 次 fetch，mockResolvedValue 共享同一 Response body 会被首次 .json() 消费完，第二次报 "body stream already read"。
- z-index 三层：tooltip(100) < piece-qty/zoom(1000) < per-type(1100) < ptype-preview(1200)。
**US-019 — ControlPanel 主面板精简（删内外两档+片名）** — 2026-08-11
分支：ralph/nesting-tab-linkage
改动：
- `materialSorting-web/src/lib/params.ts` —— FormState 删 d_ext/d_int/tol_ext/tol_int 字段；DEFAULT_FORM 同步删；collectParams 改为 params 永远全 0（不再从 form 读两档）；删 unused `num()` helper；per_type 解析逻辑保留不变；头部注释 + 不变量文档更新（US-019 修订）。
- `materialSorting-web/src/components/ControlPanel/ControlPanel.tsx` —— 删 ErodeInputs/ToleranceInputs import + JSX 渲染；hint 文案去掉「内部片 = 单排/双排/...」改为「重合 erode / 旋转公差按每片型 v0.3 上限覆盖 —— 见『高级配置』弹窗」（删除内部/外部区分）。
- 删除 `materialSorting-web/src/components/ControlPanel/ErodeInputs.tsx` + `ToleranceInputs.tsx`（无独立 __tests__，CSS 规则保留向后兼容）。
- `materialSorting-web/src/lib/__tests__/params.test.ts` —— 重写：删 `legacyCollectParams`/`legacyNum` 参考实现 + 11 组对比；新增「FormState 无 d_ext 等字段」+「params 永远全 0（4 组 form 对比）」+「全 10 ptype 填」用例；per_type 单档/双档/空白/显式 0 用例保留并补充覆盖；总数 19 项。
- `materialSorting-web/src/components/ControlPanel/__tests__/ControlPanel.test.tsx` —— AC#2 用例删 d_ext/d_int/tol_ext/tol_int 默认值断言 + 加「主面板不再渲染」is-null 断言；新增独立「US-019 AC#6 主面板不再渲染内外两档输入」用例（断言 #d_ext/#d_int/#tol_ext/#tol_int 均 null + 不含「内/外两档」文案 + PerTypeOverrides 按钮仍在）；AC#6 cfg.params 期望从 `{d_int:10,...}` 改为全 0；总数 24 项。
- `.docs/technical/agent-component-map.md` —— US-004 表删 ErodeInputs/ToleranceInputs 行；params.ts / ControlPanel.tsx / params.test.ts / ControlPanel.test.tsx 行描述同步 US-019；US-004 关键不变量 #1/#2/#3/#5 修订（删除「四档」表述、删 d_int=10 默认、删 `id=d_ext` legacy id 列举）；末尾 vanilla-id 列举同步。
- `.docs/technical/agent-api-reference.md` —— /ws/solve start payload 示例 params 改为全 0 + 注释说明 US-019 起前端永远传全 0。
验收：
- typecheck 通过（tsc --noEmit 无错）。
- npm run test 全绿：32 文件 / 448 项通过（params.test.ts 19 项、ControlPanel.test.tsx 24 项均绿）。
- npm run build 无报错（92 modules、产物 185.59 kB）。
- 关键不变量：后端 build_instance 入参契约不变（params 仍传，只是全 0；per_type 仍传），后端无改动。
- 主面板不再渲染 d_ext/d_int/tol_ext/tol_int 输入（ControlPanel.test.tsx 两条断言）。
**未做浏览器验证**：本故事无新 SVG/坐标变换（只是删输入组件 + 改 params 默认），AC 仅要求 typecheck + 单测 + build，故跳过 chrome-devtools-mp；浏览器视觉回归（主面板无两档输入、hint 文案、高级配置按钮仍可开 modal）留作 US-021 自动 commit 集成时统一核对。
---
**未做浏览器验证**：本故事无新 SVG/坐标变换（PiecePreviewSVG compact 仅复用现有 5 层渲染 + 跳过 label + pad=2），AC 仅要求 typecheck + 单测 + build，故跳过 chrome-devtools-mcp；浏览器视觉回归（缩略图 64×64 + 放大预览 + 双层 ESC + D7 预填）留作 US-021 自动 commit 集成时统一核对。
---
## US-021 — 解析成功自动 commit + 自动切超排（2026-08-11）
### 交付
- **新建 `src/hooks/useCommitToNesting.ts`** —— commit(doc_id, filename?) → Promise<{ok, summary?, error?}>。fetch POST /api/commit-to-nesting JSON body。防连击（committingRef + commitStatus==='committing' 双重防护）。commit done → setNestingEnabled(true) + setTab('nesting')（D1 闭环）。commit fail → commitStatus='error' 不切 Tab（D5）。
- **改造 `src/hooks/useParseDxf.ts`** —— 解析成功（setState status='done'）后 `void commit(doc.doc_id, doc.filename)`（D1 副作用，不阻塞预览）。进入 uploading 时同步清 commit 字段。
- **扩 `src/store/uploadStore.ts`** —— commitStatus/commitError/commitSummary 三字段 + reset() 同步清。CommitStatus + CommitSummary 类型导出。
- **改造 `src/components/preview/UploadPanel.tsx`** —— 加 commit 状态行（committing→「应用中…」/ done→「已应用至超排：N 裁片，M 码」/ error→「应用失败：msg」）。复用 .upload-status 三态 className。
### 测试
- **新建 `src/hooks/__tests__/useCommitToNesting.test.tsx`** —— 15 项单测（≥10 AC 满足）：fetch URL/method/body/Content-Type / 200→commitSummary+done / filename optional / 422/404/400→error / non-JSON→statusText / 防连击仅一次 / committingRef reset / network error / commitStatus transition idle→committing→done/error / commit done→D1 setNestingEnabled+setTab / commit fail→D5 不切 Tab / entering committing clears stale error。
- **改造 `src/hooks/__tests__/useParseDxf.test.tsx`** —— 原 15 项更新 fetch 计数（成功路径 parse+commit=2 / 两次 upload=4）+ beforeEach/afterEach 加 uiStore reset。**新增 7 项 US-021 集成**（≥6 AC 满足）：parse done→auto-trigger commit（calls[1] body 验证）/ commit done→activeTab=nesting / commit done→nestingEnabled=true / commit fail→不切 Tab / commit fail→commitError / commit done→commitSummary / parse fail→commit 不触发。
- **扩 `src/store/__tests__/uploadStore.test.ts`** —— 新增 5 项 commit 字段测试。
- **更新 `src/components/preview/__tests__/UploadPanel.test.tsx`** —— 2 项 fetch 计数 + uiStore reset。
### 结果
- `npm run typecheck` ✅、`npm run test` 33 文件 475 项全绿 ✅、`npm run build` 无报错 ✅。
- `python -c "from materialsorting.web.server import app"` 导入通过 ✅（后端无改动，仅验证不回归）。
## 2026-08-11 — US-022 求解输入数量 demand（per-size，0 跳过）
**分支**: ralph/nesting-tab-linkage
**目标**: 上传预览页编辑的每片每码数量真正参与排料（per-size demand；0=排除），解析后默认每片每码 hydrate=1。
**实现要点**:
1. **后端共享 labeling 模块**（新文件 `nesting_engine/labeling.py`）：
   - `label_for(idx)` / `centroid(poly)` / `size_sort_key(size)` / `compute_size_ptype_labels(pieces, gmap, group_names)`。
   - 单一真相源 —— parse/commit/pieces_export 三处标注均 import 此模块，避免 server.py `_label_for` 与 pieces_export 逻辑重复（nesting_engine 不能 import web，故共享层放本层）。
   - 依赖方向合规：仅 import 标准库 + duck-typed PieceOutline 属性。
2. **intermediate 加 label 字段**：
   - `server.py _commit_to_nesting_sync`：调 `compute_size_ptype_labels(pieces, gmap, GROUP_NAMES)` → `{(size, ptype): label}`，每片 NestPiece 写 `label`（L/R 同 ptype 共享）。
   - `pieces_export.py`：baseline CLI 解析母版（MASTER_DXF_GLOB）标 label；母版缺失则 label=null（向后兼容）。
   - **AC#5 label 对齐不变量**：commit 走 NestPiece（归一化+镜像），parse 走 PieceOutline（原始坐标），坐标系不同；但两者源自同一母版的 `explore.collect_pieces`，对原始 pieces 施行与 `_build_parse_payload` 完全一致的排序键 `(-centroid_y, centroid_x, -area_mm2, block_name, piece_index)` + `label_for(idx)` 标注 → 按 (size, ptype) 严格对齐。M1787 验证 10/10 ptype 对齐（决策：label 稳定，不改用 (name,size) 为 key）。
3. **WS /ws/solve 入参增 quantities**：
   - `server.py ws_solve` 读 `msg['quantities']`（dict | None），传给 build_instance。
   - `solver.py build_instance` 加 `quantities=None` 参数：按 `(piece.label, str(piece.size))` 查 N → `spyrrow.Item(demand=N)`；demand=0 跳过（D2）；piece 缺 label 或 quantities=None → demand=1（向后兼容）。
   - 验证：build_instance 单片/对片 demand=0 skip、demand=2、缺 label 回退 5 项测试全过。
4. **前端 quantities 序列化 + WS payload**：
   - `types/ws.ts StartPayload` 加 `quantities: Record<string, Record<string, number>> | null`。
   - `lib/params.ts serializeQuantities(qtyMap, sizes)` 纯函数：per-size 取 perSize（过滤未选中码），global 展开 globalValue 到全部选中码 sizeKey。空→null。7 项单测。
   - `useSolveRun.ts StartConfig` 加可选 `quantities`，透传到 payload（`cfg.quantities ?? null`）。
   - `ControlPanel.tsx handleStart`：`useQtyStore.getState().quantities`（不订阅）+ sizesNum → serializeQuantities → cfg.quantities。
   - `NestingPage.tsx handleStart`：透传 cfg.quantities 到 start()（N seed 共用）。
5. **qtyStore 加 hydrateDefaults(sizes, labels)**：
   - sizes × labels 交叉积每项填 1（per-size 模式）。与已有 hydrateDefault(entries) 语义同、入参形式不同。空 sizes/labels → 空 map。4 项单测。
   - PreviewPage 仍用 entries 版（更通用：各码 ptype 集合可能不同），新 action 供 useParseDxf 集成层按 (sizes, labels) 调用（AC#1 compliance）。
**验证**:
- `npm run typecheck` 通过；`npm run test` 488/488 全绿（+17 项新增：useSolveRun 2 + params 7 + qtyStore 4 + key 枚举 fix + 其他不变）。
- `npm run build` 成功。
- `python -c "from materialsorting.web.server import app"` 导入通过；solver/pieces_export/labeling 均 import OK。
- 架构依赖方向不反向（nesting_engine 不 import web）。
- AC#5 label 对齐 M1787 验证 10/10。
- build_instance demand=0 skip / demand=2 / 缺 label 回退 5 项测试全过。
- ms-pieces-export 重新生成 intermediate，128 片均带 label（A-J 10 标签，pair type L/R 共享）。
**未做浏览器验证**：本故事无 SVG/坐标变换（仅 store/hook/payload 扩展 + 后端 demand 逻辑），AC 仅要求 typecheck + 单测 + import 检查。AC#7 curl+WS 人工验证（改某片某码 0→manifest 该片不出现）由 build_instance 测试等价覆盖（demand=0 skip 已验证）。浏览器视觉回归留作整体回归。
---
- **未做浏览器验证**：本故事无 SVG/坐标变换（仅 store 字段扩展 + hook + UploadPanel 状态行 DOM），AC 仅要求 typecheck + 单测 + build。浏览器视觉回归（「应用中…」loading + 「已应用至超排」摘要 + 自动切 Tab + commit fail 红字）留作后续整体回归。
---
## US-023 (2026-08-11) — PTYPE_COLORS 清理遗留 6 片型
**改动**：
- `materialSorting-server/src/materialsorting/nesting_engine/sparrow_baseline.py` `PTYPE_COLORS`：删 6 遗留（门头/门里/拉链/表袋/双袋/侧袋）+ 加 5 v0.3 实际（机头/单排/双排/火机袋/裤耳），共 10 类与 intermediate `pieces[*].ptype` 完全对齐。
- `CLAUDE.md`「已知问题」段第 1 条（PTYPE_COLORS 遗留）删除；`.docs/technical/agent-file-map.md` PTYPE_COLORS 常量代码块同步更新 + 已知问题 #1 删除（原 2-6 → 1-5 重编号）。
**关键决策（PRD 补位）**：PRD US-023 描述仅列 4 个新片型色（单排=#e377c2 / 双排=#ff1493 / 火机袋=#8c564b / 裤耳=#17becf），但 v0.3 实际 10 片型含 **机头**（intermediate 验证：前片/前袋/单排/双排/后片/后袋/机头/火机袋/腰/裤耳）。AC 要求「屏幕所有 v0.3 片型彩色正确（无灰色 fallback）」，故补 **机头=#bcbd22**（d3 category10 末位色，与前片/后片同饱和度；原 `双袋` 用过该值但属遗留删除）。文件 .docs/technical/agent-file-map.md:364 早已标注「新增 5 个」—— PRD 描述属遗漏，按 AC 与 file-map 口径补位。色值均「待版师屏幕定色」。
**不变量验证**：
- 调用方 `web/solver.py:128` / `web/export.py:77,112,113` / `sparrow_baseline.py:132,147` 均用 `PTYPE_COLORS.get(ptype, DEFAULT_COLOR)` dict 查找 —— 签名零改动。
- `DEFAULT_COLOR='#bbbbbb'` 兜底保留。
- `python -c "from materialsorting.web.server import app"` 导入通过；10 v0.3 ptype 查表 0 个 gray fallback。
- 启动 ms-web（PID 34932）→ `/api/ptypes` 返 10 类型 → 验后 taskkill。
---
## US-024 (2026-08-11) — 展示+导出切新版裁片（5 层）
**目标**：超排页 NestSVG + 最终导出（PNG/R12-DXF）每片裁片均展示毛版+净版+内部线+刺口+布纹线 5 层（与上传预览页同口径）；求解仍用外轮廓做碰撞。
**改动**：
- `materialSorting-server/src/materialsorting/dxf_parser/export_dxf.py`：`write_piece_dxf` 写 5 层（layer1 毛版闭合 POLYLINE / layer14 净版闭合 POLYLINE / layer8 内部线多条 POLYLINE / layer4 刺口 POINT / layer7 布纹线 LINE）；notch 仅存 POINT 位置法线丢弃；`main()` 改用 `collect_pieces_with_details`。
- `materialSorting-server/src/materialsorting/nesting_bounds/load_pieces.py`：扩 NestPiece 加 4 字段（net_polygon/internal_lines/notches/grain_line default_factory 向后兼容）；新增 `_read_piece_full` 读 5 层 + notch 法线按 outline 最近边重算；新增 `_rotate_normal / _grain_rotation_deg / _apply_layer_transforms` 共享 transform 链；`load_nest_pieces` L 与 R 共享 transform。
- `materialSorting-server/src/materialsorting/nesting_engine/pieces_export.py` + `web/server.py _commit_to_nesting_sync`：intermediate 每片加 4 字段透传。
- `materialSorting-server/src/materialsorting/web/solver.py`：build_instance pid_meta 加 4 字段（`.get()` 兼容）。
- `materialSorting-server/src/materialsorting/web/server.py ws_solve`：manifest pieces[] 加 4 字段。
- `materialSorting-server/src/materialsorting/web/export.py`：`LAYER5_COLOR_*` 字面量与前端 LAYER5_COLORS 同色；`_transform_normal` 法线随片旋转；`placed_to_world` 扩 5 层变换；`render_png` 5 层叠加；`write_marker_dxf` 多 layer 各自独立 POLYLINE/POINT/LINE entity。
- `materialSorting-web/src/types/piece.ts`：PieceInfo 加可选 4 字段；新增 Notch / GrainLine 类型。
- `materialSorting-web/src/constants/colors.ts`：新增 `LAYER5_COLORS` 共享常量 + 导出 `NOTCH_LEN_MM = 8`。
- `materialSorting-web/src/components/preview/PiecePreviewSVG.tsx`：5 层颜色改 import LAYER5_COLORS（行为不变）。
- `materialSorting-web/src/components/nests/NestSVG.tsx`：PieceEntry 增 netEl/internalEls/notchEls/grainEl；manifest 到达时按数据有无创建（pointerEvents='none'）；frame 切换 setAttribute points/x1y1x2y2/display；新增 `transformPt` 单点辅助。
- `materialSorting-web/src/components/nests/__tests__/NestSVG.test.tsx`：+8 项 5 层渲染单测。
- `materialSorting-web/src/__tests__/useSolveRun.test.tsx`：+1 项 manifest 5 层保真测试。
**验证**：
- `cd materialSorting-web && npm run typecheck` 通过；`npm run test` 33 文件 497/497 全绿（+9 项新增）。
- `python -c "from materialsorting.web.server import app"` 导入通过。
- 后端 e2e smoke 176 片全链路 5 层：master→collect→write_piece_dxf→_read_piece_full（notch 法线重算）→load_nest_pieces（共享 transform L+R）→intermediate.json（net=128/internal=88/notch=104/grain=128）→manifest→export PNG 5 层 + R12-DXF layer id 集 {0,1,4,7,8,14} 全齐。
- `/api/ptypes` 10 ptype 代表裁片带 5 层（D11 副作用：US-018 缩略图自动升级 5 层无需改代码）。
- 5 层中 4 层仅渲染/导出透传不参与 sparrow NFP 碰撞（关键不变量 AC#1）；5 层节点只在 manifest 到达时建一次 frame 切换只 setAttribute 不重建 DOM（AC#2 性能保护）。
**未做浏览器视觉验证**：chrome-devtools-mcp 工具不在本会话工具集；NestSVG 5 层渲染（绿 dashed 净版 + 橙内部线 + 黄刺口 + 红 dashed 布纹线）已用 8 项 NestSVG 单测 + e2e smoke 验证 5 层节点存在 + setAttribute 写入 + display 切换 + rotation transform 正确；导出 PNG/DXF 5 层已用 ezdxf 回读 layer id 集 + render_png 调用栈验证。版师屏幕视觉回归 + ET2008 真机读 R12-DXF（D4 硬验收）留作后续整体回归。
**未做浏览器视觉验证**：chrome-devtools-mcp 工具不在本会话工具集；AC 的「求解后屏幕彩色无灰」视觉回归已用代码级断言替代（10/10 ptype 查表均非 #bbbbbb）。版师屏幕定色 + ET2008 导出色觉核对留作后续整体回归。
---
## US-024 (2026-08-11) — 展示+导出切新版裁片（5 层）
**目标**：超排页 NestSVG + 最终导出（PNG/R12-DXF）每片裁片均展示毛版+净版+内部线+刺口+布纹线 5 层（与上传预览页同口径）；求解仍用外轮廓做碰撞。
**实现要点**:
1. **后端 5 层管线全链路打通**：
   - `dxf_parser/export_dxf.py`：`write_piece_dxf` 写 5 层（layer1 毛版闭合 POLYLINE / layer14 净版闭合 POLYLINE / layer8 内部线多条 POLYLINE / layer4 刺口 POINT / layer7 布纹线 LINE）。notch 仅存 POINT 位置，法线丢弃。`main()` 改用 `collect_pieces_with_details` 取代 `explore.collect_pieces`。
   - `nesting_bounds/load_pieces.py`：扩 `NestPiece` 加 4 字段（net_polygon / internal_lines / notches / grain_line，default_factory 向后兼容）；新增 `_read_piece_full(dxf_path)` 读 5 层 + notch 法线按 outline 最近边重算（与 collect._nearest_edge_with_normal 同算法）；新增 `_rotate_normal / _grain_rotation_deg / _apply_layer_transforms` 把 5 层原始数据按 rotate→mirror→normalize 链统一变换；`load_nest_pieces` L 与 R 共享 transform 链。
   - `nesting_engine/pieces_export.py` + `web/server.py _commit_to_nesting_sync`：intermediate 每片加 4 字段（与 NestPiece 同名透传，`.get()` 默认空/None 兼容旧 intermediate）。
   - `web/solver.py build_instance`：pid_meta 加 4 字段（`.get()` 兼容）。
   - `web/server.py ws_solve`：manifest pieces[] 加 4 字段。
2. **前端 NestSVG 5 层渲染（命令式 setAttribute）**：
   - `types/piece.ts`：PieceInfo 加可选 `net_polygon? / internal_lines? / notches? / grain_line?`；新增 `Notch = [number, number, number, number]`（x,y,nx,ny）+ `GrainLine = [number, number, number, number]` 类型。
   - `constants/colors.ts`：新增 `LAYER5_COLORS`（ROUGH_FILL/ROUGH_STROKE/NET/INTERNAL/NOTCH/GRAIN）+ 导出 `NOTCH_LEN_MM = 8`，被 PiecePreviewSVG + NestSVG 共用（视觉一致）。
   - `components/preview/PiecePreviewSVG.tsx`：5 层颜色字面量改 import LAYER5_COLORS（行为不变）。
   - `components/nests/NestSVG.tsx`：PieceEntry 增 netEl/internalEls/notchEls/grainEl。manifest 到达时按数据有无创建对应节点（pointerEvents='none'，不干扰毛版 mousemove tooltip）；frame 渲染 placed→5 层 setAttribute('points'/'x1'/'y1'/'x2'/'y2')+display=''，unplaced→display='none'。notch 端点 = `P ± NOTCH_LEN_MM/2 * unit_normal` 各按 transformPt 变换（与 lib/geometry pointsStr 同公式单点版）；grain 端点按 transformPt 变换。
3. **导出 PNG + R12-DXF 5 层（web/export.py）**：
   - `LAYER5_COLOR_NET/INTERNAL/NOTCH/GRAIN` 字面量与前端 LAYER5_COLORS 同色（绿 #33cc33 / 橙 #ff8c1a / 黄 #ffd700 / 红 #e53e3e）。
   - `_transform_normal(nx, ny, rot)` 法线随片旋转。
   - `placed_to_world` 扩 5 层变换（notch 点按点变换 + 法线按向量旋转）。
   - `render_png` 5 层叠加（毛版半透明蓝 + 净版绿虚线 + 内部线橙 + 刺口黄短线段 NOTCH_LEN_MM=8 + 布纹线红虚线）。
   - `write_marker_dxf` 多 layer（outline layer1 + net layer14 + internal layer8 + notch layer4 POINT + grain layer7 LINE），各自独立 POLYLINE/POINT/LINE entity（R12 + POLYLINE 非 LWPOLYLINE，ET2008 兼容）。
**验证**:
- `cd materialSorting-web && npm run typecheck` ✅；`npm run test` 33 文件 497/497 全绿 ✅（NestSVG.test.tsx +8 项 5 层渲染 + useSolveRun.test.tsx +1 项 manifest 分发保真）。
- `python -c "from materialsorting.web.server import app"` 导入通过 ✅。
- 后端 e2e smoke 176 片全链路：master → collect（layer1+14+8+4+7 五层 IR）→ write_piece_dxf（5 层单裁片 DXF）→ _read_piece_full（5 层读 + notch 法线重算）→ load_nest_pieces（共享 transform 链 L+R 镜像）→ intermediate.json 5 层字段（前片_30：net=179pts/internal=4/notches=8/grain 有；分布 net=128/128 internal=88/128 notches=104/128 grain=128/128）→ manifest 5 层 → export PNG 5 层叠加 + R12-DXF layer id 集 {0,1,4,7,8,14} 全齐。
- `/api/ptypes` 10 ptype 代表裁片带 5 层（US-018 缩略图自动从 v1 外轮廓升级为 5 层，US-018 代码无需改动 —— D11 副作用验证）。
- 5 层中 4 层仅渲染/导出透传不参与 sparrow NFP 碰撞（关键不变量 AC#1）；5 层节点只在 manifest 到达时建一次 frame 切换只 setAttribute 不重建 DOM（AC#2 性能保护）。
**未做浏览器视觉验证**：chrome-devtools-mcp 工具不在本会话工具集；NestSVG 5 层渲染（绿 dashed 净版 + 橙内部线 + 黄刺口 + 红 dashed 布纹线）已用 8 项 NestSVG 单测 + e2e smoke 验证 5 层节点存在 + setAttribute 写入 + display 切换 + rotation transform 正确；导出 PNG/DXF 5 层已用 ezdxf 回读 layer id 集 + render_png 调用栈验证。版师屏幕视觉回归 + ET2008 真机读 R12-DXF（D4 硬验收）留作后续整体回归。
---
**US-025（后端求解进程化 solve_worker + solve_with_callback_proc）— 完成**（2026-08-12）
- 新增 `materialSorting-server/src/materialsorting/web/solve_worker.py`（141 行）：顶层函数 `solve_worker(pieces_snapshot, gate_mm, solve_params, result_queue)`，Windows spawn 可 pickle（无闭包、参数全 JSON）。子进程内 build_instance → 投 {kind:manifest}（pid_meta/total_area/n_eroded/gate_mm）→ instance.solve(ProgressQueue) + drain → 投 {kind:frame,report}* → 末尾投 {kind:final,final} 或 {kind:error,message}。spyrrow 对象绝不跨进程。
- `solver.py` 新增 `solve_with_callback_proc(pieces_snapshot, gate_mm, solve_params, *, on_manifest, on_report, drain_interval=0.2) → (process, final_data, elapsed, err)`：spawn `multiprocessing.Process(target=solve_worker)` + Queue，主进程 drain 分发；manifest→on_manifest、frame→on_report（density 双口径换算在主进程做：density_sparrow←原值、density←total_area/(width*gate)）、final/error 记录。循环退出条件 `queue.Empty AND not alive`（drain 完残余防漏 final）。finally 块必 terminate→cancel_join_thread→限时 drain(≤50ms)→join(timeout=5)，绝不阻塞。子进程 crash 未投 error 时 err='worker process exited unexpectedly (code=<exitcode>)'。旧 `solve_with_callback` 保留不删。
- 新建 `materialSorting-server/tests/`（US-025 首批测试）：conftest.py 注 sys.path + real_or_synthetic_pieces/synthetic_pieces fixture；test_solve_proc.py 5 项（①正常求解 + density 双口径正确 ②terminate 5s 内返回 ③build_instance 抛错 ④外部 kill 不 hang ⑤solve_worker 可 pickle）全绿（4s 跑完）。
- 文档同步：.docs/technical/agent-api-reference.md 加 solve_with_callback_proc + solve_worker 函数表 + US-025 进程化桥说明；.docs/technical/agent-file-map.md 更新 solver.py 行数 + 新增 solve_worker.py 行 + 关键不变量 #11（求解进程化）；web/AGENTS.md 加 US-025 关键约定 + tests/AGENTS.md 新建。
- 验收：python -c "from materialsorting.web.solve_worker import solve_worker; from materialsorting.web.solver import solve_with_callback_proc" 导入无异常；python -m pytest tests/ 全绿；分层依赖未反向（grep 验证）；旧 solve_with_callback 保留（server.py ws_solve 未切换，系统行为零变化）。
- 跳过浏览器验证：本 Story 纯后端进程化，无 SVG/坐标变换/UI 改动（US-028 才是 UI Story）。
---
## US-026 WS /ws/solve 支持 stop 消息（持续读 + 进程终止 + 协议扩展）— 2026-08-12
**改动文件**：
- `materialSorting-server/src/materialsorting/web/server.py`：ws_solve 从「只读一次 start」改为 write loop 内联 + read loop 后台 task 双向并发；build_instance 移入子进程（改调 solve_with_callback_proc）；stop → terminate+join → 直发 stopped → 投 SENTINEL；WebSocketDisconnect → terminate+join 防孤儿（修旧 bug）；_terminate_solve_process 幂等封装；finally 显式 ws.close()。
- `materialSorting-server/src/materialsorting/web/solver.py`：solve_with_callback_proc 新增 on_process 回调（子进程 start 后立即回调交出 Process 句柄）。
- `materialSorting-web/src/types/ws.ts`：新增 StopPayload / ClientMsg 联合 / StoppedMsg / ServerMsg 增 StoppedMsg。
- `materialSorting-server/tests/test_ws_stop.py`：新增 3 项 WS 测试（stop+stopped / 断连清理 / 正常 final 回归）。
- `materialSorting-server/pyproject.toml`：dev deps 加 httpx（TestClient WS 测试依赖）。
- `.docs/technical/agent-api-reference.md` / `agent-file-map.md` / `agent-component-map.md`：WS 章节、solver 表、ws.ts 描述、关键不变量同步。
- `web/AGENTS.md`：新增 US-026 关键约定段。
**关键实现决策**：
- write loop 内联（主流程）+ read loop 后台 task（非 asyncio.gather）：gather 在 write loop 先完成时 hang 等 read loop（TestClient receive_json 不响应 cancel），inline 方式让 write loop 收 SENTINEL 后直接 break → finally 兜底。
- stopped 由 read loop 直发（不经 queue）：避免 stop 后残余 frame 在 queue 里排在 stopped 前面 → 客户端先收一堆 frame 再收 stopped。
- finally 显式 `await ws.close()`：TestClient（anyio portal）下 endpoint 返回不会自动让 client 端 receive_json 抛 disconnect，必须显式 close。
- 不 await read_task：TestClient 下 ws.receive_json() 阻塞在线程安全队列上，task.cancel() 的 CancelledError 无法投递，await 会永久挂起。
- 验收：python -c "from materialsorting.web.server import app" 导入无异常；python -m pytest tests/ 8 passed（5 solve_proc + 3 ws_stop）；npm run typecheck + npm run build 无报错；npm run test 514 passed。
- 跳过浏览器验证：本 Story 纯后端 WS + 前端类型扩展，无 SVG/坐标变换/UI 改动（US-028 才是 UI Story）。
## US-027 前端 stop() + 求解状态机（phase: idle/running/stopped/done/error）
改动文件：
- types/solvePhase.ts（新建）：导出 SolvePhase 五态类型，供 US-028 SolveControls 复用。
- store/runRegistry.ts：RunRecord 新增 stopped: boolean 字段（默认 false；收到 {type:'stopped'} 置 true；与 error 互斥）。
- hooks/useSolveRun.ts：返回值 {start, isStarted} → {start, stop, isStarted}（向后兼容）。新增 stop()：遍历 runRegistry.list()，对每个 readyState===WebSocket.OPEN 的 rec.ws 发 {action:'stop'}（非 OPEN 跳过；send 异常 catch）。onmessage 新增 case 'stopped'：rec.stopped=true + finish() 触发 onDone（不重算 finalDensity，lastFrame 保留停止时刻帧）。
- components/NestingPage.tsx：solving:boolean → phase:SolvePhase 状态机。handleStart setPhase('running') + lastStartCfgRef 保存参数。onDone 全部到齐后区分：allStopped→stopped / hasError→error / 否则 done。新增 handleStop（调 stop()，不立即 setPhase）+ handleRestart（handleStart(lastStartCfgRef.current)）。ControlPanel 收 solving={phase==='running'} + 可选 onStop/onRestart/phase（US-028 接线）。
- ControlPanel/ControlPanel.tsx：props 加可选 onStop/onRestart/phase；向 5 个输入子组件透传 disabled={solving}（running 态冻结）。
- ControlPanel/{SizePicker,ParamForm,MultiSeedControls,PresetButtons,PerTypeOverrides}.tsx：各加 disabled?: boolean prop，映射到原生 disabled。
- __tests__/useSolveRun.stop.test.tsx（新建）：6 项测试（stop OPEN/CLOSED + stopped finish + 3 phase 转换 + running 冻结）。
- .docs/technical/agent-component-map.md：加 US-027 落地段 + 9 条关键不变量；更新 header / NestingPage / useSolveRun 描述。
关键决策：
- onDone phase 优先级「全 stopped→stopped、有 error→error、否则 done」：per-run stopped 与 error 互斥（useSolveRun case 分支不并置），故 allStopped 蕴含无 error，检查顺序安全。
- stop() 仅对 OPEN 发：Mock WebSocket 须定义静态常量 CONNECTING/OPEN/CLOSING/CLOSED，否则 WebSocket.OPEN===undefined 导致 stop 永远不发（测试踩坑修复）。
- handleStop 不立即 setPhase：等后端回 {type:'stopped'} → onmessage → finish → onDone 统一切（与 final/error 路径一致，避免多 seed 部分 stopped 竞争）。
- handleRestart 用 lastStartCfgRef（上次 start 参数）；用户改参数后走 ControlPanel.onStart → handleStart 新参数覆盖 ref（双路径自然保证）。
- ControlPanel solving prop 保留（US-028 才改 phase）；onStop/onRestart/phase 可选 props 本 Story 不接线。
验收：npm run typecheck 无报错；npm run test 520 passed（514 原 + 6 新）；npm run build 无报错。
跳过浏览器验证：本 Story 无 SVG/坐标变换（仅状态机 + 输入 disabled），AC 仅要求 typecheck+单测+build；浏览器完整流程（idle→running→stopped→restart→done）验证留作 US-028（UI Story，SolveControls 接线后统一核对）。
================================================================
US-028 按钮组组件 SolveControls + ControlPanel 接线（删除 StartButton）
================================================================
实现：
- 新建 SolveControls.tsx：按 phase 五态渲染按钮组（idle→#start「开始求解」绿 / running→#stop「停止」红 #b5462f / stopped|error→#restart「重新开始」绿 / done→#restart「再次求解」绿）。每按钮 type=button + aria-label（含「求解」语义）+ .solve-btn.<phase> 类名。
- 删除 StartButton.tsx（确认无其它引用：6 处「StartButton」均为注释历史语境，preview/SizeTabs/Switch 仅同色系类比）。
- ControlPanel.tsx：props solving:boolean → phase:SolvePhase（必传）+ onStop/onRestart 从可选改必传；内部 const solving = phase==='running' 派生透传 5 输入组件 disabled + ExportButtons solving prop；const partial = phase==='stopped'||phase==='error' 派生透传 ExportButtons；handleStart 守卫 if (solving) 语义不变。
- ExportButtons.tsx：新增 partial?:boolean prop。partial=true 时 .dim.small.warn（橙黄 #d8a23a 600）显示「导出的是停止 / 出错时刻的中间方案，非最终最优解。」取代默认「最终方案」提示；文件名仍按当前 density 命名（不加 _partial 后缀，AC#3 仅 UI 提示区分）。
- NestingPage.tsx：ControlPanel 调用去 solving={phase==='running'}，仅传 phase + onStop + onRestart（ControlPanel 内部派生）。
- style.css：button#start 规则合并 button#start,button#restart（绿 #2ea06c 主操作语义）；新增 button#stop（红 #b5462f 警示 + hover #d05a40 + disabled #555）；新增 .dim.small.warn（橙黄 #d8a23a 600 中间方案导出提示）。
- SolveControls.test.tsx 新建 7 项（5 phase × 渲染正确按钮 + aria-label + 点击调对应 handler + 误触防护 + a11y type=button/tabIndex=0 + 按钮总数恒为 1）。
- ControlPanel.test.tsx 改造：renderPanel opts.solving→opts.phase（必传 onStop/onRestart 兜底）；solving=true 用例 → phase=running（#stop + 参数冻结）；新增 phase=stopped/done/error 各 1 用例。
- useSolveRun.stop.test.tsx 改造：删除 startDisabled() helper（#start 在非 idle 态不存在）；3a/3b/3c 用例改查 #stop（running）+ #restart + textContent/aria-label（stopped「重新开始」/ done「再次求解」/ error「重新开始」）。
验收：
- npm run typecheck：无报错。
- npm run test：530 passed（520 原 + 7 SolveControls 新 + 3 ControlPanel phase 新）；34 + 1 = 35 文件全绿。
- npm run build：tsc --noEmit && vite build 成功，static/assets/index-B06il1iZ.js + index-koBqi37l.css。
- 浏览器验证（Chrome CDP --remote-debugging-port=9222 + Python websockets 驱动 headless Chrome + ms-web :8000 跑最新构建产物）：
  - idle：#start「开始求解」+ aria-label="开始求解" + class="solve-btn start" ✓
  - 选码 + 点开始求解 → running：#stop「停止」+ aria-label="停止求解" + class="solve-btn stop"，#start 消失，SVG polygon 渲染（WS 真实连接 + 帧推送）✓
  - 点停止 → stopped：#restart「重新开始」+ aria-label="重新开始求解"，status「已停止：seed 0（保留中间方案，可导出）」，partial warn「导出的是停止 / 出错时刻的中间方案，非最终最优解。」✓
  - 点重新开始 → running：#stop 回归（反复 stop+restart 多次无异常）✓
  - 清理：Chrome + ms-web PID 25652 已 taskkill，端口 8000/9222 确认 LISTENING 不再存在。
## US-029 Tour 基础设施：tourStore + 高亮引擎 + 右上角入口 (2026-08-13)
  - 新建 src/tour/types.ts（Placement/TourStep/TourDef）、src/tour/steps/index.ts（TOUR_VERSION='1' + DEMO_PREVIEW_TOUR 2 步假 tour）、src/store/tourStore.ts（activeTour/stepIndex/seen + localStorage 持久化 + TOUR_VERSION 版本号强制重看；markSeen 同步写 version 防部分清除误清）、src/tour/useTour.ts（advance-on-ready 轮询骨架；200ms 轮询 + 等待态 + close/stepIndex 清定时器）、src/tour/TourOverlay.tsx（Portal body z=2000；spotlight box-shadow 镂空 + bubble placement 定位 + 零尺寸居中兜底 + useLayoutEffect imperative 定位 + resize/scroll capture listener）。
  - 改 App.tsx（挂 TourOverlay 单例）、TabBar.tsx（右上角 .tour-entry + 下拉菜单「重置全部指引」=resetSeen+start('preview')；点击外部/ESC 关闭；aria-haspopup/aria-expanded a11y）、style.css（.tour-overlay/.tour-spotlight/.tour-bubble/.tour-title/.tour-body/.tour-waiting/.tour-btn-*/.tour-entry/.tour-menu*/.tour-menu-item 暗背景 #26282e + #2ea06c 同色系）。
  - 单测：tourStore.test.ts 8 项（默认 null / start / next+prev floor clamp / close / markSeen hydrate / resetSeen / TOUR_VERSION 不一致清 seen / markSeen 幂等）+ TourOverlay.test.tsx 5 项（null 不渲染 / 激活渲染 / spotlight 贴 rect / 零尺寸居中 / 步骤切换跟随）= 13 新增，全绿（552 total）。
  - typecheck ✓ / build ✓ / package.json 无新依赖 ✓。
  - 浏览器验证：ms-web 启动成功（:8000 HTTP 200）；chrome-devtools-mcp 不在本会话工具集，核心定位逻辑用 mockRect 单测覆盖（spotlight 贴目标 rect + 零尺寸回退 + 步骤切换）。视觉回归留作 US-030/032 集成。
  - 清理：ms-web PID 31124 已 taskkill，端口 8000 确认 LISTENING 不再存在。
## US-030 preview tour 全量 + advance-on-ready 完整 + 首次自动触发（2026-08-13）
  - 新建 src/tour/steps/previewTour.ts — 5 步 previewTour（upload/parsed/set-qty/committed/goto-nesting）。ready 谓词：parsed 读 uploadStore.status==='done'&&doc!==null；committed 读 commitStatus==='done'；goto-nesting 读 uiStore.activeTab==='nesting'；upload/set-qty 告知型（无 ready）。goto-nesting 无 before（不强制切回 preview）。
  - 改 src/tour/steps/index.ts — DEMO_PREVIEW_TOUR 删除 → TOURS:Partial<Record<TabId,TourDef>>={preview:previewTour}（US-031 补 nesting）。TOUR_VERSION='1' 不变。
  - 改 src/tour/useTour.ts — advance-on-ready 完整模型：从 US-029「next() 检查目标步 ready」改为「step-change effect 检查当前步 ready」——进入 ready=false 联动步时切等待态+200ms 轮询，ready 翻 true 自动推进（最后一步=markSeen+close，非最后=storeNext）。新增 useTourAutoTrigger 独立 hook（subscribe activeTab + 300ms 延迟 + 三重 guard seen/activeTour/TOURS[tab]）。
  - 改 App.tsx — 顶层调 useTourAutoTrigger()。
  - 锚点：UploadPanel .drop-zone 加 data-tour="drop-zone"；SizeTabs 容器加 data-tour="size-tabs"；ParsedPiecesView .piece-card-head 加 data-tour="piece-card-head"；TabBar 超排 button 加 data-tour="tab-nesting"；commit 行已有 data-testid="commit-status"。
  - 测试：新建 useTour.test.tsx 5 项（vi.mock 注入可控 tour + vi.hoisted spy + fake timers）——告知型直接推进 / 等待态 readyHint+disabled / 轮询 ready 翻 true 自动推进+停 / before 副作用 / close 无残留定时器。TourOverlay.test.tsx 改 previewTour 选择器 + 新增等待态 1 项（共 6 项）。
  - 验收：typecheck clean / npm run test 558/558 全绿 / npm run build 无报错（static/ 产出）。
  - 浏览器验证：chrome-devtools-mcp 不在本会话工具集；advance-on-ready 逻辑用 useTour.test.tsx 5 项 + TourOverlay 等待态 1 项单测覆盖（fake timers 验证轮询推进 + close 无残留）。端到端浏览器回归留作 US-031/032 集成。清理：stale server PID 21508 已 taskkill，端口 8000 确认 free。
## US-031 nesting tour 全量 + 求解状态联动 (2026-08-13)
  - 新建 src/tour/steps/nestingTour.ts：5 步 nestingTour（doc-banner/params/solve/result/export）。前 3 步告知型（无 ready，点下一步直接推进）；result/export 联动型（ready 读 runRegistry.list().some(r=>r.lastFrame!==null) 帧快照，绕开 NestingPage phase 是 useState 外部读不到的问题）。每步 before 调 ensureNestingTab（幂等）。result 步气泡附带提及收敛曲线（不单独高亮/锚点）；回放非主流程不单独成步。
  - steps/index.ts：TOURS 注册 nesting: nestingTour（Partial<Record<TabId,TourDef>> 双 Tab 完整；US-030 useTourAutoTrigger 已读 TOURS[tab] 自动生效，!seen.nesting && 进 nesting Tab → 延迟 300ms start('nesting')）。
  - 5 锚点 data-tour 落地：ControlPanel.tsx .doc-banner 加 data-tour="doc-banner"；SizePicker.tsx .field 根容器加 data-tour="param-form"；ControlPanel.tsx 在 <SolveControls> 外包 <div data-tour="start-btn">（不侵入 SolveControls 自身，保持 button#start 查询不破）；NestingPage.tsx .nest-wrap 加 data-tour="nest-wrap"；ExportButtons.tsx .export-group 加 data-tour="export-group"。
  - 新建 src/tour/__tests__/nestingTour.test.tsx：5 项单测（ready 无帧 false / 有帧 true / 5 锚点 NestingPage 渲染后 query 全命中 / 5 步 id 序列 / 前 3 告知+后 2 联动结构）。测 3 stub fetch /api/ptypes 防 PtypePreviewModal act warning。
  - 验收：typecheck clean / npm run test 563/563 全绿（+5 新）/ npm run build 无报错（static/ 产出）。
  - 浏览器验证：chrome-devtools-mcp 不在本会话工具集；advance-on-ready 逻辑复用 US-030 useTour.test.tsx 5 项 + nestingTour.test.tsx 5 项 + TourOverlay 6 项覆盖（fake timers 验证轮询推进 + close 无残留 + runRegistry 帧快照 ready 谓词）。端到端浏览器回归（上传→commit→进超排→tour 自动起→开始求解→step4/5 自动推进→导出）留作 US-032 集成。无后台进程残留（本 Story 未启动 ms-web）。
## US-032 手动入口完善 + 关闭交互打磨 + 完整单测 (2026-08-13)
  - TabBar.tsx 下拉菜单补全三项：①「重看上传预览指引」→ start('preview')（强制重放）；②「重看超排指引」→ start('nesting')；③「重置全部指引」→ resetSeen()（清 localStorage seen，不启动 tour；区别于旧 US-029 resetSeen+start 假 tour）。点外部/ESC 关闭 + toggle。
  - TourOverlay.tsx 关闭交互完备：ESC 键 window keydown→close（仅 active 时挂 listener）；遮罩 mousedown e.target===e.currentTarget→close（spotlight pointer-events:none 穿透到 overlay，bubble pointer-events:auto 不关闭）；「跳过」按钮 tour.skip（markSeen+close，视为已读不再自动触发）。readTargetRect→readRect(el)（接收 element 避免 querySelector 重复）。
  - TourOverlay.tsx prefers-reduced-motion：useEffect 内 window.matchMedia('(prefers-reduced-motion: reduce)')，matches=true 时 overlay 加 .tour-reduced-motion class；CSS .tour-reduced-motion .tour-spotlight/.tour-bubble { transition: none !important; } 禁用过渡动画。
  - TourOverlay.tsx scrollIntoView：useLayoutEffect 内高亮前 el.scrollIntoView({block:'nearest'})（typeof guard 防 jsdom 未实现），避免目标在视口外时聚光灯贴视口边缘外。
  - useTour.ts 加 skip() 方法：markSeen(activeTour)+clearPolling()+storeClose（区别于 close 仅 clearPolling+storeClose）；返回值加 skip。
  - StrictMode 双 mount 幂等：所有 listener（resize/scroll capture / ESC keydown / matchMedia change）在 useEffect cleanup 中卸载；advance-on-ready polling 在 cleanup 中 clearInterval。before() 幂等（ensurePreviewTab/ensureNestingTab 均 if-check 后 setTab）。
  - style.css 加 .tour-reduced-motion 规则（transition:none !important 覆盖 spotlight/bubble 0.15s ease-out）。
  - 单测（+18 新，581 total 全绿）：
    - TourOverlay.test.tsx +6：ESC 关闭 / 遮罩 mousedown 关闭（mock target=currentTarget=overlay）/ bubble 点击不关闭（mock target=bubble）/ skip markSeen+close / reduced-motion=true 加 class / reduced-motion=false 不加 class。
    - TabBar.test.tsx +7：菜单默认不渲染+展开三项 / replay-preview→start('preview') / replay-nesting→start('nesting') / reset→resetSeen 不启动 tour / 点外部关闭 / ESC 关闭 / toggle。
    - useTour.test.tsx +2：skip markSeen+close / skip 从等待态清轮询+markSeen+close（fake timers 验证无残留定时器）。
    - tourStore.test.ts +3：markSeen 后版本号同步写 localStorage / bump TOUR_VERSION 后 init 强制清 seen / resetSeen 不影响 version key。
  - 验收：typecheck clean / npm run test 581/581 全绿（+18 新）/ npm run build 无报错（static/ 产出）/ package.json 无新依赖 / py backend import OK。
  - 浏览器验证：chrome-devtools-mcp 不在本会话工具集；核心逻辑用 18 项新单测覆盖（mock matchMedia / mock mousedown target/currentTarget / fake timers 验证 skip 无残留定时器）。浏览器端到端回归（重放 preview/nesting、重置后进 Tab 自动触发、ESC/遮罩/跳过关闭、reduced-motion）留作整体回归。无后台进程残留（本 Story 未启动 ms-web）。
## [2026-08-13] US-033 后端 PLT/HPGL 导出生成器与路由（branch: ralph/plt-export）
  - 背景：WT「高速绘图 V8.8 网络版」+ LIKE 绘图仪原生吃 PLT/HPGL，DXF 在该软件实测无法打印 → 新增 PLT 导出链路。
  - 实现：
    - `materialSorting-server/src/materialsorting/web/export.py` 新增 `write_marker_plt(world_pieces, *, width_mm, gate_mm, title) -> bytes` + HPGL 常量（`_PLT_SCALE=40`/`_PLT_VELOCITY=80`/`_PEN_OUTLINE=1`..`_PEN_BORDER=6`/`_ETX=chr(3)`）+ 辅助函数 `_plt_pt`（mm×40 round clamp 非负）+ `_plt_polyline(closed, points)`（PU 首点 + PD 其余点，closed=True 末尾追加首点物理闭合）。指令序列：`IN;`→`VS80;`→`SP6;`门幅框→逐片`SP1-SP5`5层→`LB<title>chr(3);` ASCII 标题；空层跳过；纯 ASCII bytes，无临时文件、无新 pip 依赖。docstring 显式约束「世界坐标 Y 向上，绝不带 SVG `scale(1,-1)` 翻转」。
    - `materialSorting-server/src/materialsorting/web/server.py` 第 47 行 import 扩 `write_marker_plt`；`/export` 路由 `elif fmt == 'dxf':` 之后、`else:` 之前插 `elif fmt == 'plt':` 分支，title 复用 DXF 同款 ASCII（`M1787 util=<pct>% L=<L>cm gate=<gate> seed=<seed>`），`media, ext = 'application/plt', 'plt'`，文件名走现有 `ext` 变量自动命中。
    - 新建 `materialSorting-server/tests/test_export_plt.py`（17 项单测，覆盖 spec AC#13 全部要点 + 防御边界）。
  - 验收：`pytest tests/test_export_plt.py` 17/17 全绿；`pytest tests/` 全套 25/25 全绿（17 新 + 8 原有，零回归）；`py -m materialsorting.web.server` 入口可 import、相对导入无误；分层依赖未反向（web→engine→bounds→parser）；无新 pip 依赖；全 ASCII 输出，`.decode('ascii')` 不抛异常。
  - 无浏览器验证：US-033 是纯后端 PLT 文本生成 + /export 路由分支，无 SVG/UI 改动（MVP 不做物理设备验收，现场 WT V8.8 + LIKE 绘图仪实测留给用户落地后反馈）。回归校验通过端到端单测（`placed_to_world` → `write_marker_plt`）覆盖：5 层笔号语义、闭合不变量、坐标 ×40 缩放、LB+ETX 终止、多片计数。
  - 无后台进程残留（本 Story 未启动 ms-web；端口 8000 上的 python PID 15904 是会话开始前已存在的进程，非本 Story 引入）。
## US-034 前端 PLT 导出格式选项与测试（2026-08-13）
  - 改动范围：纯前端，仅 `materialSorting-web/src/lib/download.ts` 扩 ExportFmt 联合类型 `'png' | 'dxf' | 'plt'` + EXPORT_FORMATS 在 DXF 与 PNG 之间插 `{value:'plt', label:'PLT'}`（生产交付格式族相邻），DEFAULT_EXPORT_FMT 仍 'dxf'。
  - 零代码改动验证点（US-007 数据驱动设计承诺兑现）：useExport.ts 的 `` 正在生成 ${fmt.toUpperCase()} … `` 模板对 'plt' → 'PLT' 自动命中；parseContentDisposition(cd, fmt) 兜底 nesting.${fmt} 自动产出 'nesting.plt'；ExportButtons.tsx 的 EXPORT_FORMATS.map 数据驱动渲染 <option>，PLT 自动出现。三个文件均零改动，仅通过类型透传 + 模板字符串 + 数据驱动下拉框三种机制吃下新格式。
  - 测试更新（3 文件 +2 用例 +1 断言）：download.test.ts「空 CD 兜底」组加 expect(parseContentDisposition('', 'plt')).toBe('nesting.plt')；useExport.test.tsx 参照 DXF 用例新增 exportAs('plt', [28]) → onStatus('正在生成 PLT …')；ExportButtons.test.tsx「select 2 options」→「3 options (DXF/PLT/PNG)」+ 新增「切 PLT + 点导出 → onExport('plt')」。全量 npm test：593 passed（+2 新增）；npm run build：tsc --noEmit + vite build 无错。
  - 无浏览器验证：US-034 是纯类型扩展 + 数据驱动下拉框 + 测试断言更新，无 SVG/坐标/可视化逻辑改动（UI Verification 触发条件未命中）。后端 PLT 链路在 US-033 已就绪并通过端到端单测，前端 fmt='plt' 透传到 POST /export 由 US-033 路由分支接住，无需重复 UI 验证。
  - 无后台进程残留（本 Story 未启动 ms-web / vite dev；纯单元测试 + 静态类型检查）。
## 2026-08-16 | ralph/preview-qty-matrix | US-001 qtyStore 数据层简化+整行填充
  - 数据层重构：types/qty.ts PieceQuantity 改 { perSize, baseValue }（删 QtyMode/globalValue/globalSource）；qtyStore 删 setPieceGlobal、getPieceDisplay 简化为 {qty, editable}（editable 仅「该码无此裁片」= perSize 缺 sizeKey 时 false，reason/sizeLabel 删除）；setPiecePerSize 删 global 回切逻辑（保留函数名，baseValue 不动、新建 label 兜底 1）；新增 setRowAll(label, sizes, value)（整行写 perSize 经 clampQty + baseValue=value，sizes 外码非破坏保留）；hydrateDefault/hydrateDefaults 合并单一 hydrate(entries)（以 grep 生产调用方为准：唯一生产调用方 PreviewPage 用 entries 形式，故保留该签名；每 (label,sizeKey)=1 且 baseValue=1）。
  - lib/params.ts serializeQuantities 删 global 展开分支，per-size 路径逻辑逐字段不变（显式 0 保留 / 未勾选码过滤 / 'null' sizeKey 兜底 / 空→null）——WS quantities 线格式不变，后端主管线零改动。
  - 组件涟漪（仅为保持 typecheck/grep AC 绿的最小机械改动，UI 重构留 US-002/003）：PreviewPage hydrateDefault→hydrate 改名；PieceQtyDialog 删 draftGlobal/Switch 行（确定仅 setPiecePerSize；组件整体拆除在 US-003）；ParsedPiecesView span.disabled title 改固定文案（reason 已删）；SizePicker effectiveDemand 删 global 分支。
  - 测试改写：qtyStore.test.ts（删 global 约半数用例，新增 setRowAll×4 / hydrate baseValue×4 / getPieceDisplay 新三分支）；params.test.ts serializeQuantities 8 项（新增 'null' 兜底 + baseValue 不序列化 + label 全过滤→null 用例）；PieceQtyDialog.test 15→12 项；ParsedPiecesView/PreviewPage/SizePicker 测试删 global 用例（PreviewPage 端到端改写为 per-size 流：弹窗 +2 → 确定 → 仅 30 码 perSize=3、28 码保持 1）。
  - 验收核验：grep setPieceGlobal/QtyMode/globalValue/globalSource/hydrateDefault(src) 全 0；setRowAll('A',[28,29,30],2)→三码=2 且 baseValue===2、hydrate 每 (label,size)=1 且 baseValue=1（单测断言）；clampQty 公式不变；npm run typecheck / npm run test（39 文件 584 项全绿）/ npm run build 无报错。
  - 实现踩坑：getPieceDisplay editable 初版写成 `!(sk in perSize)`（语义写反），584 项里 6 项挂掉暴露后修正为 `sk in perSize`——「该码无此裁片」应为 false。
  - 文档同步（/sync-docs）：agent-component-map.md 更新现行态描述（store 树/PreviewPage/PieceQtyDialog/params/SizePicker 行）+ US-011/US-012 节加「矩阵化重构 US-001 部分取代」标注 + 文末新增「矩阵化重构 US-001」节（文件表 + 6 条不变量）；AGENTS.md（materialSorting-web）US-011 节整体改写为新契约 + US-012 节标注 + 文件分工/树行同步；business-overview.md 工作台交互第 2 条删 global 描述。
  - 无浏览器验证：US-001 为纯数据层重构（store/序列化 + 机械涟漪），无 SVG 渲染/坐标变换/可视化逻辑改动（UI Verification 触发条件未命中）；矩阵 UI 的浏览器验证按 story 计划留在 US-002（矩阵渲染）与 US-003（端到端）。
  - 无后台进程残留（本 Story 未启动 ms-web / vite dev / Chrome；纯单元测试 + 静态类型检查 + 构建）。
## 2026-08-16 | ralph/preview-qty-matrix | US-002 QtyMatrix 数量矩阵组件
- 新建 `materialSorting-web/src/components/preview/QtyMatrix.tsx`（488 行）：主组件 QtyMatrix + 同文件子组件 QtyMatrixCell（draft 草稿 + blur/Enter/Tab 提交 clampQty + onFocus select() 全选覆盖输入 + focusedRef 守卫的草稿同步）与 RowFillPopover（初值=baseValue，取消/遮罩 mousedown/ESC 三路关闭 + Enter 快捷应用）。订阅 uploadStore doc/activeSize/setSize/openZoom + qtyStore quantities/setPiecePerSize/setRowAll；doc=null 渲染 null。结构：工具条（总片数 + 全 0 红色警示 + 重置为默认 1）→ sticky 表头（[裁片] + 各码列头 button 点击 setSize + [合计]）→ 数据行 ×N（行头=A 徽章+名+compact 缩略图点击 openZoom(label,activeSize)+悬浮填充按钮）→ 底部 sticky 每码小计行。
- 行列模型：行=全码 label 并集（doc.sizes 升序首次出现 = 最小码 pieces 顺序优先）；列=doc.sizes 全码（null 殿后「通用」，无 null 不渲染该列）；缺片格（getPieceDisplay editable=false）渲染 disabled「—」不计小计；0 格 .zero 可编辑；特例 .override = !rowAllSame && v!==base（base 兜底 1，整行同值不高亮）。
- 行填充 setRowAll(label, rowSizes(label), value)——只写该 label 实际存在的码，防 phantom perSize 键污染 getPieceDisplay/serializeQuantities。Enter/Tab 手动 focusNextCell（:not([disabled]) 平铺顺序 + 末格回卷，跳过缺片格）。小计 Σdemand 口径（US-004 升级物理 ×2 口径）。
- `materialSorting-web/src/style.css` +~30 条 .qty-matrix 系列规则：.qty-matrix-scroll max-height 45vh 内滚；border-collapse:separate + 自绘边框（collapse 吞 sticky 格边框）；sticky z 层级 thead 3/corner 5/rowhead 2/tfoot 3/subtotal-rowhead 4；.qty-cell.zero（#121317）/override（#2a2417+#d8a23a+#ffd98a）/missing（灰禁改）；.qty-fill-btn opacity:0 行头 hover 显现；暗底 #26282e + #2ea06c 同色系，无 CSS 框架。
- 新建 `materialSorting-web/src/components/preview/__tests__/QtyMatrix.test.tsx`（32 项 / 7 describe）：行列结构 6 + 列头切码 2 + 缩略图 openZoom 1 + 格内编辑 6（blur clamp 150→99/-3→0/2.7→2 + Enter/Tab 跳格跳缺片 + 末格回卷）+ 0/特例高亮 5 + 行填充 popover 7（apply/baseValue/无 phantom 'null' 键/DOM 同步/clamp/取消/ESC/遮罩/Enter/初值=baseValue）+ 小计与总片数 4 + 重置 1（全量 store 断言）。
- 验收核验：npm run typecheck 0 error；npm run test 616/616（40 文件，+32 新增零回归）；npm run build 无报错。
- 浏览器验证（chrome-devtools-mcp 不在本会话工具集 → 临时 Vite harness + CDP headless Chrome，US-028 先例）：matrix-harness.html 挂 QtyMatrix + 合成 M1787 形态 doc（11 码 28-38 × 10 片型）经 stores 直灌；headless Chrome --remote-debugging-port=9223（隔离 profile）+ Python websockets 驱动，24/24 断言全过——11 列码序 28..38 / 10 行 / 行头徽章+名+svg / thead+corner+rowhead+tfoot sticky computed / max-height 360px(45vh@800) / 纵向内滚表头钉住 / 预设 override 2 格 + zero 1 格 / 总片数 110 / blur 提交 A@28 2→7（store 7 + 28 码小计 16 + 总 115）/ Enter 提交 3 且焦点跳下一格 / 行填充 B×3（11 格=3、行合计 33、baseValue 3、无高亮、popover 关闭）/ 列头点击 setSize(30)+.active+aria-pressed / 缩略图 openZoom {label:'A',size:30} / 总>0 无警示 / 800px 视口横滚 + 首列钉住。截图 out/us002_wide1366.png + out/us002_narrow800_hscroll.png。
- 实现踩坑：(1) CSS border-collapse:collapse 会吞 sticky 单元格边框 → separate + 自绘边框；(2) React 18 同一 JS task 内 synthetic type+blur 丢提交（onChange flush 晚于 onBlur 读旧草稿）→ CDP 驱动拆两个 evaluate；(3) (number|null)[] reduce 需显式 reduce<number> 泛型；(4) harness 必须引 style.css 且 #root 不能定宽，否则 sticky/45vh 断言全挂。
- 清理：自启 headless Chrome（PID 4484，chrome-us002 隔离 profile）已 Stop-Process + profile 目录删除，端口 9223 无监听；matrix-harness.html + out/us002_{a,b,cdp}.py 临时文件已删。会话开始前已存在的 vite :5173（PID 10040）/ms-web :8000（PID 28964）/用户 Chrome（PID 30256）非本 Story 启动，未动。
- 文档同步（/sync-docs）：.docs/technical/agent-component-map.md（覆盖列表 + preview/ 文件树 QtyMatrix.tsx/QtyMatrix.test.tsx + style.css 行 + 文末「矩阵化重构 US-002 落地」节含 8 条关键不变量）+ .docs/technical/todo.md（Recently Completed 加 US-002 行）；materialSorting-web/AGENTS.md（文件分工树两行 + 「矩阵化重构 US-002 关键约定」10 条）。后端零改动（agent-file-map/agent-api-reference 无涉及）。
- 未接入 PreviewPage / 未加 data-tour 锚点 / 未删 SizeTabs/PieceQtyDialog（均按 PRD 留 US-003/US-005）。
## 矩阵化重构 US-003 拆除旧交互+预览页集成+全 0 拦截（2026-08-16）
- 删除：SizeTabs.tsx / PieceQtyDialog.tsx / Switch.tsx + 三测试文件（git rm）；`grep openQtyDialog src/` = 0；uploadStore 删 QtyDialogTarget/qtyDialog/openQtyDialog/closeQtyDialog（zoom/activeSize/setSize 保留，reset 同步收窄）。
- PreviewPage：挂 `<QtyMatrix/>` 替 `<SizeTabs/>`，删 `<PieceQtyDialog/>` 单例（PieceZoomModal 保留为唯一模态）；doc_id→hydrate/resetQuantities 与 setNestingEnabled 两条 subscribe effect 原样不动。
- ParsedPiecesView：语义降级为「按码图形预览」—— 区标题 `.parsed-pieces-title`「图形预览 · 码 X」（null→通用，防御空态保留）；卡片头数量改只读 span「N 份」（editable=false 仅缺片时 .disabled+title）；卡片体 openZoom + role/tabIndex/Enter/Space 全保留。PieceZoomModal 头部单位同步「片」→「份」（FR-9）。
- ControlPanel.handleStart 全 0 拦截：computeTotalCutPieces(doc, form.sizes, quantities)===0 → onStatus 提示 + 不发 WS start（doc=null 返回 null 不拦截，保 fallback 开发模式与既有用例）。
- style.css：删 .size-tabs/.size-chip + US-012 弹窗/Switch 全段；.piece-card-qty 收敛只读样式（去 cursor/hover/focus）；新增 .parsed-pieces-title。
- 测试改写：uploadStore.test 删 qtyDialog 6+1 项；ParsedPiecesView.test 重写 19 项（区标题 4 + 只读数量 4 + openZoom a11y 4）；PreviewPage.test 重写 24 项（QtyMatrix 挂载/列头切码端到端/模态仅 Zoom（qtyDialog 字段不存在断言）/矩阵格子编辑 A@30=3→store+卡片头 3份/US-016 八项不动）；ControlPanel.test 新增 5 项 start guard（全 0 拦截/仅勾全 0 码/默认不误拦/doc=null 不拦/线格式回归 A@28=2→payload quantities.A['28']===2）。589/589 全绿 + typecheck clean + build 无报错。
- 冒烟：npm run build 产物 + ms-web 启动 GET / 200 + POST /api/parse-dxf 200（后端零改动）；chrome-devtools-mcp 不在本会话工具集（同 US-032 情况），浏览器端到端回归（上传→矩阵→列头切码→放大→求解）留作 US-005 一并验证，已在 AGENTS.md 记录。
- 文档同步（/sync-docs）：.docs/technical/agent-component-map.md（覆盖列表 + 文件树删三组件/改 PreviewPage/ParsedPiecesView/PieceZoomModal/QtyMatrix/store 行 + ControlPanel 行加全 0 拦截 + previewTour 失配标注 + 文末「矩阵化重构 US-003 落地」节含 6 条关键不变量 + US-001 不变量 #5 修正）；.docs/technical/todo.md（Recently Completed 加 US-003 行）；materialSorting-web/AGENTS.md（文件分工树 + 「矩阵化重构 US-003 关键约定」7 条 + US-002 节头部未接入说明改已接入）。
- 未做（按 PRD 留 US-004/005）：parse 透传 ptype/paired + 物理片数口径（US-004）；tour 锚点迁移矩阵 + TOUR_VERSION bump + business-overview/CLAUDE.md 文档同步（US-005）。
- [2026-08-16 16:45] 矩阵化重构 US-004 完成（parse 透传 ptype/paired + 物理片数口径）：
  - 后端 server.py：import PAIR_TYPES（nesting_bounds.load_pieces，web→nesting_bounds 向下依赖合规）；_build_parse_payload 入口 assign_group_no(pieces)（与 _commit_to_nesting_sync 同 gmap），每片 additive 加 ptype=GROUP_NAMES.get(gmap.get(group_key))（无映射 null）+ paired=ptype in PAIR_TYPES。排序/标注/label 对齐不变量零改动。
  - 前端：types/parsed.ts 加可选 ptype?/paired?（缺字段 ×1 兜底）；QtyMatrix 新增 pairedOf/multOf/rowPaired（逐格取乘数）+ 行头 .qty-paired-badge「×2」徽章（data-testid=qty-paired-<label>）+ 行合计/每码小计/工具条总片数升级物理口径；SizePicker.computeTotalCutPieces 累加 ×(paired?2:1)（全 0 拦截语义不变）；style.css 新增 .qty-paired-badge。
  - 测试：QtyMatrix.test 32→38（新「US-004 物理片数口径+配对徽章」describe 6 项；原 Σdemand describe 改名缺 paired ×1 兼容）；SizePicker.test +6（paired×2 单/多码、×demand 复合、demand=0、缺字段兜底、UI 展示）。
  - 验证：npm typecheck clean；vitest 601/601 全绿；npm build 无报错；后端 pytest 39/39；ms-web 真服务 curl M1787 → 110 片全含 ptype/paired（paired 六类=PAIR_TYPES、内片 false）+ 原字段完整 + compute_size_ptype_labels 对齐 0 失配。⚠️ 端口 8000 有上一会话遗留进程（旧代码），taskkill 后重启才验证成功——启动 ms-web 前先查 netstat。
  - 浏览器验证：chrome-devtools-mcp 不在本会话工具集（与 US-032/US-003 同况），UI 回归以 12 项新 jsdom 单测 + curl e2e 覆盖，浏览器端整体回归留作 US-005。
  - 文档同步：agent-api-reference.md（parse-dxf 响应 schema + ptype/paired 节）；agent-component-map.md（覆盖列表 + 文件树 4 行 + SizePicker 两表行 + 文末「矩阵化重构 US-004 落地」节含 5 条不变量）；todo.md（Recently Completed 加行）；materialSorting-web/AGENTS.md（文件分工树 4 行 + 响应契约行 + 文末「US-004 关键约定」7 条）；materialSorting-server/.../web/AGENTS.md（响应字段行加 ptype/paired 说明）。
  - 未做（按 PRD 留 US-005）：tour 锚点迁矩阵 + TOUR_VERSION bump + business-overview/CLAUDE.md 文档同步 + 浏览器端整体回归。
  - US-005 tour 锚点更新+文档同步：QtyMatrix.tsx 落地 data-tour="qty-matrix"（根容器）+ data-tour="qty-rowhead"（行头 th）；ParsedPiecesView 删死锚点 data-tour="piece-card-head"；previewTour parsed/set-qty 两步锚点迁矩阵 + 文案改矩阵操作描述（列头切码看图形预览 / 格内编辑份数 / 行头填充默认值 / 特例高亮 / 配对 ×2）；TOUR_VERSION '1'→'2' bump（tourStore init 清老用户 seen）+ index.ts 版本历史注释。
  - US-005 测试：新建 src/tour/steps/__tests__/previewTour.test.tsx 5 项（5 步 id 序列 / 锚点迁矩阵且旧选择器零残留+其余三步不动 / TOUR_VERSION==='2' / 文案矩阵关键词 / 锚点在渲染后 QtyMatrix 上 querySelector 命中）；TourOverlay.test.tsx step1 mock 锚点 size-tabs→qty-matrix 跟迁；606/606 全绿 + typecheck clean + build 无报错。
  - US-005 文档同步：agent-component-map.md（覆盖清单 + tour 文件树 3 行 + QtyMatrix/ParsedPiecesView 行 + US-008/US-014 节加「已被矩阵化重构取代」警告头 + US-030 节加锚点迁移警告头 + 文末「矩阵化重构 US-005 落地」节含 5 条不变量）；business-overview.md 工作台交互第 1/2 条改矩阵描述（删「点卡片头编辑数量」旧交互）；CLAUDE.md 数据流主线加数量矩阵一句；materialSorting-web/AGENTS.md（文件分工头 + tour 树 2 行 + US-003 节失配中间态/浏览器验证两条改落地态 + 文末「矩阵化重构 US-005 关键约定」4 条）；todo.md Recently Completed 加行。
  - US-005 备注：chrome-devtools-mcp 不在本会话工具集（与 US-032/矩阵化 US-002 同况）；本故事无 SVG/坐标变换/可视化逻辑改动（仅锚点属性 + 文案 + 版本号），锚点渲染命中由 previewTour.test.tsx jsdom querySelector 断言覆盖，浏览器整体回归留待后续会话。
# US-001 后端数据模型+commit 管线去名称化（2026-08-18）
- labeling.py：assign_codes/collect_master_codes 签名删 gmap/group_names；「有效片」= 全部 size≠None 片（未录入名称组不再丢片）；删 compute_size_ptype_labels；新增 sequential_sort_key（T4：group_key 前置排序键，同一 block 模板跨码同号）。
- export_dxf.py：删 GROUP_NAMES / assign_group_no，仅剩 write_piece_dxf（文件名 {label}_{size}.dxf，g 码由调用方决定）。
- load_pieces.py：删 PAIR_TYPES / ALL_TYPES / _mirror_x / mirror 分支；load_nest_pieces 改 pieces_manifest.json 驱动（[{file,label,size}]），每文件 rotate→normalize，NestPiece pid=f'{label}_{size}'（无 ptype/side）；旧目录明确报错「请重新 commit」。
- server.py：管线重排 collect → assign_codes 最先 → 写 {label}_{size}.dxf + manifest → manifest 驱动加载 → intermediate schema v2（无 ptype/side，顶层 label_representatives）；parse 响应删 name/ptype/paired；/api/ptypes 键改 g 码（_LABEL_REPRESENTATIVE_FIELDS）。
- solver.py / export.py：ptype 下标访问改 .get() 垫片 —— /ws/solve 与 /export 在 v2 intermediate 上跑通不崩（颜色/图例暂降级默认值，US-002 全量重做）；solver.load_pieces 明确拒绝 v1 旧档。
- 测试：test_labeling.py 重写（13 项）、test_label_representatives.py 更名、test_commit_pipeline.py 新增（7 项：合成无名 block 母版 0 丢片全片有码 / manifest 落盘 / AC#5 逐片对齐 / idempotent / 旧数据报错）；全量 pytest 60/60 绿。
- 真实母版验证：M1787 重 commit（doc_id us001recommit）→ intermediate 110 条 = 母版轮廓数，g01..g10 × 11 码，parse↔intermediate 逐片 label 对齐；/ws/solve + /export 三格式正常产出。
- 文档同步：3×AGENTS.md（nesting_engine/web/tests）+ agent-api-reference.md + agent-file-map.md + CLAUDE.md 的镜像/名称/GROUP_NAMES 表述全部改 v2 口径。
## 2026-08-18 · US-002 求解+导出+CLI 去名称化（LABEL_COLORS）
- sparrow_baseline：删 `PTYPE_COLORS`，新增 `LABEL_PALETTE`（16 色 d3 系循环表，tableau10+6 pastel）+ `label_color(label)=PALETTE[(code_sort_key-1)%16]` 单一真相源（非 g 码/None 兜底 `DEFAULT_COLOR`）；SVG 图例聚合键 ptype→label、按 `code_sort_key` 数值序、标题「片型图例」→「裁片图例」。
- web/solver：`per_type` 切 `{label:{sizeKey:{d?,tol?}}}` 命中即覆盖（未命中/缺维度/旧 ptype 键回退 `params.d_ext/tol_ext`）；删 `internal = ptype in INTERNAL_TYPES` 分支（`d_int`/`tol_int` 仍接受但无消费方）；pid_meta/WS manifest 删 `ptype` 键、`color=label_color(label)`；demand (label,sizeKey) 直译与多副本不变量保持。
- web/export：删 `TYPE_ACI`/`TYPE_ORDER`；新增 `label_aci(label)=((code-1)%24)+1`（兜底 7）；PNG 图例条目=placed 的 g 码并集（数值序）；`placed_to_world` 输出无 ptype、color=label_color；PNG 质心 g 码/DXF TEXT `g03-28`/PLT 零文字口径不动。
- constraints：删 `PAIR_TYPES` 与成对齐套校验（L=R，服务于已删的合成镜像）；数量/门幅/用料校验保留，`validate` 不再读 ptype/side。
- sparrow_experiments：删 `INTERNAL_TYPES`；内片集合改 `--internal g04,g07` CLI 参数（`build_pieces`/`run_one` 加 `internal_labels` 形参，items_meta 键 ptype→label）；`ms-sparrow-exp` 入口实测（--internal g01,g04 → 22 片命中腐蚀，不给 → 0 片）。
- 新增 `tests/test_solver_label.py` 9 项（label_color 循环/兜底、label_aci 公式/循环、per_type 命中覆盖+no-op+demand 直译+pid_meta label 键、validate 裸 pid 可用）；全套 pytest 69/69 绿。
- 密度对拍：改前/改后同 seed(0) 同几何（110 片全 demand=1，t21）density 均 0.8843202590942383、placed_items 逐条 byte-identical（sparrow 同 seed 确定性先验证过两轮）。
- 端到端：起 ms-web（先清掉 8000 口残留旧实例）→ `/api/ptypes` 键 g01..g10；WS start per_type={'g01':{'28':{'d':1.5}}} 对照实验 n_eroded 0→1（仅 g01_28 被腐蚀）；`/export` PNG/DXF/PLT 三格式实测零中文片型名（DXF 中文 grep 0 命中、layer1 ACI 集与公式全等、PLT 纯 ASCII）；CLI SVG 图例全 g 码。UI 数据链路（manifest color/label）经活服务核验，无浏览器 MCP 工具可用故以服务端核验 + PNG 目检替代。
- 文档同步：CLAUDE.md（共享层/label_color/--internal）、nesting_engine+web+tests 三份 AGENTS.md、agent-api-reference（export 函数表/ACI 公式/manifest 契约/build_instance）、agent-file-map（constraints/sparrow_baseline/experiments/labeling/solver/export + 数据流 hop 表 + NestPiece 表去 ptype/side 残留）。
## 2026-08-18 US-003 前端契约与显示层去名称化
- 类型契约：types/parsed.ts ParsedPiece 删 name/ptype/paired（v2 与 US-001 后端 1:1）；types/piece.ts PieceInfo 删 ptype；types/ptype.ts representatives 键 = 裁片 g 码；types/ws.ts per_type 注释随动。
- 常量：constants/v03.ts 删 V03_PTYPES（仅留 MAX_OVERLAP_MM/MAX_ROTATION_TOL_DEG）；lib/params.ts DEFAULT_FORM.per_type={}（不再依赖固定清单）。
- store：controlPanelStore previewPtype→previewLabel（openPreviewLabel/closePreviewLabel），键 = g 码。
- 显示层：QtyMatrix 删 pairedOf/multOf——每码小计/每裁片合计/工具条总片数全改 Σ perSize 数量（一份 = 母版一个轮廓，不合成镜像；title 口径文案更新）；SizePicker.computeTotalCutPieces 同口径；PieceZoomModal 头部只显 g 码徽章 + 份 + 码（name span 删）；PtypePreviewModal 头部显 g 码（rep.label ?? Record 键）；NestSVG 命中判定 dataset.ptype→dataset.label，tooltip「g03 · 码28」。
- PerTypeOverridesModal：列集 = /api/ptypes reps 键 ∪ form.per_type 已配置键，compareByLabel 数值序（先长度再字典序，g99<g100）；列头恒 .qty-label-badge g 码（V03_PTYPES/.ptype-name 兜底删）；testid/aria = d-${label}/tol-${label}（US-004 矩阵化重建在此之上落地）。
- tour：previewTour set-qty 文案改 Σ 数量口径、committed「按裁片 g 码切单裁片」；nestingTour「按片型」→「按裁片 g 码」；TOUR_VERSION '6'→'7'（数量语义重大变更强制重看）。
- 测试改写（592/592 绿）：PerTypeOverridesModal/PtypePreviewModal/ControlPanel/SizePicker/QtyMatrix/PreviewPage/PieceZoomModal/NestSVG/Tooltip/useSolveRun/controlPanelStore/params/previewTour 等 —— fixtures 全 g 码化，配对 ×2 用例改写为 Σ 口径断言。
- 验收 grep：`grep -rn paired src` 生产代码 0 命中；V03_PTYPES 仅存于历史注释；typecheck + vitest + build 全绿。
- 浏览器验证（CDP headless Chrome harness scripts/us003_verify.mjs，13/13 PASS + 6 截图 out/us003_verify/）：上传母版 → 矩阵列头 g01..g10 零中文名、总片数 = Σ 格子 = 110、无 ×2 徽章；放大预览头部 g01 徽章无 name span；高级配置 10 列 g 码缩略图 + 二层预览「g01-放大预览」；求解渲染 20 polygon data-label（零 data-ptype）+ tooltip「g01 · 码28面积 2894.3 cm²」。
- 文档同步：agent-component-map（header 追加 US-003 条目 + 8 处行内改写）、agent-api-reference（前端收口注记）、materialSorting-web/AGENTS.md（10 处）。
## 2026-08-18 US-004 高级配置弹窗矩阵化（g码×码号 d/tol）
- 数据层：types/v03.ts PerTypeOverrides 改两级嵌套 Record<label, Record<sizeKey, {d?,tol?}>>；lib/params.ts 增 PerTypeFormMap（弹窗草稿字符串形状）+ perTypeSizeKey（number→String / null→'null'，与 serializeQuantities 同口径单一实现）；collectParams 双层遍历 label→sizeKey（空串档剥离、双侧全空格剔除、整体空→null）。
- URL 分享：perTypeToUrlParam → `label@sizeKey=d,tol;...`（仅非空格产出）；perTypeFromUrlParam 正则 `^(g\d{1,4})@(null|\d{1,4})=([^;,]*),([^;]*)$` 逐段校验，旧 ptype 键/中文/NaN/乱拼段静默跳过不抛错（lenient decode）。
- PerTypeOverridesModal 矩阵化重建：行 = doc.sizes（升序 null 通用殿后）∪ 已配置 sizeKey 追加行（旧配置不静默丢），doc=null → SIZES fallback；列 = /api/ptypes reps 键 ∪ 已配置键，compareByLabel 数值序（先长度再字典序 g99<g100）；列头 = 缩略图（点击放大预览）+ .qty-label-badge g 码 + .qty-rowfill-btn「≡」；格 = (g 码, 码号) d/tol 双输入（testid d-/tol-${label}-${sk}，空 = 继承默认 0/0，blur clampDraft 收边）；doc 缺片格「—」disabled（label 不在 doc 不判缺，parse/commit 暂不同步不阻塞）。
- ColumnFillPopover（文件尾内部组件）：「≡」整列设值弹层，Portal body + fixed 表容器可视区中心（QtyMatrix 同款定位）；复用 .qty-fill-* 样式 + --per-type modifier（z 1150/1160 盖过 overlay 1100）；应用写全部非 missing 行（不造 phantom 键），留空侧 = 继承默认；Enter 快捷应用。
- 三层独立 ESC：modal listener 判 previewLabel===null && fill===null 才关；PtypePreviewModal 只关预览；popover 只关自己。草稿 + 确定：mount 深拷贝（不再预填 '0'），确定 pruneDraft 剔全空格回写。
- style.css：.per-type-rowhead 52px / .ptype-col 92px、.qty-colhead-meta 居中、.per-type-cell 双 input 堆叠、.per-type-missing、.qty-popover-backdrop--per-type(1150) / .qty-fill-popover--per-type(1160)。
- PtypePreviewModal：US-003 已 label 键就绪，浏览器联调缩略图 → g 码放大预览正常（harness 断言）；nestingTour params 步文案改矩阵口径（不 bump TOUR_VERSION）。
- 测试：PerTypeOverridesModal.test.tsx 全量重写 30 项；params.test.ts per_type 嵌套组重写 + URL 分享 6 项新增；ControlPanel.test.tsx 矩阵断言（d-g01-28 + cfg.per_type['g01']['28']={d:1,tol:1}）；useSolveRun fixture 随动 —— npx vitest run 605/605 绿、tsc --noEmit 零错、npm run build 过（209.50 kB）。
- 后端命中冒烟：build_instance per_type={'g03':{'28':{'d':1.5}}} → n_eroded 0→1（仅 g03_28 被腐蚀，(label,sizeKey) 键命中）。
2026-08-18 [US-005] 测试收口+文档同步+新基线对拍（ralph/piece-code-refactor）
- grep 验收：后端+前端非测试/非注释程序代码中文片型名（前片/后片/腰/前袋/后袋/机头/单排/双排/短双排/火机袋/裤耳）0 命中（仅 docstring 工艺说明保留）；GROUP_NAMES/PAIR_TYPES/INTERNAL_TYPES/ALL_TYPES 程序内 0 定义。
- 新基线（US-005 记录在案）：重 commit M1787(2) → intermediate 110 片（g01-g10 × 11 码 28-38，全 demand=1，无 ptype/side）；600s {0,180} 无 erode seed 0 → real 85.59%（原面积口径 total_area/(5459.4×1980)=0.855865，用布 5459.4mm，~160s 早停 n_reports=2500）；sparrow 自报 88.72% 为 1910 约束带口径；同 seed 经 ms-sparrow-baseline 重跑 placed_items 位级一致、density 一致（可复现）。旧基线 85.79%（176 片含 L/R 合成镜像）归档不再对拍。
- 全量验证：pytest 69/69 全绿（含 3 个 test_ws_stop，实际全过无例外）；vitest 605/605 + npm run build + typecheck 全绿；ms-explore/ms-sparrow-baseline/ms-sparrow-exp/ms-web 四 CLI 入口逐个跑通；分层依赖未反向（web→engine→bounds→parser）。
- 文档同步 8 份：business-overview（数量即一切/110 片/新基线 85.59%/实体表口径注记）、排料规则_详细版（§1.1 引擎不合成镜像、§2.3 成对改工艺核对项、附录优先级链第 2 条）、agent-api-reference（invariant 3 无镜像变换/ptypes degrade 口径/WS sizes 注释）、agent-component-map（QtyMatrix/PieceZoomModal/SizePicker 行 + TOUR_VERSION '7' + US-018/011/矩阵化节 superseded 警告）、CLAUDE.md（nesting_engine 行 sequential_sort_key）、nesting_engine/AGENTS.md（labeling 现行口径）、materialSorting-web/AGENTS.md（文件树+US-032 两项口径+US-011 g 码 key+US-013 头部现行结构+矩阵化 US-003/004 节 superseded+US-002 转置行列模型+CSS 布局 16855fd 重构+TOUR_VERSION 链）；dxf_parser/web 两份 AGENTS.md 核对已现行无需改。
- 浏览器验证（CDP headless Chrome harness scripts/us004_verify.mjs，15/15 PASS + 截图 out/us004_verify/）：上传母版 → 矩阵 10 动态 g 码列 × doc.sizes 11 码号行 + d/tol 格 + 10 个 ≡；三层 ESC（预览/整列设值独立）；配 g03@28 d=1.5 + g05 整列 d=2 → 确定 → 选码 → 开始求解 → patch WebSocket.prototype.send 捕获 WS start payload 断言 per_type.g03['28'].d===1.5（端到端 AC）+ g05 全码号键。
- 文档同步：agent-component-map（header + US-018 节 5 行改写 + 不变量 2/3/6/7 修订 + 新增 11~13 + params.ts/style.css/PerTypeOverrides.tsx/ControlPanel.test.tsx 行）、agent-api-reference（WS per_type 注释补 US-004 矩阵产出与 sizeKey 口径）、materialSorting-web/AGENTS.md（header + 目录注释 + US-018 节 US-004 矩阵条目重写 + params 约定 3 处修订）。
## 2026-08-19 · ralph/config-driven-solve-cli · US-001 配置加载与校验模块（cli/config.py）
- 新建 `materialsorting/cli/` 子包（`__init__.py` 仅 docstring：cli → web → nesting_engine → nesting_bounds → dxf_parser 最上层编排，绝不 import web.server）+ `cli/config.py`（ConfigError(ValueError) / NestRunConfig frozen dataclass / load_config）。
- 7 键 schema 严格校验：未知顶层键（旧 seed/multi_seed/seed_count 按未知键报错并附「已由 seeds 列表取代」提示）；master_dxf 绝对直用、相对 CWD→paths.REPO_DIR 两候选解析（延迟 import paths 保持模块级仅标准库），失败列两个候选绝对路径；sizes 空列表报错（build_instance 把 [] 当不过滤的静默歧义）；seeds 非空/非负/不重复/整数列表（缺省 [0]）；gate_mm>0、time 正整数（缺省 300）；per_type 键 ^g\d{1,3}$、值仅 d/tol ≥0，超 MAX_OVERLAP_MM=10 / MAX_ROTATION_TOL_DEG=45 不抛错但 UserWarning 引用常量名+钳制说明（延迟 import constraints 单一真相源）；quantities 键同 g 码正则、码号键 ^[0-9]+$ 或 'null'（solver str(size) 查 demand 口径）、数量 JSON 数字 ≥0 整数（字符串 "1" 报错提示「应写 1 而非 "1"」）。
- 新建 `tests/test_cli_config.py` 21 用例全绿（`.venv/Scripts/python.exe -m pytest materialSorting-server/tests/test_cli_config.py`）：示例配置 AC 全断言 + 相对路径三态解析矩阵（CWD 命中/仓库根兜底/两候选报错）+ 各字段错误矩阵 + 上限警告/恰等不警告 + 模块纯度（AST 断言模块级 import 仅标准库、子进程 `python -m materialsorting.cli.config` 退出码 0 零输出、ConfigError 是 ValueError 子类）。
- 质量闸：`python -m materialsorting.cli.config` 冒烟零副作用；compileall 通过；`git diff -- materialSorting-server/src/materialsorting/web/` 为空（web 层零改动）。
- 全量回归：87 passed + 3 failed（test_ws_stop.py 三例）——**预先存在**（stash 我的改动后在干净树复现同样失败）：环境数据漂移——当前真实 intermediate 为 5336 母版 sizes 30..40，而该测试硬编码 sizes:[28] → total_area=0。与 US-001 无关，留待 US-005 web 回归记录处置。
- 文档同步（/sync-docs）：`.docs/technical/agent-file-map.md`（分层图加 cli 顶层 + 目录树 cli/ 条目 + 新增「cli/ — 配置驱动求解 CLI」节）、`tests/AGENTS.md`（test_cli_config.py 行）。
## 2026-08-19 · ralph/config-driven-solve-cli · US-002 commit 管线编排（paths 常量 + cli/pipeline.py）
- 实现：`paths.py` 追加 `CONFIG_RUNS_DIR = OUT_DIR/config_runs`（docstring 子目录约定注明 CLI 专属、禁写 INTERMEDIATE/uploads）；新建 `cli/pipeline.py`（`new_run_dir(run_name)` 时间戳目录 `<name>_<YYYYMMDD-HHMMSS>` + `commit_from_config(cfg, run_dir)` 镜像 `_commit_to_nesting_sync`：collect → assign_codes → write_piece_dxf `{label}_{size}.dxf` + pieces_manifest.json 到 run_dir/pieces/ → load_nest_pieces → intermediate 落 run_dir；piece 条目经 `_piece_record` 与 server 逐字段一致含 rounding 位数；刻意差异=不写 .bak / 省略 label_representatives / gate_mm 写 cfg.gate_mm 配置驱动）。commit 只消费 master_dxf/gate_mm（sizes/quantities/per_type 求解期才用，commit 全量切片与 web 同口径）。
- 真实母版冒烟（data/configs/5336_coded_sizes32-38.json）：run_dir 内切片 DXF 110 + manifest 110 = 母版 size≠None 轮廓数 ✓、intermediate gate_mm=1980 ✓、`web.solver.load_pieces` 读回 110 片 ✓、web 事实源 INTERMEDIATE sha256+mtime 与 uploads/ 8861 文件快照运行前后不变 ✓、与既有 web intermediate（同母版）对拍 pid 集合相等 / total_area_mm2=10512780.4 相等 / 同 pid polygon 逐点相等（且 piece 记录全字段相等，超出 AC 的加强验证）✓。
- 新建 `tests/test_cli_pipeline.py` 8 用例全绿：run_dir 产物计数、gate_mm 配置驱动、solver 读回、web 事实源哨兵不变、**与 web commit 逐字段对拍**（合成母版小数坐标暴露 rounding 差异）、new_run_dir 时间戳幂等、AST 分层（全模块禁 import web.server + 模块级兄弟包仅向下）、`python -m` 零输出冒烟。
- 质量闸：`python -m materialsorting.cli.pipeline` 导入冒烟零副作用；compileall 通过；`git diff -- materialSorting-server/src/materialsorting/web/` 为空（web 层零改动）；全量回归 95 passed + 3 failed（test_ws_stop.py 三例，预先存在的环境数据漂移，与 US-002 无关，留待 US-005 处置）。
- 文档同步（/sync-docs）：`.docs/technical/agent-file-map.md`（目录树 cli/ 加 pipeline.py 行 + cli 节 pipeline.py 表行 + paths.py 节 CONFIG_RUNS_DIR 行与行数 24→30）、`tests/AGENTS.md`（test_cli_pipeline.py 行）。
## US-003 求解封装 + CLI 入口 + console_script 注册（2026-08-19）
- 实现：`cli/pipeline.solve_pieces`（读 run_dir intermediate → `web.solver.build_instance` + `solve_with_callback` **threading 版**进程内直跑；每帧直接复用 `web.solver._apply_density_dual` 换算双口径 —— density=原面积口径 total_area/(width*gate)、density_sparrow=sparrow 自报，同函数=同公式零复制；末解校验 placed==Σdemand 不齐抛 RuntimeError）；`cli/run_config.py`（argparse config/--name/--time/--quiet；run_name=配置 stem 或 --name，Windows 非法字符→'_'；逐 seeds 串行；进度仅打原面积口径新最优帧+30s 心跳；result.json=config 回显(time 为生效值)+commit 摘要+solve 数组；main 首行 stdout/stderr.reconfigure(utf-8)；退出码 0/1/2）；pyproject [project.scripts] 注册 `ms-run-config` 并 pip install -e 刷新。
- 关键决策：web.solver 走**函数内延迟 import**（pipeline 模块级不 import web，`python -m materialsorting.cli.pipeline` 导入冒烟保持零副作用；cli → web.solver 向下依赖合规，绝不 import web.server）。
- 验收：冒烟 `ms-run-config data/configs/5336_coded_sizes32-38.json --time 5` 退出码 0，n_items=70（10 g 码×码 32-38）、n_eroded=0、placed_items=70、real_density=80.84%∈(0,1)、width_mm=4187>0；result.json 落 run_dir 且 stdout 末行含 real_density（原面积口径）+用布长度+片数+耗时+run_dir 完整路径，中文不乱码；`git diff -- materialSorting-server/src/materialsorting/web/` 为空；`python -m materialsorting.cli.run_config --help` 正常。
- 质量闸：新增 tests/test_cli_run_config.py 11 例全过（solve_pieces 口径/demand/erode/不完整解、main 端到端/quiet/--name 清洗/退出码 1 矩阵、web 事实源哨兵零触碰、分层 AST：run_config 全模块不 import web + pipeline 仅 solve_pieces 内唯一延迟 import web.solver）；全量回归 106 passed + 3 failed（test_ws_stop.py 三例为 US-002 起已知环境数据漂移，留待 US-005，与本次无关）；compileall 通过。
- 文档同步（/sync-docs）：`.docs/technical/agent-file-map.md`（pyproject 6→7 个 console_scripts、目录树 cli/ 加 run_config.py 行 + pipeline.py 行补 solve_pieces、cli 表 pipeline 行补 solve_pieces + 新增 run_config.py 行、入口表加 ms-run-config 行）、`tests/AGENTS.md`（test_cli_run_config.py 行）。
## US-004 多 seed 串行多轮与 best 汇总（2026-08-19）
- 实现：cli/run_config.py 增量三处（US-003 已建 for-seed 循环骨架）：①多 seed 启动打印预计总时长 `len(seeds)×time`（不含解析/切片）+ 每轮「第 i/N 轮（seed=s）」标记（quiet 抑制轮标记，单 seed 不打印）；②result.json 增 `best` 键 = per-seed 解中 real_density（原面积口径）最大者的完整指标 dict（并列取先执行者，max 首个极大值）；③多 seed 末段打「各 seed real_density」汇总行 + 「best = seed N」行，末行汇总取 best（单 seed best=唯一解 → 输出与 US-003 逐字一致，零回归）。种子不要求连续（[0,42] 合法），只串行不并行；commit 仅一次、每轮 solve_pieces 重建 build_instance 复用产物（US-003 已有语义，本轮加 spy 测试锁死）。
- 质量闸：tests/test_cli_run_config.py 11→14 例全过（新增 multi-seed 串行+best / 单 seed 显式=缺省回归 / [0,42] 非连续种子）；全量 109 passed + 3 failed（test_ws_stop.py 三例仍为 US-002 起已知环境数据漂移，归 US-005）；compileall + import 冒烟通过。
